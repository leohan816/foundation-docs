# Cosmile Worker Handoff — WU-D Catalog, Single KRW Price, and Foundation Snapshot Consumption

```text
MISSION_ID: COSMILE_O1_KOREA_GOLDEN_COMMERCE_IMPLEMENTATION_V1
WORKUNIT: WU-D
ACTOR: cosmile
ROLE: Cosmile repository-owner Worker
REQUIRED_MODEL: Opus 4.8 (1M)
REQUIRED_EFFORT: max
REQUIRED_SKILL: /fable-builder
RETURN_TO: foundation-advisor
```

## Exact starting state and reviewed basis

```text
REPOSITORY: /home/leo/Project/.worktrees/Cosmile/COSMILE_O1_KOREA_GOLDEN_COMMERCE_IMPLEMENTATION_V1
BRANCH: implementation/cosmile-o1-korea-golden-commerce-v1-20260717
EXPECTED_BASE_HEAD: 3ea1b211b6111678add9f0e2814c289ed96adca4
EXPECTED_UPSTREAM: origin/implementation/cosmile-o1-korea-golden-commerce-v1-20260717
EXPECTED_UPSTREAM_STATE: EQUAL_0_0
EXPECTED_WORKTREE_STATE: CLEAN
WU0_SCHEMA_HEAD: c559e7cd132e7b837dc38d01395f790499abb70d
WUC_FINAL_REVIEWED_HEAD: 3ea1b211b6111678add9f0e2814c289ed96adca4
WUC_FINAL_REVIEW_VERDICT: PASS
FOUNDATION_SNAPSHOT_REPOSITORY: /home/leo/Project/.worktrees/FOUNDATION/COSMILE_O1_KOREA_GOLDEN_COMMERCE_IMPLEMENTATION_V1
FOUNDATION_SNAPSHOT_BRANCH: implementation/cosmile-o1-foundation-snapshot-v1-20260717
FOUNDATION_SNAPSHOT_FINAL_REVIEWED_HEAD: 73ff00361d9fa88ab57c17858210c1e080dfde1a
FOUNDATION_SNAPSHOT_REVIEW_VERDICT: PASS
REVIEWED_DESIGN_COMMIT: a1ac8016eba01d1ffef20836fe7f16ace3b591c5
INDEPENDENT_DESIGN_REVIEW_COMMIT: daacd8a69318315437cc33e124455baf6db93e91
INDEPENDENT_DESIGN_REVIEW_VERDICT: PASS
FOUNDER_DELIVERY_DECISION: VERSIONED_NON_PRODUCTION_LOCAL_FILE_BUNDLE
FOUNDER_SCOPE: WU-D single authoritative KRW price plus approved local Foundation snapshot consumption
```

Verify all pins and live session/model/effort/workspace/skill before any edit. Read current repository
rules, the reviewed integrated design, the final reviewed Foundation snapshot implementation at its
exact head, and `app/docs/testing/TEST_MEANING_POLICY.md`. Stop on mismatch, tracked drift, scope
conflict, or unsafe runtime need. Preserve reviewed history; no amend/rebase/squash/force-push.

## Exact WU-D objective

Implement the smallest non-production repository lane that:

1. reads and verifies the exact Foundation `fsnap-bundle-1.0` local-file bundle contract;
2. imports only approved, non-production, immutable snapshot pins and sequenced lifecycle notices
   into the already-created WU-0 `FoundationProductSnapshot` and `SkuBinding` substrate;
3. exposes a fail-closed catalog decision for a bound SKU from the currently effective approved
   local snapshot plus Cosmile-owned commerce state;
4. makes `CommerceSku` / active `CommerceOffer` the only KRW price authority for this target lane;
5. leaves existing public/mock routes unchanged and unactivated; the later WU-F harness may call
   this explicit target lane, while legacy mock surfaces remain outside the completion claim.

This is not a public catalog cutover and not a broad frontend rewrite.

## Frozen Foundation bundle contract

Consume, do not reinterpret, the reviewed Foundation implementation at `73ff0036...`:

- layout: `bundle_meta.json`, `manifests/manifest_%08d.json`,
  `snapshots/<foundation_product_id>/<snapshot_content_sha256>.json`;
- exact constants: `fsnap-bundle-1.0`, `fsnap-1.0`, `NOT_LIVE_SALE_EVIDENCE`,
  `non_production=true`;
- canonical bytes: JSON object keys sorted by Unicode code point, UTF-8, minimal separators,
  non-ASCII literal, only null/bool/integer/string/list/string-keyed object;
- seven-field pin: `foundation_product_id`, `vault_commit`, `product_tree_hash`, `data_version`,
  `formula_version`, `snapshot_schema_version`, `snapshot_content_sha256`;
- content hash, manifest sequence, snapshot filename/path, manifest-entry pin, approval scope/status,
  expected-file set, notice schema, and supersedes resolvability must all validate fail-closed;
- only `APPROVED_FOR_COMMERCE_DISPLAY` snapshots can become current display inputs;
- price, stock, sales status, shipping, promotion, assessment, and other Foundation-excluded
  commercial fields must be rejected at any content depth;
- duplicate identical replay is a zero-effect category; sequence gaps, tamper, unknown fields/states,
  silent replacement, invalid correction, and unresolvable lineage block the affected import;
- correction/supersession/withdrawal/gate-change are immutable sequenced notices, never in-place edits;
- `WITHDRAWN` contains the affected SKU; Foundation outage alone never means withdrawal;
- no stale duration is authorized. `STALE_LAST_APPROVED` is fail-closed for a new target catalog
  decision until a later explicit policy exists;
- optional ack is category-only operational evidence and is not a safety or activation gate.

Do not copy Python implementation mechanically. Achieve byte-compatible verification and prove it
with cross-language fixtures generated by the reviewed Foundation implementation in an attributable
temporary directory. No Foundation repository write is allowed.

## Snapshot persistence and binding rules

- use only existing WU-0 schema; no schema/migration/generated-client commit;
- insert immutable snapshot pin rows idempotently by content SHA; equal replay is no-op;
- a different document for the same immutable identity fails closed;
- preserve a resolvable supersedes chain; never rewrite or delete historical snapshots;
- a SKU binding is Cosmile-owned and injective on `cosmileSkuId`;
- a binding must match the snapshot product and optional variant; conflicting rebind fails closed;
- missing initial snapshot/binding, invalid gate, withdrawn, stale-without-policy, tamper, gap, or
  repository error yields a closed category and no sellable catalog decision;
- order-line snapshot binding remains later WU-E work; do not modify `OrderItem` here;
- no sellability is inferred from ELT membership. Rights/MFDS/imagery/human-review/provenance gates
  must be explicitly passing in the consumed snapshot for a target catalog decision;
- the representative and boundary SKUs may be exercised only with synthetic restricted internal
  fixtures marked `NOT_LIVE_SALE_EVIDENCE`; no real rights/MFDS/imagery approval is claimed.

## Single authoritative KRW price

- for this target lane, price is resolved only from an active, matching `CommerceOffer` or active
  matching/default `CommerceSku`, through the server-side `resolveUnitPrice` authority;
- currency is exactly `KRW`; price is a positive safe integer within an explicit bounded maximum;
- no client, snapshot content, mock commerce map, Foundation field, URL, or caller amount can supply
  or override the charge/display price;
- remove the Foundation/mock-price fallback from `resolveUnitPrice`; no legacy mock value may be the
  target lane's fallback;
- the target catalog projection must use the same resolved server price that later checkout uses;
- price change on a repeated catalog/checkout decision returns an explicit reconfirmation/hold
  category rather than silently accepting a stale caller value;
- stock and sales state remain Cosmile-owned; WU-C remains the purchase-time availability guard.

## Exact allowed product paths

Only these paths may change:

- `app/src/lib/foundation/snapshotContract.ts` (new)
- `app/src/lib/foundation/snapshotBundle.ts` (new)
- `app/src/lib/foundation/snapshotRepository.ts` (new)
- `app/src/lib/foundation/snapshotCatalog.ts` (new)
- `app/src/lib/sku.ts` (narrow removal of mock/Foundation price fallback only)
- `app/scripts/o1_foundation_snapshot_contract.vitest.ts` (new)
- `app/scripts/o1_foundation_snapshot_bundle.vitest.ts` (new)
- `app/scripts/o1_catalog_price_authority.vitest.ts` (new)
- `app/scripts/o1_foundation_snapshot_import.dbtest.py` (new)

Do not modify `foundationProductClient.ts`, existing pages/routes/components, mock data, cart,
checkout, order, payment, inventory, Prisma schema/migrations, package/lockfiles, generated clients,
Foundation, or SIASIU. If another product path, a public cutover, a schema change, or a new transport
is necessary, stop and return exact evidence.

## Required tests and safety

Pure tests must cover positive and adjacent-negative cases for canonical serialization/hash parity,
all bundle/meta/manifest/snapshot/pin/approval/gate/notice validations, unexpected files, sequence
gaps, replay, correction, supersession, withdrawal, stale-without-policy, unresolvable lineage,
excluded commercial keys at multiple depths, integer/string/Unicode/path bounds, and error
minimization. Use bounded category/status/count output only.

Catalog tests must prove:

- target projection requires approved current snapshot, passing gates, exact SKU binding, active
  Cosmile SKU, and KRW server price;
- representative and boundary fixture behavior is explicit and synthetic-only;
- missing/stale/withdrawn/tampered/gap/unapproved/gate-failed/binding-conflict/unknown fail closed;
- active offer > requested SKU > default SKU price order is preserved;
- no Foundation/mock/caller price fallback remains;
- display price equals the later server price result; stale caller price requires reconfirmation;
- Foundation path outage after a previously imported approved local snapshot does not invalidate
  ordinary commerce merely because the producer is down;
- no public route, runtime activation, transport, endpoint, provider, or network exists.

The database import rehearsal may use only a fresh disposable local PostgreSQL process, synthetic
data, Unix socket or loopback, no external network/image pull, and the already-committed WU-0
migration. Exercise idempotent replay, gap/tamper zero-write, insert/current/correction/withdrawal,
binding injectivity/conflict, transaction rollback, and historical immutability. Record process/data
directory identity, environment target inspection without values, commands, bounded counts,
shutdown/removal checks, and final Git state. Do not alter any existing process/container/volume/DB.

Run and record:

- the three focused WU-D Vitest files;
- the disposable PostgreSQL import rehearsal;
- the WU-0 migration regression;
- the full safe Vitest suite;
- exact counts/status, cleanup, cross-language Foundation-bundle parity, no-transport/no-route proof,
  and exact path containment.

Use existing dependencies only. No install, real/shared DB, provider, credential, customer data, PII,
payment, external network, build, public exposure, production, or live access.

## Commit, evidence, and stop

Create additive candidate commit(s) only if required to keep implementation and tests reviewable;
record every commit and do not amend/rebase/squash. Do not push before independent review PASS. Do
not begin WU-B/E/F/G.

Write only these foundation-docs artifacts for Advisor publication:

- `runs/shared/COSMILE_O1_KOREA_GOLDEN_COMMERCE_IMPLEMENTATION_V1/50_COSMILE_WUD_CATALOG_SNAPSHOT_RESULT.md`
- `runs/shared/COSMILE_O1_KOREA_GOLDEN_COMMERCE_IMPLEMENTATION_V1/50_COSMILE_WUD_CATALOG_SNAPSHOT_POINTER.md`

The result must include exact head(s), base-to-candidate diff, all nine path dispositions, bundle
compatibility matrix, lifecycle/gate/price decisions, test commands/counts, disposable-runtime and
cleanup evidence, residual unknowns, explicit legacy/public mock exclusion, and confirmation that no
route/transport/payment/order/runtime was activated. Return to `foundation-advisor` and STOP.
