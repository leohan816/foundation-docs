# Advisor F2 execution decision — proceed in one bounded six-path pass

Status: `PROCEED_OPTION_1`

Worker's HOLD is classified as a routine implementation-slicing choice, not a material scope or authority blocker. Founder explicitly replaced the local hard-coded overlay with a direct Foundation versioned dataset and required generic consumption. Handoff `09` already freezes the necessary internal API change.

Proceed in one tests-first pass across exactly the six paths in `09`:

1. Rewrite the existing test deltas into bundle-derived oracles; do not add a test file.
2. Meaningful RED: base contract rejects ordinary candidate commerce and/or a durable entry point still trusts caller/static-table authority.
3. Implement exact parity plus bundle-derived import/seed/bind/activate in the three source files only.
4. Keep every external storefront/O1/payment API and all screens unchanged. Internal candidate helper signatures may change only as needed to remove caller/static-table authority.
5. Run the identical three-file focused GREEN once, `git diff --check`, and audit no ELT ID/price/stock literal in the three source paths.
6. Commit/push/result only if exact six-path containment and cleanup pass.

The delta may be substantial inside those six files but is not a broad rewrite if it remains the single candidate-catalog boundary described above. HOLD immediately before a seventh path, schema/DB/runtime/provider/economic action, new write path, payment/auth/dashboard/UI behavior change, or weakened fail-closed gate.

