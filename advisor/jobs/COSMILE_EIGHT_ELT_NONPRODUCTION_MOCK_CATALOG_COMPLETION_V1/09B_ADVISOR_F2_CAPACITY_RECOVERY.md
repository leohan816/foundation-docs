# Advisor F2 capacity recovery — serial slices in the same Worker session

Status: `PROCEED_SLICE_A_THEN_B`

The second HOLD is `WORKER_TURN_CONTEXT_CAPACITY`, not a technical/authority blocker. Preserve `cosmile:claude.0`, Opus 5/xhigh, role, process, pane and current worktree state. No clear/restart/exit/model/actor change.

## Slice A — execute now, no reread

Exact paths:

1. `app/src/lib/foundation/snapshotContract.ts`
2. `app/scripts/o1_test_candidate_catalog.vitest.ts`

- Restore only the current stale hard-coded delta in the named test file to base, then add a compact candidate-commerce parity block. The prior 7-vs-8 RED remains preserved in docs/turn evidence and is superseded, not erased.
- Keep the other two uncommitted test files byte-preserved and untouched.
- RED: run only the named test selection proving base rejects Foundation ordinary `content.commerce` and lacks candidate-only validation.
- Implement exact Foundation parity already mapped: exact five keys, KRW, nonnegative integers, active/in_stock, TEST_ONLY_CANDIDATE only, exclusions elsewhere unchanged.
- Run the identical selection once for GREEN, `git diff --check`, commit/non-force push only these two paths. Dirty residue may consist only of the two preserved test files.
- Return compact A result, no docs artifact yet.

## Slice B — only after Advisor A gate

Will use the remaining four frozen paths plus the already-admitted parity test file: bundle-derived plan in `o1TestCandidateCatalog.ts`, internally rederived import/seed/bind/activate in `snapshotRepository.ts`, three focused suites, identical full three-file GREEN, cleanup, final F2 commit/result. No further design choice remains.

HOLD only for a path outside handoff `09`, schema/DB/runtime/provider/economic action, external O1/UI API change, weakened denial, or another no-delta capacity failure. Do not reread the 720-line suite before Slice A.

