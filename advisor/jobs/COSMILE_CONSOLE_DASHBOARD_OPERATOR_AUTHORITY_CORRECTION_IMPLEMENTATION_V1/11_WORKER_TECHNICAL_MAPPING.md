# 11 Worker Technical Mapping — Console/Dashboard/Operator Authority Correction (P0, read-only) — CORRECTED per 14/15

PINS: product `/home/leo/Project/.worktrees/Cosmile/COSMILE_CONSOLE_DASHBOARD_OPERATOR_AUTHORITY_CORRECTION_IMPLEMENTATION_V1` · branch `implementation/cosmile-console-dashboard-authority-correction-v1-20260724` · HEAD `3dc5129` (clean, no upstream). Contract: design pkg `40/31/21/22`, mapping gate `14`, correction `15`. Product paths under `app/`. **Read-only; proposes; implements nothing.**

Authority note (15): the Founder decisions are **CLOSED** for this mission — M1–M5 are an executable module plan (no longer UD-blocked). Frozen choices: one provider-neutral `OperatorPrincipal`; ConsoleUser/Google credential bind (authenticate only, never grant); existing Google issuer/subject validation + env allowlist remain an **independent eligibility** requirement and the new resolver **must not** read customer `AuthIdentity`/session; 14-name catalog frozen; separately-persisted grant with scope/lifecycle/revocation, evaluation reads principal/binding/grant every request, default-deny; **existing step-up secret compare + in-process single-use nonce stay UNCHANGED** (no `StepUpFreshness` persistence — multi-instance durability is a Controlled Live residual); sensitive audit adds **nullable** attribution to the exact existing model `ConsoleAuditLog`; no grant UI/API/seed (a clean candidate grants nobody by source or migration).

## 1. Contract-to-code table (M1–M5)

| M | Approved contract | Current load-bearing symbol/path | Proposed exact symbol/path | Focused test | Failure category | Preserved invariant |
|---|---|---|---|---|---|---|
| M1 | 40 §2; 21 §2.2/§9; Dashboard=evidence-bounded ops, no deletion of wrong-namespace | O1 views under `console/{orders,orders/[orderId],fulfillment,finance,settings}/page.tsx`; `components/console/{O1ConsoleQueue,O1ConsoleFulfillment,O1ConsoleFinance}.tsx`; shell `components/console/ConsoleNav.tsx:27-100` | ADD `app/src/app/dashboard/{layout,page,requests/page,requests/[orderId]/page,fulfillment/page,finance/page,activity/page,settings/page}.tsx`; `app/src/app/lab/{page,[capabilityId]/page}.tsx`; `app/src/components/operator/OperatorShell.tsx`; `app/src/lib/dashboard/capabilityRegistry.ts` | `app/scripts/o1_dashboard_ia.vitest.ts` (source-contract: routes exist, reuse-not-restyle, truth-strip+`운영 권한 계약 대기`, no `/console/*` delete) | IA/namespace | legacy `/console/*` O1 pages retained (parity; no delete/redirect, 40 §9); no economic reinterpretation |
| M2 | 21 §5/§10.6; 31 C6/R1; screen-guard hardening w/o command-gate weakening | `console/layout.tsx:10-13` not an auth boundary; per-page `requireConsoleUser` (`guard.ts:5`) | explicit `requireConsoleUser()` in each new `dashboard/**/page.tsx`+`lab/**`; DENIED=`이 계정에는 O1 운영 권한이 없습니다.` | `app/scripts/o1_dashboard_guard.vitest.ts` | screen-auth | command gates unchanged; screen access never substitutes for command authority (31 C6) |
| M3 | 40 §5; 31 C1 INV-0; 15§1-3 provider-neutral principal; resolver must not read customer session | operator derived from CUSTOMER session `o1CommerceRuntime.ts:537-547` (`o1OperatorForCustomer` → `AuthIdentity WHERE customerId`); env allowlist `auth/o1Operator.ts:19,52-66` | schema `OperatorPrincipal`+`OperatorCredentialBinding`; new `app/src/lib/operator/{contracts,principal}.ts` (`resolveOperatorPrincipal(env, operatorCredential)` — reads principal+binding+grant + independent allowlist eligibility, NO customer `AuthIdentity`/session); operator-session read `app/src/lib/console/session.ts` (bind only) | `app/scripts/operator_principal.dbtest.py` (synthetic principal/binding rows; login mints NO capability; customer session never resolves an operator) | schema+resolver | INV-0: Console/customer session alone never O1 economic authority; email/display/session-id never keys; issuer/subject eligibility preserved |
| M4 | 40 §6; 31 C2/C3/C7; 15§4/§7 frozen 14-catalog + persisted grant + default-deny eval | `permission.ts:6-34`; `order/contracts` `SensitiveAction`; env allowlist→fixed `role:'admin'` `o1Operator.ts:65` | new `app/src/lib/operator/capability.ts` (frozen 14 names, 40 §6); schema `CapabilityGrant`; `app/src/lib/operator/evaluate.ts` (default-deny 5-req; reads principal/binding/grant state per request); wire at existing O1 routes (add check around existing gate, no weakening) | `app/scripts/operator_evaluate.vitest.ts` + `app/scripts/operator_capability_grant.dbtest.py` (default-deny; scope match; catalog≠grant; clean candidate grants nobody) | schema+eval | Git defines catalog only, never grants (C7); default-deny; O1 command boundary authoritative (C6) |
| M5 | 40 §7; 31 C4/C5; 15§5-6 nonce UNCHANGED, nullable attribution on EXACT existing audit | in-process nonce `o1Operator.ts:72-88` (keep); econ audit `ConsoleAuditLog` via `order/repository.ts:31` `writeAuditTx` (actorRole+action+meta, **no operator subject**); `serviceRequestRepository.ts:517-624` | additive nullable `operatorPrincipalId?` on **`ConsoleAuditLog`** (`schema.prisma:355`) + write it in `order/repository.ts:29-32`/`serviceRequestRepository.ts`; revocation via `evaluate.ts` reading principal/grant state each request. **No `StepUpFreshness` table; nonce unchanged.** | `app/scripts/operator_audit_attribution.dbtest.py` (subject attributed on refund/fulfillment/support; revoke effective next command; audit still transactional/rolls back) | schema(additive col)+audit | step-up single-use before mutation, secret-alone-insufficient, timing-safe, zero-effect failure, transactional fail-closed audit — all PRESERVED |

## 2. Module sequence + exact path ceiling (no wildcards)

- **M1 (NO_SCHEMA).** ADD: `app/src/app/dashboard/layout.tsx`, `…/page.tsx`, `…/requests/page.tsx`, `…/requests/[orderId]/page.tsx`, `…/fulfillment/page.tsx`, `…/finance/page.tsx`, `…/activity/page.tsx`, `…/settings/page.tsx`, `app/src/app/lab/page.tsx`, `app/src/app/lab/[capabilityId]/page.tsx`, `app/src/components/operator/OperatorShell.tsx`, `app/src/lib/dashboard/capabilityRegistry.ts`, `app/scripts/o1_dashboard_ia.vitest.ts`. No `/console/*` edit/delete.
- **M2 (NO_SCHEMA).** EDIT only M1-new `dashboard/**/page.tsx`+`lab/**` (add `requireConsoleUser`); ADD `app/scripts/o1_dashboard_guard.vitest.ts`. Ceiling excludes `console/layout.tsx`.
- **M3 (schema).** `app/prisma/schema.prisma`(+one additive migration), `app/src/lib/operator/{contracts,principal}.ts`, `app/scripts/operator_principal.dbtest.py`. (Resolver replaces the operator use of `o1CommerceRuntime.ts:537-547`; that customer-session function is retained only for any customer-facing use, not operator authority.)
- **M4 (schema).** `app/src/lib/operator/{capability,evaluate}.ts`, `app/prisma/schema.prisma`(+migration), `app/src/app/api/o1/operator/reconciliation/route.ts`, `app/src/app/api/o1/operator/orders/[orderId]/refund/route.ts`, `app/scripts/operator_evaluate.vitest.ts`, `app/scripts/operator_capability_grant.dbtest.py`.
- **M5 (schema additive col).** `app/prisma/schema.prisma`(+migration adding nullable `operatorPrincipalId` to `ConsoleAuditLog`), `app/src/lib/order/repository.ts`, `app/src/lib/order/serviceRequestRepository.ts`, `app/scripts/operator_audit_attribution.dbtest.py`.

## 3. Minimum additive schema (M3–M5; NO WRITE here)

`app/prisma/schema.prisma`, additive-only, no edit/backfill of existing rows:
- `model OperatorPrincipal { id String @id, subjectRef String @unique, issuer String, displayLabel String?, state OperatorState @default(active), createdAt DateTime @default(now()) }`; `enum OperatorState { active suspended revoked }`.
- `model OperatorCredentialBinding { id String @id, operatorPrincipalId String, kind String, consoleUserId String?, subjectRef String?, createdAt DateTime @default(now()) }` + FK `operatorPrincipalId→OperatorPrincipal.id`, FK `consoleUserId→ConsoleUser.id`, `@@unique([kind, subjectRef])`, `@@index([operatorPrincipalId])`. Binding authenticates only.
- `model CapabilityGrant { id String @id, operatorPrincipalId String, capability String, scope Json?, source String, grantedByPrincipalId String?, reasonCategory String, activatedAt DateTime, expiresAt DateTime?, state GrantState @default(active), revocationEvidence String?, createdAt DateTime @default(now()) }` + FK `operatorPrincipalId→OperatorPrincipal.id`, `@@index([operatorPrincipalId, capability, state])`; `enum GrantState { active suspended revoked expired }`.
- Additive **nullable** column on the EXACT existing model: `ConsoleAuditLog.operatorPrincipalId String?` (`schema.prisma:355`) + `@@index([operatorPrincipalId])`. **No new audit model.** **No `StepUpFreshness` model.**
- Migration: forward=create the 3 models/2 enums + the nullable column; **down**=drop them (fail-closed direction); **no-backfill**=existing `ConsoleAuditLog` rows stay `operatorPrincipalId=null` (truthfully unattributed) and no principal/binding/grant exists → **clean candidate grants nobody** (15§7); **deletion**=principal/grant transition to `revoked`, never hard-delete (audit integrity). Disposable Postgres only; tests insert synthetic principal/binding/grant rows.
- Why no smaller reuse: `ConsoleUser/Session`=password identity (not a principal); env allowlist has no activation/expiry/revocation record; `ConsoleAuditLog.userId` is a `ConsoleUser` FK so a non-ConsoleUser operator is un-attributable → a nullable `operatorPrincipalId` is the minimum. (Durable/multi-instance step-up freshness deliberately **not** added — Controlled Live residual per 15§5.)

## 4. OperatorPrincipal + Console binding + independent eligibility (INV-0)

`resolveOperatorPrincipal(env, operatorCredential)` (new `lib/operator/principal.ts`): (1) verify the operator's OWN credential (Console session binding and/or approved Google credential) → `OperatorCredentialBinding` → `OperatorPrincipal`; (2) require principal `state=active`; (3) **independent eligibility** = the unchanged Google `(issuer,subject)` structural validation + env allowlist (`o1Operator.ts:26,52-66`) evaluated against the **operator credential’s** subject — never the customer session; (4) hand to `evaluate.ts` for the exact capability grant. It **must not** query `AuthIdentity WHERE customerId` or any shopper session (removes `o1CommerceRuntime.ts:537-547` from the operator path). A Console credential authenticates the human and binds the principal but mints no capability; customer identity is never an operator credential/grant source.

## 5. Capability→read/command matrix (all reviewed Dashboard endpoints)

Catalog (frozen 14): `console.workspace.read, console.workspace.request_mock, dashboard.operations.read, orders.read, service_requests.read, service_requests.support_acknowledge, fulfillment.read, shipment.record, inventory_hold.read, reconciliation.read, reconciliation.recover, refund.full_execute, audit.sensitive_read, settings.boundary_read`.

| Dashboard datum / endpoint | current source | required capability | step-up |
|---|---|---|---|
| D01 처리필요요청 (queue) | `o1OperatorServiceRequestQueue(50)` (`console/orders/page.tsx:15`) | `dashboard.operations.read` + `service_requests.read` | no |
| D02 요청상세 read | GET `/api/o1/operator/orders/[orderId]` | `service_requests.read` | no |
| D02 support-ack | POST support-ack route (`serviceRequestRepository.ts:517-624`) | `service_requests.support_acknowledge` | yes |
| D02 full TEST refund | POST `/api/o1/operator/orders/[orderId]/refund` | `refund.full_execute` | yes (nonce-before-mutation) |
| D02 record-only shipment | fulfillment transition (`order/repository.ts:275`) | `shipment.record` | yes |
| D03 출고대기 | `o1OperatorOrderList(50)` (`console/fulfillment/page.tsx:11`) | `fulfillment.read` + `orders.read` | no |
| D04 재고위험 | order-level committed/HOLD only; aggregate = `UNAVAILABLE` | `inventory_hold.read` | no |
| D05 정합성 HOLD | GET `/api/o1/operator/reconciliation` | `reconciliation.read` | no |
| D06 보호된 복구 | POST `/api/o1/operator/reconciliation` | `reconciliation.recover` | yes |
| D07 최근 민감기록 | `ConsoleAuditLog` econ rows; no allowlisted list read yet = `UNAVAILABLE` | `audit.sensitive_read` | no |
| Console workspace / settings | `console/*`; `console/settings` | `console.workspace.read`/`.request_mock`; `settings.boundary_read` | no |

Every protected request: `evaluate` = active principal + active/unexpired/unrevoked exact grant + scope match + action-bound step-up (unchanged nonce) + server command-bound enforcement + transactional audit. Screen guard (M2) gates views; command check (M4) gates mutations; a Console owner session lacking the O1 boundary stays denied.

## 6. Preserve current O1 semantics (31 §4 — DO NOT WEAKEN)

Google `(issuer,subject)` structural match + issuer load-bearing + email-never-a-key (`o1Operator.ts:26,58,60-63`, `googleOidc.ts:20,80`); default-deny/unconfigured→deny (`o1Operator.ts:57`, `stepUp.ts:21`); action/scope/role/reason-bound **single-use in-process nonce UNCHANGED** + secret-alone-insufficient + `timingSafeEqual` (`o1Operator.ts:72-140`, `stepUp.ts:41-70`); nonce consumed before provider/mutation (`refund/route.ts:30`); full-only refund `inventoryRestored:false` + committed/HOLD reservation (`refund/route.ts:7-8,61`); count-only reconciliation (`reconciliation/route.ts:60-89`); transactional fail-closed economic audit on `ConsoleAuditLog` that rolls back on audit failure (`order/repository.ts:8,29-32`); idempotency/replay. Operator attribution is **added additively** as nullable `operatorPrincipalId` (subject-ref/category only) — never PII, provider payload, key, or target id.

## 7. Route/component transition (40 §2, 21 §2.2/§6/§7)

`/console`→conversation/mock only (`V0 MOCK · 실제 서비스 제어 없음`, msg=`요청 기록`, approve/reject mock, execute/publish disabled). `/dashboard`(new)→Main Now reusing O1Console*+classifier (overview D01,D03,D04,D05,D07; detail D02,D06) with truth strip + `운영 권한 계약 대기`; missing aggregate inventory (D04)/recent-activity (D07) = `UNAVAILABLE` not zero. `/lab`(new)→read-only registry from static `capabilityRegistry.ts`, 6 gates, no execute/promote/dry-run. **Compatibility:** current O1 operator routes (`/api/o1/operator/*`) keep runtime flag + independent allowlist eligibility + unchanged step-up, but their operator resolution switches from customer-session (`o1OperatorForCustomer`) to `resolveOperatorPrincipal`; behavior on a customer-only session becomes deny (removes customer-session authority) — no economic-outcome change. Wrong-namespace `/console/*` O1 pages: **no delete/redirect** in this mission (retain as transition evidence until parity, 15§8); the only named non-mutating transition is the additive `/dashboard` host.

## 8. Tests-first + DB boundary + final evidence + cleanup/rollback

Per module RED→GREEN: M1 `vitest run scripts/o1_dashboard_ia.vitest.ts`; M2 `vitest run scripts/o1_dashboard_guard.vitest.ts`; M3 `scripts/operator_principal.dbtest.py`; M4 `vitest run scripts/operator_evaluate.vitest.ts` + `scripts/operator_capability_grant.dbtest.py`; M5 `scripts/operator_audit_attribution.dbtest.py`. One disposable `postgres:16-alpine` (loopback, tmpfs, `--pull=never`) applies committed + the new additive migrations; **tests create synthetic principal/binding/grant rows** (no seed/UI/API). Final (impl pin): `npm run typecheck`+`npm run build` (generate-first)+focused integration+browser on isolated preview + Korean-font/mobile/a11y re-validation. Cleanup: drop disposable container + mission temp; keep durable stores. Rollback: per-module `git revert`; additive migrations down-drop the 3 models/2 enums/1 nullable column without touching existing data.

## 9. Final gates + residuals + STOP

- Gates: M1/M2 tests-first source-contract green; M3–M5 disposable-DB default-deny/attribution/revocation green; INV-0 + all §6 invariants preserved; clean candidate grants nobody; typecheck/build/Korean-font/mobile/a11y re-established at impl pin.
- **Residuals (Controlled Live, not this mission):** multi-instance/durable step-up freshness and a real production step-up (in-process nonce kept, production stays deny-all) (15§5); allowlisted recent-sensitive-activity read for D07; aggregate inventory-risk read for D04.
- STOP only if direct source proves a frozen choice needs weakening O1 semantics, a broader schema, or an unapproved authority/economic decision — none found; the plan is implementable additively without weakening. No implementation performed.

RETURN_TO: foundation-advisor.
