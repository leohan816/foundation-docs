# 148 — Final Hard/Safety Independent Review

MISSION: `COSMILE_CONSOLE_DASHBOARD_OPERATOR_AUTHORITY_CORRECTION_IMPLEMENTATION_V1` · PASS `IMPLEMENTATION_REVIEW` · TIER `HARD_IMPORTANT_SAFETY`
MODEL/EFFORT: Fable 5 (`claude-fable-5`) / max — session directive re-set to Fable 5 before dispatch, matching launcher; Advisor live-binding is authority. Independent Reviewer (same reviewer as design review 51 and Lab review 145); read-only; no test/build/browser/runtime/DB/provider/mutation/patch/commit/push.
SKILL: `/home/leo/Project/skill/fable-sentinel/SKILL.md` (byte-identical to loaded) + `delta-review`, `safety-review`, `provenance-review`, `contract-review`, `review-classification`.
LAUNCHER 147 VERIFIED: docs HEAD `df636595` ✓ blob `75ec7979` ✓ sha256 `7a33644a` ✓.
REVIEWED RANGE: `3dc5129b..6486019e` on `implementation/cosmile-console-dashboard-authority-correction-v1-20260724` — **16 commits / 59 paths** (re-enumerated, matches expectation), candidate clean, HEAD==upstream, base is ancestor. Design authority 40/51/60-61 and freeze 16 applied; safety claims re-verified in source, not trusted from results.

## Ten questions — verdicts (source-grounded)
1. **YES.** `OperatorPrincipal` is provider-neutral with **no customer/shopper column on any authority table** (migration comment + DDL verified); `resolvePrincipal` reads only bindings/principal/env-allowlist and "never reads a customer identity or session"; `getShopper`/`o1OperatorForCustomer` removed from all six operator routes (`git grep`: zero remaining callers; definition retained, undeleted); Console credential authenticates via binding only.
2. **YES.** Catalog (exact 14 design-§6 entries) is definition-only — test-proven "valid capability with no grant still denies" (`grant_missing`); grants are persistent rows with closed state/scope/lifecycle CHECKs; evaluation is default-deny at every step (unauthenticated → binding missing/inactive → principal inactive → exactly-one active Google binding (0 or >1 denies) → env-allowlist eligibility → exactly-one active/unexpired/scope-covering grant; **ambiguous pair denies**); repository re-reads state per request, so suspension/revocation is effective at the next command evaluation; enforced at all six command boundaries with exact capability+scope (`refund.full_execute` order-scoped; `reconciliation.read/recover` global; `service_requests.read` order/global; `support_acknowledge`, `shipment.record` order-scoped; queue requires BOTH global read grants).
3. **YES.** Sensitive routes preserve everything: flag-gate first, opaque 403, single-use nonce consumed before provider/mutation, dual refund verifiers, full-only refund + `inventoryRestored:false` + reservation committed/HOLD, fixed recovery limit, no operator identity into the bridge, closed outcomes — the diffs substitute only the authorization source. Support/shipment gained no step-up (freeze-conformant). Route-authority test pins flag-first + authority-before-body/nonce/core ordering and no-customer-session-import per route.
4. **YES.** `writeAudit(Tx)` writes `operatorPrincipalId` inside the same transaction; unknown principal = RESTRICT FK violation → whole mutation rolls back (fail-closed attribution); customer rows (`customer_pre_capture_cancel`, `customer_paid_unshipped_cancel_request`, `customer_shipped_support_request`) and system rows explicitly null; idempotent/read-only paths proven zero-audit by source-contract test (ce13a74) + attribution dbtest (48/48).
5. **YES.** One additive migration: three new empty tables + one nullable audit column; named CHECKs close states/kinds/scopes/lifecycle; all FKs RESTRICT; no backfill/seed/rename/retype; migration creates no principal/binding/grant ("a clean candidate grants NOBODY"). `down.sql` **aborts unless pristine** (any authority row or attributed audit row blocks the drop) — fail-closed reversal. Golden Commerce semantics untouched (order-lane diffs are attribution-params only). Migration dbtest 95/95.
6. **YES.** Dashboard reads are capability-gated (root `dashboard.operations.read` + per-read grant), flag-off and read-failures render `UNAVAILABLE` (never zero); D04 `UNAVAILABLE · 집계 조회 계약 없음` and D07 `UNAVAILABLE · 조회 계약 없음` as designed; only reviewed O1-backed operations surface; detail reuses the reviewed panel/APIs.
7. **YES.** Predecessor `/console` operational pages and `/o1/operator/**` are truthful read-only transition surfaces (verified sample + M3C source-contract test over all seven files: session gate + flag kept, **no O1 data read, no action, no redirect/delete**, link to Dashboard); no legacy/mock/admin bypass (Dashboard + O1 APIs remain the sole authorization boundaries).
8. **YES.** `/lab` = read-only 31-candidate registry (exact reviewed IDs, unique 31, `NONE_READ_ONLY`, immutable rows — gate 103 PASS). Verified at this exact candidate by my 145 review: Lab-local section = 31 links / 0 controls; the 13 page-wide buttons are global chrome (10-group tree confirmed).
9. **YES.** Tests are meaningful and unweakened: adversarial default-deny contract suite; per-route capability/ordering pins; 3 disposable-DB tests ALL PASS (migration 95, repository revocation/default-deny/zero-write via real Prisma, attribution 48); module RED→GREEN chains; typecheck + non-production build PASS (all routes dynamic, no build-time DB); fixture realignments (cf92a7c, 6486019) track reviewed contract changes; disclosed skips (1 + 6) are infra-gated, recorded in 121.
10. **YES.** Provenance is exemplary: final gate 121 honestly returned **HOLD** on its own worker command bug (browser probe not validly executed), refused a possibly-forbidden silent rerun (모르면 멈춘다/행동 진실), and the Advisor closed the gap via the corrected Lab-scoped browser evidence (141/142 PASS at `6486019e`, reviewed at 145). Path deviations (labRegistry.ts; decomposed M3B/M3C tests) were Advisor-frozen at module level and documented. Non-production claim ceiling maintained; Git clean/upstream-equal; canonical package/lock/schema hashes recorded unchanged.

## Safety procedures (P1–P4)
No safety-weakening path: the grant layer only **adds** deny-biased conditions on top of the preserved env-allowlist + step-up + flag chain; no rule demotes or bypasses a protection; same-trigger effects are consistent across freeze/code/tests; lifecycle transitions (suspend/revoke/expire) all deny and are re-read per request. STOP conditions: none triggered.

## Blocking findings
**None.**

## Residual risks (Controlled-Live blockers; require Leo/GPT acceptance — none blocks this candidate)
- R1 [carried, UD5] Step-up freshness remains in-process/single-instance (deliberately unchanged per freeze); production step-up remains deny-all.
- R2 [bootstrap] No seed/grant-management route exists (by design): a clean DB **denies everyone** — fail-closed but non-operational until a separately authorized provisioning mission creates principal/bindings/grants.
- R3 [evidence] Browser evidence is the single corrected `/lab` probe; fuller Korean-font/mobile/a11y browser validation remains open (design-§10 gate 8 carried).
- R4 [hygiene] `o1OperatorForCustomer` retained with zero callers — retirement candidate for a later mission; disclosed test skips (1+6) remain infra-gated.
- Design residuals UD1/UD2/UD4/UD6 are now substantially implemented (structural customer separation, dual-binding principal, DB grant lifecycle/revocation, transactional subject attribution); UD3 granularity is realized via the 14-capability catalog with per-route exact checks.

## Verdict
`PASS_WITH_RISK` — the cumulative candidate faithfully implements the reviewed authority design with zero blocking findings and no safety-weakening path: structural customer/operator separation is now real, grants are explicit default-deny with fail-closed ambiguity and next-evaluation revocation, every sensitive invariant (nonce-before-mutation, full-only no-restore refund, fixed-scope recovery, transactional attribution, opaque denial) is preserved and test-pinned, and the schema is minimally additive with a pristine-guarded down path. Not plain PASS because R1–R3 are real Controlled-Live blockers requiring explicit Leo/GPT acceptance (durable freshness, authority bootstrap, full browser/a11y evidence). Per V2, PASS_WITH_RISK never auto-advances. Reviewer performed no patch and grants no final approval.
RETURN_TO: `foundation-advisor` · STOP.
