# 129 — Independent Zero-Data Visual Review

MISSION: `COSMILE_CORE_OPERATIONS_DASHBOARD_V1` · PHASE `ZERO_DATA_VISUAL_REVIEW` · TIER `HARD_IMPORTANT_SAFETY`
SESSION/MODEL/EFFORT/CWD: independent Foundation Reviewer session, idle before dispatch; **actual last observed session model directive is `claude-opus-5`**, effort max; CWD `/home/leo/Project/.worktrees/Cosmile/COSMILE_CORE_OPERATIONS_DASHBOARD_V1`. Handoff `128` names `Fable 5`/max and asserts "the existing Fable binding remains unchanged", and this dispatch supplied no live-binding verification — see P1. I cannot self-verify the serving model from runtime.
SKILL/REFS: `/home/leo/Project/skill/fable-sentinel/SKILL.md` + `contract-review`, `safety-review`, `provenance-review`, `review-classification` only. Agent Office operating model, `roles/reviewer.md` and `RESULT_REPORTING_PROTOCOL.md` are current as read (last changed `901a35d`, 2026-07-19, before this session).
PROVENANCE: docs HEAD `b4dbbc57` = dispatch pin ✓; handoff `128` blob `fc0e5d76`, sha256 `4d124197` (recorded — the dispatch supplied no expected values); handoff's own docs pin `59e671e5` is an ancestor of HEAD ✓. **Image `124` blob `d4d35292` ✓ and sha256 `63bc7d15` ✓ both match the handoff exactly.** Read-only inspection only; no patch, command, test, browser, runtime, network, DB, provider, commit or push.

## Six questions
1. **YES.** The image preserves the accepted shell (250px grouped evergreen rail with `주문` selected, `Console/Dashboard/Lab` switcher, `검토된 운영 정보만 표시`) and renders a successful zero read as a real operations page: eyebrow, visible `주문` H1, description, `합성 비프로덕션 데이터` badge, a three-part bounded summary, and a `주문 목록` card whose **three real column headers stay visible** while one contained panel says `확인할 주문이 없습니다`. It is neither a raw message nor a fabricated KPI, and no sample row is invented.
2. **YES.** Every element maps to fields that exist. Verified in source: `O1ConsoleFulfillmentRow` is exactly `{orderId, orderNo, dbStatus}`; the component renders `orderNo ?? "—"`, `orderStatusLabel(dbStatus)` and link text `공용 주문 상세 확인` to `/dashboard/requests/${encodeURIComponent(orderId)}` — which is precisely what contract §"Non-empty row proof" states, including `orderId` remaining only in the key and encoded href. Summary is scoped to the loaded list (`현재 조회된 목록 기준`), and the footer states raw values and internal identifiers are not shown.
3. **YES.** The filter is one labeled `주문 상태` select whose permitted labels mirror the existing mapper exactly (`결제 대기` … `확인할 수 없음`), is explicitly client-local over the already-loaded `dbStatus` rows with "no fetch, navigation, URL mutation, new read, or count request", and the contract orders it **omitted** if an implementation cannot keep it purely local. Note it is the one element needing a new client component (state only, no new read); omission is already authorized.
4. **YES.** Fulfillment reuses the same three fields with `출고` bound strictly to the existing `fulfilled → 출고 완료` label and shipment/carrier/tracking/dispatch/address explicitly excluded. For requests, `consoleQueueRowView` returns exactly `{orderNo, kindStatusLabel, requestedAtText, categoryBadge, href}` with `orderId` confined to the href/key and unknown values failing closed to `확인 필요`/HOLD — matching the contract's four visible columns and its ban on customer identity, request text, payment/refund/provider identifiers and actions.
5. **YES.** All three pages retain distinct flag-off `UNAVAILABLE`, root `DENIED`, route-specific per-read `DENIED`, read-failure `UNAVAILABLE` and `repository_error` handling, and the contract scopes itself to "after authorization and a successful read where `rows.length === 0`", leaving those paths unchanged.
6. **YES.** Breakpoints (`≥1024` / `761–1023` / `≤760` disclosure), a labeled 680px-minimum scroll region that never converts column meaning to unlabeled text, 200% zoom, semantic order, `role="status"`, `aria-live="polite"`, `scope="col"`, persistent filter label, 3px focus ring and reduced-motion are all specified. Nothing requires a new route, read, schema, command or capability.

## Blocking finding
- **B1 [safety/contract regression · image `124` + candidate `123`]** The rail reintroduces the promissory inert copy `준비 중`: `123` contains **8** occurrences (6× `준비 중`, 2× `집계 준비 중`) and **zero** occurrences of the accepted labels. This directly reverses correction **C1**, which was accepted at `112`, implemented at `96b363c7` as `NOT_IMPLEMENTED: "아직 구현되지 않음"` / `UNAVAILABLE: "집계 조회 계약 없음"`, and is now pinned by a permanent oracle — `expect(shell, "promissory copy must not return").not.toContain("준비 중")` — verified at `118`. Contract `122` never mentions the inert copy, so the change is unsanctioned rather than a deliberate revision; the artifact appears built from the pre-correction `108` lineage. Implemented as drawn it would re-imply capabilities that do not exist and fail an existing focused test. Smallest correction: restore the two implemented labels in `123`/`124` and add one contract line pinning them as shell-owned and non-promissory.

## Residual items
- **P1 [provenance]** The tier-`HARD_IMPORTANT_SAFETY` admission names `Fable 5`/max, but this dispatch verified no live binding and the last session directive I observed was `claude-opus-5`. Advisor should confirm or re-pin the binding; the visual findings above are unaffected by which model produced them, but the admission record should not stand unreconciled.
- **P2** The dispatch omitted handoff blob/sha256 pins; I verified the docs commit and path and recorded the actual values above.
- **P3** Korean legibility and layout are judged from the Designer's static render; no implemented surface was rendered, and the adjacent fulfillment/requests grammar has no mockup by design (contract-level only).

## Verdict
`NEEDS_PATCH` — the zero-data grammar itself is strong and safe: it keeps the real column structure visible, derives every field from `orderNo`, mapped `dbStatus` and the existing encoded detail destination, invents no customer, payment, shipment, tracking, amount, provider or internal-ID data, keeps zero distinct from denied, unavailable and repository failure, bounds the optional filter to the already-returned array with omission pre-authorized, and maps to fulfillment and requests within existing projections. One defect blocks acceptance as drawn: B1 reintroduces `준비 중`, reversing an already accepted, implemented and test-guarded truthfulness correction. It is narrow and patchable within scope; the rest of the candidate needs no rework. Reviewer performed no patch, accepts no risk and grants no approval.
RETURN_TO: `foundation-advisor` · STOP.
