# 95 — M5C Independent Delta Review

MISSION: `COSMILE_CORE_OPERATIONS_DASHBOARD_V1` · PASS `IMPLEMENTATION_REVIEW` · TIER `HARD_IMPORTANT_SAFETY`
MODEL/EFFORT/CWD: `Fable 5` / max, independently verified live binding; exact candidate CWD `/home/leo/Project/.worktrees/Cosmile/COSMILE_CORE_OPERATIONS_DASHBOARD_V1`. Independent Reviewer, separate from Worker/Advisor; read-only.
SKILL/REFS: `/fable-sentinel` + `delta-review`, `safety-review`, `provenance-review`, `review-classification` only. Current Agent Office role authority controls.
HANDOFF 94 VERIFIED: docs HEAD `5e5c8189` ✓ blob `b34ac0f1` ✓ sha256 `9c89c426` ✓.
REVIEWED DELTA: `c5b7edd7..fa90003d` — 1 commit, **exactly the 4 named paths**, +241 −178; candidate clean, HEAD==upstream, base is ancestor. Load-bearing context read: `orderStatusLabel` only. Pinned evidence: design `87_` @ `8e4f8cd7`, Worker result `92_` @ `0b72e6e7`. No test, build, typecheck, browser, DB, provider, runtime, network, mutation, commit or push by this review; prior cumulative review not repeated.

## Eight questions — verdicts (source-verified)
1. **YES.** The shell is now `fixed inset-0 flex flex-col`; the content row is `flex min-h-0 flex-1 lg:flex-row`; the rail carries `lg:h-full lg:w-[250px] lg:overflow-y-auto` and `main` alone carries `overflow-y-auto`. The prior single scroll container (`overflow-y-auto` on the outer div) is gone, so at `lg` the 11-row rail is stationary at 250px full height and cannot scroll away with Dashboard body scroll.
2. **YES.** The row list is `flex overflow-x-auto lg:block lg:overflow-visible` with `border-b … lg:border-b-0 lg:border-r`, so the horizontal strip exists only below `lg`. `SpaceSwitcher` (Console/Dashboard/Lab) is untouched in this delta and remains the sole top space switcher.
3. **YES.** All seven supported destinations are byte-identical (`/dashboard`, `/orders`, `/fulfillment`, `/requests`, `/finance`, `/activity`, `/settings`). Inert rows keep `aria-disabled`, no href and no handler, and now state their own truth as code + Korean label: Customers/Products/Payments & Refunds `NOT_IMPLEMENTED · 아직 구현되지 않음`, Inventory `UNAVAILABLE · 현재 조회할 수 없음` — matching `87_` §4.2 exactly and correcting the prior blanket "not implemented" for Inventory, whose real cause is the absent aggregate read contract.
4. **YES.** Root `dashboard.operations.read` still gates first, each per-read grant is re-authorized and re-checked against the root principal (`operatorRef !==` → denied), and every failure path (throw or unauthorized) fails closed. Reads remain exactly one each: request queue, `o1OperatorOrderList(50)`, reconciliation projection — M5C **merges** the former fulfillment state and the new recent-orders reuse into one `orderRead()` so no second order call exists. `DENIED` now renders the token with **no** fact at all (previously a copy string), which tightens rather than relaxes default-deny.
5. **YES.** `boundedRecentOrders` slices the already-returned rows to `slice(0, 3)` and maps only `orderNo` and `dbStatus`; the panel renders rows only when `orders.state.kind === "confirmed"`, otherwise the state panel alone. `dbStatus` is rendered through the existing closed `orderStatusLabel` map, which defaults unknown input to `확인할 수 없음` and invents no status. No customer, price, total, refund, payment, provider, date or derived field appears — matching `87_` §4.5.
6. **YES.** D04/D07 keep the exact frozen `UNAVAILABLE · 집계 조회 계약 없음` / `UNAVAILABLE · 조회 계약 없음` as static evidence gaps; counts appear only from a confirmed read (`stateForCount`), zero only when explicitly confirmed; the six-state token/label map remains single-sourced and every state renders as code + Korean text, never colour alone. No KPI, aggregate or silent zero is fabricated.
7. **YES.** No oracle was deleted or weakened: shell `it()` count 6 → 6, reads 9 → **10**. The two retitled reads tests track design-sanctioned changes (`PAGE_THESIS` → `PAGE_HEADING` per §4.3; the seven-field ledger row → three compact action rows per §2/§4.3 — `87_` explicitly directs "Do not repeat the seven headings … Authority and prohibition remain product rules, not dashboard ornament", with prohibition preserved at page level via `PAGE_NOTE`). A **new** oracle encodes §4.5: single read (`count(…o1OperatorOrderList\(50\)…) === 1`, labelled "no second order read"), `slice(0, 3)`, the two safe fields, confirmed-only rendering, and a forbidden-field sweep (`customerId`, `total`, `paidAt`, `provider`, `refundedAt`, `tokenHash`). Security/authority/Storefront-separation/no-mutation assertions remain in force in both files (13 and 14 matching oracles).
8. **YES — notably candid.** `92_` discloses that the frozen command ran **three** times, not two: RED `6 failed / 10 passed`, a first GREEN attempt at `1 failed / 15 passed` because an assertion the Worker had written still pinned D04/D07 to the removed `CardState` shape, then `16/16` after re-encoding that oracle onto the new static evidence-gap entries — stating explicitly that it pinned `id`, `name` and the exact frozen copy together (strictly stronger), that no product source was touched between those runs and that no expected value was lowered. It also discloses a fourth invocation that errored and ran nothing, that no typecheck/build/browser/DB/runtime ran, and that `scripts/o1_dashboard_reads.vitest.ts` sits outside both the path ceiling and the permitted command yet asserts against the rewritten home — verified statically, explicitly "not a run". Independent re-derivation of the re-encoded oracle and the run-count claim is consistent with the delta.

## Blocking findings
**None.**

## Residual risks (named; no acceptance offered)
- **R1 [regression risk, next gate]** Pre-existing `app/scripts/o1_dashboard_reads.vitest.ts` asserts against the rewritten `dashboard/page.tsx` but is outside the M5C ceiling and was **never run** this phase. The Worker mitigated by verifying every dependent string statically; that reduces but does not eliminate the risk. The next gate should run the ungated suite before acceptance.
- **R2** No typecheck or build ran, so type-level regressions in the two rewritten sources are unverified, and the new `orderStatusLabel` import into the home is source-verified only.
- **R3** `87_` §7 browser acceptance is unverified: 250px rail starting below the 88px chrome and stationary at 1440×900, 200% zoom, 390px without horizontal overflow, keyboard focus order, and Korean glyph rendering — the standing Korean-font limitation carries forward.
- **Nit, no behavioural impact:** rail persistence is achieved with a flex viewport column plus `lg:h-full` / per-pane `overflow-y-auto` rather than the contract's literal `calc(100dvh - 88px)`; equivalent in behaviour (rail stationary, only `main` scrolls), with exact pixel geometry falling under R3.

## Verdict
`PASS_WITH_RISK` — the delta does exactly what the pinned correction contract sanctions: it makes the desktop operations rail persistent and independently stationary, confines the horizontal strip to below desktop, tells the truth about each inert capability, and makes the home action-first while **preserving every authority and economic boundary** — same root/per-read capability order, same-principal checks, one read each, tightened default-deny, and a recent-orders panel that reuses the already-returned bounded result with two safe fields and no second call. Tests gained an oracle rather than losing one, and the Worker report is truthful about its extra run and its limits. Not plain PASS solely because R1–R3 are unclosed verification gaps — chiefly the unrun out-of-ceiling test that asserts against a rewritten file — which the next gate must close. No blocking finding, no patch required. Reviewer accepts no risk and grants no approval.
RETURN_TO: `foundation-advisor` · STOP.
