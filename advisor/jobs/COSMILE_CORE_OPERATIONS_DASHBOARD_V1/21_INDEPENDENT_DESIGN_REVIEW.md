# 21 — Independent Design Review (M1)

MISSION: `COSMILE_CORE_OPERATIONS_DASHBOARD_V1` · PHASE `M1_INDEPENDENT_DESIGN_REVIEW` · PASS `DESIGN_REVIEW` · TIER `NORMAL_COMPLEX_BOUNDED`
MODEL/EFFORT: `claude-opus-5` / max — Advisor mid-review model update, live-verified by Advisor (not self-verifiable from runtime; review began under the prior binding, all findings re-stated here from the same read-only evidence).
SKILL: `/home/leo/Project/skill/fable-sentinel/SKILL.md` + `review-classification`, `contract-review`, `delta-review` only. Independent Reviewer; no Designer/Worker role; read-only — no tests/build/typecheck/browser/DB/provider, no patch/commit/push, no redesign or broad audit.
LAUNCHER 20 VERIFIED: docs HEAD `74e22c97` ✓ blob `c0bb71f3` ✓ sha256 `0ce12478` ✓.
SUBJECT (snapshot-fixed): docs delta `8091e71b..0e29bbe9` = exactly the 4 designer files. All 4 blobs are **byte-identical** at candidate `0e29bbe9` and at launcher HEAD `74e22c97` (which only adds `20_`): contract `01e0f5ae`, mockup `a0a5d23b`, result `539dee06`, pointer `b476553b`. Docs worktree clean. Product `6486019e`, clean, unchanged — no product delta in this mission.

## Questions — verdicts (source-grounded)
1. **YES.** The collision premise is verified as-built, not invented: `layout.tsx:21` sets `isConsole = path.startsWith("/console")` as the *only* chrome exclusion, so `/dashboard` and `/lab` fall to the else-branch mounting `device/status/AppHeader/CategoryNav/MallTabs/ShippingPopup/CategoryDrawer/ProductCartFab` (`:32-49`). `OperatorShell.tsx:50-59` is a `fixed inset-0` neutral frame with `SpaceSwitcher` and **no left nav** — exactly as the contract states. Observation is labeled read-only/no-login, and missing Korean glyphs are explicitly *not* treated as copy evidence.
2. **YES.** §4 fixes the exact 11-row order (Dashboard, Orders, Customers, Products, Inventory, Fulfillment, Payments & Refunds, Support, Reconciliation, Audit, Settings); all 11 labels are present in the mockup. Home order §4 and acceptance #3 place truth strip → thesis → work ledger → blocked-work panel **before** summary facts; the KPI row/hero was removed in Pass 2.
3. **YES, with one gap (non-blocking).** The six states carry distinct Korean labels, screen-reader equivalents, meanings and action rules; priority is `DENIED → NOT_CONFIGURED → UNAVAILABLE → NOT_IMPLEMENTED → confirmed value` and "a runtime state never silently becomes zero" — fail-closed. **Gap:** `HOLD` is absent from the §5 priority chain although §6, §7 and acceptance #6 require HOLD to remove controls and the implemented classifier has a `hold` mode (`o1ConsoleView`); a row that is simultaneously HOLD and UNAVAILABLE has no stated precedence. Both outcomes remove controls and forbid mutation, so it fails safe either way — but the implementer must not have to choose. See F1.
4. **YES.** Audit is `NOT_IMPLEMENTED`/`UNAVAILABLE · 조회 계약 없음` with no number and no action (matches implemented `dashboard/activity`); aggregate inventory is `NOT_IMPLEMENTED` at nav with the order-level fact left in detail; no product/customer/revenue KPI anywhere. Unsupported nav rows are `aria-disabled=true`, no href, no click handler → no accidental action authority. Mockup shows **zero numerals presented as operational facts** (111 text nodes inspected).
5. **YES.** Console stays `대화·검토/계획/근거` with persistent `V0 MOCK · 실제 서비스 제어 없음` (matches as-built `console/page.tsx`) and is explicitly not returned to an operational Dashboard; Lab stays a read-only registry with no execution/approval/promotion/provider call — consistent with the Lab surface I independently verified at this same product HEAD (145: 31 links / 0 Lab-local controls).
6. **YES — sufficient and testable.** §9 fixes landmarks (one global-space `nav`, one operations `nav`, one `main`, one `h1`), `aria-current=page`, 44×44 targets, visible 2px focus ring + 2px offset, DOM order = visual order, state as **text** (English token + Korean label) with color supplemental, `role=alert` for denied/failure, one `aria-live=polite`, no two-axis scroll at 200%, Korean line-height ≥1.5, no 10px operational copy, and `prefers-reduced-motion` with a no-motion default. Acceptance #8 makes these pre-implementation-review gates.
7. **YES.** The mockup implements the contract: a 1920×1100 canvas holding the 1440×1000 desktop and 390-wide mobile frames; all 11 nav labels; the six state labels; truth strip `격리 비프로덕션 · 합성 데이터`; thesis `오늘 처리할 일`; legend `표현 규칙 · 운영 데이터 아님`. **No Storefront/device/promo/cart chrome and no bottom nav** (targeted greps for cart/장바구니/9:41/87%/bezel/bottom-nav/찜/카테고리 return 0; the single "KPI" hit is the banner `NO INVENTED KPI`). Examples are labeled non-data via `M1 DESIGN ONLY · NO LIVE DATA`, satisfying acceptance #5.
8. **YES.** §10 seams are bounded reuse of the substrate implemented at this HEAD: classify operator spaces in `layout.tsx` before mounting Storefront providers; add left nav + bounded mobile drawer to `OperatorShell` while retaining the switcher; replace the equal-card list in `dashboard/page.tsx` with the ledger while keeping current data contracts/states; reuse `O1ConsoleQueue`/`O1ConsoleFulfillment`/`O1ConsoleFinance`/`O1OperatorPanel` and the single `o1ConsoleView` source. Explicit: no new schema, endpoint, role, grant, provider integration, KPI, customer projection or action.

## Blocking findings
**None.**

## Findings (non-blocking; document-level)
- **F1 [contract · implementer-decidability]** `11_` §5 priority chain omits `HOLD` while §6/§7/§11.6 mandate HOLD behavior. Smallest correction: add HOLD to the §5 chain (predecessor placed it directly after DENIED) and state its precedence against UNAVAILABLE/NOT_CONFIGURED.
- **F2 [contract · source-of-truth]** The datum-state vocabulary is now declared in two places without a stated relationship: the predecessor reviewed IA contract (`…OPERATOR_AUTHORITY_DESIGN_V1/21_` §3.2) fixes a 7-state set (`NOT_COLLECTED`, `STALE`, `SYNTHETIC`, `UNVERIFIED`, different priority incl. HOLD) while `11_` §5 fixes a different 6-state set (adds `CONFIRMED`/`NOT_IMPLEMENTED`, drops the other four). Both would read as current at approval time. Smallest correction: one supersession/relationship line in `11_` naming the predecessor and the exact scope superseded. Note: the shipped code currently emits only 4 kinds (`confirmed`, `confirmed_zero`, `unavailable`, `denied`), so `NOT_CONFIGURED`/`NOT_IMPLEMENTED` are additive presentation states requiring implementation.
- **F3 [mockup ↔ contract copy]** Mockup renders UNAVAILABLE as `UNAVAILABLE · 조회할 수 없음` (contract: `현재 조회할 수 없음`) and the mobile notice as `모바일은 사실 확인만` (contract §8 gives the full two-sentence copy). Contract copy governs; align at implementation.

## Residual risks / limitations (no acceptance offered)
- **Korean-font visual validation remains open and is preserved as a finding** (not disproved by existing evidence): the inspection runtime had no Korean font, so glyph/kerning/200% appearance is unverified; §9 correctly makes a deployed Korean-glyph smoke test mandatory.
- The Designer disclosed that the SVG was **not reopened after authoring** (per Advisor prohibition), so no visual self-check exists. My inspection covers structure/copy/absence-of-chrome only, not rendered appearance.
- Public no-login Dashboard is default-denied, so no authorized runtime data view was observed; aggregate inventory and sensitive-activity list still have no read contract (both correctly rendered `NOT_IMPLEMENTED`/`UNAVAILABLE`).
- Launcher context-list note: `app/src/app/dashboard/orders/page.tsx` does not exist at this HEAD (dashboard contains `activity, finance, fulfillment, layout.tsx, page.tsx, requests, settings`). The contract correctly cites `/dashboard/requests`; no design impact.

## Verdict
`PASS_WITH_RISK` — the contract maps current source faithfully (collision premise verified at `layout.tsx:21`), fixes an exact 11-item Korean-first desktop IA with action/blocked work ahead of every summary fact, defines six distinct fail-closed states that never fabricate a zero, keeps unsupported audit/inventory/KPI reads inert and unauthorized, preserves Console and Lab semantics, sets a sufficient and testable a11y/responsive floor, and is implementable through bounded reuse with no schema, endpoint, grant or economic change. No blocking finding. Not plain PASS because F1/F2 are genuine document-level contract gaps that should be closed before implementation (an implementer could otherwise choose a HOLD precedence or the wrong state vocabulary), and the Korean-glyph validation limitation remains open by design. Reviewer performed no patch, accepts no risk, and grants no approval.
RETURN_TO: `foundation-advisor` · STOP.
