# M5E public browser acceptance checkpoint

MISSION: `COSMILE_CORE_OPERATIONS_DASHBOARD_V1`
STATUS: `PASS_WITH_REQUIRED_LEO_AUTHENTICATED_CHECKPOINT`

## Pins and runtime

- Product: `implementation/cosmile-core-operations-dashboard-v1-20260725@fa90003d0ca84b01bbfe0bfa7206b447b6c8d546`
- Product Git: clean/upstream-equal.
- Public non-production URL: `https://cosmile.leohan.net/dashboard`
- Owned runtime: exact mission app on loopback port `3000`; all process CWDs match the mission app.
- Previous `.next` quarantined under the protected mission runtime directory; clean current `.next/dev` regenerated by the single replacement runtime.

## Direct clean-profile browser evidence

| Viewport | Result |
|---|---|
| 1440×900 | HTTP 200; operations nav 11 rows; `x=0`, `width=250`; main `x=250`, `overflow-y=auto`; after synthetic browser-only main scroll `700px`, nav coordinates unchanged and window scroll remained 0 |
| 1024×900 | HTTP 200; same 250px stationary left rail and 11 rows |
| 390×844 | HTTP 200; same 11 rows in compact horizontal list; document width equals viewport width |

- Top space switcher: Console / Dashboard / Lab only.
- Inert truth: Inventory `UNAVAILABLE`; Customers/Products/Payments & Refunds `NOT_IMPLEMENTED`.
- Storefront chrome matches: 0.
- Browser console errors: 0; page errors: 0.
- Served Next client scripts: predecessor-worktree references 0; current-worktree reference present.
- Clean profile remained `DENIED`, proving default-deny and no authority bypass.
- Screenshot: protected owner-only runtime artifact `m5c-dashboard-public-desktop.png`, mode 0600.
- Browser residue: 0.

## Closed review residuals

- `o1_dashboard_reads.vitest.ts`: 47/47 PASS.
- Direct non-incremental TypeScript check: PASS, no diagnostics.
- Rendered route compilation and clean-client ownership: PASS through the public browser gate above.

## Required Leo checkpoint

Authenticated Dashboard content is not available to the clean-profile evidence session. Leo must use the existing authenticated operator browser session, hard-refresh the exact URL, and confirm:

1. the complete 11-row menu is a persistent 250px left sidebar;
2. it remains visible while the Dashboard body scrolls;
3. the home shows `지금 처리할 일`, `운영 요약`, and `최근 주문` rather than the old verbose ledger.

Local headless Chromium lacks a Korean-capable font and rendered tofu glyphs despite loaded font state. Korean copy presence is source/DOM-confirmed, but visual glyph acceptance remains Leo/browser-dependent.

No product/config/schema/DB/auth/capability/provider/economic mutation occurred in this gate. Mission is not closed before Leo confirms the authenticated presentation.
