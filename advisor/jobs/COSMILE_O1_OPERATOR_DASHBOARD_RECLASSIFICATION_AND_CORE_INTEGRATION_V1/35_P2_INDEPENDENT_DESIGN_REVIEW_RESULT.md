# P2 Independent Design Review — Result

MISSION_ID: `COSMILE_O1_OPERATOR_DASHBOARD_RECLASSIFICATION_AND_CORE_INTEGRATION_V1` · PHASE `P2_INDEPENDENT_DESIGN_REVIEW`
REVIEW_PASS: `DESIGN_REVIEW` · TIER `HARD_IMPORTANT_SAFETY` · ACTOR: independent Foundation Reviewer (Fable 5/max, fresh context)
SKILL: `/fable-sentinel` + review-classification, delta-review, safety-review, contract-review, provenance-review
HANDOFF 34 verified: docs commit `de07839`, blob `98160f8`, SHA256 `b1e78a84…` — all match; on-disk = committed blob.

## Reviewed subject (every read bound to these snapshots)
- Product worktree `…/.worktrees/Cosmile/COSMILE_O1_OPERATOR_DASHBOARD_RECLASSIFICATION_AND_CORE_INTEGRATION_V1`, branch `implementation/cosmile-o1-operator-dashboard-core-v1-20260723`, clean at candidate `33bf816` (base `1e2475a` is ancestor; exactly one commit; delta = exactly the 3 added files: 통합설계서.md + 데스크톱/모바일.svg — verified `diff --name-status`).
- Docs worktree at `de07839` (handoff-stated `eb9aa9b` is its parent). Supplemental 20/21/22/31/33 read in full; early P3 (33) treated as supplemental only.
- Load-bearing source verified at 33bf816: `api/o1/operator/{orders,orders/[orderId],refund,shipment,support,reconciliation}/route.ts`; `o1CommerceRuntime.ts` (allowlist :537 fail-closed, nonce single-registry :56-58, verifiers :583); `o1ReliabilityRuntime.ts` (count-only clamp :411-483); `o1LegacyLaneIsolation.ts` + `api/admin/orders/[orderId]/status/route.ts:22-31`; `O1OperatorPanel.tsx` (classifier :41, surface :55, legacy dual surface :241-246); `console/page.tsx:7-18`; `console/settings/page.tsx`; `ConsoleNav.tsx`.

## Required questions — verdicts
1. YES — one independent Korean-first Console (§0/§1.5); `/o1/operator` + legacy write/chat = transition/retirement evidence only; deletion/redirect explicitly excluded and STOP-gated (§0/§11).
2. YES — working scope = frozen O1 lanes only (§1.4 동작 column, §7); no general dashboard/WMS/finance/complete-Console product.
3. YES — all deferred domains are label-only `준비 중 · 동작 없음`, non-link/`aria-disabled`, zero route/form/action/sample/fake-live (§0, §1.4-1.5, §7, AC3-4); both SVGs comply (badge + no affordance).
4. YES — §2.4/§2.5 match verified server truth: refund = flag→allowlist→route-side single-use nonce consumption (refund/route.ts:30)→step-up ×2 of one grant→idempotency→audit fail-closed, full-only, `inventoryRestored:false`; support = bodyless, no step-up, non-economic; shipment = record-only; recovery = step-up/nonce + counts-only; lane isolation retained fail-closed (status/route.ts:28-31, NaN→owned); no client eligibility (§11 STOP); no new economic authority anywhere.
5. YES — §2.2 closed vocabulary is truthful/fail-closed (error never becomes 0, HOLD = control 0, no optimistic success, nonce_expired → secret clear + fresh reload); overview forbids fabricated KPI (AC4); mappings correspond to actual closed server outcomes.
6. YES — mobile = overview/queue triage only; every protected/complex mutation is desktop-only (§1.6-1.7, AC11); mobile SVG renders no mutation control and states the desktop boundary.
7. YES — 87 rows are the exact census ID space (S01-S08,N01-N14,L01-L02,W01-W12,D01-D19,A01-A10,T01-T04,O01-O18); tallies independently re-counted: 4+12+42+16+13=87 and 8+3+7+4+5=27, both matching the tables; 27 Worker rows 1:1 with 21_P1 matrix names/order. Candidate-local vocabulary (adds `transition`) is declared in §8, so divergence from P1 counts is the sanctioned reclassification, not drift. Semantic spot-checks consistent (O16 request-null refund retired; admin_order_status_api retired for O1 with isolation kept; no deferred domain maps to an active destination).
8. YES structurally — exact 1440×1024 / 390×844; coordinate arithmetic shows no overlap/clipping (desktop content ≤1412×992; mobile last card ends 756 < footer 776; 44px mobile targets); stylesheets carry the recorded refinement values (caps 11/small 12/caption 13; mobile chip `O1 확인`); no gradient/image/filter/external href; per-canvas `디자인 예시 · 실데이터 아님` + `<desc>`. Korean glyph/kerning/200% remain open for a Korean-font viewer (§4.2 honest limitation); all Korean copy verified in SVG source text.
9. PRESERVES — P3 principle (frontend over UNCHANGED O1 server contracts; no new API/schema/economic path; additive only) matches §0/§11; its single collision (WU-E) is surfaced as an explicit Advisor-decision STOP, not a silent conflict.
   a. Five active destinations JUSTIFIED inside the 9-item IA: each carries a distinct frozen truth/action family on distinct existing endpoints (orders = queue/detail + refund/support; fulfillment = reservation/HOLD + record-only shipment; finance = count-only recon + protected recovery; overview = composition of those two reads; settings = env/authority read-only). None mints data or authority; further collapse would mix protected action families in one screen against the one-action discipline.
   b. YES with bounds — `/console` root (verified legacy chat auto-redirect that also CREATES a conversation per visit, console/page.tsx:10-17) and legacy `/console/settings` (static; partly false vs O1 truth — "Shipping/Refund 미구현·Inventory 미구현" settings/page.tsx:16-18 while O1 has record-only shipment + reservation/commit/HOLD) may be repaired within minimum scope: repair deletes/redirects no legacy evidence (chat/jobs stay reachable at `/console/c/[id]`, `/console/jobs`), removes an unrequested DB write from the root edge, and removes false capability copy. Bound: execute as its own explicitly-approved WU (design §11 + P3 WU-E STOP already require this) and keep chat/artifact routes mounted as ACC deferred evidence.
   c. ACCEPTABLE with F2 — authority cannot be reimplemented client-side (all routes re-verify flag+allowlist(+nonce/step-up) server-side; worst-case client drift = truth defect refused 403/409), but the view-model drift channel must be closed by importing the exported pure functions, not re-typing them.
10. YES — delta contains zero runtime/schema files; P3 §7 model census at 33bf816 + 21_P1 `NONE_EXPECTED`; economic authority = existing reviewed routes exclusively; any needed change ⇒ STOP stands. Schema `NONE` and economic-authority `NONE` are supportable.

## Findings (material; severity · path · evidence · smallest bounded correction)
- F1 [contract/provenance · MEDIUM] Dual console-IA authority unaddressed: `설계자료/COSMILE_CONSOLE_IA_V2.md:13-29` (14-menu IA, named 정본 by `설계자료/COSMILE_콘솔_설계서.md:251`) ↔ 통합설계서 §1.4/§6 (9-IA, "ConsoleNav repair"); the candidate cites neither legacy doc (grep 0 hits). On approval both would read as current for the same `/console` IA — P11-type source-of-truth pollution at approval time. Correction: add a relation/supersession-plan block to 통합설계서 naming both files, and stamp `SUPERSEDED_BY` pointers on them at approval/integration.
- F2 [delta/truth-drift · MEDIUM] "reuse semantics, not the file" (§6 `O1OperatorPanel` row; P3 WU-A/B) is mechanically unpinned. Correction: require import (or verbatim extraction to one shared pure module) of `classifyOperatorRequestMode` / `operatorActionSurface` / closed vocabulary (`O1OperatorPanel.tsx:41,55`) as the single source, and state that the legacy dual-action surface (`O1OperatorPanel.tsx:241-246`) is excluded from the 주문·고객 지원 detail (design already retires O16).

## Non-blocking notes for Advisor integration
- WU-E overview/settings acceptance must mirror WU-B's server-side O1 allowlist re-check + §2.2 denied state (§2.5 already mandates it).
- Dual-session precondition (console shell session + Google operator identity via `getShopper`→`o1OperatorForCustomer`) is inherited unchanged; record it in WU-B/WU-E notes.
- Korean-font-viewer glyph/200% inspection remains the open visual residual (§4.2), including tight chip fits (e.g. desktop `비프로덕션 · TEST` badge).

## Excluded scope
No tests/build/typecheck/DB/runtime/provider/network, no patch/commit/push/dispatch, no broad repository audit (per handoff). Deferred-IA behavior, retirement/redirect execution, and implementation approval were not reviewed and are not granted. Temporary rendering: none performed (structural text review only; no artifacts created).

## Verdict
`PASS_WITH_CORRECTIONS` (V2 mapping: NEEDS_PATCH, document-level only). Rationale: all ten handoff questions close YES on independently verified source truth; no STOP condition exists (no safety-weakening path, no implementer-undecidable value, no join/enum breakdown; F1 pollutes only if approved unpatched). Product writes MAY proceed after Advisor integrates F1+F2 into the approved design / WU acceptance — except the `/console` root + `/console/settings` repurpose (WU-E), which additionally requires its already-designed explicit approval. Reviewer performed no patch and grants no final mission approval.
RETURN_TO: `foundation-advisor`
