# R3/R4 Toss TEST Runtime Execution — Cosmile Worker

MISSION_ID: `COSMILE_O1_TOSS_RELIABILITY_RUNTIME_VERIFICATION_V1`
MODULES: `R3_ACTUAL_TOSS_TEST` then `R4_INVARIANT_CLOSURE`
PRODUCT_HEAD: `c8c18b5cd0c4c01d8fdcbb1601265db3d6855cf3`
WORKTREE: `/home/leo/Project/.worktrees/Cosmile/COSMILE_O1_TOSS_RELIABILITY_RUNTIME_VERIFICATION_V1`
ACTOR: existing `cosmile:0.0` / Cosmile Worker / Opus 4.8 / xhigh
SKILL: `/fable-builder`; `implementation-execution`, `contract-to-code-mapping`, `test-design-before-code`; `implementation-report-template` only for compact return.
VERIFICATION: `DELTA_ONLY_VERIFICATION: REQUIRED`
PRODUCT_OR_SCHEMA_WRITE: `PROHIBITED`
PROVIDER: official Toss Payments V2 TEST only

## Accepted dependencies

- R2A controlled notification scenarios: PASS (`44_`/`45_`).
- R2B restart/lease/exhaustion: PASS (`36_`/`37_`). Do not rerun.
- R2C reviewed seam: Fable 5/max PASS, 0 blocking (`50_`/`51_`); reviewed real-boundary rerun PASS 17/17 (`52_`/`53_`). Do not rerun.
- R2D operator HTTP boundary: PASS 6/6 (`42_`/`43_`). Do not rerun.
- R3 admission: `BLOCKED_OWNER_ACTION` resolved. Durable TEST store now exists at `/home/leo/Project/Cosmile/.secrets/nonproduction/toss-test.env`; Advisor verified exact allowed names SET, regular non-symlink `leo:leo 0600`, parents `0700`, Git-excluded, and no mission-local secret copy.

## Exact evidence objective

Use exactly one official Toss TEST payment window to prove, with provider identifiers retained only in process memory:

1. the existing checkout creates one `action_required` intent with an unclaimed payment key;
2. the official V2 TEST SDK uses the existing `sandbox.paymentResult=SUCCESS` path without any card/wallet/bank credential;
3. before the browser success return reaches the app, the real `queryPaymentByOrderNo` recovery path observes the exact merchant-order/positive-KRW/currency binding and returns the expected non-terminal `confirming` category, with zero internal economic effect;
4. the captured success return then uses the existing server confirmation path to create exactly one succeeded capture and one paid order with committed inventory;
5. query-by-payment-key observes the bound `DONE` provider state;
6. one allowlisted synthetic operator request with a fresh nonce and process-local step-up secret performs exactly one existing full captured-payment TEST refund, with no partial amount and no new refund authority;
7. query-by-payment-key observes `CANCELED` with zero remaining balance; provider cancel evidence is exactly one;
8. capture replay is idempotent and refund-route nonce replay is refused, producing no second economic effect;
9. after an app restart on the same disposable DB, the order/refund/payment/inventory/customer/operator/reconciliation projections remain consistent;
10. one synthetic exhausted/received inbox plus `webhook_unverified` task remains visible but nonclaimable/nonreprocessed; an authorized reconciliation POST reports zero economic change.

Actual provider evidence and locally seeded/synthetic invariant evidence must be labelled separately. No claim that Toss itself delivered a webhook is allowed.

## Exact read ceiling

- `app/src/components/commerce/O1TossCheckout.tsx`
- `app/src/app/api/o1/checkout/start/route.ts`
- `app/src/app/api/o1/checkout/toss/success/route.ts`
- `app/src/app/api/o1/operator/orders/[orderId]/route.ts`
- `app/src/app/api/o1/operator/orders/[orderId]/refund/route.ts`
- `app/src/app/api/o1/operator/reconciliation/route.ts`
- `app/src/lib/runtime/o1NonprodConfig.ts`
- `app/src/lib/runtime/o1CommerceRuntime.ts`
- `app/src/lib/runtime/o1ReliabilityRuntime.ts`
- `app/src/lib/payment/{contracts,repository,service,tossV2,tossSandboxTransport,webhook}.ts`
- `app/src/lib/order/{contracts,repository,service}.ts`
- `app/src/lib/auth/{contracts,repository,session}.ts`
- `app/src/lib/{shopper,prisma,cart}.ts`
- `app/scripts/o1_nonprod_fixture_setup.vitest.ts`
- `app/prisma/schema.prisma` and exactly nine committed `migration.sql` files
- `app/vitest.config.ts`

No tracked file may be edited, generated, staged, committed, or pushed.

## Exact runtime and temporary paths

- ignored worktree root: `app/scripts/tmp/cosmile-o1-r3r4-provider-verification/` (`0700`);
- one driver: `r3r4_toss_test_provider.vitest.ts` (`0600`), using Node 24 built-in HTTP/WebSocket for loopback CDP; no browser-automation dependency;
- external root: `/home/leo/Project/.mission-tmp/COSMILE_O1_TOSS_RELIABILITY_RUNTIME_VERIFICATION_V1/r3r4/` (`0700`), containing only:
  - `foundation-bundle/` generated by the committed one-shot fixture boundary;
  - `chromium-profile/` generated by the browser and deleted at teardown.
- disposable DB container: `cosmile-o1-r3r4-pg-20260720`, `postgres:16-alpine`, `--pull=never`, tmpfs, synthetic credentials, bound only to `127.0.0.1:55445`;
- app: existing Next runtime only on `127.0.0.1:31081`;
- Chromium: existing `/home/leo/.cache/ms-playwright/chromium-1228/chrome-linux64/chrome`, headless, profile above, remote debugging only on `127.0.0.1:9223`;
- result: `/home/leo/Project/.mission-tmp/COSMILE_O1_TOSS_RELIABILITY_RUNTIME_VERIFICATION_V1/worker/56_R3_R4_TOSS_TEST_RESULT.md` (`0600`, at most 55 lines);
- pointer: `/home/leo/Project/.mission-tmp/COSMILE_O1_TOSS_RELIABILITY_RUNTIME_VERIFICATION_V1/worker/57_R3_R4_TOSS_TEST_POINTER.md` (`0600`).

No log, PID, trace, screenshot, video, HAR, downloaded file, browser profile outside the exact root, raw response, provider body, payment/order/customer/operator identifier, key, cookie, nonce, token, or secret artifact may be created or printed. Child stdout/stderr goes to `/dev/null`.

## Credential and network boundary

- Read the durable store into the approved child-process environment only; never print, hash, copy, rewrite, delete, or include its values in argv, logs, evidence, shell tracing, tmux, docs, or Git.
- Pre-network categorical gate: store remains regular/non-symlink `leo:leo 0600`; exact secret/client names SET once; prefixes are TEST; no unknown keys; `NODE_ENV` is not production; `O1_TOSS_MODE=test`; `O1_TOSS_SANDBOX_ONESHOT=1` is process-local only; `O1_TOSS_LOCAL_SUBSTITUTE` is absent.
- Allowed external origins only: `https://js.tosspayments.com/v2/standard` in Chromium and `https://api.tosspayments.com` through the existing bounded server transport. Everything else is loopback. No Google, tunnel, callback registration, public ingress, live key, real payment instrument, or provider console/contract action.
- Compare durable-store inode/size/mtime/owner/mode before/after internally and report only `UNCHANGED:YES`; do not hash it. Never delete it during cleanup.

## Frozen sequence and gates

1. Verify clean/upstream-equal head, exact prerequisite evidence, durable-store categorical readiness, existing local toolchain/Chromium, absent temp/container/ports, unchanged package/lock/schema.
2. Read only the ceiling. Freeze one driver that uses existing session/identity stores and synthetic Google-shaped customer/operator identities, existing fixture/catalog/cart/checkout/payment/refund/reconciliation boundaries, real Prisma repositories, and loopback CDP. Synthetic subjects/tokens/secrets and all provider/internal identifiers remain memory-only.
3. Create only the protected roots/driver. Perform read-only driver containment inspection and return `READY_TO_PROVIDER` before starting DB/app/browser or any external request. STOP if another file/dependency/public hostname/product change is needed.
4. After Advisor resume: start the disposable DB; apply exactly nine migrations; run the committed one-shot fixture once with its exact one-shot/preserve flags; create the minimum synthetic customer/operator/cart/session state through existing Prisma/auth/cart boundaries only.
5. Start the app with the durable TEST credentials injected only into its environment plus process-local non-secret gates: runtime and Google readiness ON, `O1_TOSS_MODE=test`, one-shot ON, local substitute absent, public base `http://127.0.0.1:31081`, external bundle, synthetic operator allowlist/step-up. No Google request.
6. Start loopback headless Chromium. Use CDP only to set the synthetic session cookie, click the existing TEST checkout button, and temporarily intercept the first local success navigation. Retain its parameters only in memory. No page content, URL, identifier, or provider response is logged.
7. Execute objectives 3–8 exactly once. The initial query-by-order is actual-provider evidence; the notification itself, if a local provider-shaped notification is needed to enter the existing intake boundary, is explicitly synthetic/untrusted. No second payment window.
8. Restart only the app once against the same DB and execute objectives 9–10. Use only synthetic/category-count ORM state for the exhausted visibility record. No direct SQL, new schema, or provider call for the exhausted record.
9. Capture only status/category/count/boolean evidence. Unconditionally stop the complete app/browser process trees; remove DB container, temp driver/root, bundle/browser profile, `.next`, attributable `next-env.d.ts`/tsbuildinfo; verify ports 55445/31081/9223 closed, container/process/temp absent, product clean/upstream-equal at unchanged head, package/lock/schema unchanged, and durable TEST store unchanged/preserved.

No install, copy/symlink dependency, generate, build, typecheck, broad/repository test, public exposure, Google, live payment, real PII, direct SQL, schema/migration change, product write, second payment, partial refund, cancellation/return feature, R2 rerun, or Reviewer in this Worker turn.

## STOP / verdict

STOP before provider access if localhost TEST return, CDP containment, existing SDK sandbox path, durable TEST store, or exact one-payment plan cannot be proven safe. STOP immediately for live/prod key/mode, real instrument request, unexpected origin, identifier/secret output, extra path, economic semantic change, provider ambiguity, or cleanup uncertainty.

Return `PASS | HOLD_WITH_NAMED_BLOCKERS`, actual-provider scenario matrix, synthetic R4 matrix, exactly-once counts, durable-store unchanged, cleanup, Git state, and `STOP`. Final cumulative independent review is a separate Advisor dispatch after PASS.
