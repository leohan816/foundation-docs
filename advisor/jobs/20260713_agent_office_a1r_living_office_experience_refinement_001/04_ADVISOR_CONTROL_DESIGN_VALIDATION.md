# Advisor Validation — Control Static Design Candidate

- Mission: `AGENT_OFFICE_A1R_LIVING_OFFICE_EXPERIENCE_REFINEMENT_001`
- Base: `58a484b088b4e57e7b3ee6e582f54c7f3ce6dc85`
- Control candidate: `d33dfc97a04077ded1a19c26d9806cb745166d73`
- Validation verdict: `NEEDS_NARROW_CONTROL_PATCH_BEFORE_INDEPENDENT_REVIEW`
- Scope: static design documents and mockup evidence only

## Evidence accepted

- The candidate is a direct child of the authorized base and is pushed to
  `origin/a1r/living-office-experience-refinement-001`.
- The exact diff contains 15 authorized `docs/**` paths and zero runtime,
  source, test, config, package, or dependency changes.
- All five SVG sources are paired with full-size PNG exports with readable
  Korean text.
- The full-office design materially corrects the original debug-card failure:
  two registry-derived Team Pods are separated, Agent Office remains a
  Foundation Team member, technical facts move to progressive disclosure,
  default relationship lines are absent, and the Advisor delivery shell is
  visibly disabled.
- Registry truth, authority boundaries, fail-closed unknown handling, full
  drawer facts, Technical Dashboard secondary status, and deferred operational
  authority remain explicit.

## Finding A1R-ADV-01 — deferred-batch mapping contradicts Founder mission

The Founder activation table requires:

- role-specific basic animation: implemented in A-1R;
- real runtime-state-driven animation: Batch B;
- dynamic Team Pod / actor admission: Batch C;
- real Advisor delivery: Batch D unless unchanged approved reuse is proven;
- live `current status` response: Batch D;
- Worker-to-Reviewer-to-Advisor movement: Batch D;
- real completion and handoff choreography: Batch D;
- operational Channy watchdog and bounded retry: Batch E;
- ambient sound: E-2.

The candidate instead defines DCR-01 as static presentation only, defers idle
role animation to Batch B, and places DCR-06, DCR-07, and DCR-08 in Batch B.
This is a direct design-contract mismatch, not an aesthetic preference.

Required correction:

- Design A-1R basic role animation for the later A-1R implementation, using
  only accepted fixture/evidence state and with neutral/reduced/static
  equivalents. This does not authorize live runtime-state integration.
- Keep real runtime-state-driven behavior in Batch B.
- Restore the exact C/D/E/E-2 batch mapping above.
- Keep `DELIVERY_DISABLED`; no runtime or implementation authorization is
  created by the documentation correction.

## Finding A1R-ADV-02 — static mockups contain avoidable spatial collisions

Direct inspection of the full-size PNGs found visible conflicts in:

- `a1r-advisor-team-pod.png`: actor labels intersect actor/monitor/desk
  silhouettes, the Foundation Advisor label is partially obscured by the
  Reviewer zone, and central labels compete for the same visual space.
- `a1r-full-office-mobile.png`: several right-aligned critical-state labels
  collide with or crowd actor names/roles at the 390px design width, most
  visibly Foundation Control and Independent Reviewer.

These conflict with the mission's design-review criterion of no obvious
spatial conflict/overlap and with the later zero-overlap contract.

Required correction:

- Re-layout only the affected mockups so actor, monitor, desk, name, role, Team
  identity, and critical state remain distinct and readable.
- Preserve all actors, Team memberships, role anchors, Mission Boards, Korean
  labels, and truthful state examples.
- Re-export only affected PNGs from the corrected SVGs and inspect them at
  original size.

## Proportional continuation

This is a pre-review correction so the independent Reviewer receives one
coherent candidate. Control must patch only the named documents/assets and
must not redo the full design or touch implementation. Advisor will inspect
the exact delta, then dispatch one independent design-contract review. If that
review finds a routine issue, only the affected section is patched and the same
Reviewer performs a narrow delta review.

