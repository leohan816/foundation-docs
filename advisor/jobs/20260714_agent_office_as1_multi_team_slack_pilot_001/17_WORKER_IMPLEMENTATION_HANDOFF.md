# Phase A Worker Implementation Handoff

TARGET_ACTOR: Agent Office Worker

TARGET_SESSION: `agent-office-opus`

MISSION_ID: `AGENT_OFFICE_AS1_MULTI_TEAM_SLACK_PILOT_001`

REQUIRED_SKILL: `/home/leo/Project/skill/fable-builder/SKILL.md`

MODEL_EFFORT: `Opus 4.8 / Ultracode` (verify directly before work)

## Exact authority

Read and execute:

`advisor/jobs/20260714_agent_office_as1_multi_team_slack_pilot_001/16_WORKER_IMPLEMENTATION_BRIEF.md`

Read the target repository's `AGENTS.md`, `CLAUDE.md`, required role/run/result
protocols, frozen reviewed design, independent PASS result, and Advisor design
acceptance directly. Historical reports are evidence, not scope.

Work only in:

`/home/leo/Project/.worktrees/agent-office/AGENT_OFFICE_AS1_MULTI_TEAM_SLACK_PILOT_001`

The exact parent must be:

`81a8c3474380a7e427516d6f5e57c97ad88c6c9b`

The branch must be:

`feature/as1-multi-team-slack-pilot-001`

## Execution

Implement all ten Phase A WorkUnits and only the exact allowed paths. Use
project-authored fakes for every Phase A Slack/tmux side effect. Preserve the
reviewed two-stage authority split, F01-D1 time semantics, fixed profile
lineage, Exact Delivery v2 compatibility, and default-disconnected state.

Do not access a real secret file, create an app/token/grant/lease, connect
Slack, send tmux input, use a real network, activate a pilot, or perform owner
setup. Do not create agents/subagents or delegate. Do not review your work.

Run the exact focused checks, correct routine defects within scope, write the
durable result and pointer in protocol order, commit and non-force push, return
the pointer to `agent-office-advisor`, and STOP.

If the pinned official Slack packages cannot satisfy the reviewed boundary
without a material security-model redesign, stop with the concrete blocker.
Ordinary code/test/type/lint failures are routine Worker patch work and are not
a reason to ask Leo.
