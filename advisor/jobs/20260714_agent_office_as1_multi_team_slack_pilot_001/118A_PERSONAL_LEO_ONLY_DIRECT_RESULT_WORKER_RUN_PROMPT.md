Activate `/fable-builder`, then execute the exact committed handoff
`118_PERSONAL_LEO_ONLY_DIRECT_RESULT_WORKER_HANDOFF.md` from its committed current-mission
authority revision.

Use the existing `agent-office-opus` Worker and current verified Opus 4.8 Max profile in
the named implementation worktree. Preserve the authorized uncommitted Behavior-1 delta
on remote baseline `a3f48d0` and the two pre-existing untracked R2 artifacts. Obey the
six-path allowlist and run only the two exact `-t` filtered tests, changed-file ESLint,
and exact diff-check. Return one complete committed/non-force-pushed candidate or one
concrete blocker to `agent-office-advisor`; then STOP.
