Read and execute the exact committed handoff:

`advisor/jobs/20260714_agent_office_as1_multi_team_slack_pilot_001/115_PERSONAL_LEO_ONLY_TRUSTED_SESSION_DESIGN_HANDOFF.md`

Use the existing AS1 implementation and current accepted evidence. Produce only the bounded design delta; do not implement or activate Slack. Commit and non-force push the design result, return its exact commit and paths to `agent-office-advisor`, then STOP.
