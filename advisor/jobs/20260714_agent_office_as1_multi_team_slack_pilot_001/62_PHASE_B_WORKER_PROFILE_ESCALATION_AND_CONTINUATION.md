# AS1 Phase B Worker Profile Escalation And Continuation

MISSION_ID: `AGENT_OFFICE_AS1_MULTI_TEAM_SLACK_PILOT_001`

ACTOR: `agent-office-advisor`

STATUS: `AUTHORIZED_CONTINUATION`

## Trigger

The Worker completed the exact bridge/lock dependency and its focused gates,
then explicitly stated that it could not complete the remaining approved paths
in the current xhigh pass and proposed committing partial progress. Task size or
context duration is not a mission STOP condition, and partial implementation is
not an accepted Phase B candidate.

This is treated as demonstrated profile insufficiency, not a transient tool or
environment failure. The offline matching toolchain is available and the
implemented bridge tests are green.

## Explicit profile dispatch

- `SELECTED_MODEL`: Opus 4.8
- `SELECTED_MODE`: Ultracode
- `SELECTED_EFFORT`: max
- `REQUIRED_SKILL`: `/fable-builder` remains loaded
- `WHY_NOT_LOWER`: xhigh was explicitly reported insufficient for completing
  the coupled reviewed implementation
- `WHY_NOT_HIGHER`: max is the next sufficient profile; no demonstrated max
  capability limitation justifies a higher profile
- `ESCALATION_TRIGGER`: only a concrete material security/authority conflict or
  demonstrated max capability insufficiency

## Continuation rule

Preserve the current uncommitted, green bridge/lock work. Do not commit or
return partial progress. Continue sequentially in the same Worker session and
same authorized worktree until all 14 implementation paths are coherently
wired, the focused gates pass, and the two evidence-only results are committed
and pushed. No subagent, new session, scope expansion, secret access, live
Slack, live tmux mutation, or pilot activation is authorized.

NEXT_ACTOR: `agent-office-opus`

RETURN_TO: `agent-office-advisor`
