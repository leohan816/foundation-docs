Activate `/fable-builder`, then execute the exact committed handoff
`121_PERSONAL_SLACK_FIXED_ANSWER_COMMAND_WORKER_HANDOFF.md` from its committed
current-mission authority revision. Use the existing `agent-office-opus` Worker
and current verified profile. Change only the two allowed paths, run only the
named focused gate, commit and non-force push one complete candidate, and stop.
