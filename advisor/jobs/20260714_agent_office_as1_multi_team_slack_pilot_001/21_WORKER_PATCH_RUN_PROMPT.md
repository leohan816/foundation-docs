Use `/fable-builder` and execute the exact committed patch handoff:

`advisor/jobs/20260714_agent_office_as1_multi_team_slack_pilot_001/21_ADVISOR_IMPLEMENTATION_PATCH_HANDOFF.md`

Mission: `AGENT_OFFICE_AS1_MULTI_TEAM_SLACK_PILOT_001`

Work only in the existing authorized worktree and branch named by the handoff.
Verify the live session/model/effort, skill hash, clean/upstream-equal HEAD, and
no conflicting writer before editing. Read the immutable independent
`NEEDS_PATCH` result directly and repair every `B01`-`B09` finding. Do not rely
on this prompt as a summary of scope.

Continue through routine code/test/doc corrections without asking Leo or the
Advisor. Stop only for the handoff's exact material SDK/design blocker or an
unsafe coordinate mismatch. Do not use agents/subagents, real Slack, secrets,
real tmux mutation, owner setup, or any unlisted file. Commit and non-force push
the repaired source, corrected Worker result, and pointer in protocol order;
return the exact pointer to `agent-office-advisor`; then STOP.
