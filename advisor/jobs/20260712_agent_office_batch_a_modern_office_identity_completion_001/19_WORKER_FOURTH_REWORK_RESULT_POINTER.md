# Worker Fourth Rework Result Pointer (A4-1, A4-2, A4-3)

```text
WORKER_FOURTH_REWORK_RESULT_POINTER
MISSION_ID: AGENT_OFFICE_BATCH_A_MODERN_OFFICE_IDENTITY_COMPLETION_001
ACTOR: Agent Office Worker
RESULT_FILE: /home/leo/Project/foundation-docs/runs/agent-office/20260712_agent_office_batch_a_modern_office_identity_completion_001/WORKER_FOURTH_REWORK_RESULT.md
FOUNDATION_DOCS_COMMIT: a05bdfcafdf641b27e03c4ddf318a3640fcba7fd
TARGET_REPO: /home/leo/Project/agent-office-batch-a-001
TARGET_BRANCH: batch-a/modern-office-identity-001
REVIEWED_BASE / PRIOR_CANDIDATE: fcd55a2df04aa14284fceaab12c653492edf22f2
FOURTH_REWORK_COMMIT: 43107b9c087a5d172d5f670e6b01bd75ab9ac1db
PUSH_STATUS: non-force pushed to origin/batch-a/modern-office-identity-001 (fcd55a2..43107b9, fast-forward); local == upstream
RETURN_TO: Advisor
PROPOSED_NEXT_ACTOR: Advisor
STOP
```

Summary: Sentinel second delta re-review `NEEDS_PATCH` (A4-1/A4-2/A4-3) closed within the exact `09N`
scope plus test-exactness addendum `61`; the accepted 100% composition, A3-1, I2-1/I2-3/I2-4, the
17-field drawer, strict CSP/Pixi lazy/non-blank boundary, and every authority/security boundary are
preserved (**no baseline changed**). **A4-1** — at high root text (≥ ~1.3×, where the ≤22% Office bound
would break) the overlay switches to an explicit, production-observable roster-equivalent mode: a `10rem`
probe measured **pre-paint by a `useLayoutEffect`** (so an initial high-text mount never paints the ~31%
card wall) plus a `ResizeObserver`, an explicit `data-office-label-mode` marker, zero partial on-canvas
labels (never 1..N-1), and every actor's complete first layer (incl. Team) in the always-visible roster;
reduced/static/keyboard/screen-reader/forced-colors equivalent, no work/authority inferred. **A4-2** — one
shared combined predicate binds the desktop gate to the exact visible actor set (`data-actor-label-count`):
exactly one nine-field, sourced, Team-bearing label per actor within the coverage bound, and a negative
challenge that hides **exactly one** label to fail that same predicate while union coverage alone still
passes (coverage never sufficient); the 200% state requires the marker, zero partial labels, exact
visible-actor↔`data-actor-roster` id set equality, and one complete row per actor (role, name, seven
sourced facts incl. Team). **A4-3** — the accessible label name announces the full source for every compact
fact (Team, process, AI identity, model, effort, AI runtime, operational state), with a unit assertion that
fails on any omission. Gates green: `npm run check` (lint+typecheck+`npm test` **632/632**/build), CD-3 6/6,
Living Office 3/3, composed 3/3 (deterministic), prototype 20/20 byte-identical, demo 43/23, fresh build +
fail-closed loopback rehearsal. No baseline changed; forbidden paths untouched; no suppressions; no Grok;
no agents; no self-review/approval; no force push. Fresh unmasked 100%/200%/mobile/static pixels inspected.
Independent same-session Sentinel delta re-review, Advisor audit, and Leo/GPT approval remain external.
