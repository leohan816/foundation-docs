# Advisor Validation - Sentinel PRC Delta Re-Review

## Verdict

`ACCEPT_NEEDS_PATCH__ROUTE_FIVE_EXACT_CORRECTIONS_TO_SAME_CONTROL`

The same Sentinel returned evidence-backed `NEEDS_PATCH`. PRC-2, PRC-3, and
PRC-4 are closed. The remaining five corrections are technical contract details
inside the approved scope and require no Founder decision.

## Remaining Corrections

1. **PRC-1:** describe `aiRuntimeState` as the existing projector's RT + (B)
   arbitration and preserve the actual per-result provenance rules.
2. **PRC-5:** make project key, current actor ordering, responsible Advisor
   validity/non-render behavior, selection fallback, and literal viewport/clock/
   camera defaults total.
3. **PRC-6:** separate the four-key raw `LivingOfficePresentationV1` parser from
   the complete later wrapper parser; every wrapper field must be declared once.
4. **PRC-7:** define the literal `LivingOfficeStructuralProjection` interface
   and the exact production caller passing it to overlay/mirror/HUD.
5. **PRC-8:** use installed Vite 8.1.4/Rolldown 1.1.5 output types and one exact
   `write:false` JS-API invocation; traverse returned chunk module/import edges.

## Preserved Closures

- no second operational progress truth;
- cues remain exactly empty;
- diagnostics never create motion;
- owned sentinels and duplicate identity handling remain closed;
- all existing product/accessibility/security/authority/fallback/Channy/
  rollback/no-Grok/excluded-session/Batch B-E boundaries remain unchanged.

## Routing

- next actor: same `foundation-control` session
- after exact patch: Advisor diff validation and same Sentinel narrow re-review
- Worker: remains stopped clean
- Leo/GPT: no action

