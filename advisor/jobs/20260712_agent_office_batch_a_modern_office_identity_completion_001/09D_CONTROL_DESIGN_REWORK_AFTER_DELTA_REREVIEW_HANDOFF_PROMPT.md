TARGET_ACTOR: Control-Rework
TARGET_SESSION: same existing foundation-control session
SOURCE_ADVISOR_JOB: ../foundation-docs/advisor/jobs/20260712_agent_office_batch_a_modern_office_identity_completion_001
DO_NOT_PASTE_INTO: Advisor session
RETURN_RESULT_TO: Advisor
GPT_DIRECT_USE: inspect only

# Control Design Rework After Sentinel Delta NEEDS_PATCH

Read directly:

- `19_ADVISOR_SENTINEL_DELTA_REREVIEW_VALIDATION.md`
- `/home/leo/Project/foundation-docs/runs/agent-office/20260712_agent_office_batch_a_modern_office_identity_completion_001/SENTINEL_DESIGN_DELTA_REREVIEW_RESULT.md`
- every current Agent Office source file cited by R1-R4.

Patch R1-R4 only in the exact same four design-document paths. Resolve the
semantics from actual source rather than preserving the previous closure claim.
The patch must provide a distinct unknown process value, evidence-exact runtime
states, a complete non-elevating activity/state projection, implementable static
versus dynamic ownership with one exact join operation, and an exact closed file
proposal without globs.

Preserve the accepted design and the already-closed PWA/renderer failure matrix.
Do not modify runtime source, tests, package/config, media, auth, transport, PWA
code, DB, secrets, remote/public state, production state, or Batch B-E. Use no
agents, sub-agents, delegated contexts, or new sessions. Do not self-review.

Update the existing Control result and pointer with the new design commit,
changed sections, direct-read evidence, and an R1-R4 closure table. Commit and
non-force push exact paths only. Return to Advisor and stop.
