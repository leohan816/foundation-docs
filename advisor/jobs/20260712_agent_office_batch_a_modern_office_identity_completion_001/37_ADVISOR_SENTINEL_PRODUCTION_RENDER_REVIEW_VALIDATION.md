# Advisor Validation - Sentinel Production Render Contract Review

## Verdict

`ACCEPT_NEEDS_PATCH__ROUTE_CONSOLIDATED_PRC1_PRC8_TO_SAME_CONTROL`

The independent Sentinel verdict `NEEDS_PATCH` is evidence-backed. All eight
findings are technical documentation-contract defects patchable inside the
approved Batch A scope. No new Founder product, authority, security, or risk
decision is required. Worker implementation remains stopped.

## Accepted Findings

1. **PRC-1:** registry-carried `advisorTeam` was mislabeled RT-owned, and several
   presentation-only fields have no literal registry field or closed mapping.
2. **PRC-2:** committed WorkUnit-plan progress would create a second operational
   truth, violating S3.
3. **PRC-3:** diagnostics and unavailable evidence cannot construct
   `PixelCueInput`; no cue is safer than fabricated activity.
4. **PRC-4:** output sentinels are wrong and first-wins duplicate actor identity
   is order-dependent and weaker than the existing fail-closed registry rule.
5. **PRC-5:** the committed visual layout contract is not complete enough to
   build required pod/project/membership/Advisor/selection fields.
6. **PRC-6:** the parser is not attached to `client.ts:parseProjection`, the
   actual untrusted JSON boundary, and nested validation/defaults are incomplete.
7. **PRC-7:** active text both edits and does not edit `frame-projector.ts`; DOM
   overlay/mirror props remain prototype-typed with no adapter/extraction plan.
8. **PRC-8:** manifest/module evidence and runtime-validator test path are not
   exact under the closed scope.

## Patch Direction

- Preserve per-field provenance: transport inside the runtime envelope does not
  change registry authority.
- Use only the existing runtime manifest/projection for WorkUnit/gate progress;
  if no exact existing field is available, display a fail-closed value rather
  than adding a committed operational catalog.
- Use an exact accepted structured cue source already present in current runtime
  contracts, with field-for-field mapping, or emit `cues=[]`. Diagnostics never
  create work/routing/review motion.
- Normalize invalid outputs to their actual owned sentinels and reject all rows
  for duplicate actor IDs or hard-fail the input deterministically.
- Define every committed visual layout field, closed mapping, canonical ordering,
  responsible-Advisor rule, membership rule, and literal visual default without
  operational-state ownership.
- Validate raw `livingOffice` at `client.ts:parseProjection` before storing it,
  then validate the composed render input before lazy renderer entry.
- State consistently that `frame-projector.ts` is behavior-preservingly edited
  for core extraction. Define fixture-free structural props/adapters for overlay,
  mirror, and HUD while leaving `prototype-entry.tsx` unchanged.
- Define exact Vite JS API build output/module-map/import-edge evidence and one
  literal validator test path. Remove every “or sibling” escape.

## Routing

- next actor: same `foundation-control` session
- after patch: Advisor exact-diff validation, then same `foundation-reviewer-sol`
  delta re-review
- Worker: stopped clean; do not resume
- Leo/GPT: no action required

