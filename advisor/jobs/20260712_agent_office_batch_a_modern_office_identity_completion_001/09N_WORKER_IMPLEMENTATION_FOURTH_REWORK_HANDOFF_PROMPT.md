TARGET_ACTOR: Worker-Rework
TARGET_SESSION: separate role session, never Advisor session
SOURCE_ADVISOR_JOB: ../foundation-docs/advisor/jobs/20260712_agent_office_batch_a_modern_office_identity_completion_001
DO_NOT_PASTE_INTO: Advisor session
RETURN_RESULT_TO: Advisor
GPT_DIRECT_USE: inspect only, do not execute unless acting as the target session

This instruction goes to: same existing `agent-office-opus` Worker session
Do not paste into: Advisor, Control, Reviewer, or GPT strategy session
Return result to: Advisor

# Agent Office Batch A — Focused Fourth Implementation Rework

Use `/fable-builder` in the existing `agent-office-opus` session.

- Mission: `AGENT_OFFICE_BATCH_A_MODERN_OFFICE_IDENTITY_COMPLETION_001`
- Mode: routine bounded A3 implementation/test/as-built correction
- Repo/worktree: `/home/leo/Project/agent-office-batch-a-001`
- Branch: `batch-a/modern-office-identity-001`
- Required clean/upstream-equal base: `fcd55a2df04aa14284fceaab12c653492edf22f2`

Use no agent, sub-agent, delegated context, or temporary session. Do not
redesign from zero, self-review, accept risk, approve, or start Batch B-E.

## Read Directly

1. `/home/leo/Project/foundation-docs/runs/agent-office/20260712_agent_office_batch_a_modern_office_identity_completion_001/SENTINEL_IMPLEMENTATION_SECOND_DELTA_REREVIEW_RESULT.md`
2. `/home/leo/Project/foundation-docs/advisor/jobs/20260712_agent_office_batch_a_modern_office_identity_completion_001/60_ADVISOR_SENTINEL_SECOND_DELTA_VALIDATION.md`
3. current contract §2.7 and the exact `1187b9a..fcd55a2` implementation

Verify actual Opus 4.8 Ultracode, `/fable-builder`, workspace, clean branch and
upstream, no concurrent writer, no Grok reuse, and no excluded `agent-office`
input before editing.

## A4-1 — 200-Percent Office-First Equivalent

Preserve the accepted normal 100% composition exactly unless a coupled fix is
necessary. At 200% text, the current eight cards cover 31.4159% of the office
and again become the primary wall.

Implement one production-truthful solution:

- either keep every visible actor's on-canvas label and satisfy the same <=22%
  Office coverage/association/Team/source contract at 200%; or
- switch at the real high-text accessibility state to an explicit, deterministic
  semantic/roster-equivalent mode in which partial canvas labels are forbidden,
  every visible actor's complete first layer remains readable and reachable,
  and the Office remains primary.

If using an equivalent mode, it must be triggered by production-observable text
scale/accessibility state, not a test-only class or fixture. It must have an
explicit DOM state marker, work at initial load and after a text-scale change,
remain static/reduced-motion/keyboard/screen-reader equivalent, and never infer
work or authority. A state with 1..N-1 labels is invalid; use either all N labels
or the explicit complete equivalent.

Rerun the same meaningful Office/cardinality/Team/source/association assertions
after setting 200% text. Inspect fresh unmasked 100%, 200%, mobile, and static
pixels directly.

## A4-2 — Bind Gates to the Exact Visible Actor Set

For normal production desktop mode:

1. expected actor ids come from the visible production actor set / existing
   `data-actor-label-count` contract;
2. require exactly one visible in-viewport label for every expected actor id;
3. require all nine first-layer fields and a source indication on every label;
4. run readability, overlap, coverage, Team, and association checks against the
   exact same complete set;
5. add a deterministic negative challenge proving that hiding/removing any one
   label fails the cardinality/Office predicate even if the roster remains full.

For an explicit 200% semantic-equivalent mode, require zero partial canvas
labels and exactly one complete roster/equivalent row with all first-layer facts
for every visible actor. The roster is additional evidence at normal desktop,
not a substitute for a missing normal-mode card.

The coverage threshold alone must never be sufficient.

## A4-3 — Exact Accessible Source Contract

Make `actorLabelAccessibleName` include full source names for **every** compact
fact, including model, effort, AI runtime, and operational state. Keep Team,
process, and AI identity sources. Add exact unit/browser assertions that fail if
any full source is omitted. Do not weaken or merely rewrite the as-built claim.

## Allowed Scope

Only as required for A4-1 through A4-3:

- `src/ui/pixel/living-office-actor-overlay.tsx`
- `src/ui/pixel/living-office.css`
- directly coupled existing actor-summary/overlay/Living Office browser tests;
- the two authorized Living Office baseline files if fresh evidence changes
  them;
- directly affected Batch A as-built contract/design/plan/index text.

If production text-scale observation genuinely requires one additional existing
UI helper/source path, stop before editing and return the exact path and reason
to Advisor. Do not silently broaden scope.

## Preserve and Forbid

Preserve A3-1, accepted 100% pixels, I2-1/I2-3/I2-4, 17-field drawer/focus,
strict CSP/Pixi lazy/nonblank boundary, mobile, forced-colors, reduced/static,
prototype, M1 fallback, auth/security/authority, and no-Batch-B behavior.

Forbidden: partial hidden desktop facts, test-only production behavior, sub-10px
text, color/hover-only meaning, stale/masked evidence, assertion weakening,
threshold-only proof, skips, config/dependency/package/lock changes,
suppression/`eslint-disable`/`@ts-ignore`/`@ts-expect-error`, Grok/excluded
session input, agents/sub-agents, force push, protected/main, auth/DB/remote/
prod/Hermes/Batch B-E work.

## Delta-First Gates

During iteration run only focused actor-summary/overlay and authenticated Living
Office 100/200/mobile/static checks. Once stable, run affected composed and
prototype boundaries. Run the complete candidate gate once before return
because the shared production UI/accessibility path changed. Report every
failure and resolution honestly.

At minimum return:

- exact normal/equivalent cardinality and negative hidden-label proof;
- 100% and 200% coverage/equivalence/Team/source/association results;
- focused unit/browser/accessibility results and fresh inspected pixels;
- final `npm run check` once, affected composed/prototype/browser gates, build
  and loopback rehearsal as required by Batch A;
- clean `git diff --check`, scope/suppression checks, output cleanup, non-force
  push, local/upstream equality, and rollback.

## Result Contract

Write:

`/home/leo/Project/foundation-docs/runs/agent-office/20260712_agent_office_batch_a_modern_office_identity_completion_001/WORKER_FOURTH_REWORK_RESULT.md`

Pointer:

`/home/leo/Project/foundation-docs/advisor/jobs/20260712_agent_office_batch_a_modern_office_identity_completion_001/19_WORKER_FOURTH_REWORK_RESULT_POINTER.md`

Commit and non-force push the candidate branch. Commit/push only the exact
foundation-docs result/pointer with explicit path staging, preserve unrelated
dirt, return the short pointer to Advisor, and STOP.

