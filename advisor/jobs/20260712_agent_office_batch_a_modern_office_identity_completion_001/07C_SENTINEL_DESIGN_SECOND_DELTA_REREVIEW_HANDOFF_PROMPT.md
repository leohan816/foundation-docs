TARGET_ACTOR: Sentinel-ReReview
TARGET_SESSION: same existing independent Sentinel session
SOURCE_ADVISOR_JOB: ../foundation-docs/advisor/jobs/20260712_agent_office_batch_a_modern_office_identity_completion_001
DO_NOT_PASTE_INTO: Advisor session
RETURN_RESULT_TO: Advisor
GPT_DIRECT_USE: inspect only, do not execute unless acting as the target session

이 지시문을 붙여넣을 대상: 같은 독립 Sentinel 세션
이 지시문을 붙여넣으면 안 되는 곳: Advisor 세션 / GPT 전략 세션
작업 결과 반환 대상: Advisor

# Sentinel Second Design Delta Re-review

Use `/fable-sentinel`. Reload the role boundary and delta-review, contract,
provenance, and review-classification references. Remain read-only and
independent. Do not trust Control or Advisor closure claims.

Review exact delta:

`60a5a720ca52ffdc3805318b4c40a61d0199b4b1..77681d9ed5dae3567115082945508f8474308812`

Read directly:

- `SENTINEL_DESIGN_DELTA_REREVIEW_RESULT.md` from the mission result path;
- `19_ADVISOR_SENTINEL_DELTA_REREVIEW_VALIDATION.md`;
- `20_ADVISOR_CONTROL_SECOND_PATCH_VALIDATION.md`;
- the four exact before/after design paths;
- every current source file cited by the patch.

Verify:

1. R1: unknown process evidence is distinct from proven offline/no-process and
   every positive runtime value has implementable accepted evidence. Attached
   state, name, timestamp, proximity, or prose must not prove readiness/work.
2. R2: operational display is a complete non-elevating function of the current
   accepted projector result. Check every current WorkUnit state and every
   `ROLE_ACTIVITIES` member through the actual source behavior.
3. R3: fact ownership, mint/validate/project/join order, full-outer operation,
   registry-only/evidence-only behavior, provenance, conflict handling, and
   no-store-back rule are implementable and non-contradictory.
4. R4: the file proposal is genuinely closed. Specifically challenge references
   to assets “named by the handoff”, baseline directories, wildcard-pattern test
   names, conditional scripts, and any unnamed file class. Decide whether an
   implementation Worker could edit a path not enumerated in the canonical
   design without first returning to Advisor.
5. No regression in previously accepted Office-first, secondary-view,
   eager-shell, auth, authority, security, transport, accessibility, fallback,
   Channy, rollback, no-Grok-reuse, excluded-session, or Batch B-E boundaries.
6. The exact delta contains documentation only and does not authorize Worker
   implementation or final approval.

Verdict contract: `PASS`, `PASS_WITH_RISK`, `NEEDS_PATCH`, or `FAIL`.

Write the result to:

`../foundation-docs/runs/agent-office/20260712_agent_office_batch_a_modern_office_identity_completion_001/SENTINEL_DESIGN_SECOND_DELTA_REREVIEW_RESULT.md`

Write the pointer to:

`../foundation-docs/advisor/jobs/20260712_agent_office_batch_a_modern_office_identity_completion_001/20_SENTINEL_DESIGN_SECOND_DELTA_REREVIEW_RESULT_POINTER.md`

Commit and non-force push only the exact result and pointer. Do not patch Agent
Office or Advisor artifacts. Return to Advisor and stop.
