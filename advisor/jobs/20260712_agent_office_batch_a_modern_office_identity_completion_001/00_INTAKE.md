# Batch A Intake

Mission: `AGENT_OFFICE_BATCH_A_MODERN_OFFICE_IDENTITY_COMPLETION_001`

Decision: `PROCEED_WITH_LIMITS`

The Founder explicitly authorizes a private/local runnable Agent Office Batch A
implementation. The reviewed living pixel-office candidate at
`ac8ba75d3a128385beaeeac58ae5bf54c03d23f2` is the exact implementation base.
Batch A must integrate that candidate into the real application shell; it may
not close as documentation-only or prototype-only work.

## Actor train

1. `foundation-advisor`: Advisor, GPT-5.6 SOL xhigh.
2. `foundation-control`: bounded `CONTROL_MASTER_DESIGN_MODE` delta only.
3. `agent-office-opus`: implementation Worker, Opus 4.8 Ultracode,
   `/fable-builder` required.
4. `foundation-reviewer-sol`: independent Sentinel, GPT-5.6 SOL xhigh.
5. Leo/GPT: final approval only.

## Verified entry state

- Product base and upstream:
  `shadow/agent-office-m1-2-spatial-office` at `ac8ba75`, equal to origin.
- Clean isolated worktree:
  `/home/leo/Project/agent-office-batch-a-001`.
- Implementation branch:
  `batch-a/modern-office-identity-001` at `ac8ba75`.
- The original checkout's pre-existing untracked `.grok/`, `grok-max`, `grokx`,
  and `grokx-max` are excluded and untouched.
- The failed Grok pilot branch and worktree no longer exist; its audit and stop
  record remain forensic evidence in Foundation Docs. No Grok code may be
  recreated, copied, cherry-picked, adapted, or used as behavior evidence.
- `agent-office-opus` was live-verified as Claude Code, Opus 4.8 (1M context),
  Ultracode, idle, before dispatch preparation.
- `foundation-reviewer-sol` was live-verified as Codex GPT-5.6 SOL xhigh, idle,
  in `/home/leo/Project/foundation-reviewer`.
- The excluded historical Agent Office Codex context receives no input.

## Hard boundaries

No DB, schema, migration, secret, new auth mode, public/remote exposure,
production, Hermes, arbitrary shell endpoint, browser-direct Worker/Reviewer
dispatch, automatic tmux discovery, Batch B-E, protected-branch merge, force
push, new session, agent, sub-agent, or delegated context.

