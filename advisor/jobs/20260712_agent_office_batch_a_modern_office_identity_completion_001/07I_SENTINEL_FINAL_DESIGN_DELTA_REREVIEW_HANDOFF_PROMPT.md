TARGET_ACTOR: Sentinel-ReReview
TARGET_SESSION: separate role session, never Advisor session
SOURCE_ADVISOR_JOB: ../foundation-docs/advisor/jobs/20260712_agent_office_batch_a_modern_office_identity_completion_001
DO_NOT_PASTE_INTO: Advisor session
RETURN_RESULT_TO: Advisor
GPT_DIRECT_USE: inspect only, do not execute unless acting as the target session

This instruction goes to: same existing Foundation Reviewer SOL session
Do not paste into: Advisor, Control, Worker, or GPT strategy session
Return result to: Advisor

# Sentinel Final Design Delta Re-Review

Required skill: `/fable-sentinel`

Mission: `AGENT_OFFICE_BATCH_A_MODERN_OFFICE_IDENTITY_COMPLETION_001`

Mode: independent read-only delta re-review; no patch.

Target: `/home/leo/Project/agent-office-batch-a-001`

Previously reviewed candidate: `e8531a306a28b4f2858a49b32cc2b3c1bfb4ce6d`

Final candidate: `d65716c27e258e5cfc332a8b68a58583697ffca8`

Read directly:

1. your full prior `SENTINEL_PRC1_PRC8_DELTA_REREVIEW_RESULT.md`;
2. Advisor validations 40, 41, and 42;
3. exact cumulative delta `e8531a3..d65716c` and the current four canonical
   design documents;
4. actual cited source and installed Vite/Rolldown declarations as needed.

No agents, sub-agents, delegated contexts, or temporary sessions.

## Required Questions

For PRC-1 through PRC-8 return `CLOSED`, `PARTIAL__BLOCKING`, `NOT_CLOSED`, or
`REGRESSION`, with direct current-file and source evidence.

Explicitly verify:

1. `aiRuntimeState` is the exact projector RT+(B) arbitration with correct
   per-result provenance and is not mislabeled B-only.
2. No second operational progress truth exists; fail-closed progress stays zero.
3. Cues are explicitly empty and diagnostics cannot manufacture activity.
4. Owned sentinels and duplicate identity handling remain closed.
5. Layout construction is total: sole `projectKey`, complete 14-state actor
   priority plus tie-break, exact responsible-Advisor validity and pod omission,
   selection/M1 fallback, and literal viewport/time/camera defaults.
6. Raw four-key `LivingOfficePresentationV1` parsing and the seven-field wrapper
   parsing are distinct and complete at their two real boundaries.
7. `LivingOfficeStructuralProjection` is literal and the exact production caller
   passes pods/frame without touching the prototype entry or importing fixtures.
8. Vite 8.1.4/Rolldown 1.1.5 `write:false` output-graph test is implementable
   using `facadeModuleId`, `.modules`, import edges, and `.code`.
9. The stale phrases in Advisor validation 41 are absent, while legitimate
   actor-level `advisorTeam=UNASSIGNED` remains intact.
10. No product, accessibility, security, authority, rollback, no-Grok,
    excluded-session, or Batch B-E boundary regressed.

Do not trust Control or Advisor summaries. Do not patch, implement, resume the
Worker, accept risk, or approve.

## Verdict

Return exactly `PASS`, `PASS_WITH_RISK`, `NEEDS_PATCH`, or `FAIL`.

Write result:

`/home/leo/Project/foundation-docs/runs/agent-office/20260712_agent_office_batch_a_modern_office_identity_completion_001/SENTINEL_FINAL_DESIGN_DELTA_REREVIEW_RESULT.md`

Write pointer:

`/home/leo/Project/foundation-docs/advisor/jobs/20260712_agent_office_batch_a_modern_office_identity_completion_001/43_SENTINEL_FINAL_DESIGN_DELTA_REREVIEW_RESULT_POINTER.md`

Commit and push only those exact two foundation-docs files. Return to Advisor
and STOP.

