TARGET_ACTOR: Worker-Rework
TARGET_SESSION: separate role session, never Advisor session
SOURCE_ADVISOR_JOB: ../foundation-docs/advisor/jobs/20260712_agent_office_batch_a_modern_office_identity_completion_001
DO_NOT_PASTE_INTO: Advisor session
RETURN_RESULT_TO: Advisor
GPT_DIRECT_USE: inspect only, do not execute unless acting as the target session

This instruction goes to: same existing `agent-office-opus` Worker session
Do not paste into: Advisor, Control, Reviewer, or GPT strategy session
Return result to: Advisor

# Agent Office Batch A — Focused Third Implementation Rework

Use `/fable-builder` in the existing `agent-office-opus` session.

Mission: `AGENT_OFFICE_BATCH_A_MODERN_OFFICE_IDENTITY_COMPLETION_001`

Mode: narrow routine implementation rework. No redesign from zero, review, risk
acceptance, Founder approval, or Batch B-E work. Use no agent, sub-agent,
delegated context, or temporary session.

## Exact Base and Evidence

- Repository/worktree: `/home/leo/Project/agent-office-batch-a-001`
- Branch: `batch-a/modern-office-identity-001`
- Required clean/upstream-equal base: `1187b9ae37077f22e697680bf531f9e475f005bf`
- Advisor direct validation:
  `/home/leo/Project/foundation-docs/advisor/jobs/20260712_agent_office_batch_a_modern_office_identity_completion_001/58_ADVISOR_WORKER_SECOND_REWORK_VALIDATION.md`
- Prior Worker result:
  `/home/leo/Project/foundation-docs/runs/agent-office/20260712_agent_office_batch_a_modern_office_identity_completion_001/WORKER_SECOND_REWORK_RESULT.md`

Read those exact artifacts and the current Batch A first-layer requirements. Do
not execute from memory. Before editing verify the exact session, live Opus 4.8
Ultracode identity, `/fable-builder`, workspace, clean branch/upstream, no
concurrent writer, no Grok reuse, and no input to excluded `agent-office`.

## Required Patch A3-1 — Complete First-Layer Team Fact

The Founder-required first layer includes current Team or `UNASSIGNED`. Add
`advisorTeam` to the exact compact first-layer contract and implementation:

- production actor label;
- accessible actor-label name;
- always-visible roster/mobile equivalent;
- exact unit and browser assertions;
- directly affected as-built contract/design text.

Keep stable identity separate from mutable assignment. Render the existing
fact envelope's truthful value/source/status; infer nothing from project,
session name, position, pod, proximity, or prior state.

## Required Patch A3-2 — Compact Office-First Desktop Composition

The fresh current-candidate desktop capture shows the eight 196px stacked
cards obscuring much of the office, actors, and work surfaces. Passing a
rectangle-overlap test is not product closure.

Required outcomes:

1. Keep every required compact fact represented and readable at >=10px, with
   truthful source indication and full values available in the roster/drawer.
2. Recompose the production-only labels into a genuinely compact layout (for
   example a dense multi-column fact grid or an equivalently clear design),
   while preserving deterministic actor association.
3. The Office world, characters, and symbolic work surfaces must remain the
   primary visual signal on desktop. Labels must be secondary and must not form
   a wall of cards across the world.
4. Add a deterministic production-canvas coverage/critical-surface occlusion
   assertion, not only pairwise card overlap. It must fail the current
   `1187b9a` composition and pass the corrected one.
5. Preserve no label-to-label overlap, no unacceptable actor overlap,
   connectors/association, 200-percent text equivalence, keyboard/drawer,
   forced-colors, reduced-motion/static, and screen-reader meaning.
6. Mobile may retain the accepted no-on-canvas-label plus always-visible roster
   equivalent, but every actor and Team fact must remain present and readable.
7. Generate and directly inspect fresh unmasked desktop, mobile, 200-percent,
   and static/reduced-motion evidence. Do not reuse or mask stale media.

## Preserve Unchanged

- I2-1 truthful `PENDING`/initialized backend state;
- I2-3 exact total pre-assembly validation;
- I2-4 bounded Channy semantic parity and honest production copy;
- 17-field detail drawer and focus restoration;
- strict CSP/Pixi lazy boundary and non-blank canvas proof;
- current auth, command, delivery, security, M1/static fallback, and authority
  boundaries.

## Allowed Scope

Only as needed for A3-1/A3-2:

- `src/ui/pixel/living-office-actor-overlay.tsx`
- `src/ui/pixel/living-office-semantic-mirror.tsx`
- `src/ui/pixel/living-office.css`
- directly coupled existing actor-summary/overlay/Living Office browser tests;
- current Living Office baseline files when fresh evidence requires them;
- the directly affected Batch A as-built contract/design/index text.

Do not modify input validation, renderer lifecycle, Channy behavior, package or
lock files, TypeScript/Playwright config, auth/server/transport code, prototype
fixtures, or unrelated source. If another source path is genuinely required,
stop and return the exact conflict before editing it.

## Forbidden

- hidden desktop facts/actors, sub-10px text, hover-only meaning, color-only
  meaning, stale/masked evidence, threshold-only screenshot proof, test skips,
  weakened assertions, file-wide suppression, `eslint-disable`, `@ts-ignore`,
  or `@ts-expect-error`;
- Grok pilot code or excluded `agent-office` input;
- agents/sub-agents, self-review, risk acceptance, force push, protected/main
  action, auth/DB/remote/prod/Hermes/Batch B-E changes.

## Delta-First Gates

During iteration run only focused actor-summary/overlay and Living Office
browser checks. Once stable, run the affected integration/browser gates. Run
the required complete candidate gate once before return because shared
production rendering and accessibility changed. Report every failure and its
resolution honestly.

At minimum return:

- focused exact first-layer/placement/coverage tests;
- authenticated Living Office desktop/mobile/200-percent/reduced/static gates;
- accessibility and direct visual inspection evidence;
- `npm run check` once on the final candidate;
- production build and local loopback start/stop/zero-listener gate if required
  by the existing Batch A completion contract;
- `git diff --check`, forbidden-scope/suppression checks, clean worktree,
  non-force push, local/upstream equality, and rollback.

## Result Contract

Write the long result to:

`/home/leo/Project/foundation-docs/runs/agent-office/20260712_agent_office_batch_a_modern_office_identity_completion_001/WORKER_THIRD_REWORK_RESULT.md`

Write the pointer to:

`/home/leo/Project/foundation-docs/advisor/jobs/20260712_agent_office_batch_a_modern_office_identity_completion_001/17_WORKER_THIRD_REWORK_RESULT_POINTER.md`

Commit and non-force push the authorized candidate branch. Commit/push only the
exact foundation-docs result/pointer files with explicit path staging. Preserve
unrelated foundation-docs dirt. Return the short pointer to Advisor and STOP.
Independent same-session Sentinel delta re-review remains external.

