# Advisor Sentinel Implementation Review Validation

- Mission: `AGENT_OFFICE_BATCH_A_MODERN_OFFICE_IDENTITY_COMPLETION_001`
- Reviewed candidate: `0b2f923a8456eea9b2b7df5b4cec1ae8c6d26760`
- Sentinel verdict: `NEEDS_PATCH`
- Advisor decision: `ROUTE_EXACT_ROUTINE_BATCH_A_PATCH`

## Finding Classification

| Finding | Classification | Advisor disposition |
|---|---|---|
| SIR-1 blank production Pixi / false ready state | `CODE_DEFECT` | Patch in Batch A; preserve strict CSP. |
| SIR-2 production theme/contrast failure | `CODE_DEFECT` | Patch and add full-surface accessibility coverage. |
| SIR-3 masked false-positive visual gate | `EVIDENCE_AND_TEST_DEFECT` | Add unmasked production proof and nonblank/error assertions. |
| SIR-4 nested layout validator can throw | `CODE_AND_CONTRACT_CONFORMANCE_DEFECT` | Implement total no-throw validation and hostile tests. |
| SIR-5 production Channy constant STOP | `CODE_AND_DOCUMENTATION_DEFECT` | Implement fixture-free eight-state ambient sequence and correct claims. |

All five findings are patchable within the approved Batch A product intent and
accepted design. No new product, authority, security, dependency, deployment,
or Founder decision is required. This is the authorized routine patch loop:
same Worker, then same independent Reviewer delta re-review.

## Exact Source-Scope Amendment

The accepted closed source list already includes the production renderer,
projector, Channy, validator, Living Office CSS, and required tests. Add only
these exact existing paths where necessary to close the reproduced defects:

- `src/ui/styles.css` for the authenticated application navigation/shell style;
- `src/ui/pixel/pixi-public-export-bridge.js` and
  `src/ui/pixel/pixi-public-export-bridge.d.ts` only if the CSP-safe Pixi static
  registration cannot be isolated in the already-authorized
  `production-pixel-office-chunk.tsx`. Any bridge edit must preserve the public
  package-root import contract and unchanged prototype behavior.

No other previously unnamed source or test path is authorized. A new exact
path must return to Advisor before editing.

## Mandatory Boundaries

- Do not add `unsafe-eval` to CSP or weaken any security header.
- Do not edit `package.json`, lockfiles, existing Playwright configs,
  `prototype-entry.tsx`, prototype fixtures, or historical baselines.
- Do not import prototype timeline/scenario/projection data into production.
- Do not add lint/type/test suppression.
- Do not add operational authority to Channy or infer operational state.
- Do not use Grok code or the excluded historical `agent-office` session.
- Do not start Batch B–E.

## Required Closure Evidence

1. strict-CSP authenticated production has zero page errors and a successful
   Pixi initialization;
2. renderer status/backend is truthful and initialization failure degrades to
   DOM static, then M1 as designed;
3. production canvas has initialized intrinsic dimensions and explicit
   nonblank pixel proof;
4. unmasked authenticated desktop/mobile screenshots and refreshed continuous
   production motion evidence are directly inspectable;
5. full `.living-office-surface` Axe coverage passes desktop, mobile,
   reduced/static, forced-colors, and 200% text;
6. nested render-input validation is complete, deterministic, and no-throw for
   hostile shapes, duplicates, invalid enums/maps/membership/selection;
7. production Channy covers the fixture-free eight ambient states while
   retaining `authorityRole: none`;
8. all previous gates and exact numeric totals are rerun honestly;
9. generated untracked test-result directories are reconciled before return;
10. branch is clean and upstream-equal after non-force push.
