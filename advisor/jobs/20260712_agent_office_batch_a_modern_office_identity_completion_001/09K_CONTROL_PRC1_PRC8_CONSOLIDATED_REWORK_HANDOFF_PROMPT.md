TARGET_ACTOR: Control-Rework
TARGET_SESSION: separate role session, never Advisor session
SOURCE_ADVISOR_JOB: ../foundation-docs/advisor/jobs/20260712_agent_office_batch_a_modern_office_identity_completion_001
DO_NOT_PASTE_INTO: Advisor session
RETURN_RESULT_TO: Advisor
GPT_DIRECT_USE: inspect only, do not execute unless acting as the target session

이 지시문을 붙여넣을 대상: 기존 Foundation Control 세션
이 지시문을 붙여넣으면 안 되는 곳: Advisor / Worker / Reviewer / GPT 전략 세션
작업 결과 반환 대상: Advisor

# Control Rework - Sentinel PRC-1 Through PRC-8

Mode: `CONTROL_MASTER_DESIGN_MODE`

Mission: `AGENT_OFFICE_BATCH_A_MODERN_OFFICE_IDENTITY_COMPLETION_001`

Target: `/home/leo/Project/agent-office-batch-a-001`

Branch: `batch-a/modern-office-identity-001`

Candidate to patch: `2e0dddfcd8131206f63780c7613bc7d1a03f496d`

Read directly:

1. the complete Sentinel result
   `SENTINEL_PRODUCTION_RENDER_CONTRACT_DELTA_REVIEW_RESULT.md`;
2. `37_ADVISOR_SENTINEL_PRODUCTION_RENDER_REVIEW_VALIDATION.md`;
3. all four current canonical Batch A docs;
4. every actual source/test path cited by PRC-1 through PRC-8;
5. actual runtime dashboard/manifest/projection and existing accepted spatial
   cue contracts before choosing progress or cue sources.

No agents, sub-agents, delegated contexts, or temporary sessions. Do not execute
from memory.

## Exact Task

Patch only the same four canonical Batch A documentation paths. Close every
PRC-1 through PRC-8 finding without erasing it.

### PRC-1 - Source Authority

- `advisorTeam` remains the (A) committed registry organizational binding,
  carried in a projected envelope; it is not RT-owned.
- Mission/WorkUnit/activity/operational state remain RT-owned.
- Define closed total mappings for `roleCategory`, `presentationPodId`,
  `responsibleAdvisorRoleInstanceId`, project identity, and every other
  presentation field not literally present in the registry.

### PRC-2 - One Work Truth

- Remove committed WU-plan/gate progress as operational truth.
- Read actual current runtime manifest/dashboard/projection fields and name the
  exact existing RT-owned source if it provides progress.
- If exact progress is unavailable, specify the fail-closed display without a
  second catalog. Include `totalGates` and exact completion semantics.

### PRC-3 - Cues

- Diagnostics may suppress motion or render diagnostics only; they never create
  delivery/work/review/routing cues.
- Either name one exact existing accepted structured cue source and map every
  required `PixelCueInput` field, or use `cues=[]` for Batch A.
- Never invent IDs, anchors, kinds, revision, sequence, or accepted/freshness
  state.

### PRC-4 - Sentinels And Identity Collision

- invalid `operationalState` -> `UNKNOWN`;
- invalid `aiRuntimeState` -> `AI_RUNTIME_UNKNOWN`;
- `UNKNOWN_OR_STALE` remains only the observable input vocabulary;
- duplicate `roleInstanceId` at the untrusted boundary drops all duplicate rows
  or hard-fails the presentation deterministically, never first-wins.

### PRC-5 - Complete Visual Layout Contract

Define the complete exact versioned `CommittedOfficeLayoutConfigV1` interface,
canonical export/source, deterministic ordering, project identity fields,
Advisor Team/pod mapping, responsible Advisor resolution, membership, selected
default, labels, all `PixelPodInput` fields, counts, blocker behavior, and missing
or multiple assignment fallback. Presentation config must not own operational
mission structure or infer authority from location.

### PRC-6 - Actual Runtime Validation Boundary

- Name `src/ui/runtime/client.ts:parseProjection` as the first parser of raw
  `livingOffice` JSON before client state.
- Name the composition step and a second complete render-input validation before
  lazy renderer entry.
- Define nested exact-key/envelope/enum/identity/pod/membership/Advisor/selection/
  anchor/numeric bounds and literal defaults; monotonic zero is valid when the
  contract allows it.
- No cast is validation.

### PRC-7 - Consistent Extraction And DOM Props

- State consistently that `frame-projector.ts` is edited only to consume
  extracted fixture-free core while preserving its public prototype behavior.
- Define one fixture-free structural projection/props contract used by actor
  overlay, semantic mirror, and HUD in production.
- Define how existing prototype projection adapts to those props without editing
  `prototype-entry.tsx`.
- Name every new/edited source/test path literally. Production imports no
  prototype projection type, projector, or fixture.

### PRC-8 - Exact Bundle/Test Evidence

- Define exact Vite JS API build options enabling manifest/output capture only
  in the named test invocation.
- Use Rollup `OutputChunk.modules` (or an equally exact named source) to classify
  modules, then traverse `imports`/`dynamicImports` for eager/lazy reachability.
- Keep static import scans and built prototype-marker exclusion.
- Choose one literal runtime-validator test path and list it everywhere; remove
  “or exact sibling” and every open class.

## Preserve

Preserve WU-02/03/04 implementation, U1-U3/S3/R2/T3, actor-overlay ownership,
Office-first and secondary views, accessibility, security/authority/delivery,
static/M1 fallback, Channy, rollback, no-Grok, excluded session, and Batch B-E
boundaries. No new product or risk decision.

## Forbidden

No source/test/config/assets/media/script implementation; no server; no Worker or
Reviewer invocation; no agents; no DB/secret/env/remote/public/prod; no authority
change; no self-review/risk acceptance/final approval.

## Result

Commit and non-force push only the four canonical docs. Update only the exact
Control result/pointer in foundation-docs. Return exact commits to Advisor and
STOP. The same independent SOL Sentinel must re-review the exact delta.

