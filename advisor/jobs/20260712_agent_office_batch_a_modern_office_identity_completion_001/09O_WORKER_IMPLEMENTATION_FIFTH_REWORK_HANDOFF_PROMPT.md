TARGET_ACTOR: Worker-Rework
TARGET_SESSION: separate role session, never Advisor session
SOURCE_ADVISOR_JOB: ../foundation-docs/advisor/jobs/20260712_agent_office_batch_a_modern_office_identity_completion_001
DO_NOT_PASTE_INTO: Advisor session
RETURN_RESULT_TO: Advisor
GPT_DIRECT_USE: inspect only, do not execute unless acting as the target session

This instruction goes to: same existing `agent-office-opus` Worker session
Do not paste into: Advisor, Control, Reviewer, excluded historical `agent-office`, or GPT strategy session
Return result to: Advisor

# Agent Office Batch A - Fifth Focused Implementation Rework

Use `/fable-builder` in the same existing `agent-office-opus` Worker session.

- Mission: `AGENT_OFFICE_BATCH_A_MODERN_OFFICE_IDENTITY_COMPLETION_001`
- Model/effort: verify actual Opus 4.8 Ultracode before work
- Repo/worktree: `/home/leo/Project/agent-office-batch-a-001`
- Branch: `batch-a/modern-office-identity-001`
- Required base: clean/upstream-equal `43107b9c087a5d172d5f670e6b01bd75ab9ac1db`
- Skill: `/fable-builder`

Use no agent, sub-agent, delegated context, or temporary session. Do not contact
the excluded historical `agent-office` session. Do not use the quarantined Grok
branch/worktree/code. No Control redesign is required: this is implementation
conformance to the already accepted A4 contract.

## Read Directly

1. `63_ADVISOR_SENTINEL_THIRD_DELTA_VALIDATION.md`
2. `SENTINEL_IMPLEMENTATION_THIRD_DELTA_REREVIEW_RESULT.md`
3. prior `61_ADVISOR_FOURTH_REWORK_TEST_EXACTNESS_ADDENDUM.md`
4. current production overlay, semantic roster, production scene, drawer, CSS, affected tests, and affected as-built sections
5. current Batch A Founder authorization and contract section 2.7

## A5-1 - operable high-text equivalent and focus restoration

- Preserve the real text-scale probe, pre-paint switch, explicit mode marker, and zero partial/on-canvas labels at high text.
- Give every high-text roster actor one native keyboard/pointer-operable trigger that opens the same existing actor's 17-field drawer. Do not duplicate drawer logic or actor state.
- Keep stable actor identity and evidence provenance unchanged. A roster trigger creates no authority, assignment, dispatch, or work inference.
- Closing with button or Escape must restore focus to the actual visible invoker. If the original normal-scale label became hidden after a text-scale transition, restore to the same actor's visible roster-equivalent trigger instead of `BODY` or another actor.
- Prove initial 200% operation and normal-to-high transition: keyboard activation, exact actor drawer, close/Escape, focus restoration, no partial labels, and return to normal.
- Preserve mobile, reduced-motion/static, forced-colors, screen-reader, and existing normal-scale drawer behavior.

## A5-2 - exact anti-shortcut predicates

Use one reusable normal-label predicate bound to an authoritative expected actor
ID set from the production frame/contract, not IDs derived from labels or roster
elements under test. It must require:

- exact actor-ID set equality and exactly one label per expected actor;
- every label present in/intersecting the production viewport with positive area;
- the exact nine unique required first-layer field keys with non-empty values;
- exactly the seven expected sourced fact keys, each with a non-empty source;
- Team and the existing Office-coverage bound.

The high-text roster predicate must require exact authoritative actor-ID set
equality, exactly one row per actor, non-empty role and stable name, the exact
seven unique mapped fact keys with non-empty values, and one non-empty source per
fact.

Keep the existing exactly-one-hidden/coverage-alone/restored challenge. Add
focused challenges proving the same predicates reject:

- a wrong but unique actor ID;
- a required field replaced by a duplicate field;
- empty source values;
- all labels outside the production viewport;
- empty roster role/name;
- a roster fact replaced by a duplicate fact.

Do not mutate production behavior merely to satisfy tests. Avoid tautological
expected sets sourced from the same DOM being challenged.

## A5-3 - exact source phrase unit gate

- Keep the correct production source-name implementation.
- Assert each fact's exact expected `PIXEL_ACTOR_FACT_SOURCE_LABELS[source]` phrase or the exact complete accessible name.
- Add a focused negative case that removes or abbreviates each expected source in turn and must fail.
- A generic uppercase-token regex is insufficient.

## Allowed Scope

Only files directly needed for the existing production overlay/semantic roster
interaction, focus path, exact tests, and directly affected as-built/index text.
Likely source areas include:

- `src/ui/pixel/living-office-actor-overlay.tsx`
- `src/ui/pixel/living-office-semantic-mirror.tsx`
- `src/ui/pixel/production-pixel-world-scene.tsx`
- directly affected `src/ui/pixel/living-office.css`
- directly affected unit/E2E tests
- the four current A4 as-built/index documents

If another source file is materially required, record the exact coupling before
touching it and keep it within the same drawer/roster interaction boundary.

## Forbidden

No redesign, new dependency/config/baseline unless changed pixels genuinely
require a fresh authorized baseline, lint/type weakening, suppression, raw
terminal/private data, auth/server/transport/command expansion, DB, remote/live,
Batch B-E, force push, final approval, or unrelated refactor. Preserve all prior
accepted normal-scale pixels and security/authority boundaries.

## Verification Policy

During iteration run only affected unit and Living Office browser tests. Before
publication run one stabilized candidate gate covering affected unit/E2E,
targeted lint/typecheck, build, and any broader gate whose direct dependency was
actually changed. Do not repeatedly rerun unrelated suites without a regression
signal. Inspect initial 200%, transition/focus, normal desktop, and mobile/reduced
equivalents directly. Remove disposable output and leave no listener.

## Result Contract

Commit and non-force push the exact patch to the same branch. Write:

`/home/leo/Project/foundation-docs/runs/agent-office/20260712_agent_office_batch_a_modern_office_identity_completion_001/WORKER_FIFTH_REWORK_RESULT.md`

Pointer:

`/home/leo/Project/foundation-docs/advisor/jobs/20260712_agent_office_batch_a_modern_office_identity_completion_001/21_WORKER_FIFTH_REWORK_RESULT_POINTER.md`

Report exact base/candidate, changed files, each A5 closure, tests and failures,
visual/focus evidence, scope attestations, Git/upstream state, cleanup, and
rollback. Commit/push only the result/pointer in foundation-docs with explicit
path staging. Return the pointer to Advisor and STOP.
