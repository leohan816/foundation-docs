# Advisor Validation - Control PRC-1 Through PRC-8 Correction

## Verdict

`READY_FOR_SAME_SENTINEL_DELTA_REREVIEW__NOT_YET_ACCEPTED`

Candidate `e8531a3` responds to all eight Sentinel findings in the exact four
canonical documents and is ready for same-Sentinel delta re-review. Worker
implementation remains stopped.

## Exact Verification

- before: `2e0dddfcd8131206f63780c7613bc7d1a03f496d`
- after: `e8531a306a28b4f2858a49b32cc2b3c1bfb4ce6d`
- exact changed paths: four canonical Batch A docs only
- target: clean and upstream-equal
- Control source/test/config changes: zero
- foundation-docs result/pointer commit: `1ae9976`

## Closure Mapping

- PRC-1: per-field authority corrected and closed presentation mappings added.
- PRC-2: no committed WorkUnit/gate catalog; all pod progress counts are
  fail-closed `0`.
- PRC-3: Batch A `cues=[]`; diagnostics suppress/render only.
- PRC-4: owned sentinels corrected; duplicate actor identity is drop-all or
  deterministic hard failure.
- PRC-5: complete committed visual layout config and construction rules added.
- PRC-6: raw client boundary and composed render-input boundary both named.
- PRC-7: frame-projector edit made consistent; fixture-free structural DOM props
  defined; prototype entry remains unchanged.
- PRC-8: Vite/Rollup module and edge evidence specified; one literal validator
  test path selected.

## Delta Re-Review Focus

The same Sentinel must verify closure from the actual delta and source, including:

1. whether `aiRuntimeState` authority is described accurately as the accepted
   composition of RT work/error state and (B) attestations, rather than B alone;
2. whether first-boundary validation matches the actual raw
   `LivingOfficePresentationV1` shape directly, before the later render-input
   wrapper introduces `operational`;
3. whether the current-actor priority order and every pod/project identity source
   are total and deterministic;
4. whether an invalid responsible Advisor causes a non-renderable/fallback pod
   rather than using an authority-looking sentinel as a role ID;
5. whether the exact Vite JS API options can expose `OutputChunk.modules` and
   import edges without relying on a committed manifest or string heuristics;
6. whether all closed source/test paths required by the corrected contract are
   literally enumerated and no contradictory historical active statement remains.

A contradiction remains `NEEDS_PATCH`. A clean `PASS` returns to Advisor for
final design acceptance and exact Worker resume amendment.

## Routing

- next actor: same independent `foundation-reviewer-sol`
- model/effort: GPT-5.6 SOL xhigh
- skill: `/fable-sentinel`
- target delta: `2e0dddf..e8531a3`
- return: Advisor

