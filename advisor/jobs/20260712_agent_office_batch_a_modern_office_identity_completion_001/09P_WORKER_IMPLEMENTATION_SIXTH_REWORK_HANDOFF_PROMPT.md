TARGET_ACTOR: Worker-Rework
TARGET_SESSION: separate role session, never Advisor session
SOURCE_ADVISOR_JOB: ../foundation-docs/advisor/jobs/20260712_agent_office_batch_a_modern_office_identity_completion_001
DO_NOT_PASTE_INTO: Advisor session
RETURN_RESULT_TO: Advisor
GPT_DIRECT_USE: inspect only, do not execute unless acting as the target session

This instruction goes to: same existing `agent-office-opus` Worker session
Do not paste into: Advisor, Control, Reviewer, excluded historical `agent-office`, or GPT strategy session
Return result to: Advisor

# Agent Office Batch A - Sixth Focused Evidence Rework

Use `/fable-builder` in the same existing `agent-office-opus` Worker session.

- Mission: `AGENT_OFFICE_BATCH_A_MODERN_OFFICE_IDENTITY_COMPLETION_001`
- Model/effort: verify actual Opus 4.8 Ultracode before work
- Repo/worktree: `/home/leo/Project/agent-office-batch-a-001`
- Branch: `batch-a/modern-office-identity-001`
- Required base: clean/upstream-equal `95e493ce61e268d6352b3805692835f4b612a4ff`
- Skill: `/fable-builder`

Use no agent, sub-agent, delegated context, or temporary session. Do not contact
the excluded historical `agent-office` session. Do not use the quarantined Grok
branch/worktree/code. No Control redesign is required: the production behavior
is accepted and this patch closes exact E2E evidence and as-built accuracy only.

## Read Directly

1. `66_ADVISOR_SENTINEL_FOURTH_DELTA_VALIDATION.md`
2. `SENTINEL_IMPLEMENTATION_FOURTH_DELTA_REREVIEW_RESULT.md`
3. `65_ADVISOR_WORKER_FIFTH_REWORK_VALIDATION.md`
4. current `tests/e2e/living-pixel-office.spec.ts`
5. only directly affected as-built statements and load-bearing production markup needed to identify actual value/trigger fields

## A6-1 - exact initial-200% browser proof

- In the existing initial-200% browser path, operate an actor's native roster trigger before resetting text scale.
- Prove exact actor drawer content, Enter/Close or Escape, same-visible-trigger focus restoration, zero partial canvas labels, and subsequent normal recovery.
- Keep the later normal-to-high transition proof.
- Reconcile the two as-built statements to the exact committed proof; do not broaden them.

## A6-2 - actual fact value exactness

- Make the actual fact value independently addressable or otherwise exactly extractable from field labels and source text.
- In both normal-label and roster predicates, require each actual fact value itself to trim non-empty.
- Add mutations that empty one normal-label actual value and one roster actual value while retaining field labels, keys, source attributes, and source text. The same predicates must return false.
- Restore the authentic DOM and prove the same predicates return true.
- Retain every existing named negative challenge.

## A6-3 - exact roster trigger binding

- Require exactly one roster trigger per authoritative actor row.
- Require exact equality among trigger `data-actor-roster-trigger`, row actor ID, and authoritative actor ID.
- Add wrong-trigger-ID and duplicate-trigger mutations against that same predicate; each must fail.
- Restore the authentic DOM and prove the same predicate passes.
- Do not create a second drawer, actor store, or product authority path.

## Allowed Scope

Prefer changes only to:

- `tests/e2e/living-pixel-office.spec.ts`
- the smallest directly coupled semantic roster/label markup file if a dedicated actual-value marker is required
- the two directly affected as-built documents
- directly affected index/contract text only if necessary for factual consistency

Do not change accepted pixels, baselines, dependencies, config, auth/server/CSP,
runtime authority, drawer architecture, Channy behavior, or unrelated tests.

## Verification Policy

During iteration run the Living Office E2E only. Before publication run one
stabilized affected gate: Living Office E2E, directly affected unit tests if
production markup changed, targeted ESLint, typecheck, build, and `git diff
--check`. Run a broader suite only if a changed dependency or concrete regression
signal justifies it. Directly inspect initial 200%, normal-to-high focus, and
restored authentic state. Remove disposable output and leave no listener.

## Result Contract

Commit and non-force push the exact patch to the same branch. Write:

`/home/leo/Project/foundation-docs/runs/agent-office/20260712_agent_office_batch_a_modern_office_identity_completion_001/WORKER_SIXTH_REWORK_RESULT.md`

Pointer:

`/home/leo/Project/foundation-docs/advisor/jobs/20260712_agent_office_batch_a_modern_office_identity_completion_001/23_WORKER_SIXTH_REWORK_RESULT_POINTER.md`

Report exact base/candidate, changed files, A6-1 through A6-3 closure, every
command and failure, focused proof, scope attestations, Git/upstream state,
cleanup, and rollback. Commit/push only the result/pointer in foundation-docs
with explicit path staging. Return the pointer to Advisor and STOP.
