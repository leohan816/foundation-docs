TARGET_ACTOR: Worker-Rework
TARGET_SESSION: separate role session, never Advisor session
SOURCE_ADVISOR_JOB: ../foundation-docs/advisor/jobs/20260712_agent_office_batch_a_modern_office_identity_completion_001
DO_NOT_PASTE_INTO: Advisor session
RETURN_RESULT_TO: Advisor
GPT_DIRECT_USE: inspect only, do not execute unless acting as the target session

# Agent Office Batch A Implementation Rework

Use `/fable-builder` in the same existing `agent-office-opus` Worker session.
Do not use agents/sub-agents or the excluded historical `agent-office` session.

## Read Directly

- `/home/leo/Project/foundation-docs/runs/agent-office/20260712_agent_office_batch_a_modern_office_identity_completion_001/SENTINEL_IMPLEMENTATION_REVIEW_RESULT.md`
- `/home/leo/Project/foundation-docs/advisor/jobs/20260712_agent_office_batch_a_modern_office_identity_completion_001/54_ADVISOR_SENTINEL_IMPLEMENTATION_REVIEW_VALIDATION.md`
- final accepted Batch A design and Advisor amendments 48 through 52
- actual candidate source at `0b2f923a8456eea9b2b7df5b4cec1ae8c6d26760`

## Exact Patch Objective

Close SIR-1 through SIR-5 without broadening Batch A:

1. Keep CSP strict. Make the lazy production Pixi path CSP-compatible using
   public package-root exports already installed. Do not add `unsafe-eval` to
   CSP. Publish ready/backend only after successful initialization; convert
   synchronous and asynchronous initialization failures into truthful
   DOM-static/M1 fallback.
2. Apply the modern light-office variables and readable foreground to the real
   `.living-office-surface`. Add intentional responsive, keyboard, and
   `:focus-visible` styling for authenticated Office/Technical navigation.
3. Replace the false-positive visual gate with honest coverage: zero page
   errors, successful `onInit`, initialized intrinsic dimensions, explicit
   nonblank production pixels, unmasked desktop/mobile captures, and refreshed
   continuous authenticated-production motion evidence. Supplemental masked
   DOM screenshots may remain only when accurately labelled and when they do
   not obscure the overlay being claimed.
4. Runtime-validate the complete nested `CommittedOfficeLayoutConfigV1` before
   assembly. Reject malformed shapes, unknown keys, duplicate pod/actor IDs,
   invalid closed enums, incomplete role maps, invalid Advisor membership,
   anchors/selection/numerics, and all hostile input with the declared
   deterministic `{ok:false, reason, fallbackTier}` result. Never throw.
5. Implement a fixture-free production presentation clock and eight-state
   Channy ambient sequence (`WALK`, `STOP`, `SNIFF`, `SIT`, `EAT`, `DRINK`,
   `SLEEP`, `PLAY`) with `authorityRole: none`. It must not create operational
   evidence, dispatch, recovery, or state inference.
6. Correct the four authorized as-built docs and Worker claims to match the
   patched implementation and exact evidence.
7. Reconcile only the generated untracked `test-results-batch-a-living-office/`
   and `test-results-composed/` outputs before final status. Do not delete or
   alter unrelated user files.

## Exact Allowed Patch Paths

Use only the already accepted closed paths needed for these findings, plus the
two exact conditional bridge paths and `src/ui/styles.css` authorized by
Advisor document 54:

- `src/ui/styles.css`;
- `src/ui/pixel/living-office.css`;
- `src/ui/pixel/production-pixel-office-chunk.tsx`;
- `src/ui/pixel/production-renderer-boundary.tsx`;
- `src/ui/pixel/production-pixel-world-scene.tsx`;
- `src/ui/pixel/pixel-render-host.tsx`;
- `src/ui/pixel/production-frame-projector.ts`;
- `src/ui/pixel/frame-core.ts`;
- `src/ui/pixel/presentation-clock.ts`;
- `src/ui/pixel/channy-sprite.tsx`;
- conditional `src/ui/pixel/pixi-public-export-bridge.js` and
  `src/ui/pixel/pixi-public-export-bridge.d.ts`;
- `src/application/organization/production-render-input.ts`;
- `tests/contract/production-render-input.test.ts`;
- `tests/ui/pixel-renderer-lifecycle.test.tsx`;
- `tests/ui/pixi-public-export-bridge.test.ts`;
- `tests/ui/pixel-world-semantic-parity.test.tsx`;
- `tests/acceptance/production-pixel-prototype-boundary.test.ts`;
- `tests/e2e/living-pixel-office.spec.ts`;
- `tests/e2e-composed/application-office-scene.spec.ts`;
- the two already-authorized new Batch A baseline directories only;
- `scripts/local-office-rehearsal.mjs` only if needed to prove truthful
  production initialization/teardown;
- the four already-authorized canonical Batch A documents.

If another exact source/test/config path is materially required, stop at a
clean checkpoint and return the path and evidence to Advisor before editing.

## Required Gates

- focused hostile validator tests;
- focused renderer lifecycle/public export/CD-3 tests;
- full `npm run check`;
- Living Office E2E with unmasked production evidence and full-surface Axe;
- composed E2E;
- prototype 20/20 unchanged;
- demo E2E;
- production continuous-motion evidence and direct visual artifacts;
- loopback start/open/verify/stop with zero listener/lock residue;
- `git diff --check`, no suppressions, no forbidden diff;
- clean worktree and local/upstream equality.

Write the rework result to:

`/home/leo/Project/foundation-docs/runs/agent-office/20260712_agent_office_batch_a_modern_office_identity_completion_001/WORKER_REWORK_RESULT.md`

Write the pointer to:

`/home/leo/Project/foundation-docs/advisor/jobs/20260712_agent_office_batch_a_modern_office_identity_completion_001/14_WORKER_REWORK_RESULT_POINTER.md`

Commit and non-force push the candidate branch and exact foundation-docs result
files. Report every failed and successful command honestly. Return to Advisor
and stop; do not self-review or approve.
