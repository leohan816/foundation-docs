# Advisor Brief - Batch A Modern Office Identity Completion

## Entry verdict

`PROCEED_WITH_LIMITS`

The product decision, implementation base, actor train, and review contract are
complete. Control is required to publish a bounded integration-design delta
before the Worker receives implementation authority.

## Product outcome

Produce an actually runnable loopback/private Agent Office in which the living
pixel office is the primary application experience and the existing technical,
control, evidence, and communication surfaces remain secondary. Reuse accepted
M1/M1.2 foundations; do not rewrite them without an integration reason.

## Current facts

- `ac8ba75` contains the reviewed isolated living pixel prototype, verified
  actor fact projection, actor overlays/drawer, modern palette, Channy state
  sequence, semantic mirror, static fallbacks, and visual baselines.
- The real shell still routes production/authenticated presentation through the
  existing dashboard and spatial-office path; the living pixel renderer is
  selected only by `surface=living-pixel-prototype` in the synthetic demo.
- The dashboard currently dominates information hierarchy and technical data.
- Existing auth, exact Advisor delivery, security, PWA, runtime projection,
  M1 fixed-station fallback, and communication capabilities must remain intact.
- Batch A uses a local/static registry projection. It does not implement live
  tmux/process/model discovery.

## Design questions delegated to Control

Control must resolve technical integration only:

1. application-shell route and lazy renderer composition;
2. Office-first navigation with secondary technical/control views;
3. one source frame for Pixi, compact labels, drawer, and semantic fallback;
4. stable identity versus mutable assignment and Advisor relationship;
5. fail-closed local/static registry schema and UNKNOWN vocabulary;
6. role-specific symbolic work surfaces with no private terminal/source data;
7. accessibility, responsive, reduced-motion, static, and M1 fallback behavior;
8. exact implementation WorkUnits, file scope, tests, rollback, and gates.

Control must not choose new product policy, change authority, implement runtime,
or enter Batch B-E.

## Expected implementation slices

- `BA-WU-01`: application shell and Office-first navigation.
- `BA-WU-02`: static registry and organization projection.
- `BA-WU-03`: compact actor summaries and truthful state vocabulary.
- `BA-WU-04`: accessible actor detail drawer.
- `BA-WU-05`: role-specific symbolic surfaces.
- `BA-WU-06`: Channy and modern-office integration.
- `BA-WU-07`: responsive, accessibility, fallback, and regression integration.
- `BA-WU-08`: local run tooling, current visual evidence, and documentation.
- `BA-WU-09`: Worker result and exact Git evidence.

Control may refine these boundaries without expanding product scope.

## Review route

Primary reviewer: `foundation-reviewer-sol`, GPT-5.6 SOL xhigh, Level 2
implementation/security/accessibility/visual review. Max is reserved for an
actual Level 3 discovery. Reviewer is read-only and cannot patch.

## STOP conditions

- material new Founder decision;
- auth, transport, secret, DB, PII, remote, production, or authority expansion;
- inability to preserve M1/M1.2 fallback and rollback;
- `PASS_WITH_RISK`, unresolved `FAIL`, or destructive action;
- need to send input to the excluded historical Agent Office context.

