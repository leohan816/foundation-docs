# Final Canonical Ownership Correction

This is the final ownership decision for the current bounded task.

## Current Authority

- `agent-office`: current Team, Actor, role, authority, Advisor-led routine,
  onboarding, protocol, dispatch, and tmux/runtime-binding rules.
- `foundation-docs`: mission evidence, audits, historical decisions, migration
  records, and pointers only.
- Each product repository: source code and project-specific rules; root
  `AGENTS.md` and `CLAUDE.md` carry only the responsible Advisor, participating
  roles, Agent Office canonical pointers, and local restrictions/gates.
- tmux: runtime of the registered Actor; never actor or project identity.

Use the existing Agent Office `docs/agent/` structure. Do not create
`office-knowledge/`, `.agent-office/`, a second role system, or a second
registry. Reuse completed preservation work and review only this ownership and
reference delta.

## Advisor Validation Invariant

An Advisor validates every instruction against actual evidence and authority
before execution. It must not blindly execute a contradictory, unsafe,
over-scoped, or stale instruction. It returns the exact conflict and safe
correction to Leo/GPT when necessary.

No product tests, visual tests, broad audit, Slack implementation, or next
mission is authorized.

## Required Visible Deferral

The existing machine registry actor/Team bindings are not modified by this
documentation-only mission. This limitation must remain visible in the Worker
result, Reviewer result, migration record, and Advisor final report.

Before AS1 Slack Pilot begins, a separate minimal config-only delta must align
the machine registry bindings with the approved current Team relationships.
This mission must neither implement that delta nor start AS1.
