# Final Runtime Record Finding-Closure Patch Brief

## Exact Route

- Same Worker/session: `agent-office-opus`.
- Required skill: `/fable-builder`; read
  `/home/leo/Project/skill/fable-builder/SKILL.md`.
- Repository/branch: `/home/leo/Project/agent-office`,
  `shadow/agent-office-m1-2-spatial-office`.
- Exact base: `8f47cef796d8b66b5b3192a02f78bc9d94abecce`.
- Allowed repository file only:
  `docs/agent/ACTOR_PROJECT_BINDING_MIGRATION.md`.
- Prior Reviewer result:
  `/home/leo/Project/foundation-docs/runs/agent-office/20260714_agent_office_actor_project_binding_normalization_001/FINAL_RUNTIME_RECORD_SENTINEL_RESULT.md`.
- Durable Advisor observation:
  `/home/leo/Project/foundation-docs/advisor/jobs/20260714_agent_office_actor_project_binding_normalization_001/58_FINAL_RUNTIME_BINDING_OBSERVATION.md`.
- Updated result file only:
  `/home/leo/Project/foundation-docs/runs/agent-office/20260714_agent_office_actor_project_binding_normalization_001/FINAL_RUNTIME_RECORD_PATCH_WORKER_RESULT.md`.

Close only the three independent findings.

## Required Patch

1. Cite the committed Advisor observation for exact `pane_current_path`
   provenance. Keep session/process names explicitly non-authoritative for actor,
   model, effort, readiness, or work state.
2. Replace the universal rollback claim. State that Git reverts restore only
   committed documentation and pointers; they do not automatically recreate
   removed role folders or reverse live tmux/session path normalization. Describe
   any complete operational rollback as separately authorized manual work, not an
   action performed or authorized here.
3. Replace or support “removed after preservation checks.” Cite the mission
   intake and foundation-docs preservation commit
   `076f0f4f7594ada02759f76c8239877dc99a100c`, while distinguishing durable
   content preservation from the directly observed current folder absence.
4. Preserve the no-product-capability boundary and the unchanged machine-registry
   / mandatory separate config-only pre-AS1 gate exactly.

Update the Worker result truthfully with the prior `NEEDS_PATCH`, exact patch,
checks, skips, final commit, upstream equality, and rollback scope.

## Forbidden and Checks

- No other candidate file, product test, product/runtime source, registry/config,
  Slack/AS1, tmux/folder action, another repository change, broad review, merge,
  protected/main push, or force push.
- Verify exact one-file diff, cited evidence exists and is committed,
  `git diff --check`, final HEAD/upstream equality, and unrelated Grok paths
  untouched.

Commit and non-force push the authorized branch. Write the updated result, return
it to `agent-office-advisor`, and stop without dispatching the Reviewer.
