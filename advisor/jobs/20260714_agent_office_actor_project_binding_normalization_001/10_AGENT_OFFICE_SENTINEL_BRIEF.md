# Agent Office Sentinel Brief

Review the exact Agent Office base/candidate read-only. Start with the canonical
actor/Team and role-document model, not test totals. Verify reuse of the accepted
`58a484b` registry rather than creation of a second registry; current Team and
session names; required actor fields; fail-closed runtime/model/effort facts;
project versus actor separation; concise root references; overlay; exact changed
files; no Slack/product/visual/transport activation; focused test accuracy;
clean/upstream state. Use `/fable-sentinel`. Do not patch.

Treat the responsible-Advisor chain as a product invariant. Any registry,
authority profile, Team record, or future Slack mapping that permits a
subordinate actor to self-assign, dispatch, bypass Advisor intake/result routing,
or become a Team leader must fail review.

Verdict: `PASS | PASS_WITH_RISK | NEEDS_PATCH | FAIL`.
