# 29 — Advisor Design-Correction Gate

MISSION: `COSMILE_MODERN_CUSTOMER_STOREFRONT_VISUAL_REFRESH_V1`
DECISION: `BOUNDED_ADDENDUM_REQUIRED`

## Accepted

- F1, F4, F5, and F6 are closed in the exact admitted design paths.
- Corrected `13` and `14` were independently rendered by Advisor at `1440×900` and `390×844` using the frozen local Noto CJK/FONTCONFIG environment.
- Visual result: Korean legible; tofu, clipping, and overlap absent; temporary Chromium, profile, PNG, and controller artifacts deleted.
- Product worktree remains clean; runtime/data/provider/economic effects remain `0`.

## Exact remaining corrections

Only these evidence contradictions remain:

1. `12` still says `CatalogCard` radius `14 px`, while accepted `13` and disposition `25` freeze radius `16 px`. Change the contract to radius `16 px` and state the exact 1440 reference card size `312×194` with `112×170` media rail.
2. The small cart-count badge text in `13`/`14` still uses white on persimmon. Use ink on persimmon so the declared ≥4.5:1 text rule has no exception.
3. `12` must state that focus remains on the originating Cart action for every Add result; the persistent Cart link is next in tab order.
4. `27` must report render provenance truthfully: the Designer raw-headless child stalled and was contained without output; Advisor completed the finite CDP render and inspection. No Actor/session was terminated.

Exact edit ceiling: `12`, `13`, `14`, `27`. No new file, render, source read, product write, or implementation.

