# 85 Living Pixel-Office Prototype Completion Worker Brief

## Assignment

- Actor: Agent Office Worker
- Session: same existing `agent-office/%13` only
- Model/effort: `<Codex 5.6 SOL: Ultra>`
- Mission: `AGENT_OFFICE_M1_2_SPATIAL_ANIMATED_ADVISOR_TEAM_OFFICE`
- Repository/branch: `/home/leo/Project/agent-office` / `shadow/agent-office-m1-2-spatial-office`
- Required base: `56385b894ff18fd65a4e59f2c4e75f73ea2a56d9`, upstream-equal

Read directly all governing artifacts `79` through `85`, the clean Fable5
design result, current instructions, actual source/tests/config/Git/media state,
and both prior blocker results. The complete prior brief remains binding.

## Additional Exact Scope

The mutation allowlist now includes exactly the three existing composed PNGs
listed in `85_ADVISOR_COMPOSED_VISUAL_BASELINE_RECONCILIATION.md`, in addition
to the prior exact 16 existing PNGs and prior clarified correction paths.

No source, CSS, fixture, spec, config, threshold, package, authority,
authentication, or delivery change is authorized for either reconciliation.

## Required Completion

1. Revalidate the exact restored entry state and all prior gates.
2. Reapply the reviewed bridge/consumer/test/lifecycle/dependency/ESLint
   correction.
3. Pass focused test, lint, strict typecheck, full 505-test train, builds,
   dependency audit, and diff checks.
4. Regenerate and inspect the exact prior 16 default historical PNGs and the
   exact three composed PNGs. No other baseline path may change. Record old/new
   hashes and material differences; current `Shashu` variants must be absent.
5. Run default and composed browser trains again without update mode. Both must
   pass fully.
6. Run the dedicated 13-scene prototype Playwright train and all required
   lifecycle/semantic/accessibility/security/performance/load/cleanup checks.
7. Regenerate all eight ignored prototype media files from the running corrected
   prototype. Verify and directly inspect lineage, duration, dimensions, hashes,
   ignored state, all PNGs, and representative video/GIF frames.
8. Run the evidence verifier, exact scope/naming/dependency/suppression scans,
   `git diff --check`, and residual-process/listener checks.
9. Stage only the complete clarified allowlist, including exactly 19 historical
   PNGs. Commit and non-force push the target candidate.
10. Publish and push:

```text
../foundation-docs/runs/agent-office/20260711_agent_office_m1_2_spatial_animated_advisor_team_office/WORKER_LIVING_PIXEL_OFFICE_PROTOTYPE_COMPLETION_RESULT.md
../foundation-docs/advisor/jobs/20260711_agent_office_m1_2_spatial_animated_advisor_team_office/86_WORKER_LIVING_PIXEL_OFFICE_PROTOTYPE_COMPLETION_RESULT_POINTER.md
```

Stop on any unexpected path, failed gate, unreviewed behavior change, media
defect, or incomplete cleanup. Do not invoke Fable5, claim visual acceptance,
start authenticated/full integration, or start another mission.
