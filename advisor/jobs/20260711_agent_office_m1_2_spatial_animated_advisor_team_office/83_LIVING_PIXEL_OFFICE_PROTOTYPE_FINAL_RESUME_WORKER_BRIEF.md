# 83 Living Pixel-Office Prototype Final Resume Worker Brief

## Assignment

- Actor: Agent Office Worker
- Session: same existing `agent-office/%13` session only
- Model and effort: `<Codex 5.6 SOL: Ultra>`
- Mission: `AGENT_OFFICE_M1_2_SPATIAL_ANIMATED_ADVISOR_TEAM_OFFICE`
- WorkUnits: resume bounded `AO12-PWU-07..09` only
- Repository: `/home/leo/Project/agent-office`
- Branch: `shadow/agent-office-m1-2-spatial-office`
- Required base: `56385b894ff18fd65a4e59f2c4e75f73ea2a56d9`, upstream-equal

## Required Reads

Read directly the prior briefs/results/pointers `79` through `82`,
`83_ADVISOR_HISTORICAL_VISUAL_BASELINE_RECONCILIATION.md`, the clean Fable5
design result, current Agent Office instructions, current source/tests/config,
and actual Git/media state.

The prior correction brief and ESLint clarification remain binding. This final
resume adds only the exact 16 existing historical baseline PNGs to the mutation
allowlist and uses new result/pointer paths.

## Exact Baseline Paths

Regeneration authority is limited to the existing PNG files under:

```text
tests/e2e/baselines/office-scene.spec.ts/
tests/e2e/baselines/spatial-office-accessibility.spec.ts/
tests/e2e/baselines/spatial-office-motion.spec.ts/
tests/e2e/baselines/spatial-office-static.spec.ts/
```

There must be exactly 16 changed PNG paths and no new path. Do not mutate any
historical scene source, CSS, fixture, browser spec/configuration, threshold, or
package to obtain those bytes.

## Required Execution

1. Revalidate the exact restored 69-entry prototype/media state, base/upstream,
   empty index, owner-only ignored artifact root, exact package/compiler
   identities, browser runtime, and free loopback port.
2. Reapply the complete reviewed bridge correction, bounded declaration, four
   consumer cleanups, focused test, lifecycle name correction, dependency gate,
   and exact single-path ESLint configuration from briefs `79` and `81`.
3. Rerun the successful focused test, unfiltered lint, strict typecheck, full
   sequential tests, builds, dependency audit, and diff checks.
4. Regenerate only the exact 16 stale historical PNGs through the configured
   Playwright tests. Record old/new hashes and visual differences. Directly
   inspect all 16 at useful detail. Confirm all current labels use `SIASIU`.
5. Run `npm run test:e2e:demo` again without snapshot update. It must pass
   completely.
6. Continue with composed browser verification, the dedicated 13-scene pixel
   prototype Playwright suite, all lifecycle/semantic/accessibility/security/
   performance/load/cleanup gates, and the evidence verifier.
7. Regenerate all eight ignored prototype media artifacts from the actually
   running corrected prototype. Verify lineage, duration, dimensions, sizes,
   hashes, ignored status, and directly inspect all PNGs plus representative
   WebM/GIF frames.
8. Confirm no deep imports, suppressions, broad declarations, dependency or
   strictness drift, current `Shashu` variants, residual listeners, browsers,
   recorders, or unapproved path changes.
9. Stage only the complete clarified candidate allowlist, including exactly the
   16 PNGs. Commit and non-force push the Agent Office candidate.
10. Publish the exact new result and pointer, return to Advisor, and stop.

## New Result Paths

```text
../foundation-docs/runs/agent-office/20260711_agent_office_m1_2_spatial_animated_advisor_team_office/WORKER_LIVING_PIXEL_OFFICE_PROTOTYPE_FINAL_RESUME_RESULT.md
../foundation-docs/advisor/jobs/20260711_agent_office_m1_2_spatial_animated_advisor_team_office/84_WORKER_LIVING_PIXEL_OFFICE_PROTOTYPE_FINAL_RESUME_RESULT_POINTER.md
```

Commit/push only those exact Foundation Docs files after the target candidate is
committed and pushed.

## STOP Conditions

All prior STOP conditions remain. Also stop if:

- regeneration requires a source/CSS/test/config/threshold/package change;
- the changed historical baseline set is not exactly the existing 16 PNGs;
- any regenerated image contains a current `Shashu` variant or a material
  visual/accessibility defect;
- the unfiltered post-regeneration browser train does not pass;
- any subsequent test, media, evidence, cleanup, or Git gate fails.

Do not invoke Fable5, claim visual acceptance, start authenticated/full
integration, or start another mission.
