# 81 Living Pixel-Office Prototype Correction Resume Worker Brief

## Assignment

- Actor: Agent Office Worker
- Session: same existing `agent-office/%13` session only
- Model and effort: `<Codex 5.6 SOL: Ultra>`
- Mission: `AGENT_OFFICE_M1_2_SPATIAL_ANIMATED_ADVISOR_TEAM_OFFICE`
- WorkUnits: resume bounded `AO12-PWU-07..09` only
- Repository: `/home/leo/Project/agent-office`
- Branch: `shadow/agent-office-m1-2-spatial-office`
- Required base: `56385b894ff18fd65a4e59f2c4e75f73ea2a56d9`, upstream-equal

## Required Reads

Read directly:

- `79_LIVING_PIXEL_OFFICE_PROTOTYPE_CORRECTION_WORKER_BRIEF.md`;
- `WORKER_LIVING_PIXEL_OFFICE_PROTOTYPE_CORRECTION_RESULT.md`;
- `80_WORKER_LIVING_PIXEL_OFFICE_PROTOTYPE_CORRECTION_RESULT_POINTER.md`;
- `81_ADVISOR_ESLINT_PATH_CLARIFICATION.md`;
- the prior clean Fable5 design review result and pointer; and
- actual current Agent Office instructions, Git state, prepared source, media,
  configuration, and package identities.

The prior brief remains binding except that this clarification adds exactly
`eslint.config.mjs` to the mutation allowlist and uses new result/pointer paths.

## Exact Additional Change

In `eslint.config.mjs`, add one path-specific
`tseslint.configs.disableTypeChecked` override covering only:

```text
src/ui/pixel/pixi-public-export-bridge.js
```

Normal JavaScript/recommended lint must remain active. Do not add globals unless
the bridge actually requires a standard runtime global and the existing lint
train proves it; do not broaden the file pattern.

## Required Resume

1. Revalidate the byte-exact restored prepared worktree, HEAD/upstream, empty
   index, ignored owner-only artifact root, exact package/compiler identities,
   and free loopback test port.
2. Reapply the exact reviewed public-root bridge, bounded declaration, four
   consumer cleanups, focused test, and five-dependency gate correction from
   brief `79`.
3. Apply only the exact ESLint path correction above.
4. Run the complete unfiltered verification train from brief `79`, including
   focused tests, lint, strict typecheck, full tests, all builds/audits,
   Playwright suites, performance/accessibility/security/semantic checks, and
   the evidence verifier.
5. Regenerate all eight ignored visual artifacts from the actually running
   corrected prototype. Verify and directly inspect their lineage, dimensions,
   duration, hashes, ignored status, and representative frames.
6. Verify no deep imports, suppressions, broad declarations, dependency or
   strictness drift, residual listeners, browsers, or recording processes.
7. Stage only the inherited exact prototype allowlist, the four additions from
   brief `79`, and `eslint.config.mjs`; commit and non-force push the exact
   candidate.
8. Publish the new result and pointer below, return to Advisor, and stop.

## Result Paths

Full result:

```text
../foundation-docs/runs/agent-office/20260711_agent_office_m1_2_spatial_animated_advisor_team_office/WORKER_LIVING_PIXEL_OFFICE_PROTOTYPE_CORRECTION_RESUME_RESULT.md
```

Pointer:

```text
../foundation-docs/advisor/jobs/20260711_agent_office_m1_2_spatial_animated_advisor_team_office/82_WORKER_LIVING_PIXEL_OFFICE_PROTOTYPE_CORRECTION_RESUME_RESULT_POINTER.md
```

Commit/push only those exact Foundation Docs files after the Agent Office
candidate is committed and pushed.

## STOP Conditions

All prior STOP conditions remain. Also stop if:

- the single-file override does not make the complete unfiltered lint command
  valid;
- any broader ESLint weakening is required;
- any required test, browser, media, accessibility, security, semantic,
  performance, cleanup, or Git gate fails;
- any path outside the clarified exact allowlist is required.

Do not invoke Fable5, claim visual acceptance, start authenticated/full
integration, or start another mission.
