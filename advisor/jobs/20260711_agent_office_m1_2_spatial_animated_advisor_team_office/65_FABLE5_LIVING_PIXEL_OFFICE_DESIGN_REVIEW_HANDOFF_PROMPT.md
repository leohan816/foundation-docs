# Fable5 Living Pixel-Office Design Review Handoff

TARGET_ACTOR: Sentinel
TARGET_SESSION: same existing independent `reviewer-fable5/%5`, never Advisor or Worker
SOURCE_ADVISOR_JOB: `../foundation-docs/advisor/jobs/20260711_agent_office_m1_2_spatial_animated_advisor_team_office`
DO_NOT_PASTE_INTO: Advisor session or `agent-office` Worker
RETURN_RESULT_TO: Advisor
GPT_DIRECT_USE: inspect only

Load `/fable-sentinel` and perform the Level-3
`DESIGN_REVIEW__M1_2_LIVING_PIXEL_OFFICE_RENDERER_AND_PROTOTYPE_GATE`.

Read directly, not from summaries:

- Leo/GPT decision `59_LEO_GPT_LIVING_PIXEL_OFFICE_FINAL_PATCH_DECISION.md`;
- patch manifest `59_M1_2_LIVING_PIXEL_OFFICE_PATCH_MANIFEST.json`;
- review brief `62_FABLE5_PIXEL_OFFICE_DESIGN_REVIEW_BRIEF.md`;
- Advisor validation `64_ADVISOR_LIVING_PIXEL_OFFICE_DESIGN_VALIDATION.md`;
- Worker result and pointer;
- Agent Office commit `9611d0da1479ca5e7a9677641fe767a6b39b4a38` and its exact
  `48c8dbd9f2c5ecea68c28e85137d75db595ef5f9..9611d0d` diff;
- all five changed canonical documents;
- the current source, package, PWA, Vite, Playwright, projection, cue,
  accessibility, fallback, security, and M1 authority surfaces cited by the
  design; and
- actual Git/upstream/dependency/tool state.

Do not trust Worker or Advisor claims. Reproduce load-bearing evidence.

Explicitly verify:

1. the primary viewport is designed as one living game-like office world, not
   cards inside a canvas;
2. all Advisor Team Pods coexist visibly and the Single Advisor Team invariant,
   no-clone rule, assignment fail-closed behavior, and SIASIU naming remain exact;
3. React plus `@pixi/react`/PixiJS selection is feasible with current React,
   Vite, PWA, TypeScript, Playwright, SSR/client, lifecycle, resize, ticker,
   context-loss, teardown, backend, bundle, and cleanup constraints;
4. the DOM semantic mirror cannot silently diverge from Pixi and remains a
   complete keyboard/screen-reader/reduced-motion/static/high-contrast/mobile
   fallback;
5. actors have actual walk/sit/type/review/carry/return/coffee/lounge cycles and
   Channy has roam/sit/eat/drink/sleep/play cycles without operational authority;
6. project-authored code-native art, hashes, ownership, licensing, no external
   asset, and no protected-style imitation boundaries are sufficient;
7. all task movement derives only from accepted structured evidence, with
   precedence, deduplication, stale/conflict suppression, no delayed replay,
   bounded cues, and no invented activity;
8. camera and visual proximity remain presentation-only and cannot create
   authority or evidence;
9. all thirteen acceptance scenes are direct, reproducible, and sufficient to
   demonstrate the Founder intent;
10. the WebM is planned as an actual continuous Playwright recording; MP4 and
    GIF must derive from that WebM; the five PNGs, exact filenames, durations,
    paths, hashes, scenario, source commit, and reproduction commands are all
    enforced;
11. current `ffmpeg`/`ffprobe` absence is handled fail-closed without a hidden
    substitute, installation assumption, or weakened media requirement;
12. performance, memory, object/texture, accessibility, teardown, security,
    rollback, production fixture isolation, and ignored-artifact gates are
    measurable rather than aspirational;
13. a design `PASS` opens only the bounded synthetic prototype and cannot open
    authenticated/full integration; and
14. no runtime/package/test/asset/auth/authority/transport/DB/network change or
    hidden product decision exists in this design delta.

Classify every finding as `DESIGN_DEFECT`, `PRODUCT_INTENT_DEFECT`,
`SECURITY_OR_AUTHORITY_DEFECT`, `ACCESSIBILITY_DEFECT`, `EVIDENCE_GAP`,
`EXECUTION_PREREQUISITE`, or `INFORMATIONAL`.

Verdict: `PASS | PASS_WITH_RISK | NEEDS_PATCH | FAIL`.

Use no agent, sub-agent, delegated context, substitute Reviewer, new session, or
temporary session. Do not patch Agent Office or Advisor files. Write and push
only the exact review result and pointer. Return an ASCII-only pointer to
Advisor and stop. Fable5 does not grant prototype/full integration/final product
approval.
