# 85 Advisor Composed Visual Baseline Reconciliation

## Decision

`PROCEED_WITH_LIMITS`

The composed browser stop is the same stale-evidence class already verified for
the 16 default-browser PNGs. The current authenticated composed source renders
the approved `SIASIU` identity, while the three committed expected images still
show the forbidden current name `Shashu` and an earlier station presentation.

Add only these three existing PNGs to the prior reconciliation:

```text
tests/e2e-composed/baselines/application-office-scene.spec.ts/application-office-desktop-1440x900.png
tests/e2e-composed/baselines/application-office-scene.spec.ts/application-office-mobile-390x844.png
tests/e2e-composed/baselines/application-office-scene.spec.ts/application-office-reduced-motion-1440x900.png
```

All safeguards from `83_ADVISOR_HISTORICAL_VISUAL_BASELINE_RECONCILIATION.md`
remain binding. No composed source, CSS, fixture, spec, config, threshold,
package, authority, authentication, or delivery behavior may change. Regenerate
from current approved source in the configured runtime, inspect all three at
useful detail, rerun composed without update mode, and include all 19 reconciled
historical PNGs in the independent Fable5 visual review.

This is evidence reconciliation only. It does not approve visual direction,
authenticated/full prototype integration, production, or final product status.
