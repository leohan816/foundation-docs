TARGET_ACTOR: Agent Office Worker
TARGET_SESSION: same existing agent-office session, never Advisor or Reviewer
SOURCE_ADVISOR_JOB: ../foundation-docs/advisor/jobs/20260711_agent_office_m1_2_spatial_animated_advisor_team_office
DO_NOT_PASTE_INTO: Advisor session or reviewer-fable5 session
RETURN_RESULT_TO: Advisor
GPT_DIRECT_USE: inspect only

# Agent Office M1.2 AO12-C Evidence-Backed Spatial Motion

Read directly and execute:

1. `39_ADVISOR_AO12_B_ACCEPTANCE_AND_AO12_C_GATE.md`
2. `40_AO12_C_WORKER_BRIEF.md`
3. implementation manifest `23_M1_2_IMPLEMENTATION_MANIFEST.json`
4. chained Leo/GPT decision, design freeze, and all frozen M1.2 canonical docs
5. corrected AO12-B Fable5 result/pointer at Foundation Docs commit `6e2a231`
6. actual Agent Office AO12-B commit `4b751c6`, active repo rules, current M1
   event/activity/scene contracts, static spatial implementation, and all M1
   and AO12-B baseline bytes

Implement only `AO12-IWU-09..11` on exact base
`4b751c6af5b7a1091251273776af3ee8cf1af316` and only within the exact allowlist.
Use GPT-5.6 SOL Ultra in this same existing Worker session. Do not create or
delegate any agent, sub-agent, Designer, Reviewer, or temporary context.

Implement a pure structured-event cue projector/reducer, bounded routes and
operational/verified-idle/Channy presentation, and direct accessibility/visual/
performance proof. Only a new accepted `LIVE_DELTA` may create task motion.
Stale, unknown, offline, conflicting, unassigned, replayed, prose-shaped, and
unverified inputs fail closed to static truth. Keep the work synthetic
test/demo-only; do not mount it in authenticated production runtime or start
AO12-D.

Run every required check, inspect every new visual baseline, measure every
performance target honestly, preserve all M1/AO12-B baselines and M1 authority,
audit exact diff/staged paths, commit and non-force push the exact Agent Office
batch, then write and push only the exact Worker result and pointer in
Foundation Docs. Terminal output must be ASCII-only. Return the pointer to
Advisor and STOP. Do not invoke Fable5 or start AO12-D.
