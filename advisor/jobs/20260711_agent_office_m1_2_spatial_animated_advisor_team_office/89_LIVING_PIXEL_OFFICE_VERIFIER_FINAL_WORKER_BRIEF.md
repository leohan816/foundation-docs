# 89 Living Pixel-Office Verifier Final Worker Brief

## Assignment

- Actor: Agent Office Worker
- Session: same existing `agent-office/%13` only
- Model/effort: `<Codex 5.6 SOL: Ultra>`
- Mission: `AGENT_OFFICE_M1_2_SPATIAL_ANIMATED_ADVISOR_TEAM_OFFICE`
- Repository/branch: `/home/leo/Project/agent-office` / `shadow/agent-office-m1-2-spatial-office`
- Required base: `56385b894ff18fd65a4e59f2c4e75f73ea2a56d9`, upstream-equal

Read directly all governing artifacts `79` through `89`, the clean Fable5 design
result, current instructions, actual source/tests/config/Git/media state, and all
prior blocker results.

## Exact Additional Scope

In addition to the already reviewed candidate and exact 26-PNG reconciliation,
patch only `scripts/verify-living-pixel-prototype-evidence.mjs` as specified in
`89_ADVISOR_EVIDENCE_VERIFIER_RECONCILIATION.md`.

The verifier must model exactly 39 accepted baseline paths: 13 new living-
prototype paths plus 26 reconciled existing paths. It must reject every other
changed, staged, or untracked baseline path and retain every prior non-baseline
gate unchanged.

## Required Completion

1. Revalidate the restored repository state, current regenerated ignored media,
   base/upstream/index, exact path sets, package/compiler/browser pins, and free
   ports.
2. Reapply the reviewed public-root bridge correction, single-file ESLint
   override, focused contract, consumer cleanup, dependency gate, and browser
   identity evidence.
3. Implement only the exact verifier reconciliation above. Add focused automated
   coverage if needed, but do not change screenshot specs, configs, thresholds,
   application behavior, CSS, fixtures, package pins, or compiler settings.
4. Pass focused bridge/verifier tests, unfiltered lint, strict typecheck, full
   87-file/505-test-or-greater train, builds, dependency audit, and diff checks.
5. Regenerate exactly the authorized 26 existing PNGs; inspect all 26 and require
   exact path equality.
6. Pass default browser, composed browser, and dedicated 13-scene/20-test
   prototype trains without filters or snapshot-update mode for final proof.
7. Regenerate and inspect all eight ignored media artifacts and representative
   WebM/MP4/GIF frames.
8. Run the corrected evidence verifier before staging and again after exact
   staging. Both runs must pass and report the exact evidence contract.
9. Verify no forbidden current `Shashu` naming, deep imports, suppressions,
   unexpected paths, residual processes/listeners, or tracked media.
10. Commit and non-force push the exact Agent Office candidate. Publish and push:

```text
../foundation-docs/runs/agent-office/20260711_agent_office_m1_2_spatial_animated_advisor_team_office/WORKER_LIVING_PIXEL_OFFICE_VERIFIER_FINAL_RESULT.md
../foundation-docs/advisor/jobs/20260711_agent_office_m1_2_spatial_animated_advisor_team_office/90_WORKER_LIVING_PIXEL_OFFICE_VERIFIER_FINAL_RESULT_POINTER.md
```

Stop on any unexpected path or failed gate. Do not invoke Fable5, claim visual
acceptance, start full authenticated integration, or start another mission.
