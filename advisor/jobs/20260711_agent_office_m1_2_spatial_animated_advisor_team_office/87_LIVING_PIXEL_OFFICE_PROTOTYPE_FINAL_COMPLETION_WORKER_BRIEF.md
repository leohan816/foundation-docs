# 87 Living Pixel-Office Prototype Final Completion Worker Brief

## Assignment

- Actor: Agent Office Worker
- Session: same existing `agent-office/%13` only
- Model/effort: `<Codex 5.6 SOL: Ultra>`
- Mission: `AGENT_OFFICE_M1_2_SPATIAL_ANIMATED_ADVISOR_TEAM_OFFICE`
- Repository/branch: `/home/leo/Project/agent-office` / `shadow/agent-office-m1-2-spatial-office`
- Required base: `56385b894ff18fd65a4e59f2c4e75f73ea2a56d9`, upstream-equal

Read directly all governing artifacts `79` through `87`, the clean Fable5 design
result, current instructions, actual source/tests/config/Git/media state, and all
prior blocker results. The complete prior correction brief remains binding.

## Exact Final Scope

Reapply the already reviewed public-root Pixi compatibility correction and the
single-file ESLint typed-lint exemption exactly as previously validated.

The historical visual evidence mutation allowlist is now exactly 26 existing
PNG paths:

- the 16 default-browser paths from `83_ADVISOR_HISTORICAL_VISUAL_BASELINE_RECONCILIATION.md`;
- the three top-level composed paths from `85_ADVISOR_COMPOSED_VISUAL_BASELINE_RECONCILIATION.md`;
- the seven nested AO12-D paths from `87_ADVISOR_AO12D_VISUAL_BASELINE_RECONCILIATION.md`.

No source, CSS, fixture, spec, config, screenshot threshold, package, compiler,
authority, authentication, or delivery change is authorized by the baseline
reconciliation.

## Required Completion

1. Revalidate the exact restored 69-entry prototype state, empty index, base
   HEAD/upstream equality, package/compiler/browser pins, media boundary, and
   free local port.
2. Reapply the reviewed bridge/consumer/test/lifecycle/dependency/ESLint
   correction byte-for-byte in substance.
3. Pass the focused 24-test bridge contract, unfiltered lint, strict typecheck,
   full 87-file/505-test train, all builds, dependency audit, and diff checks.
4. Regenerate exactly the 26 authorized existing PNGs from current source.
   Require the changed baseline set to equal the exact allowlist. Record old/new
   hashes and material differences. Inspect every regenerated image at useful
   detail and reject any corrupted, blank, clipped, stale, or forbidden-current-
   name output.
5. Run default and composed browser trains again without update mode. Both must
   pass fully, including accessibility, performance, overflow, reduced-motion,
   forced-colors, mobile, tablet, and 200-percent-text assertions.
6. Run the dedicated 13-scene/20-test prototype Playwright train and all required
   lifecycle, semantic, accessibility, security, performance, load, fallback,
   and cleanup checks.
7. Regenerate all eight ignored prototype media files from the actually running
   corrected prototype. Verify lineage, duration, dimensions, hashes, ignored
   state, and direct inspection of all PNGs plus representative WebM/MP4/GIF
   frames.
8. Run the evidence verifier, exact scope/naming/dependency/suppression scans,
   `git diff --check`, and residual-process/listener checks.
9. Stage only the complete reviewed correction, prototype candidate, exact 26
   reconciled PNGs, and approved docs/tests. Commit and non-force push the target
   branch. Keep large media ignored and untracked.
10. Publish and push:

```text
../foundation-docs/runs/agent-office/20260711_agent_office_m1_2_spatial_animated_advisor_team_office/WORKER_LIVING_PIXEL_OFFICE_PROTOTYPE_FINAL_COMPLETION_RESULT.md
../foundation-docs/advisor/jobs/20260711_agent_office_m1_2_spatial_animated_advisor_team_office/88_WORKER_LIVING_PIXEL_OFFICE_PROTOTYPE_FINAL_COMPLETION_RESULT_POINTER.md
```

Stop on any unexpected path, failed gate, unreviewed behavior change, media
defect, or incomplete cleanup. Do not invoke Fable5, claim visual acceptance,
start authenticated/full integration, or start another mission.
