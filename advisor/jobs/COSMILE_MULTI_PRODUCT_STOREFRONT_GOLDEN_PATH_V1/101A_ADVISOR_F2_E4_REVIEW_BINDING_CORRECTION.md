# Advisor correction — F2 E4 review binding

This correction supersedes only the tier/model lines in handoff 101.

- Live `/model` menu exposed `Opus 4.8`, not Opus 5. User policy requires
  Opus 5 for any Opus-bound Actor and keeps Fable unchanged. No 4.8 selection
  was made; the menu was cancelled and the process remained Fable 5/max.
- The correction addresses an actual runtime failure in candidate DB admission
  that escaped the preceding hard review and protects production denial,
  exact-set activation, and predecessor lineage. This is a documented
  `HARD_IMPORTANT_SAFETY` trigger for this exact re-review.
- Binding is therefore the existing independent Reviewer, actual
  `claude-fable-5`, effort `max`, `/fable-sentinel`, same exact CWD and
  independent context. No clear/restart/exit/session change.
- All exact two-path, no-reread, no-rerun, read-only questions and result paths
  in handoff 101 remain unchanged.
