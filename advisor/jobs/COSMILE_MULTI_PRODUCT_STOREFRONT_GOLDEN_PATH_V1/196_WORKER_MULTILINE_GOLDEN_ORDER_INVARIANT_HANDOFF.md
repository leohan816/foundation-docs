# 196 — WORKER HANDOFF: MULTI-LINE GOLDEN ORDER INVARIANTS

## Binding and safety

- Same Cosmile Worker; actual Opus 5 / xhigh; exact mission CWD.
- Product `b39e914bd65899b1dd05de277124fc9eb0ffc80a`, clean/upstream-equal.
- Runtime already restored: Toss TEST one-shot OFF; local substitute absent.
- Leo evidence accepted as `MULTILINE_ORDER_VISUAL_OK`.
- Read-only categorical invariant gate only. No product/test/DB/runtime/browser/provider/refund/economic action.

Using the active non-production DB internally without printing identifiers, order numbers, SKU keys, provider references, secrets, raw bodies, or PII, prove:

1. orders `4` (`paid 2`, `pending 2`): exactly one new paid Order;
2. exactly one new captured PaymentIntent/PaymentTransaction/provider economic effect, with duplicate intent/provider/economic effects `0`;
3. the new order has exactly `3` OrderItems, `3` distinct admitted candidate SKUs, total quantity `3`;
4. item sums, Order total, captured intent/transaction amount, and currency bind exactly to KRW `80,000`;
5. exactly one reservation/commit set for those three lines, total committed quantity `3`, with no duplicate active reservation/effect;
6. the checkout cart is closed/consumed consistently;
7. refunds `0`, reconciliation/open anomaly `0`, and no provider retry after the successful effect.

Record only counts/categories/booleans. A mismatch is HOLD; do not correct or explore alternatives.

Write only `198_WORKER_MULTILINE_GOLDEN_ORDER_INVARIANT_RESULT.md` and `199_WORKER_MULTILINE_GOLDEN_ORDER_INVARIANT_POINTER.md`, commit/non-force-push docs, return compact PASS/HOLD, STOP.
