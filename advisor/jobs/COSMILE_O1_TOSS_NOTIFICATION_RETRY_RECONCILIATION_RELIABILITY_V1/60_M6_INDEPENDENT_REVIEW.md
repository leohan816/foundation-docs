# M6 Independent Safety Review — IMPLEMENTATION_REVIEW (delta-only)

MISSION: COSMILE_O1_TOSS_NOTIFICATION_RETRY_RECONCILIATION_RELIABILITY_V1 · MODULE M6 · TIER HARD_IMPORTANT_SAFETY
ACTOR: foundation-reviewer-fable5 (independent session; no authoring/patch/commit) · MODEL claude-fable-5 · EFFORT max
SKILL: /fable-sentinel + references READ: delta-review, safety-review, provenance-review, review-classification (+SKILL.md)
ENTRY_READS: agent-office AGENTS/CLAUDE/TEAM_OPERATING_MODEL/roles-reviewer/RESULT_REPORTING_PROTOCOL + Cosmile AGENTS.md/CLAUDE.md(root+app) — done.

## Provenance verified (cryptographic)
- Handoff: branch advisor/cosmile-o1-toss-reliability-v1-20260719 HEAD=27112ec5; blob-at-path 5a9b7b0d; content SHA256 3e3fee2c…25fc — ALL MATCH.
- Worker evidence: 70_M6_RESULT.md SHA256 0ca20a3c…5950 MATCH · 71_M6_POINTER.md SHA256 947e1435…2d40 MATCH.
- Git: worktree clean; HEAD=f13d9e88=CANDIDATE=@{u} (upstream-equal); BASE..CANDIDATE = exactly 1 additive commit; delta = exactly the 7 handoff paths (+592/−2). package/lock not in delta. New exports referenced ONLY inside the 4 delta source files (no hidden coupling).
- CONTAINMENT honored: read-only; reviewer ran NO test/build/typecheck/generate/DB/provider/runtime/mutation. Test numbers below are worker-REPORTED; test MEANING independently verified from source.

## Focused questions — direct-evidence answers
- Q1 YES. GET+POST: o1RuntimeEnabled (flag AND NODE_ENV!=='production') → getShopper (server session resolve) → o1OperatorForCustomer (AuthIdentity issuer/subject → resolveOperatorFromIdentity: Google issuer + digit-subject env allowlist, default-deny, read-failure→denied). Every deny → 404/403 categorical; deny reason NOT echoed (no config oracle).
- Q2 YES. decideOperatorRecovery fixed order: strict key allowlist → type/empty check → consumeNonce (single-use registry, consumed BEFORE verify/bridge) → exact step-up → runBridge exactly once (o1ReliabilityRuntime.ts:420-441). vitest proves: extra/missing/invalid/stale/replayed/denied ⇒ consume≤1, verify≤1, bridge=0; valid ⇒ bridge=1; replay with real Set semantics ⇒ second call rejected, bridge total 1.
- Q3 YES. Body allowlist EXACTLY {stepUpSecret, stepUpNonce}; any other key (orderId/paymentKey/refundId/amount/currency/limit/providerRef each tested) → unexpected_field BEFORE nonce touch. Limit is server-fixed: route passes no options → boundedSweepLimit(undefined)=100 (MAX 500) — caller-unreachable.
- Q4 YES. Verifier = makeO1StepUpVerifier → secret gate (timingSafeEqual, unconfigured→deny-all) → makeTestOnlyExplicitStepUpVerifier: pure literal-equality matcher on all fields; scope.orderId NEVER resolved against Order. Sweep token (26 chars) + nonce (41) + Google sub pass isBoundedId (MAX_ID_LEN=256); reason 26≤64. Instance holds ONLY the order_recovery grant → refund cross-use = action_mismatch deny. order_recovery ∈ SensitiveAction (order/contracts.ts:63). No weakening; see R4.
- Q5 YES distinguishable in authorization: operator request = action order_recovery, operatorRef=Google sub, role admin, reason o1_operator_recovery_sweep; M5 continuation = action refund, system/system, reason o1_recovery_refund_finalize. runBridge port arity 0 (vitest-proven) — no identity enters the bridge. system/system CANNOT mint new refund authority: convergeRefund fails closed without a durable prior Refund row (findRecoverableRefund gates entry + verdictFor + finalize proof; contract: "NEVER a new refund"); makeRecoveryFinalizeVerifier binds every field. Delta's only bridge edit is the :186 narrowing (below).
- Q6 YES. GET returns {ok, 3 integer counts, fresh nonce} only; SQL selects count(*) only (no identifier/PII/hash/payload/key/amount ever selected); repo catch{}→null→categorical 500 "repository_error"; boundedReconciliationProjection drops extra fields + clamps negatives/non-integers to 0 (vitest). POST echoes BridgeSummary counts only. Nonce possession ≠ authority (secret+allowlist still gate).
- Q7 YES. exhausted_unverified = verifyState='exhausted' AND processedState='received' exactly; quarantine settle sets processedState='quarantined' (excluded — dbtest seed matches real settle CASE mapping); claim SQL requires verifyState='pending' (repository.ts:440) → exhausted never claimable; redelivery upsert resurrects nothing (verifyState set only WHEN IS NULL, repository.ts:274); settle fence requires 'pending' → no path completes an exhausted row. dbtest asserts visible-yet-never-claimed (attempts stay 0) across claim/lease-expiry rounds.
- Q8 YES. Stale lease/replay: fenced settle/reschedule/exhaust match 0 rows on stale token (M4/M5, unchanged); route replay = single-use registry consume→false→zero calls; step-up denial still burns the nonce (fail-closed direction). Zero unauthorized economic effect — all economic writes remain behind durable prior authority in unchanged M5 lanes.
- Q9 YES with declared limits. Number(bigint) safe at realistic counts; clamp handles anomalies. Vitest exercises the REAL exported decision/projection the route calls (12 checks = reported 12/12); dbtest (14 = reported 14/14) mirrors the repository SQL predicates LITERALLY (drift 0 verified) against real WU0+M3+M3B migrations. NOT covered (worker-declared §8): route HTTP round-trip; TS repository executed in-process; typecheck/build not rerun (handoff-forbidden) — type-consistency verified by inspection (exports/imports/signatures/role types all match).
- Q10 SUFFICIENT for the frozen manual bounded trigger: GET yields the fresh nonce; POST with {stepUpSecret, stepUpNonce} + allowlisted session completes the trigger via any authenticated HTTP client. Page is count-only display; interactive form absent by DECLARED scope decision (client-component path outside seven-path freeze) — usability gap only, no missing REQUIRED interaction.
- Q11 ACCURATE. No schema/migration/provider/network/runtime-server/economic code in delta; POST reuses the flag-gated existing bridge (production_refused double-gate). Seven-path claim, clean tree, upstream-equal, single additive commit — all independently re-verified. Worker execution evidence (disposable-PG, cleanup absent=True, no full-suite rerun) is reported-state, consistent with artifacts.
- :186 `if (d.kind !== "reschedule") return;` — in-delta, worker-declared recovery-gate carryover: compile-time narrowing, runtime NO-OP (decideVerification with 'retryable' returns only reschedule|exhaust — reliability.ts:39-44). Behavior delta of M5 bridge: none.

## Findings (P-list, delta-reviewable)
- M6-R1 [defect·low·in-scope-patchable] route.ts:44 ↔ o1ReliabilityRuntime.ts:421 — JSON body literal `null` survives `.catch(()=>({}))` (JSON.parse("null") succeeds) → Object.keys(null) throws → uncategorical 500 instead of 400. Zero consume/verify/bridge (throw precedes all ports — fail-closed effects; contract "zero calls and effects" HOLDS). Fix: null/array/non-object body guard → invalid_authorization. Non-blocking.
- M6-R2 [residual·info] Operator trigger has no durable attribution (no audit row of WHICH operator fired the sweep); sweep-internal audits stay category-only system (M5 design). Nonprod-acceptable; production-hardening item.
- M6-R3 [residual·info] Nonce registry is in-process, bounded 256 oldest-evicted, SHARED with the refund step-up lane — operator GET churn can evict outstanding refund nonces (deny-only, fail-closed availability nuance); restart drops nonces (fail-closed). Nonprod-acceptable.
- M6-R4 [note] Deviation D1 ACCEPTED as implemented (Worker Q1): fixed sweep token in scope.orderId is bounded-valid, literal-matched, order-table-unresolved, bridge-unreached, single-grant-scoped. Recommend a later contract note or dedicated non-order scope kind for audit legibility — not required now.
- M6-R5 [note] Route comment "non-economic recovery sweep" is loose wording — the bridge converges economic state under durable PRIOR authority (no new authority, no caller economic input; enforced properties accurate).

## Verdict
PASS_WITH_CORRECTIONS — every HARD safety criterion (auth boundary, exactly-once, zero-on-deny, caller economic input rejected, identity separation, count-only redaction, exhausted-fence, containment/scope accuracy) verified against source; single low-severity robustness correction M6-R1 (plus R2/R3 residuals for production hardening). No STOP condition (no safety-weakening path, no source-of-truth pollution, no contract breakage). Corrections may ride the next approved in-scope batch; no re-review gate needed beyond delta-checking M6-R1.

RESULT_FILE: /home/leo/Project/.mission-tmp/COSMILE_O1_TOSS_NOTIFICATION_RETRY_RECONCILIATION_RELIABILITY_V1/reviewer/80_M6_INDEPENDENT_REVIEW.md
RETURN_TO: foundation-advisor
STOP
