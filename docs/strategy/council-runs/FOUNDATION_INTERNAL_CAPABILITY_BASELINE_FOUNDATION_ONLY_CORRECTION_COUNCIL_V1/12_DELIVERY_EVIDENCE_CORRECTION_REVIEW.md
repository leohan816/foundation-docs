COUNCIL_MISSION_ID:
FOUNDATION_INTERNAL_CAPABILITY_BASELINE_FOUNDATION_ONLY_CORRECTION_COUNCIL_V1
ROLE:
DELIVERY_SCOPE_EVIDENCE_AND_OPERATIONS_CHALLENGER
ROLE_CATEGORY:
CORE
SUBJECT_SHA256:
d7c2f77c8f76986bda222ad926e7994267d7641dca964c65130d7af2ffc748b8
COUNCIL_COMPOSITION:
- PRODUCT_USER_AND_BUSINESS_VALUE_CHALLENGER | CORE
- SYSTEM_ARCHITECTURE_SAFETY_AND_GOVERNANCE_CHALLENGER | CORE
- DELIVERY_SCOPE_EVIDENCE_AND_OPERATIONS_CHALLENGER | CORE
- DATA_QUALITY_LINEAGE_AND_GOVERNANCE_CHALLENGER | SPECIALIST
- AI_MODEL_BEHAVIOR_AND_EVALUATION_CHALLENGER | SPECIALIST
AUTHORITY: RECOMMENDATION_ONLY
OTHER_CURRENT_CHALLENGER_REPORTS_READ: NO

VERDICT:
SUFFICIENT_WITH_CORRECTIONS

EXECUTIVE_CHALLENGE:
The revised contract corrects the material downstream-goal drift. Foundation itself is now the explicit decision subject; SIASIU and Cosmile are non-objectives and may supply only targeted read-only evidence for an exact unresolved Foundation question. The Foundation-wide shallow census, selected isolated probes, seven separate evidence axes, non-executable single P4 candidate, and mandatory post-evidence Council/Strategy/Leo return form a decision-useful baseline rather than an implementation mission.

Four operational corrections are required before return to Leo: separate axis results from probe/gate/unknown dispositions; make the P0 estimate an approved measurable envelope rather than a self-referential bound; freeze exact probe selection and expansion rules before P2; and bound Reviewer correction/re-freeze cycles. The P1 reference to exact historical `foundation-control` implementation must also be reconciled with P3 so it cannot become default deep inspection. These are precise contract corrections, not reasons to reopen the Foundation-only outcome envelope or inspect the subject repositories now.

Runtime and blind-independence re-ACK:
- MISSION: `FOUNDATION_INTERNAL_CAPABILITY_BASELINE_FOUNDATION_ONLY_CORRECTION_COUNCIL_V1`.
- SUBJECT_SHA256: `d7c2f77c8f76986bda222ad926e7994267d7641dca964c65130d7af2ffc748b8`, live verified against the assigned subject.
- ROLE: `DELIVERY_SCOPE_EVIDENCE_AND_OPERATIONS_CHALLENGER`.
- CWD: `/home/leo/Project/council/rules/delivery-scope-evidence-operations`, live verified.
- RUNTIME: `codex-cli 0.144.5`; thread `019f6f58-67f7-7fe0-a62e-847182c21f2b`, live verified from the allowed runtime surface.
- MODEL: `UNVERIFIED`; no model identifier was exposed by the allowed live runtime surface, so none is inferred from session naming or prior missions.
- EFFORT: `UNVERIFIED`; no reasoning-effort value was exposed by the allowed live runtime surface.
- SESSION: tmux pane `%40` was exposed; the sandbox denied authoritative session-name lookup.
- BLIND_INDEPENDENCE: no other current Challenger prompt, report, pane, or output was read; no direct Challenger communication occurred.

VERIFIED_CONTRACT_FACTS:
- The subject digest matches the Strategist-supplied SHA-256 exactly.
- `CURRENT_PRIORITY: FOUNDATION_ITSELF_FIRST`; the primary question asks how much of Foundation itself is implemented, buildable, tested, runtime-demonstrated, integrated, and governed today.
- SIASIU development and Cosmile AI development are explicit non-objectives. Neither may be a target architecture, consumer requirement, or reason to deepen a Foundation path.
- Downstream evidence is Foundation-question-gated, read-only, path-bounded, timeboxed, collision-checked, and closed without inspection when not required for the decision.
- The outcome envelope covers canonical mission and knowledge provenance; product and evidence structure; retrieval/index modalities; model/provider/routing/fallback/prompt/evaluation; deterministic judgment, verdict, safety, and evidence; API/runtime and failure behavior; memory and learning controls without activation; and operations, authority, correction, rollback, and stewardship.
- The contract separates `SOURCE`, `BUILD`, `TEST`, `RUNTIME`, `INTEGRATION`, `AUTHORITY`, and `TARGET_FIT`; it prohibits inherited state and a composite `READY` score.
- P1 freezes representative probe classes before P2 and includes ordinary, missing, conflicting, stale/retracted, unsafe/unsupported, unavailable-route/provider, fallback/degraded, provenance-to-response, restart/recovery, and cleanup cases where relevant.
- P2 is selected, isolated, synthetic, non-production verification. Missing prerequisites must not be simulated into success, and listed boundary breaches produce immediate phase HOLD.
- Historical `foundation-control` evidence grants no current authority, ownership, transfer, deprecation, or implementation authority.
- P4 permits exactly one evidence-derived Foundation candidate and requires it to be non-executable, unauthorized, and pending post-evidence Council and Leo decision.
- No Advisor dispatch, implementation, product/canonical modification, Memory V3 activation, or automatic follow-up is authorized by this correction review.

INFERENCES:
- The contract can answer how much Foundation currently works without becoming exhaustive if P1 remains broad and shallow, P2 admits only decision-relevant equivalence classes, and every omitted or unavailable slice closes explicitly rather than expanding the mission.
- The downstream exception is necessary because Foundation-local source may not prove actual consumer contract, consumption, integration, or degraded behavior. It remains safe only when expected evidence, exact pin, claim ceiling, and gate decision are added to the existing admission record.
- One non-executable candidate reduces option sprawl. Requiring alternatives within that candidate is compatible with a single recommendation when alternatives are comparison evidence, not separately executable work packages.
- The listed P2 capture fields are practical for high-risk probes but disproportionate if repeated verbatim for deterministic read-only or local no-network probes. A risk-tier template with `NOT_APPLICABLE` fields preserves evidence quality while keeping documentation smaller than investigation.
- The baseline estimate remains a factual unknown until P0. Council reasoning can require a measurable envelope and closure rule but cannot supply workday, elapsed-time, cost, sample-size, or actor-capacity facts.

MATERIAL_UNKNOWNS_LEFT_FOR_BASELINE:
- `CU-001` through `CU-008` all remain open. This report does not resolve current Git/worktree state, canonical mission precedence, knowledge provenance and rights, actual capability states, historical implementation, current downstream consumption, safe probe/provider feasibility, or the next implementation priority.
- The P0 estimate must separate engineering workdays from elapsed dependencies such as actor availability, Reviewer scheduling, credentials, provider authority, and authoritative-source response.
- Exact census population, evidence volume, risk-tier distribution, representative equivalence classes, probe count, stochastic repetition count, cleanup burden, and review sample remain repository- and admission-evidence questions.
- If canonical mission precedence cannot be pinned, physical/current-state mapping may continue, but mission-relative `TARGET_FIT` and the P4 implementation candidate remain `UNVERIFIED` or blocked pending authoritative resolution.
- If a provider or downstream path lacks authority or safe feasibility, that path closes as not run/unverified under the corrected status model; it does not automatically extend the baseline.

FINDINGS:
- FINDING_ID: DSEO-CORR-R1-01
  SEVERITY: MATERIAL
  CLAIM: The contract mixes axis results, probe execution dispositions, downstream gate outcomes, and unknown-resolution dispositions, making closure mechanically ambiguous.
  CONTRACT_EVIDENCE: The common axis results list `NOT_ASSESSED`, `NOT_APPLICABLE`, `ABSENT_AT_PIN`, `PRESENT_UNVERIFIED`, `VERIFIED_PASS`, `VERIFIED_FAIL`, and `BLOCKED`; P2 separately uses `NOT_RUN`, `UNVERIFIED`, and `PROVIDER_EXECUTION_NOT_RUN`; the downstream gate uses `NOT_REQUIRED_FOR_DECISION`, `DEFERRED`, `UNVERIFIED`, and `OUT_OF_SCOPE`; unknowns use a third approved disposition taxonomy. `AUTHORITY` and `TARGET_FIT` also need meanings more precise than generic pass/fail.
  LENS_BASIS: Operationally usable evidence states, exact closure, and prevention of silent claim promotion.
  RECOMMENDATION: Keep the seven axes. Define axis-specific result vocabularies at P0, including at minimum `AUTHORITY = CURRENT_CONFIRMED | HISTORICAL_ONLY | CONFLICT | LEO_DECISION_REQUIRED | UNVERIFIED` and `TARGET_FIT = SUFFICIENT | PARTIAL | GAP | UNVERIFIED | NOT_APPLICABLE`. Put `NOT_RUN` and `PROVIDER_EXECUTION_NOT_RUN` in a separate `PROBE_EXECUTION_DISPOSITION`; put downstream outcomes in `DOWNSTREAM_GATE_DISPOSITION`; retain the approved unknown taxonomy only for unknown closure. State that none promotes another axis.

- FINDING_ID: DSEO-CORR-R1-02
  SEVERITY: MATERIAL
  CLAIM: P0 requires an estimate and deviation threshold but does not distinguish the estimate from the authority envelope that permits automatic continuation, and its duration comparison is temporally ambiguous.
  CONTRACT_EVIDENCE: P0 must “return a finite workday and elapsed-time estimate” and the mission returns if projected duration exceeds “the P0 upper bound” by `max(1 working day, 25%)`; the contract does not say who accepts the original upper bound, when it freezes, or whether external elapsed dependencies count against engineering effort.
  LENS_BASIS: Measurable timebox admission, scope-deviation return, and no self-authorized expansion.
  RECOMMENDATION: Before dispatch, have Leo approve either a numeric outer engineering-effort/elapsed-time/external-cost ceiling or an exact rule that authorizes P0 only. P0 must freeze a phase-by-phase estimate with lower/upper bounds, confidence, evidence volume, review/correction allowance, and separate elapsed dependencies. Continue automatically only if that estimate fits the approved envelope. Apply `max(1 working day, 25%)` to later reforecasts against the frozen accepted P0 upper bound; otherwise return before deeper work.

- FINDING_ID: DSEO-CORR-R1-03
  SEVERITY: MATERIAL
  CLAIM: Freezing probe classes is not sufficient to prevent cherry-picking or late scope growth unless P1 freezes the selection rule, exact cases or content-addressed fixtures, omissions, claim ceilings, and expansion triggers.
  CONTRACT_EVIDENCE: P1 lists required representative and negative classes and decides slices, while P2 says repetitions and sample claims must be justified before execution. The contract does not require the inventory denominator/equivalence classes, exact pre-P2 selection record, omitted-class rationale, or bounded expansion rule.
  LENS_BASIS: Representative evidence, reproducibility, proportional sampling, and bounded runtime work.
  RECOMMENDATION: Require a content-addressed `P1_PROBE_FREEZE` containing population/denominator where knowable, equivalence class, selected case or deterministic selection rule, negative/degraded counterpart, expected oracle, exact pins/fixtures, execution tier, repetitions where stochastic, omission rationale, maximum claim, owner, time/cost cap, and expansion trigger. Expand only for contradiction, duplicate path, failed invariant, inconsistent repeated observation, new material risk, or a finding that changes the Foundation decision, all inside the approved envelope.

- FINDING_ID: DSEO-CORR-R1-04
  SEVERITY: MATERIAL
  CLAIM: P1's requirement to include exact relevant historical `foundation-control` physical implementation can be read as default inspection and conflicts with the P3 historical-evidence gate.
  CONTRACT_EVIDENCE: P1's census list includes “exact relevant historical `foundation-control` physical implementation,” while P3 separately authorizes historical inspection as physical/prior-behavior evidence and denies authority transfer. `CU-005` leaves exact relevance unknown.
  LENS_BASIS: Foundation-only scope, actor/repository admission, and prevention of historical evidence becoming an objective.
  RECOMMENDATION: In P1, record Foundation-local references, claimed provenance, and `CU-005` without opening `foundation-control` by default. Admit exact historical paths only in P3 after a Foundation-local decision-critical provenance, dependency, duplication, or prior-behavior question is recorded with repository/path pin, owner, timebox, expected evidence, claim ceiling, collision state, and stop. Historical evidence must remain non-authoritative unless Leo separately decides ownership.

- FINDING_ID: DSEO-CORR-R1-05
  SEVERITY: MATERIAL
  CLAIM: P4 review and factual correction can become an unbounded audit/rework loop, and the Reviewer list can be interpreted as reperforming every investigation.
  CONTRACT_EVIDENCE: P4 directs the Reviewer to inspect code, sources, commands, configuration, tests, probes, traces, failures, routing, boundaries, and cleanup, then permits corrections to be re-frozen and re-reviewed “as required” without a sample rule or correction-cycle limit.
  LENS_BASIS: Review independence, delivery effort, measurable stop, and documentation-to-investigation proportionality.
  RECOMMENDATION: Freeze the Reviewer protocol at P1. Require full review of admission, prohibited-boundary, authority, failed/blocked, claim-ceiling, candidate-supporting, and cleanup evidence, plus a risk-based reproducibility sample of passing rows. Permit one bounded factual correction/re-freeze cycle inside the estimate. Further material change, repeated reproduction failure, or evidence-set expansion returns `HOLD` or `UNVERIFIED` to Strategy/Leo rather than looping.

- FINDING_ID: DSEO-CORR-R1-06
  SEVERITY: ADVISORY
  CLAIM: The evidence envelope is exact but can create more documentation than investigation unless records and outputs are risk-tiered and consolidated.
  CONTRACT_EVIDENCE: Every P2 probe records Git pins, principal, reads/writes, paths, dependencies, listeners, networks, provider/account/environment, credential names, inputs, caps, evidence, abort, teardown, and resource inventory; final artifact consolidation is not stated.
  LENS_BASIS: Practical evidence capture, operator workload, and minimum closure package.
  RECOMMENDATION: Use three templates: Tier 0 deterministic read-only with pin/method/output/failure/owner; Tier 1 isolated local execution with expected writes/resources/abort/cleanup; Tier 2 networked, credentialed, provider, sensitive, or mutation-capable execution with the full record and deny-by-default admission. Allow explicit `NOT_APPLICABLE`. Consolidate closure into six content-addressed outputs: admission/estimate manifest, capability matrix, trace/probe index, physical-authority-integration map, reviewed single candidate, and review/closure record.

FOUNDATION_ONLY_BOUNDARY:
SUFFICIENT. The contract unambiguously makes Foundation itself the decision subject and asks how much of its current canonical mission is implemented and working. SIASIU and Cosmile are explicit non-objectives, cannot supply target architecture or consumer requirements, and cannot justify deepening. Longer-term atomic matching and governed learning are capped at structural, source/control, or target-fit evidence without schema, policy, activation, promotion, or implementation. Correct DSEO-CORR-R1-04 so historical `foundation-control` inspection is also unambiguously conditional rather than a default P1 task.

DOWNSTREAM_EVIDENCE_GATE:
SUFFICIENT_WITH_CORRECTION. It is necessary and materially narrow: Foundation-local first, exact Foundation question, local insufficiency, decision impact, repository/path boundary, actor, timebox, collision state, prohibited depth, and stop condition. Add `EXPECTED_EVIDENCE`, `EXACT_PIN_OR_PINNING_METHOD`, `INSPECTION_METHOD`, `MAXIMUM_CLAIM`, `GATE_OWNER_AND_DECISION`, and `CLOSURE_DISPOSITION`. A gate may prove only current consumption, contract, integration, or degraded behavior for that exact question; it may not create a downstream objective or implementation dependency.

EVIDENCE_AND_CLAIM_CEILINGS:
The seven axes and evidence fields are sufficient in coverage, and the contract correctly rejects file existence, counts, test-source presence, historical reports, and adjacent success as runtime proof. Apply DSEO-CORR-R1-01 so axis results, execution dispositions, gate dispositions, and unknown dispositions are mechanically separate. P1 must freeze exact probe selection under DSEO-CORR-R1-03. Maximum claims remain `REVIEWED_FOUNDATION_CURRENT_STATE_AT_EXACT_NON_PRODUCTION_EVIDENCE_CEILINGS`; atomic matching is structural only; learning is source/control only; small stochastic samples are exact observations only; unavailable provider or downstream evidence remains not run/unverified; no corpus readiness, general AI reliability, production safety, legal approval, ownership transfer, or implementation readiness may be inferred.

P0_P4_BOUNDING:
Conditionally adequate after DSEO-CORR-R1-02, DSEO-CORR-R1-03, and DSEO-CORR-R1-05. P0 must freeze the accepted phase estimate and outer envelope; P1 freezes census denominators, selected slices, exact probe protocol, exclusions, and Reviewer protocol; P2 executes only admitted Foundation slices; P3 admits exact historical/downstream questions only; P4 freezes evidence, performs bounded review/correction, creates one candidate, returns, and stops. Engineering effort and external elapsed dependencies must remain separate. At expiry, each omitted, blocked, or unavailable item closes with owner, decision impact, evidence route, and disposition; expiry never authorizes extension.

P4_NON_EXECUTABLE_GATE:
SUFFICIENT. Exactly one candidate is allowed only after evidence freeze and review. Its required status, authorization, pending-decision, and no-dispatch statements are explicit. Alternatives should remain a compact comparison within the candidate, including no-build/defer, rather than multiple executable packages. The candidate cannot modify evidence, select authority, accept risk, trigger implementation, activate Memory V3, or create a follow-up mission.

POST_EVIDENCE_COUNCIL_AND_LEO_GATE:
SUFFICIENT AND NON-BYPASSABLE AS WRITTEN. The Advisor final audit returns the frozen baseline and non-executable candidate to Strategy; Strategy must route the candidate through the previously approved post-evidence Council challenge before synthesis and Leo decision. No implementation, activation, dispatch, or next mission starts automatically. Preserve the literal three status lines and final `RETURN_TO_STRATEGY_AND_LEO / STOP` in every derived instruction and closure package.

REQUIRED_CORRECTIONS:
1. Separate axis results, probe execution dispositions, downstream gate dispositions, and unknown-resolution dispositions; add axis-specific `AUTHORITY` and `TARGET_FIT` values and prohibit cross-promotion.
2. Make the P0 estimate gate measurable: identify the approving authority/envelope, freeze the accepted P0 upper bounds, separate workdays from elapsed dependencies and external cost, and apply the deviation formula only to later reforecasts.
3. Freeze exact representative/negative probe selection, omissions, oracles, pins, repetitions, claim ceilings, and expansion triggers before P2.
4. Move actual historical `foundation-control` inspection out of default P1 coverage and admit it only under a P3 exact-question record.
5. Freeze a risk-based Reviewer protocol at P1 and bound P4 to one factual correction/re-freeze cycle before return/HOLD/UNVERIFIED.
6. Add expected evidence, exact pinning method, inspection method, maximum claim, owner/admission decision, and closure disposition to the downstream gate.

OPTIONAL_IMPROVEMENTS:
- Adopt the three risk-tier probe templates and allow explicit `NOT_APPLICABLE` fields.
- Consolidate outputs into the six-item closure package so specialist adjuncts reference common evidence IDs rather than duplicate records.
- State that unresolved canonical mission precedence permits physical/current-state mapping but blocks mission-relative target-fit and the P4 priority candidate.
- Require the P0 estimate to budget evidence indexing, Reviewer work, one correction cycle, teardown, and closure rather than only probe execution.

WHAT_WOULD_CHANGE_MY_VERDICT:
- Change to `SUFFICIENT` when the six required corrections are incorporated without weakening the Foundation-only boundary, evidence ceilings, or post-evidence gate.
- Change to `HOLD` if the returned contract lets P0 self-authorize an unbounded estimate, makes historical/downstream inspection default, permits unpinned or post-selected probes, allows repeated review/re-freeze loops, conflates unavailable work with passing evidence, or weakens the non-executable Council/Leo gate.
- No Council reasoning can change the factual baseline unknowns into verified implementation or runtime facts.
CONFIDENCE:
HIGH

BOUNDARY_CONFIRMATION:
PRODUCT_REPOSITORY_INSPECTED: NO
PRODUCT_DECISION_MADE: NO
RISK_ACCEPTED: NO
SUBJECT_PATCHED: NO
ADVISOR_DISPATCHED: NO
IMPLEMENTATION_STARTED: NO
INDEPENDENT_REVIEW_CLAIMED: NO

RETURN_TO: STRATEGY_DECISION_ARCHITECT
STOP.
