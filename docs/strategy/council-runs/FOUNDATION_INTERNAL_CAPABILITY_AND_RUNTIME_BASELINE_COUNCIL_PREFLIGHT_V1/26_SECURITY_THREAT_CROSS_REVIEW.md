SESSION:
foundation-council-security-threat
ROLE:
SECURITY_THREAT_AND_ABUSE_CHALLENGER
CATEGORY:
SPECIALIST
MISSION_ID:
FOUNDATION_INTERNAL_CAPABILITY_AND_RUNTIME_BASELINE_COUNCIL_PREFLIGHT_V1
ROUND: 2_OPEN_CROSS_REVIEW
ALL_SEVEN_INITIAL_REPORTS_READ: YES
INITIAL_REPORT_IMMUTABLE: YES
OTHER_SELECTED_REPORTS_VISIBLE: YES
UNSELECTED_ROLE_OUTPUT_VISIBLE: NO

INITIAL_VERDICT:
PROCEED_WITH_CORRECTIONS
FINAL_VERDICT_AFTER_CROSS_REVIEW: PROCEED_WITH_CORRECTIONS
CONFIDENCE: HIGH
AGREED_ISSUES:
- `ISSUE-02 — Phased static-first execution`: ACCEPT. All seven reports support a shallow Foundation-wide inventory followed by checkpoint-selected runtime and downstream depth.
- `ISSUE-04 — Per-probe admission, isolation, provider authority, and cleanup`: ACCEPT with the risk tiers specified below. “Isolated non-production” is not itself a control.
- `ISSUE-05 — Physical, canonical, runtime, and decision authority`: ACCEPT. A shallow physical/authority census is baseline-default; deeper historical inspection is conditional.
- `ISSUE-06 — Representative traces and claim ceiling`: ACCEPT. Selection follows the static inventory, is predeclared, and supports only pinned path-level claims.
- `ISSUE-08 — Atomic products and matching`: ACCEPT. The maximum claim is structural readiness against a non-normative rubric using synthetic profiles, never schema, policy, privacy, security, Legal, or real-person readiness.
- `ISSUE-09 — Governed learning and Memory`: ACCEPT. Only source/control readiness and no-effect evidence are permitted; no role supports activation.
- `ISSUE-10 — Legal, rights, provider, jurisdiction, and counsel timing`: ACCEPT. Inventory fields are bounded; provider calls are affected-path gated; counsel is conditional rather than a baseline-wide admission requirement.
- `ISSUE-12 — Facts versus options`: ACCEPT the required separation. Evidence-derived options may follow only after the current-state map is frozen and independently reviewed.
- `ISSUE-13 — Unknown preservation`: ACCEPT. Factual unknowns retain their proper evidence routes, and checkpoint expiry never turns absence of evidence into resolution.
NARROWED_OR_REJECTED_ISSUES:
- `ISSUE-01`: ACCEPT_WITH_NARROWING. The exact Leo decision, allowed recommendations, exclusions, and priority-changing evidence rule are pre-dispatch requirements. Named current or approved near-term consumer evidence may be completed during P0/P1 because discovery of actual consumption is part of U-009. Reject `DOWNSTREAM_VALUE_STATE` as an eighth canonical axis; preserve it as a mandatory decision-context adjunct.
- `ISSUE-03`: ACCEPT_WITH_NARROWING. Retain the seven canonical axes. Security, Legal/Policy, behavior evaluation, trace lineage, source authority, artifact governance, and downstream decision value are applicable adjunct records, not additional top-level axes.
- `ISSUE-04`: NARROW documentation by risk tier. Deterministic read-only inspection does not need the same manifest as executable repository code or an external provider call. Every tier still has an exact subject pin, authority, evidence destination, and stop/cleanup rule.
- `ISSUE-06`: NARROW any implied universal scenario minimum. P0/P1 must derive materially distinct trust, evidence, execution, authority, and consumer paths before selecting traces; Council sets no corpus denominator or numeric sufficiency threshold.
- `ISSUE-07`: ACCEPT_WITH_NARROWING. Five repetitions for selected critical stochastic scenarios and three for selected routine stochastic scenarios are useful defaults for bounded failure discovery, not universal hard minima or reliability evidence. Deterministic paths and lower-ceiling observations may use fewer with explicit rationale; stochastic `BOUNDED_PASS` may not rest on one run.
- `ISSUE-11`: ACCEPT_WITH_NARROWING. The Independent Reviewer may challenge a frozen P1 protocol and sample before execution and verify a risk-based subset plus all blocking failures and cleanup at P4. The Reviewer does not co-design the protocol, approve architecture, accept risk, or become an evidence-producing actor.
REASON:
The seven reports independently converge on the same root correction: broad shallow visibility is necessary, while executable depth must be decision-linked, fail-closed, and claim-limited. Cross-review strengthens the initial security position by showing that probe isolation, state non-promotion, source authority, trace selection, nonactivation, and evidence/option separation are not specialist embellishments; they are shared prerequisites. It also shows where literal accumulation would overcorrect: adding every specialist concern as a new axis, requiring full manifests for static reads, fixing evaluation counts before behavior inventory, or requiring repeated Leo approval for routine work already inside a bounded authority envelope.
POSITION_CHANGED: NO
CHANGED_POSITION_DETAILS:
The verdict and core security findings do not change. The position is narrowed in four ways: use tiered probe records rather than one maximal manifest for every command; treat `5/3` as defaults for selected stochastic scenarios rather than universal minima; allow same-mission implementation options only after an immutable reviewed evidence freeze; and consolidate overlapping provider candidates into one call-gate artifact with separately closable authority, security, contractual, and data-handling subrecords.
MINIMUM_PRE_DISPATCH_CORRECTIONS:
1. Pin a `BASELINE_DECISION_CONTRACT`: exact Leo decision, permitted recommendations, explicit non-decisions, current/near-term outcome envelope, priority-changing evidence rule, no-build option, and no automatic implementation.
2. Authorize one phased mission with `P0_ADMISSION`, `P1_STATIC_FOUNDATION_WIDE_MAP`, `P2_SELECTED_ISOLATED_PROBES`, `P3_TARGETED_HISTORICAL_OR_DOWNSTREAM_CHECKS`, and `P4_EVIDENCE_FREEZE_REVIEW_AND_OPTIONS`; state exact entry, narrowing, HOLD, cleanup, and material-escalation returns.
3. Freeze the seven-axis cell vocabulary, mandatory evidence context, mismatch reporting, claim ceilings, and prohibition on composite `READY`, `SAFE`, `SECURE`, `LEGAL`, or `OPERATIONAL` promotion.
4. Add the three-tier probe admission model, deny-by-default network and credential posture, provider-specific call gate, immediate breach-stop semantics, prohibited-data scan, resource/cost limits, and verified teardown.
5. Require a physical/canonical/runtime/contract/consumer/decision authority ledger. Historical location and use are evidence only; transfer, repair, replacement, retirement, or continued ownership remain options with `IMPLEMENTATION_AUTHORIZED: NO` and U-014 preserved.
6. Require an inventory-derived trace and AI evaluation contract with explicit behavior, trust boundary, scenario selection, exact configuration/evidence pins, negative and denial cases, failure preservation, expansion triggers, and bounded claim ceiling.
7. State the matching ceiling as `STRUCTURAL_READINESS_ONLY` and the learning/Memory ceiling as `SOURCE_AND_CONTROL_READINESS_ONLY`; forbid real PII, real-profile matching, Memory V3, sender/intake, persistent candidates, autonomous jobs, tool side effects, promotion, canonical writes, and serving-authority changes.
8. Attach bounded rights, provider, evidence-artifact, and unresolved-policy dependencies to affected rows and traces without turning the mission into a Legal or security audit.
9. Freeze and independently review the factual current-state map before deriving a separate options section; require every option to state consumer/decision effect, evidence, unknowns, no-build alternative, dependency, reversibility, and Leo decision.
10. Preserve all unresolved IDs at checkpoint and expiry. Missing authority or safe execution narrows the evidence ceiling to `NOT_RUN`, `UNVERIFIED`, `BLOCKED`, or `EXTERNAL_PENDING`; it does not authorize preparation work, simulation as official evidence, scope extension, or risk acceptance.
DETAILS_BETTER_DEFINED_BY_ADVISOR_AT_P0:
- Exact repository, branch, commit, worktree, dependency, process, port, and temporary-resource pins.
- Actor capacity, collision status, phase sequencing, workday estimate, external elapsed dependencies, and the proposed schedule within Leo's pre-authorized envelope.
- The inventory-derived capability classes, current-consumer evidence, historical/dependency triggers, trace equivalence classes, selected scenarios, and expansion rationale.
- Exact axis values beyond the minimum vocabulary, evidence-index implementation, artifact paths, retention/deletion mechanics, resource ceilings, and cleanup commands.
- Behavior IDs, deterministic versus stochastic classification, scenario-specific repetition counts, competent oracles, language/locale scope, model/provider identifiers actually exposed, and cost/latency capture.
- Provider-specific account, endpoint, credential reference, egress, content class, contract/data-use facts, rate/cost bounds, and deletion behavior before the affected call.
- Numeric duration, effort, rate, cost, sampling, and quality thresholds. P0 proposes evidence-grounded values; Council invents none.
PHASE_APPROVAL_RECOMMENDATION:
Use one bounded Advisor mission with internal phase gates and mandatory return only on material escalation. Leo's pre-dispatch approval should cover P0/P1 and conditional P2/P3/P4 inside exact no-write, no-shared-resource, no-PII, no-activation, actor, provider, cost, and scope boundaries. P0/P1 do not automatically promote later claims: the Advisor records each gate predicate and may continue only when it passes. A new Leo decision is required before any new credential or provider authority, expanded network destination, real/shared/staging/production data or resource, mutation or tool authority, actor outside the approved composition, product/canonical change, Memory or learning activation, materially different implementation question, or deviation beyond Leo's predeclared duration/effort/cost rule. This avoids routine reapproval while preserving authority at every blast-radius increase.
TIMEBOX_AND_ESCALATION_RECOMMENDATION:
Before dispatch, Leo should approve an outer authority, resource, cost, and scope ceiling plus a material-deviation rule, not an invented precise calendar estimate. P0 returns a repository-grounded workday/range estimate, actor sequence, external elapsed dependencies, and confidence. The mission may continue within the approved rule. It returns `NARROW` when decision-critical evidence can be obtained with fewer probes or actors; `HOLD` when a closure-critical claim cannot be investigated safely inside authority; and `MATERIAL_ESCALATION` when expected duration, cost, actor load, exposure, provider use, or scope exceeds the approved rule. Boundary breach, prohibited-data discovery, unexpected write/egress/activation, privilege escalation, resource-cap exceedance, or cleanup failure stops the affected probe immediately regardless of timebox.
STATE_AND_EVIDENCE_MODEL_RECOMMENDATION:
Keep canonical axes `SOURCE | BUILD | TEST | RUNTIME | INTEGRATION | AUTHORITY | TARGET_FIT`. Use a minimum non-promotional result set `NOT_ASSESSED | NOT_APPLICABLE | ABSENT_AT_PIN | PRESENT_UNVERIFIED | NOT_RUN | VERIFIED_PASS_AT_PIN | VERIFIED_FAIL_AT_PIN | BLOCKED | EXTERNAL_PENDING`. Every nontrivial cell records `CAPABILITY_ID`, axis, result, evidence class, exact subject/config/environment/resource pin, observed-at time, owner, unresolved IDs, claim ceiling, and decision impact. Authority retains explicit values for current confirmed, historical only, conflict, Leo decision required, and unverified. No roll-up score is permitted.
Applicable adjuncts, referenced rather than duplicated, are: `DECISION_AND_DOWNSTREAM_VALUE_CONTEXT`; `PHYSICAL_SOURCE_AND_AUTHORITY_LEDGER`; `PROBE_ADMISSION_AND_CLEANUP_RECORD`; `TRACE_AND_TRANSFORMATION_ENVELOPE`; `SECURITY_CONTROL_EVIDENCE`; `LEGAL_POLICY_DEPENDENCY_RECORD`; `EVIDENCE_ARTIFACT_GOVERNANCE_RECORD`; and `BEHAVIOR_EVALUATION_RECORD`. A security control may be reported only as source-declared, test-evidenced, or runtime-effective in the exact pinned isolation, with prevention/denial, detection, containment, recovery, and cleanup evidence separately visible. None implies production effectiveness or a security audit.
Risk tiers are:
- `T0_STATIC_READ`: no repository code execution, credentials, network, listener, or mutable external state. Record subject/command/read scope, owner, evidence destination, prohibited-data handling, and completion/cleanup.
- `T1_LOCAL_EXECUTION`: build, test, index, database, API, or runtime in mission-owned isolation with no external provider. Add principal, dependency/install behavior, mounts and allowed writes, data source, processes/ports, resource/time limits, expected artifacts, stop rule, and before/after teardown inventory.
- `T2_EXTERNAL_OR_MUTATION_CAPABLE`: any provider/network/tool/memory/candidate/promotion-capable path, even when expected to deny. Add exact account/environment and credential reference without value, egress allowlist, contract/content/data-use status, rate/cost, tool/write authority, retention/deletion, audit identifiers, and immediate abort conditions. If no-effect isolation cannot be established before execution, the path is `NOT_RUN`.
AI_REPETITION_RECOMMENDATION:
Use five repetitions as the default for each selected critical stochastic scenario and three for each selected routine stochastic scenario, solely to discover bounded variability and preserve intermittent failures. These defaults apply after P0 identifies actual behaviors and only to scenarios used for a `BOUNDED_PASS` or material comparison. The Advisor may propose fewer when the path is deterministic or the claim is lowered to a limited observation, and may propose more within the approved time/cost ceiling when risk or observed variance warrants it; every departure needs rationale and Reviewer challenge. Never rerun away a failure, average away an unauthorized action, or infer statistical reliability, SLA, supported-language quality, general safety, or model capability from these counts.
OPTIONS_SEPARATION_RECOMMENDATION:
Keep evidence-derived implementation options in the same Advisor mission, but only after P4 freezes the complete current-state evidence map and the Independent Reviewer records its evidence, boundary, reproducibility, failure-preservation, and cleanup review. Options live in a separate immutable section or artifact and may cite but not rewrite evidence. The Reviewer does not approve an option. If the evidence map is incomplete for the decision, or option preparation requires new execution, authority, or facts outside the mission envelope, return `HOLD` or `MATERIAL_ESCALATION`; do not open an implementation mission. A separate later post-evidence Council/Strategy/Leo decision remains required before implementation.
UNKNOWN_CONSOLIDATION_RECOMMENDATION:
- Keep the Product proposal for the exact next decision and consumer/outcome envelope as a distinct Leo decision unknown; assign a unique final ID.
- Preserve Delivery's actor-capacity/collision proposal as a separately closable child of U-015, with its per-actor owner, P0 timing, evidence method, and blocking effect. It need not become another top-level ID.
- Preserve Legal `CU-LP-001` as the authoritative-rights/claims child of U-003, separate from repository provenance and physical-source evidence. Location, consumption, or a repository license file does not close authoritative permission where confirmation is required.
- Preserve Legal `CU-LP-002` as an applicability/counsel child linked to U-010. Synthetic structural investigation does not close the Founder privacy/product decision or later jurisdiction-specific interpretation.
- Consolidate Legal `CU-LP-003` and Security `CU-SEC-001` into one `EXTERNAL_PROVIDER_CALL_GATE` artifact cross-referenced to U-013. Keep independently closable child records for internal credential/network/use authority; provider account/environment and technical security scope; contractual content/data-use terms; retention/training/logging/location/deletion; and rate/cost/audit/cleanup. Each child retains its owner, method, timing, blocking effect, and approved disposition; one passed child never closes the gate.
- Keep Data `DG-CU-001` distinct because evidence-artifact access, rights, retention, redaction, and cleanup apply even without a provider call.
- Reclassify Data `DG-CU-002` as the required P0 `TRACE_COVERAGE_AND_CLAIM_CONTRACT`, not a factual product unknown. Any Leo-selected residual-risk or scope threshold remains a separately recorded Leo decision.
- Keep AI `AI-U-016` distinct because the current behavior/input/consequence/fallback contract is repository and authoritative-owner evidence needed before evaluation.
- Do not register AI `AI-U-RUNTIME-001` as open: the Round 1 freeze record supplies host-side authoritative verification of all selected sessions as Codex CLI 0.144.5, `gpt-5.6-sol`, `xhigh`, and exact role CWDs. This is authoritative-source evidence, not Council inference; the original report's contemporaneous limitation remains immutable.
- For U-003, U-004, U-007, and U-011, use lossless child closure records instead of duplicative new parents: physical/provenance versus rights; current representation versus future matching policy; configured/local behavior versus provider facts and authorized execution; current learning mechanisms versus future scope and activation authority. A parent is never globally closed by one evidence class.
MISSING_OR_OBSCURED_FINDINGS:
- The matrix partially subsumes but does not explicitly preserve `SECURITY-03`: dependency resolution, package lifecycle scripts, remote artifacts, host mounts, public listeners, and resource exhaustion are supply-chain and denial-of-service surfaces. They must appear in T1/T2 admission; a tracked-code prohibition alone does not contain them.
- The matrix should preserve Security's distinction between safe inability and breach. Missing credentials, dependencies, or authority before execution produce `NOT_RUN` or `UNVERIFIED`; an unexpected write, egress, privilege, prohibited-data exposure, activation, or cleanup failure after admission is an immediate stop and unresolved incident.
- Product's minority concern is not a reason to omit the Foundation-wide census. It is a reason to prevent target-fit gaps from becoming priorities without decision and consumer context.
- Data's minority claim ceiling remains material: without source authority, transformation lineage, and population/coverage context, a result may be an inventory or path trace but not trusted-knowledge or corpus-readiness evidence.
- AI's fixed `5/3` proposal is preserved as a specialist default, while the cross-review narrows it to selected stochastic scenarios and bounded failure discovery.
MATERIAL_DISAGREEMENTS_REMAINING:
- `MD-01`: Exact decision-contract timing and whether downstream value is an axis.
- `MD-02`: One internally gated mission versus a second Leo approval after P0/P1, and exact outer-ceiling semantics.
- `MD-03`: Minimum canonical state model versus specialist record proliferation.
- `MD-04`: Whether AI `5/3` repetitions are universal minima or risk-based defaults.
- `MD-05`: Reviewer challenge at P1 without becoming a protocol designer.
- `MD-06`: Same-mission options after evidence freeze versus a separate options mission.
- `MD-07`: Provider and composite-unknown consolidation without erasing evidence routes.
These are final wording and authority-structure disagreements; no factual unknown can resolve them. The propositions below would resolve them without another general review.
EXACT_PROPOSITION_AND_RESOLUTION_CONDITION:
- `MD-01 PROPOSITION`: “Before dispatch, Leo pins the decision, output authority, exclusions, and priority-changing evidence rule. P0/P1 may discover and verify actual downstream consumers. `DECISION_AND_DOWNSTREAM_VALUE_CONTEXT` is mandatory but is not an eighth capability axis.” `EVIDENCE`: U-009 leaves actual consumption unknown, while all reports require decision-linked depth. `RESOLUTION_CONDITION`: The corrected instruction contains that exact timing and classification and prohibits target-fit gaps from becoming priorities by themselves.
- `MD-02 PROPOSITION`: “One Leo-approved mission may pass internal gates without routine reapproval only inside a predeclared no-write/no-shared-data/no-PII/no-activation actor, provider, cost, effort, and scope envelope; every material deviation or blast-radius increase returns to Leo.” `EVIDENCE`: All roles support phased execution; repeated routine approval adds delay, while unbounded continuation erodes authority. `RESOLUTION_CONDITION`: The instruction names each gate predicate, the Leo-set material-deviation rule, and the mandatory return triggers in `PHASE_APPROVAL_RECOMMENDATION`.
- `MD-03 PROPOSITION`: “The seven axes remain canonical; specialist concerns are applicable adjunct records sharing one cell/evidence vocabulary and referenced from relevant rows.” `EVIDENCE`: Every role rejects composite readiness, but additional axes would duplicate context and create a documentation system. `RESOLUTION_CONDITION`: The corrected instruction adopts the seven axes, minimum vocabulary, adjunct list, and no-roll-up rule above.
- `MD-04 PROPOSITION`: “Five critical and three routine repetitions are defaults only for selected stochastic scenarios used for bounded pass/comparison; P0 may vary with rationale and Reviewer challenge, and no statistical reliability claim follows.” `EVIDENCE`: U-006/U-007 behavior is unknown, fixed universal counts are ungrounded before inventory, and single stochastic passes are weak. `RESOLUTION_CONDITION`: The evaluation contract states the default, variance/failure-discovery ceiling, failure-preservation rule, and variation authority.
- `MD-05 PROPOSITION`: “Advisor and Foundation Worker design and freeze the P1 protocol; Independent Reviewer challenges its adequacy before execution and later verifies evidence, failures, claims, and cleanup without coauthoring or approving architecture/risk.” `EVIDENCE`: Early independent challenge prevents invalid samples; co-design would weaken independence. `RESOLUTION_CONDITION`: Reviewer duties, prohibited decisions, and evidence-production separation are explicit.
- `MD-06 PROPOSITION`: “Options may be produced in the same mission only after an immutable independently reviewed current-state freeze, in a separate artifact with no implementation authority; new evidence or authority needs return to Strategy/Leo.” `EVIDENCE`: The plan's decision question includes a bounded proposal and already requires a later post-evidence Council, while fact/option conflation is a shared risk. `RESOLUTION_CONDITION`: P4 ordering and immutability are mandatory and no option can rewrite evidence or start work.
- `MD-07 PROPOSITION`: “Semantically overlapping provider candidates become one call-gate artifact with separately closable child routes; composite frozen unknowns use child closure records, not one broad disposition or duplicative parent IDs.” `EVIDENCE`: U-013, CU-LP-003, and CU-SEC-001 overlap but have internal, vendor, Legal, security, and execution routes. `RESOLUTION_CONDITION`: Strategy records every child owner, method, timing, blocking effect, and disposition and forbids parent closure from one child result.
WHAT_WOULD_CHANGE_MY_VERDICT:
- Change to `PROCEED` if the corrected bilingual Advisor instruction includes all minimum pre-dispatch corrections, the one-mission material-escalation boundary, tiered probe controls, exact claim ceilings, nonactivation, unknown preservation, and evidence-before-options ordering.
- Change to `HOLD` if runtime or provider execution can begin without a passed tiered admission/call gate; if P0/P1 can automatically promote into wider authority; if source/test evidence can be labeled runtime-effective, safe, secure, legal, operational, or generally AI-ready; if mutation-capable learning/Memory/tool paths can run without proved no-effect isolation; if cleanup failure can close successfully; or if factual gaps can become implementation scope without reviewed evidence and a new Leo decision.
- Change to `NOT_NEEDED` only if new authoritative, current, independently reviewed evidence already supplies the same Foundation-wide physical, authority, runtime, integration, downstream, and target-fit map. The frozen register admits no such evidence.
STOP: YES
