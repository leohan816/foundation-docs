SESSION:
foundation-council-ai-behavior

ROLE:
AI_MODEL_BEHAVIOR_AND_EVALUATION_CHALLENGER

CATEGORY:
SPECIALIST

MISSION_ID:
FOUNDATION_INTERNAL_CAPABILITY_AND_RUNTIME_BASELINE_COUNCIL_PREFLIGHT_V1

ROUND: 2_OPEN_CROSS_REVIEW

ALL_SEVEN_INITIAL_REPORTS_READ: YES

INITIAL_REPORT_IMMUTABLE: YES

OTHER_SELECTED_REPORTS_VISIBLE: YES

UNSELECTED_ROLE_OUTPUT_VISIBLE: NO

INITIAL_VERDICT:
PROCEED_WITH_CORRECTIONS

FINAL_VERDICT_AFTER_CROSS_REVIEW: PROCEED_WITH_CORRECTIONS

CONFIDENCE: HIGH

AGREED_ISSUES:

- `ISSUE-01 — ACCEPT WITH TIMING REFINEMENT.` The later baseline needs a
  pre-dispatch decision envelope and must not convert ultimate-mission gaps into current
  priorities. The exact current consumer paths are partly factual under U-009, so P0/P1
  may discover them. Before P2/P3 deepening, each selected path must have an evidence-backed
  decision consequence, named current or explicitly approved near-term outcome, or a
  boundary question whose resolution could change Leo's later decision.
- `ISSUE-02 — ACCEPT.` Use a static-first P0/P1 admission and inventory sequence, a
  mandatory scope-freeze checkpoint, then selected P2 runtime/evaluation work, conditional
  P3 historical/downstream work, and P4 evidence freeze, review, and options. No phase
  continues merely because the preceding phase found more gaps.
- `ISSUE-03 — ACCEPT WITH A MINIMAL-MODEL REFINEMENT.` Keep the seven canonical axes.
  Add mandatory cell evidence and specialist adjunct records, not a new top-level axis for
  every lens. Prohibit a composite `READY`, `SAFE`, or `OPERATIONAL` result.
- `ISSUE-04 — ACCEPT.` “Isolated non-production” must become a risk-tiered executable
  admission, stop, evidence-minimization, and cleanup contract. Missing authority lowers
  the claim ceiling; it does not authorize preparation work, new credentials, provider
  substitution, or simulation presented as official-provider evidence.
- `ISSUE-05 — ACCEPT.` All relevant Foundation and historical surfaces receive shallow
  physical/authority census coverage. Deeper `foundation-control` inspection is admitted
  only after P1 identifies an exact current dependency, duplicate behavior, contract,
  authority conflict, response path, or decision-changing option. U-014 remains Leo-owned.
- `ISSUE-06 — ACCEPT.` Trace selection must follow the inventory, use predeclared
  equivalence classes and bounded negative cases, preserve every transformation and claim
  ceiling, and expand only on declared contradictions, failures, or decision impact.
- `ISSUE-07 — ACCEPT WITH REPETITION REFINEMENT.` Every material AI path needs a
  behavior/responsibility contract, frozen evaluation manifest, repeated stochastic
  scenarios, failure taxonomy, multilingual claim boundary, verdict-preservation checks,
  and an explicit maximum claim. Fixed repetition counts are useful defaults, not universal
  statistical requirements.
- `ISSUE-08 — ACCEPT.` Atomicity and matching are `STRUCTURAL_READINESS_ONLY`. The
  minimum non-normative rubric records current entity/record grain; identifiers, aliases,
  collisions, and merge history; relationship meaning/cardinality; provenance/version/
  correction/retraction; units, locale, market, and effective time; and missing,
  conflicting, duplicated, or stale population evidence. It selects no schema, person
  taxonomy, matching rule, threshold, consent model, or PII flow. A synthetic pass proves
  only the pinned structural case and cannot establish real-person quality, permission,
  safety, or production readiness.
- `ISSUE-09 — ACCEPT.` Allowed evidence is read-only source/config/test inspection and,
  only after admission, isolated existing tests using synthetic disposable candidates with
  all writes redirected to mission-owned temporary state, external egress denied unless
  separately authorized, tools and canonical promotion disabled, and before/after no-effect
  evidence. Immediately stop the affected probe on an unexpected shared/canonical write,
  serving/index promotion, persistent candidate, Memory V3/intake/sender process, unapproved
  provider/tool/egress action, secret or real-PII contact, cap breach, or cleanup failure.
  If no-effect execution cannot be proved, the mutation-capable path is not run.
- `ISSUE-10 — ACCEPT.` Inventory-time fields are limited to known source/owner/provenance/
  rights status, claim or provider dependency, jurisdiction/market if known, data classes/
  flows if any, and the owner/evidence route for each unknown. Exact provider account,
  endpoint, credential authority, contract/content class, retention/training/logging,
  location/deletion, egress, rate, and cost fields become mandatory before an external call.
  Counsel or Leo gates attach only before an affected option, design, exposure, or
  activation—not as a blanket static-baseline prerequisite.
- `ISSUE-11 — ACCEPT.` Foundation Worker authors the integrated evidence and AI evaluation
  manifest. Control, SIASIU, and Cosmile enter only for exact P1-pinned questions and live
  no-collision/capacity evidence. The Independent Reviewer may challenge a frozen P1
  protocol before execution without authoring its cases or approving architecture; it
  later validates actual evidence, failures, cleanup, and claim ceilings independently.
- `ISSUE-12 — ACCEPT.` Facts, target-fit analysis, evidence-derived options, and Leo-owned
  decisions must remain separate. Options may be produced in the same bounded mission only
  after the current-state map is frozen and independently reviewed; no option changes the
  frozen facts or authorizes implementation.
- `ISSUE-13 — ACCEPT.` Preserve every factual unknown and its evidence route. Use child
  subresults for composite unknowns and consolidate only semantic duplicates. An unresolved
  noncritical child may close at a lower evidence ceiling; a closure-critical child returns
  bounded `HOLD`. No unknown automatically extends the mission.

NARROWED_OR_REJECTED_ISSUES:

- No matrix issue is rejected in substance.
- `ISSUE-01` is narrowed: an exact factual current consumer is not a prerequisite to P0/P1
  because U-009 exists precisely to discover current integration and consumption. Leo must
  approve the decision envelope and priority-conversion rule before dispatch; P1 must pin
  evidence-backed consumers or explicit non-consumption before consumer-dependent depth.
- `ISSUE-03` is narrowed: reject `DOWNSTREAM_VALUE_STATE` as an eighth canonical capability
  axis. Preserve it as a mandatory `DECISION_RELEVANCE` adjunct with
  `CURRENT_CONSUMER_EVIDENCE`, `OUTCOME_OR_BOUNDARY_QUESTION`, `NEXT_DECISION_IMPACT`, and
  `NO_BUILD_OR_DEFER_ALTERNATIVE`. Likewise, legal/policy, security-control, data-lineage,
  probe, and behavior-evaluation statuses are adjuncts.
- `ISSUE-04` is narrowed for proportionality: deterministic non-executing read-only
  metadata/source commands do not need a full per-command dossier. A phase-level Tier 0
  manifest is sufficient. Executing project code, creating temporary resources, using
  tools/models, or crossing a network requires progressively stricter per-probe records.
- `ISSUE-07` is narrowed from my Round 1 wording: five repetitions for critical stochastic
  scenarios and three for routine scenarios are defaults for bounded failure discovery,
  not universal minima and never statistical reliability evidence.
- `ISSUE-12` does not require a separate mission merely to derive options from already
  frozen and reviewed evidence. A separate authority is required only when option work
  needs new reads, actors, probes, external authority, product decisions, or implementation.
- `ISSUE-13` is narrowed against register growth: rights, matching applicability, provider,
  actor-capacity, and sampling proposals should become child records of existing composite
  unknowns where their semantics are already covered; distinct owners and routes must not
  be collapsed.

REASON:

All seven immutable reports agree that the baseline is needed, the current method is not
approvable unchanged, factual unknowns remain factual, and safe evidence depth must be
checkpointed. The differences are mainly where to place controls and when to require their
detail. A common seven-axis matrix plus risk-tiered adjunct records preserves every lens
without creating a second platform-documentation program. A single pre-authorized mission
with strict internal gates preserves Leo's authority while avoiding a routine approval
round trip after an admission phase that may only confirm the already approved envelope.
All seven reports matched the Round 1 freeze record before this cross-review.

POSITION_CHANGED: YES

CHANGED_POSITION_DETAILS:

The verdict did not change. I narrowed two Round 1 implementation details after cross-review:

- Round 1 stated at least five repetitions for selected critical stochastic scenarios and
  at least three for routine scenarios. Round 2 treats 5/3 as the default starting design,
  subject to the P1 behavior inventory, risk/consequence, observed variability, cost, and
  Independent Reviewer challenge. One stochastic run still cannot support consistency;
  high-consequence or variable behavior cannot become “ready” merely by completing five
  runs.
- Round 1 proposed a separate `BEHAVIOR_EVALUATION_STATE`. I retain it per behavior as a
  mandatory adjunct status, not as an eighth canonical capability axis.
- Round 1's procedural `AI-U-RUNTIME-001` is no longer open for attribution: the immutable
  Round 1 report preserves the Challenger-side limitation, while the authorized freeze
  record supplies host-side session, Codex CLI 0.144.5, `gpt-5.6-sol`, and `xhigh`
  attestation. It must not be added to the subject's factual-unknown register.

MINIMUM_PRE_DISPATCH_CORRECTIONS:

1. Freeze a `BASELINE_DECISION_ENVELOPE`: the exact decision the evidence must support;
   current or explicitly approved near-term outcome categories; excluded decisions; the
   rule that a target-fit gap is not an implementation priority; and the no-build/defer
   alternative requirement. P0/P1 may discover the actual current consumers.
2. Put P0-P4 phases, exact entry/exit predicates, one mandatory P1 scope-freeze checkpoint,
   no-auto-extension, narrowing semantics, and material-escalation return conditions in the
   Advisor instruction.
3. Freeze the seven canonical state axes, minimum common evidence envelope, independent
   mismatch reporting, specialist adjunct architecture, and explicit prohibition on
   aggregate readiness promotion.
4. Require P0/P1 to produce the physical/current-authority/source ledger, capability and
   behavior inventory, deterministic/model/provider/tool/fallback responsibility map,
   actor/collision record, probe risk classification, and inventory-derived trace/evaluation
   proposal before runtime depth.
5. Add the risk-tiered probe contract, deny-by-default provider/tool authority, evidence-
   artifact minimization, abort criteria, resource/cost limits, teardown inventory, and
   cleanup-failure `HOLD` rule.
6. Add hard claim ceilings: representative traces remain path-limited; synthetic matching
   is structural only; learning/Memory is source-and-control readiness only; source controls
   are not runtime-effective controls; local/stub evidence is not official-provider evidence;
   bounded AI runs are not general capability, safety, language-quality, SLA, or operational
   readiness proof.
7. Preserve Memory V3, sender/intake, autonomous promotion, canonical writes, real PII,
   shared/production resources, ordinary-commerce synchronous coupling, product changes,
   and implementation as non-admissible. Mutation-capable existing tests require proved
   mission-owned no-effect isolation or remain unrun.
8. Require evidence freeze and Independent Review before a separately labeled
   `EVIDENCE_DERIVED_OPTIONS` artifact. Options must carry consumer/decision relevance,
   unknowns, alternatives, reversibility, unresolved dependency gates, and
   `IMPLEMENTATION_AUTHORIZED: NO`.
9. Require lossless child closure for U-003/U-004/U-007/U-010/U-011/U-013/U-015, explicit
   expiry results, and no use of Council reasoning to close current repository, runtime,
   provider, corpus, integration, memory, or actor-capacity facts.

DETAILS_BETTER_DEFINED_BY_ADVISOR_AT_P0:

- Exact Git/worktree pins, discovered capability/behavior inventory, actor availability,
  collision status, current consumer evidence, and Foundation-versus-historical dependency
  paths.
- Repo-grounded workday and external elapsed-time estimate, actor sequencing, per-phase
  resource/cost budget, and which paths fit the pre-authorized envelope.
- The actual commands, temporary directories, ports, processes, databases, indexes,
  caches, dependencies, expected writes, synthetic fixtures, and cleanup checks for each
  admitted local probe.
- Provider-specific account/endpoint/contract/data-use/security fields and exact model/
  deployment identifiers only where those facts and use are authorized.
- The inventory-derived trace equivalence classes, behavior scenarios, supported-language
  claims, repetition counts, evaluators/oracles, acceptance criteria, expansion triggers,
  and regression comparison. P0/P1 may not weaken the pre-dispatch claim ceilings.
- The exact evidence artifact retention/minimization plan and which raw outputs cannot be
  retained safely.
- The risk-based subset the Independent Reviewer will verify. The Reviewer may challenge
  omissions but must not author the evaluated behavior or approve product architecture.

PHASE_APPROVAL_RECOMMENDATION:

Use one bounded Advisor mission with automatic internal phase gates and mandatory return
only on material escalation. Leo's approval should authorize P0/P1 and conditional P2/P3/P4
inside the same decision, repository, actor, data, provider, cost, and no-change envelope.
This avoids routine approval latency while retaining control through exact predicates.

Continuation from P1 is allowed only when the subject pins and authority ledger are stable;
the decision-relevant capability/consumer/behavior set is frozen; actor capacity and
collision gates pass; each selected probe has an admissible risk tier; the estimate fits
the pre-authorized envelope; external-provider child gates pass for any proposed call; and
no new product, canonical, PII, shared-resource, implementation, or actor authority is
needed. The Advisor may narrow or defer an affected path without new approval when doing so
reduces authority and still permits an honest decision at a lower claim ceiling.

Return `HOLD` to Strategy/Leo before continuation when the decision question or material
scope changes; a new repository/actor/provider/data class is required; tracked or canonical
changes are needed to produce evidence; provider/legal/security authority is absent for a
closure-critical call; actor collision or estimated effort exceeds the approved envelope;
an execution boundary is breached; cleanup fails; or the remaining evidence cannot answer
the approved decision even with explicit `UNVERIFIED` results. If Leo does not approve an
outer envelope and material-deviation rule, authorize P0/P1 only and require a new approval
before P2/P3.

TIMEBOX_AND_ESCALATION_RECOMMENDATION:

Leo should select an outer scope/effort/cost ceiling and material-deviation rule before
dispatch, but should not be asked to invent a precise repo-grounded duration while U-015 is
open. P0 returns engineering workday and external elapsed-time ranges with confidence and
the proposed P2/P3 selection. Automatic continuation is allowed only inside the already
approved outer ceiling. “Materially excessive” means exceeding that Leo-selected ceiling,
colliding with a protected active priority, requiring a new actor/provider/authority, or
making the baseline no longer the smallest path to the approved decision; Council should
not invent an absolute number.

At each checkpoint use:

- `CONTINUE`: all entry predicates pass and the estimate remains inside authority and
  ceiling.
- `NARROW`: an affected path cannot be admitted or is decision-irrelevant, but the approved
  question remains answerable at a lower explicit claim ceiling.
- `HOLD`: a closure-critical fact, authority, safety boundary, cleanup result, or estimate
  prevents a truthful answer inside the envelope.
- `DEFER/UNVERIFIED/PROVIDER_EXECUTION_NOT_RUN`: the path is noncritical or externally
  blocked and no adjacent evidence is promoted into its place.

STATE_AND_EVIDENCE_MODEL_RECOMMENDATION:

Keep the canonical axes exactly:

`SOURCE | BUILD | TEST | RUNTIME | INTEGRATION | AUTHORITY | TARGET_FIT`.

Every cell must include `CAPABILITY_OR_BEHAVIOR_ID`, `AXIS`, `RESULT`, `EVIDENCE_CLASS`,
`EXACT_PIN`, `ENVIRONMENT_AND_OBSERVED_AT`, `OWNER`, `UNKNOWN_IDS`, `CLAIM_CEILING`,
`DECISION_IMPACT`, and any `MISMATCH_OR_DEPENDENCY`. The common result vocabulary is
`NOT_ASSESSED | NOT_APPLICABLE | ABSENT_AT_PIN | PRESENT_UNVERIFIED | VERIFIED_PASS |
VERIFIED_FAIL | BLOCKED`. Authority and target fit may use their own bounded subvalues but
must retain the same evidence envelope. `DEFERRED`, `PROVIDER_EXECUTION_NOT_RUN`, and
`LEO_DECISION_REQUIRED` are closure/disposition values, not evidence promotion.

Required adjuncts, not top-level axes, are:

- `DECISION_RELEVANCE` and current-consumer evidence;
- source-authority, provenance, transformation-lineage, and population/coverage record;
- `BEHAVIOR_EVALUATION_STATUS` with responsibility, scenario, repetition, failure, verdict,
  fallback, tool, latency/token/cost, and claim evidence;
- security-control effectiveness record;
- legal/policy/provider dependency status;
- probe admission, evidence-artifact, abort, and cleanup record.

No adjunct may convert a failed or unrun canonical axis into a pass, and no summary may use
an unqualified `READY`, `SAFE`, `COMPLIANT`, `OPERATIONAL`, or `APPROVED` label.

AI_REPETITION_RECOMMENDATION:

Use five repetitions for each selected critical stochastic scenario and three for each
routine scenario as proportionate default starting points for bounded failure discovery.
They are not universal minima, pass thresholds, confidence intervals, or reliability
estimates. The P1 evaluation owner must propose the final counts from the behavior's
decision consequence, stochasticity/settings, route/provider cost, language coverage,
existing test evidence, and expected failure mode; the Independent Reviewer challenges
under-sampling after the manifest is frozen.

One stochastic run cannot support consistency. Every run is retained; failures are not
discarded or rerun away. Observed disagreement, verdict loss, unsupported claims, route or
fallback divergence, tool/stop failure, or materially different paraphrase/language
behavior triggers either bounded expansion inside the approved ceiling or an
`INCONCLUSIVE`, `VERIFIED_FAIL`, or `UNVERIFIED` result. High-consequence behavior does not
become generally reliable after five passes; a baseline may report only observed outcomes
and variability for the pinned scenarios.

OPTIONS_SEPARATION_RECOMMENDATION:

Keep options in the same bounded Advisor mission to avoid an unnecessary approval cycle,
but only after a current-state evidence map is immutable and the Independent Reviewer has
returned its evidence/reproducibility/boundary result. The Advisor then creates a separate
`EVIDENCE_DERIVED_OPTIONS` artifact referencing, not rewriting, the frozen evidence.
Options must include a named decision/outcome, exact evidence and unknown IDs, no-build or
defer alternative, dependencies, reversibility/rollback, estimate, claim ceiling, affected
owners, unresolved Legal/security/provider gates, and Founder decisions.

The Reviewer does not approve the option, architecture, transfer, model/provider choice,
risk, or implementation. A new mission or explicit authority is required if deriving an
option needs new evidence, new actors, new probes, scope expansion, product changes, or
implementation. The planned post-evidence Council challenge and Leo decision remain
mandatory before any implementation recommendation can become executable authority.

UNKNOWN_CONSOLIDATION_RECOMMENDATION:

- Preserve frozen U-001 through U-015 unchanged.
- Narrow Product U-N01 to a pre-dispatch decision-envelope/priority-conversion decision.
  The current decision question already exists; factual current-consumer evidence remains
  U-009 and is discovered at P1.
- Treat Delivery U-N01 actor capacity/collision as a child of U-015, with per-actor owner,
  required timing, and blocking effect preserved.
- Treat Legal CU-LP-001 as rights/claims child results of U-003, and CU-LP-002 as the
  jurisdiction/product/data-use applicability child of U-010. Their authoritative-source,
  counsel, and Leo routes remain distinct from repository structure evidence.
- Use U-013 as the parent external-execution gate, but retain separate children for:
  internal credential/network authority; Legal CU-LP-003 contract/content-use terms; and
  Security CU-SEC-001 tenant/scope/data-handling/egress/rate/cost/cleanup. Keep U-007's
  configured path, actual model behavior, latency/cost observation, and provider/runtime
  evidence as separate child results; vendor confirmation cannot close local behavior and
  repository evidence cannot close provider terms.
- Keep DG-CU-001 evidence-artifact governance distinct because it applies to local logs,
  indexes, prompts, outputs, and traces even without an external provider.
- Keep AI-U-016 behavior-contract evidence and DG-CU-002 sampling/claim-threshold evidence
  distinct but sequential: the behavior inventory precedes the inventory-derived sampling
  contract. Exact sufficiency remains implementation validation at P1.
- Split U-004 into current structural/semantic evidence and U-010-dependent future matching
  fitness. Split U-011 into current mechanism evidence and future promotion/activation
  authority. A parent closes only for decision-material children resolved by their approved
  routes.
- Do not carry AI-U-RUNTIME-001 into the subject register; the host-side freeze record is
  the authoritative resolution for Council runtime attribution, while the Round 1 report's
  original limitation remains immutable.

MISSING_OR_OBSCURED_FINDINGS:

- The matrix underrepresents AI-BEH-R1-004's model-operational manifest: exact exposed
  model/deployment identity, prompt/context assembly and truncation, route reason, retry/
  timeout/cache/fallback behavior, trace ID, token/latency/cost observation, and explicit
  replacement or rollback triggers. These are mandatory for interpreting bounded model
  evidence and must not be reduced to generic probe accounting.
- The matrix captures verdict preservation under ISSUE-06, but the final correction must
  retain the causal distinction between retrieved evidence merely appearing in context and
  actually constraining the raw model output, parsed judgment, deterministic override, final
  Foundation verdict, and downstream response. Controlled ablation/conflict evidence is
  allowed only in synthetic isolated fixtures.
- No original minority position appears deliberately obscured. Product's conditional HOLD,
  Systems' conditional downstream/historical entry, Delivery's broad-shallow-census
  position, Legal's no-technical-to-legal promotion boundary, Data's inventory-only claim
  ceiling without source authority, and AI's no-general-capability claim are all materially
  represented. The AI operational/replacement detail above is the principal compression
  loss.

MATERIAL_DISAGREEMENTS_REMAINING:

- `MD-01 — PHASE AUTHORITY AND OUTER CEILING.` One bounded mission with internal gates
  versus P0/P1-only authority remains a Leo approval-structure choice.
- `MD-02 — DOWNSTREAM VALUE PLACEMENT AND TIMING.` Product's proposed eighth state and
  named-consumer prerequisite remain in tension with the seven-axis minimum and U-009's
  factual discovery purpose.
- `MD-03 — REPETITION COUNTS.` My Round 1 fixed-minimum wording and a fully risk-based P1
  design differ on whether 5/3 binds every stochastic scenario.
- `MD-04 — REVIEWER TIMING.` Pre-execution protocol challenge may reduce late invalidation
  but must not make the Reviewer the evaluation designer or later self-reviewer.
- `MD-05 — OPTIONS MISSION BOUNDARY.` Same-mission options reduce approval overhead, while
  a separate mission provides a stronger temporal boundary if new evidence or authority is
  needed.

EXACT_PROPOSITION_AND_RESOLUTION_CONDITION:

- `MD-01 PROPOSITION:` P2/P3 may continue automatically only inside an exact Leo-approved
  decision/scope/actor/data/provider/effort/cost envelope. `EVIDENCE:` U-012, U-013, and
  U-015 prevent a precise pre-evidence execution estimate, while all roles require phase
  gates. `RESOLUTION CONDITION:` Leo approves the envelope and material-deviation return
  rule in the Advisor instruction; otherwise authority ends after P0/P1 and a new approval
  is required.
- `MD-02 PROPOSITION:` Keep seven axes and attach decision relevance; require the decision
  envelope before dispatch but discover actual current consumer evidence in P1.
  `EVIDENCE:` the seven axes measure capability state, while U-009 admits current downstream
  consumption is unknown. `RESOLUTION CONDITION:` the corrected instruction preserves
  `DECISION_RELEVANCE`, current-consumer evidence, no-build alternatives, and a pre-P2
  checkpoint. If that adjunct cannot prevent target-fit gaps from becoming priorities,
  Strategy may elevate it to an axis without changing the other seven.
- `MD-03 PROPOSITION:` 5/3 are default failure-discovery counts, not universal minima.
  `EVIDENCE:` no behavior inventory, stochastic setting, cost, language set, or consequence
  distribution is admitted, and five passes cannot establish reliability. `RESOLUTION
  CONDITION:` the P1 owner freezes risk-based counts and oracles; the Reviewer challenges
  them; any reduced count has a lower explicit claim ceiling; variability triggers expansion
  or failure/inconclusive closure inside the outer ceiling.
- `MD-04 PROPOSITION:` The Reviewer may challenge but not author the frozen P1 manifest.
  `EVIDENCE:` early challenge avoids an invalid sampling design being discovered only after
  costly runs, while authorship would weaken independence. `RESOLUTION CONDITION:` Foundation
  Worker authors, Advisor freezes and versions, Reviewer returns bounded adequacy objections,
  and later independently checks actual executions and a risk-based subset without approving
  architecture or risk.
- `MD-05 PROPOSITION:` Evidence-derived options may remain in the same mission only after
  immutable evidence freeze and independent evidence review. `EVIDENCE:` the plan already
  requires both a map and bounded proposal but forbids automatic implementation; Systems and
  Delivery identify the risk of hidden decision promotion. `RESOLUTION CONDITION:` options
  reference but do not rewrite facts, require no new evidence/authority, and carry
  `IMPLEMENTATION_AUTHORIZED: NO`; otherwise the mission stops and a separate authority is
  required.

WHAT_WOULD_CHANGE_MY_VERDICT:

- Change to `PROCEED` when the exact Advisor instruction incorporates all minimum
  pre-dispatch corrections above, including the per-behavior/evaluation and maximum-claim
  controls, while leaving repo-grounded scenario and probe details to P0/P1.
- Change to `HOLD` if the instruction permits undifferentiated full-scope execution;
  lacks a Leo-approved outer envelope or P0/P1-only fallback; allows single or favorable
  traces to support general AI readiness; permits provider/tool/mutation execution without
  exact gates; lets technical target fit imply legal, product, safety, or operational
  approval; requires tracked/canonical changes for evidence; or allows options to trigger
  implementation automatically.
- Change to `NOT_NEEDED` only if newly admitted, current, independently reviewed evidence
  already resolves the same physical, authority, runtime, integration, behavior, and
  downstream decision questions. No such evidence is present in the frozen Council packet.

STOP: YES
