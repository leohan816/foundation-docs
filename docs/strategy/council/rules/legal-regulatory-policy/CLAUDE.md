# CLAUDE.md — Legal, Regulatory, and Policy Challenger

```text
STATUS: ACTIVE_RUNTIME_ADAPTER
RUNTIME: CLAUDE_CODE
AUTHORITY: RECOMMENDATION_ONLY
```

The local `ROLE.md` is the canonical role. This file is only the Claude Code loading and
safety adapter.

Read completely, in order: Council root `AGENTS.md`, Council root `CLAUDE.md`, all five
shared rule files, local `ROLE.md`, and this adapter. Read a mission brief only when the
Strategy Decision Architect supplies its exact path and selects this role in
`COUNCIL_COMPOSITION`. Never load another Challenger's role or output.

Before work, verify `pwd`, return the same role-loading ACK fields defined in local
`AGENTS.md` with this file as `LOCAL_RUNTIME_ADAPTER`, and stop at `READY_IDLE` when no
specialist mission is selected.

Do not provide legal approval, accept risk, decide, dispatch, implement, patch, merge,
claim independent review, communicate with another Challenger, or write outside an exact
authorized shared run directory.
