# FABLE5 BLIND ASSESSMENT — V3 Package 1A Unknown Register (U-01..U-09)

- Mission: `V3_PACKAGE1A_INDEPENDENT_UNKNOWN_RESOLUTION_AND_FOUNDER_DECISION_PACKAGE`
- Actor: **Fable5 Reviewer** (Sentinel session — role, not model) · Pass: `DESIGN_REVIEW__DISCOVERY_ADVERSARIAL_PASS` · Level 3 · Skill: `/fable-sentinel` loaded (`/home/leo/Project/skill/fable-sentinel/` SKILL.md + references Read at job start)
- Date: 2026-07-10 · Return to: **Advisor** (not final approval)
- **BLIND-PASS STATUS: `BLIND_ASSESSMENT_COMPLETE`** (independence declaration in section G3)

## Freeze verification (input integrity)

Register `V3_PACKAGE1A_UNKNOWN_REGISTER.md` (474 lines) verified by triple match before any assessment:
- freeze commit `fab82c45f7e92ed2652dc6de9db55532fabb661b` — matches brief
- git blob `0eac3e290269c5154029d79864b99c9235807013` — matches brief
- sha256 `dab0ffa15452db860d3ba6f71b58401d4447e81b811c22c3c4a87a379f26e0cb` — matches brief

## Method and evidence classes

Evidence gathered by direct file reads of Cosmile (`shadow/m4-cosmile-memory` @ `e4ed668`, uncommitted V3 diff present), foundation-control, FOUNDATION, and foundation-docs mirrors. Every load-bearing claim below carries `file:line`. Evidence classes used exactly as briefed: `CURRENT_CODE` / `CANONICAL_AUTHORITY` (2026-07-09 approved design) / `HISTORICAL_EVIDENCE` (V3-0x report corpus) / `TEST` / `UNVERIFIED_RUNTIME` (no DB query, no secret value, no live call — file metadata and key NAMES only).

Coverage method disclosure (rule-6 honesty): evidence sweep ran as 5 parallel session-internal verification passes (this session's workflow tooling, ultracode directive from Leo's handoff), each given the forbidden-read list; the reviewer then **re-verified all top load-bearing findings first-hand** (listed in G7). The brief's forbidden-actions line "New session or sub-agent creation" was interpreted as forbidding new ACTOR sessions / delegation of the verdict, not in-session evidence gathering; this interpretation is flagged here for Advisor/Leo to judge rather than silently assumed. No judgment in this document is outsourced: every FACTS/CHALLENGE verdict was made by the reviewer against cited evidence.

---

# Per-unknown assessment

## U-01 — Post-order feedback input reality

- **FABLE5_CHALLENGE**: The register claims no post-order feedback input exists. Challenged by hunting hidden ingestion surfaces: all API routes, order/consult UI (including Korean labels 리뷰/별점/평점/만족도/피드백), seeds, scripts, and any raw-SQL writers — assuming the register is wrong until proven.
- **DIRECT_EVIDENCE**: `Cosmile/app/prisma/schema.prisma:862-871` + `migrations/20260706120000_v3_11b_learning_commerce_memory/migration.sql:50-63` (table exists, CURRENT_CODE); repo-wide grep `recOutcomeFeedback.(create|createMany|upsert|update|delete)` and `INSERT INTO RecOutcomeFeedback` = zero product-code hits; `app/src/app/orders/[orderId]/page.tsx:19-66` and `account/orders/page.tsx:20-49` = zero feedback controls; `app/scripts/v3_11b_db_integration.dbtest.py:61-66` = raw-SQL INSERTs (TEST class only); `V3-11D gate plan` line 69 G-D1 + line 75 "구현으로 가는가? — 아니오" (HISTORICAL_EVIDENCE); canonical design Out-Of-Scope excludes V3-11D (CANONICAL_AUTHORITY); `recOutcomeEventService.ts:2` prohibition comment (CURRENT_CODE).
- **FACTS_SUPPORTED**: All 4 KNOWN_FACTS bullets. Core claim "feedback input does not exist in reality" is TRUE and triple-anchored (schema-only shape / zero product writer / G-D1 HOLD).
- **FACTS_NOT_SUPPORTED**: The literal wording "write 호출 0" is false as a universal claim — `v3_11b_db_integration.dbtest.py:61-66` contains 7 raw-SQL INSERTs into RecOutcomeFeedback (TEST harness). Correct wording: "no product/service/route/UI write path; test-harness inserts exist."
- **HIDDEN_ASSUMPTIONS**: (1) "no UI + no create call" read as "data cannot exist" — the table is raw-SQL-writable with fake ids and has NO service-layer or DDL guard (no XOR, no identity, no PII invariant column), so any future ops/backfill script is an unguarded write path. (2) Consultation assumed the only raw-text ingress forever — a new route would become a semantic-input surface without re-triggering G-D1. (3) FRC assumed able to later supply labels — `foundationClient.ts:34-56` Frc type has NO semantic_label/adverse fields; feedback is blocked on TWO gaps (input G-D1 AND judgment-source G-D2), not one.
- **MISSING_UNKNOWNS**: feedback for never-purchased products is hard-blocked at DDL (orderItemId NOT NULL + FK; V3-04 proposed NULLABLE) — intended product decision or schema accident?; feedback after refund/cancel unguarded (no order-status tie); unlimited rows per OrderItem (V3-04 content_hash dropped; contrast RecOutcomeEvent `@@unique([orderItemId])`); no subjectRef/anonymousRef on feedback rows → consent-scoped deletion cannot reach them except via OrderItem join; the `/api/events` free-text channel (see ADD-03).
- **IRREVERSIBLE_RISKS**: `down.sql:13` DROPs RecOutcomeFeedback — rollback after any rows exist is silent data loss; FK `ON DELETE RESTRICT` (migration.sql:62) vs Order→OrderItem Cascade (schema.prisma:450) creates an order-deletion deadlock once rows exist.
- **REWARD_HACKING_RISKS**: green CHECK-constraint suite (dbtest DB2a-DB2f) on an unreachable table reported as "RecOutcomeFeedback DONE".
- **SAFE_DEFAULT_CHALLENGE**: The default is genuinely closed (no input path), but with soft edges: no feedbackId format CHECK (RecommendationEvent got a regex CHECK, migration.sql:19; this table got none); every invariant lives in docs, not DDL; and the blocklist-only `/api/events` channel already accepts client free-text under unblocked keys today (ADD-03).
- **REQUIRES_EXPERIMENT**: only after a product decision — pilot UI measuring completion/quality/selection bias; sanitizer canary against `/api/events`.
- **REQUIRES_LEGAL_OR_POLICY_REVIEW**: collecting post-purchase adverse-reaction text is health-adjacent personal data (consent framework, PIPA retention/erasure) — required before any collection.
- **REQUIRES_LEO_PRODUCT_DECISION**: whether feedback input should exist at all and in which mode (UI form / consultation-derived / none); whether never-purchased users may ever give feedback (currently DDL-impossible).
- **CONFIDENCE**: HIGH on current absence (independently re-verified). Register needs the FACTS wording correction above.

## U-02 — Semantic classification reliability

- **FABLE5_CHALLENGE**: Challenged the implied comfort that the 10-value CHECK and existing consultation semantic stack constitute progress toward reliable feedback classification. They do not: the CHECK constrains storage vocabulary, not classification quality, and the consultation stack speaks a disjoint vocabulary.
- **DIRECT_EVIDENCE**: `migration.sql:54-61` 10-value semanticLabel CHECK + severity(low/moderate/severe) + certainty(reported/repeated/verified/contradicted) — read first-hand; `contracts.py` SSC severity enum `('mild','moderate','visible_reaction','severe_or_red_flag','unknown')` and FRC `SEVERITY_CLASS=('mild','breakout','visible','red_flag')` — a THIRD disjoint vocabulary vs the DDL, with zero mapping code in any repo (negative grep for `semantic_label` across foundation_http_service and FOUNDATION); `semantic_router.py:2-41` transitional AI router (consultation-scoped); `v3_11.vitest.ts:85-114` = enum-equality and matrix-arithmetic tests, zero natural-language inputs; `scripts/foundation_routing_wider_eval.py:13-23` = 27 synthetic consultation utterances, no feedback text, no sarcasm/mixed/multilingual cases, no calibration dataset anywhere.
- **FACTS_SUPPORTED**: All 4 KNOWN_FACTS. No classifier, no corpus, no calibration exists for post-order feedback.
- **FACTS_NOT_SUPPORTED**: none contradicted. Precision caveat: whether the routing wider-eval ever RAN and what accuracy it produced is UNVERIFIED_RUNTIME (no result JSON found in reports/) — the register's claim holds on test CONTENT, not observed runtime.
- **HIDDEN_ASSUMPTIONS**: (1) DDL has NO `DEFAULT 'unclear'` — nothing at schema level routes uncertainty to the safe label; a future writer defaulting to `neutral`/`satisfied` violates no constraint. (2) "one label per row" is not "one label per order item": orderItemId is a non-unique index (schema.prisma:870) — contradictory label rows per item are schema-legal with no defined winner. (3) No cross-field coherence: `adverse_*` label with NULL severity/certainty is legal (dictionary §2.4 unknown->low+reported rule is unenforced); `satisfied` + `adverseSeverity='severe'` is equally legal. (4) Single exclusive label cannot represent a mixed statement ("좋긴 한데 얼굴이 뒤집어졌어요") in one row at all — the schema forces a lossy choice the register never names.
- **MISSING_UNKNOWNS**: classifier provenance columns — the shipped DDL silently DROPPED V3-04's mandatory `source_ref` and `content_hash` (V3-04 §3/§8) with no register flag on this contract-vs-DDL delta; reclassification history (createdAt only, no audit trail — correction = untraceable in-place rewrite); dedup semantics (duplicate ingestion both inflates M8 and spuriously escalates certainty toward `repeated`, which §5.3 escalates to safety_caution); severity-vocabulary unification owner across the three parallel enums.
- **IRREVERSIBLE_RISKS**: once labels are written live, absence of version/provenance/history columns makes post-fix bulk reclassification an untraceable rewrite — data-quality irreversibility.
- **REWARD_HACKING_RISKS**: classification-level bias is unguarded — V3-09's anti-gaming rules guard aggregation only; a classifier reading sarcasm as `satisfied` raises M8 and lowers M13 while every guard stays green; duplicate rows inflate M8 with no constraint violated; enum-equality tests (T15) presented as "semantic classification verified" — they verify vocabulary, not recognition.
- **SAFE_DEFAULT_CHALLENGE**: `unclear` exists in the enum but is aspirational — enforced by neither DDL default nor any code (the ingestion code does not exist). The consultation stack's real safe default (`core.py:593` uncertainty->mild->caution, pass forbidden) protects a DIFFERENT pipeline.
- **REQUIRES_EXPERIMENT**: yes — labeled Korean-first corpus including sarcasm/indirect complaint/mixed/incomplete text, human-annotated ground truth, inter-rater agreement, calibration eval with a pre-frozen acceptance threshold. Cannot be proven from repo state.
- **REQUIRES_LEGAL_OR_POLICY_REVIEW**: misclassification of adverse reactions touches the medical/safety boundary (workspace stop conditions treat this as first-class) — policy review on failure-handling duty.
- **REQUIRES_LEO_PRODUCT_DECISION**: whether any learning label may exist before a calibration gate passes; the acceptance threshold; Korean annotation policy ownership.
- **CONFIDENCE**: HIGH that no classifier/corpus/calibration exists; HIGH on the three-vocabulary disjunction (read first-hand).

## U-03 — Retention / erasure / data lineage

- **FABLE5_CHALLENGE**: Challenged the register's framing that lineage risk is bounded to RecOutcome*/outbox and gated by R-2 + flag-OFF. The actual lineage surface is wider and partially ACTIVE today.
- **DIRECT_EVIDENCE**: canonical design line 75 R-2 open + lines 375-381 hard-blocker language (CANONICAL_AUTHORITY); `schema.prisma:844-871` — RecOutcomeEvent/RecOutcomeFeedback have NO deleted/blocked/expired/tombstone columns while the pattern exists elsewhere in the same schema (CustomerProfile.deleted, memory overlay); V3-02/V3-07/dictionary tombstone+must_not_reappear proposals never became approved policy (HISTORICAL_EVIDENCE); `foundationSignalMapper.ts:45-58` sole outbox writer, `status:"pending"` only; outbox 5-state vocabulary is a code COMMENT on a plain String — no enum, no CHECK, no code writes sent/failed/blocked/skipped, no consumer (negative grep); `commerceEventService.ts:58` unconditional `maybeEnqueueFoundationSignal` — NO feature flag (contrast COSMILE_REC_OUTCOME_ENABLED); `foundationSignalMapper.ts:50` stores RAW `canonicalUserId`; consult-foundation route persists `foundation_decision_received` CommerceEvent rows keyed to raw userId/guestId.
- **FACTS_SUPPORTED**: all 4 KNOWN_FACTS bullets verbatim.
- **FACTS_NOT_SUPPORTED**: none contradicted; severity understated in two places — (a) the outbox erasure surface includes RAW user ids, not HMAC refs; (b) the 5-state queue lifecycle exists only on paper.
- **HIDDEN_ASSUMPTIONS**: (1) R-2's "flag ON blocked" gate is documentation-only for the outbox path — enqueue is live-by-default wherever event tracking runs. (2) `privacyLevel='user_consented'` is assigned purely from userId presence (mapper:30-31 comment admits it); ConsentRecord machinery (schema:880-893) is connected to nothing. (3) "tombstone columns exist elsewhere" does not mean tombstoning is implemented anywhere — zero code paths set CustomerProfile.deleted or memoryDeleted (negative grep).
- **MISSING_UNKNOWNS**: retention/erasure policy for `foundation_decision_received` CommerceEvent rows (raw userId/guestId + propertiesJson + referrer/pagePath); legacy `Cosmile/app/prisma/dev.db` (1.0 MB, gitignored, mtime Jul 5) sits outside all lineage discussion (UNVERIFIED_RUNTIME metadata — not opened); deletion signal type absent from the outbox whitelist (no retraction can ever propagate).
- **IRREVERSIBLE_RISKS**: irreversibility starts at first real SEND, not at RecOutcome flag-ON — the whitelist has no deletion/retraction type, so send-then-erasure-request cannot be honored; mislabeled consent rows accumulating today are baked into any later aggregate.
- **REWARD_HACKING_RISKS**: comment-only 5-state vocabulary lets a report describe "a safe queue with blocked/skipped states" that were never executed; `user_consented` column read later as consent evidence.
- **SAFE_DEFAULT_CHALLENGE**: the register's safe default ("no operational persistence, no signal expansion, no flag ON") is already violated in spirit: outbox persistence with raw ids and optimistic consent labels is unconditional today. The default must explicitly cover the outbox enqueue path and CommerceEvent decision rows, not only the flag-gated RecOutcome writer.
- **REQUIRES_EXPERIMENT**: deletion-propagation rehearsal is feasible on an ephemeral DB once a policy exists; nothing else is experimentally provable pre-policy.
- **REQUIRES_LEGAL_OR_POLICY_REVIEW**: PIPA retention/erasure obligations for rows carrying raw user identifiers; right-to-erasure propagation across derivatives.
- **REQUIRES_LEO_PRODUCT_DECISION**: retention windows; whether outbox enqueue should be flag-gated (or stopped and backfill-cleaned) BEFORE any Package 1B work.
- **CONFIDENCE**: HIGH (unconditional enqueue, raw id, comment-only states all re-verified first-hand).

## U-04 — Guest-to-login identity safety

- **FABLE5_CHALLENGE**: Challenged whether the claimed protections (separate ref minting, XOR, allowLink=false) are mechanisms or paper. Two of three are weaker than the register implies.
- **DIRECT_EVIDENCE**: `ids.ts:36-51` — subj_v2_/anon_v3_ separately HMAC-minted with separate secrets and domain strings; prod fail-closed on missing secret (ids.ts:19) (CURRENT_CODE); `recOutcomeEventService.ts:56-58` XOR -> `xor_violation`, tested both-present/both-absent (`v3_11c2_rec_outcome.vitest.ts:106-118`, TEST); `schema.prisma:895-902` SubjectRefMap.allowLink `@default(false)` — **zero runtime references** to allowLink or SubjectRefMap anywhere in Cosmile source (re-verified first-hand: only schema.prisma:899); canonical design R-3 open, "Do not infer or implement re-key/stitch/link behavior" (CANONICAL_AUTHORITY); old dictionary §2.10 stitching states vs current R-3 tension is real (HISTORICAL_EVIDENCE).
- **FACTS_SUPPORTED**: all 5 KNOWN_FACTS bullets.
- **FACTS_NOT_SUPPORTED**: none.
- **HIDDEN_ASSUMPTIONS**: (1) outcome writes key to the CURRENT SESSION identity (`mock-complete/route.ts:39` getShopper()), not the identity stored on the Order row — the only thing preventing a guest order from being outcome-keyed to a login subjectRef after login is `checkout.ts:117` ownerMatches() 404; any future order-completion path keyed by order-stored identity or admin action re-keys silently, which is exactly what R-3 forbids inferring. (2) The XOR guarantee currently rests on MOCK auth structure (shopper.ts:12-17), not real auth. (3) allowLink consent semantics are entirely unenforced — dead schema. (4) subjectRef minting takes no secretVersion parameter — rotating COSMILE_SUBJECT_SECRET today silently orphans every existing subjectRef (the rotation design assumes the unused map table).
- **MISSING_UNKNOWNS**: identity-null guest sessions (GUEST_FLAG without cookie -> {null,null} -> outcome silently dropped, fail-open) as an attribution-loss class; local-user-id reuse after account deletion re-attaching to old subj_v2_ rows (no tombstone/reuse-block at mint); guest->guest multi-device fragmentation (register frames stitching as guest->login only); shared-account/household collapse to one subjectRef.
- **IRREVERSIBLE_RISKS**: any future stitching implemented as in-place row re-key (anonymousRef->subjectRef) is irreversible without an attribution-change event log, which design decision #11 marks not-yet-approved.
- **REWARD_HACKING_RISKS**: `mock-complete/route.ts:40-41` reports failures as an aggregate count — `xor_violation` and `write_failed` are observationally indistinguishable, so an identity regression dropping all guest outcomes looks like transient DB noise ("no errors" claimable).
- **SAFE_DEFAULT_CHALLENGE**: allowLink=false is a safe default on paper that no runtime consults; a future stitching implementation can ignore the table without failing any test. A default nothing reads provides zero protection.
- **REQUIRES_EXPERIMENT**: real-auth XOR/session behavior cannot be proven pre-integration; stitching consent UX needs live-like evidence.
- **REQUIRES_LEGAL_OR_POLICY_REVIEW**: retroactive guest->login linkage is a cross-context identity join requiring explicit consent basis.
- **REQUIRES_LEO_PRODUCT_DECISION**: R-3 stitching policy itself (link at all? retroactive? consent flow?); secret-rotation strategy before any durable subjectRef accumulation.
- **CONFIDENCE**: HIGH (allowLink dead-schema and session-vs-order identity distinction re-verified).

## U-05 — Raw-text non-persistence

- **FABLE5_CHALLENGE**: Challenged both directions: (a) is raw text persisted anywhere repo-local despite the claim? (b) is the "not sent externally unless enabled" framing honest? Finding: repo-local non-persistence holds unusually well, but the external-transport framing materially understates reality, and the invariant is self-certified rather than measured.
- **DIRECT_EVIDENCE** (all CURRENT_CODE unless noted): `cosmileSemanticAdapter.ts:13-17,37` maskPii (email + Korean 0-prefixed phone only) applied in createBaseSsc; `server.py:84-86` logging no-op, `:18,36-38` in-memory 200-entry trace ring capped to exactly 4 fields (path/status/trace_id/decision_type); `core.py:50-52` — **INVARIANTS is a hardcoded constant** `{"raw_text_stored": False, "pii_stored": False, ...}` spread into every response (`:418,1574,1695,1721`) — re-verified first-hand; `core.py:1616` — **`"compose": True` hardcoded** in consult_contract payload — re-verified first-hand; `core.py:1259` semantic_classify + `:1377` composer + `:1182` router shadow (SHADOW_ON=True, `:22`) all ride llm_guard transport to api.deepseek.com; key file EXISTS on this host (`SIASIU/.secrets/deepseek_key -> ~/.secrets/deepseek_key`, mode 600 — existence only, value not read; UNVERIFIED_RUNTIME); DeepSeek call sites verified clean of print/log/file-write (failures record exception type names only).
- **FACTS_SUPPORTED**: bullets 1-3, 5-6. Repo-local non-persistence claims check out (no file/DB write of raw text on any inspected path).
- **FACTS_NOT_SUPPORTED**: bullet 4's "can send content to an external model **when enabled**" is materially misleading: enablement is not an operator opt-in — the canonical contract endpoint hardcodes compose=True, so the effective condition is merely "key file exists", which it does on this host. Every `/v1/consult_contract` call transmits (Cosmile-masked) raw text externally by default.
- **HIDDEN_ASSUMPTIONS**: (1) `raw_text_stored=false` treated as a measured invariant — it is a declarative constant that would still report False if persistence were introduced; Cosmile echoes it to its client (`route.ts:73`, `foundationClient.ts:55`) as if it were evidence. (2) Masking assumed universal — it exists only in Cosmile's createBaseSsc; the Foundation dev server and any other caller (curl, other services) deliver unmasked text. (3) Masking assumed adequate — it catches email + Korean-format phone only; names, RRNs (######-#######), addresses, order numbers, and free-text health details pass through.
- **MISSING_UNKNOWNS**: per-call-class external inventory (THREE distinct call classes ride the same key: semantic_classify, composer, router shadow — plus provider `usage` metadata retained in trace); `FISH_AUDIO_KEY` in Cosmile `.env.local` = a second external-provider credential covered by no unknown (key NAME only observed); Next.js dev-server request logging of consult POST bodies (uncontrolled by any inspected code); host-level backups capturing `dev.db` and the key file (unverifiable from repos); canary feasibility must be scoped: feasible for repo-local surfaces, NOT for provider retention (a canary proves transmission, not deletion).
- **IRREVERSIBLE_RISKS**: text transmitted to api.deepseek.com is irrecoverable at send; nothing distinguishes synthetic from real input, so the distance between "dev demo" and "live customer text at an external provider" is a single caller decision; partial masking creates a false sense of PII stripping for what crosses that boundary.
- **REWARD_HACKING_RISKS**: the INVARIANTS constant is the concrete mechanism — any audit that greps response fields "confirms" non-persistence forever; `composer_provider: "rule"` on fallback paths could support a "no external calls" claim for a request where semantic_classify DID call DeepSeek earlier (separate trace key).
- **SAFE_DEFAULT_CHALLENGE**: the register's default ("do not send live customer feedback to a provider") has NO technical enforcement: transport is on-by-default (hardcoded compose + present key), the endpoint accepts any local caller, the host binding is env-overridable (`FDSH_HOST`, server.py:15), and no kill switch exists (an `FDSH_EXTERNAL_LLM=0`-style flag defaulting OFF does not exist today). The default is purely procedural.
- **REQUIRES_EXPERIMENT**: local-surface canary sweep (feasible: surfaces are enumerable and small); provider-side retention is NOT experimentally provable — requires contractual/console evidence.
- **REQUIRES_LEGAL_OR_POLICY_REVIEW**: DeepSeek data-use/retention terms for customer consultation text; cross-border transfer under PIPA — required before any real-user text flows.
- **REQUIRES_LEO_PRODUCT_DECISION**: whether external LLM transport may remain on-by-default in dev; whether a hard kill switch (default OFF) is a precondition for any live-adjacent use.
- **CONFIDENCE**: HIGH for repo-local non-persistence (verified to specific lines); LOW for provider-side (structurally unverifiable from repos).

## U-06 — Product / learning value verification

- **FABLE5_CHALLENGE**: Challenged whether any value evidence exists and whether the proposed pilot could produce honest evidence as currently framed. Answer: no evidence exists, and the pilot as framed is Goodhart-prone and not executable.
- **DIRECT_EVIDENCE**: V3-09 M8/M9/M13 metric definitions (`:136-159`) and V3-06 T2 candidate-learning trigger (`:118`) are PROPOSED, not measured (HISTORICAL_EVIDENCE); doc-corpus grep (uplift/calibrat/pilot/KPI/baseline/corpus) over all V3 reports + canonical design: zero measured results — only `repurchase_uplift` as a proposed field and test-count baselines; `recOutcomeEventService.ts:81-82` hard-codes `recommendationId: null` + `attributionMode: "organic"` for EVERY event — no code path can attribute an outcome to a recommendation (CURRENT_CODE); flag default OFF (`:53-54`); write paths are fail-open (`recOutcomeEventService.ts:93-98`, `commerceEventService.ts` best-effort catch).
- **FACTS_SUPPORTED**: all 4 KNOWN_FACTS, including the sharpest one — C2 organic outcome data is structurally incapable of measuring recommendation performance.
- **FACTS_NOT_SUPPORTED**: none.
- **HIDDEN_ASSUMPTIONS**: (1) SAFE_DEFAULT's "preserve a kill switch" presupposes a feedback kill switch that does not exist (the only flag gates behavioral C2 events; no flag is even reserved for feedback). (2) Selection bias framed as the only bias — fail-open silent write loss is a second missing-not-at-random source corrupting any pilot denominator. (3) Pilot KPI and stop conditions assumed definable by the same actors whose work the pilot validates — no independent metric owner anywhere. (4) Feedback assumed honest — no adversarial-input assumption despite learning value being the question.
- **MISSING_UNKNOWNS**: feedback authenticity/abuse (ADD-01); per-item classification operating cost (LLM + human review queue volume) — the value question has no cost side; pilot time-box/sample-size — without a horizon an observation-only pilot runs indefinitely, accruing U-03 liabilities while never formally failing.
- **IRREVERSIBLE_RISKS**: "High reversibility" is overstated at the U-05 boundary — pilot raw text reaching an external provider is not reversible by dropping the pilot DB; socially, published "value demonstrated" numbers create decision momentum no kill switch undoes.
- **REWARD_HACKING_RISKS** (ranked): completion/volume KPIs invite UI nudging -> biased corpus justifying learning-enable; any "usefully labeled %" metric rewards `unclear`-avoidance = overconfidence (the exact U-02 failure); fail-open pipelines let "no errors" stand in for "data complete"; M13 denominator shopping (the anti-gaming warning lives ONLY in unapproved V3-09 — zero metric-integrity rules survive into the approved design); attribution creep — organic-only data cannot CONTRADICT a dashboard that relabels same-session purchases as recommendation wins, so any future recommendation-performance claim over RecOutcomeEvent should be treated as reward hacking by default.
- **SAFE_DEFAULT_CHALLENGE**: the stated default (observation-only + kill switch + stop rule) is not executable: no kill switch exists, the stop rule has no owner/horizon/threshold, and observation-only collection still creates erasure obligations from the first stored label. The stricter default consistent with the other unknowns is today's verified state: no collection at all. U-06 is DEPENDENT on U-01/U-03/U-08 decisions, not standalone.
- **REQUIRES_EXPERIMENT**: yes, by definition — value cannot be proven pre-pilot. But only with pre-registered (frozen) KPI, denominator, horizon, stop rule, and an owner independent of the implementing actor.
- **REQUIRES_LEGAL_OR_POLICY_REVIEW**: minimal, unless feedback is incentivized (advertising/inducement rules).
- **REQUIRES_LEO_PRODUCT_DECISION**: whether the value hypothesis justifies collection at all; KPI/stop-rule ownership; pilot horizon.
- **CONFIDENCE**: HIGH on absence of value evidence; HIGH on the organic-attribution structural limit (code line re-verified).

## U-07 — Foundation signal whitelist ownership

- **FABLE5_CHALLENGE**: Challenged the register's frame that the risk is a missing whitelist OWNER while nothing flows. The sharper finding: the outbox WRITE side is active, unflagged, non-conformant to its own contract, and stores raw local user ids with an unverified consent label — today.
- **DIRECT_EVIDENCE** (top items re-verified first-hand): `foundationSignalMapper.ts:50` writes `canonicalUserId: input.userId ?? null` — RAW local id, no subj_v2_ mint (ids.ts not imported); `:30-31` `privacyLevel = input.userId ? "user_consented" : "anonymous"` with comment admitting consent is assumed; `commerceEventService.ts:58` calls `maybeEnqueueFoundationSignal` unconditionally — NO feature flag, wired through trackCommerceEvent into 8+ live routes (alerts/cart/checkout/events/coupons/slice); no flush worker/consumer exists in Cosmile, FOUNDATION, or foundation-control (negative grep; only test scripts deleteMany/count); canonical design R-1 open with gate `FOUNDATION_SIGNAL_WHITELIST_CONTRACT_OWNER_GATE` (`:54-73`, CANONICAL_AUTHORITY); contract 정본 `Cosmile/설계자료/COSMILE_Foundation_Signal_Contract.md` (17-type union, v1.0 2026-06-25) vs mapper EVENT_TO_SIGNAL (`:6-19`): mapper emits 10/17 contract types AND 2 types outside the union (`cosmile.wishlist.add/remove` vs contract `cosmile.product.saved`); payloadJson (`:35-42`) omits contract-mandatory consentStatus/strength/privacy block; `app/docs/COSMILE_TO_FOUNDATION_SIGNAL_CONTRACT.md:22` misstates the implemented mappings (doc-vs-code drift).
- **FACTS_SUPPORTED**: all 4 KNOWN_FACTS — but "draft mapper" is understated: it is a non-conformant subset of its own 정본.
- **FACTS_NOT_SUPPORTED**: none contradicted.
- **HIDDEN_ASSUMPTIONS**: (1) the consent gap has NO carry-forward gate — R-1 covers whitelist ownership, R-2 retention, R-3 stitching; nothing covers outbox consent verification, while contract 정본 §0 rule 4 says user-level signals require consent. The unsafe default is PERSISTED into rows, not just computed at send time. (2) Identifier format assumed settled — raw canonicalUserId conflicts with the later Option B subject_ref direction (Foundation receives opaque contract-format refs only); R-1 resolution must also reconcile identifier format or a flush worker ships raw local ids to Foundation. (3) Existing pending rows assumed contract-conformant v1 signals — they are not (missing mandatory fields).
- **MISSING_UNKNOWNS**: governance of the ACCUMULATION itself (the register asks whether a flush worker exists, not whether unflagged enqueue is acceptable); outbox retention/erasure (no R-gate covers it); which of the two disagreeing contract documents (설계자료 정본 vs app/docs copy) any R-1 owner decision would inherit.
- **IRREVERSIBLE_RISKS**: enabling a flush worker before R-1 + consent verification + identifier reconciliation batch-transmits ALL accumulated rows (raw ids, unverified consent labels, out-of-contract types) to Foundation — disclosure to another system is not rollback-able.
- **REWARD_HACKING_RISKS**: any future "consented signal coverage" metric read from the `privacyLevel` column reports success never earned; `status='pending'` supports "no signals sent = boundary respected" while the boundary-crossing payload is fully materialized and one worker away from transmission.
- **SAFE_DEFAULT_CHALLENGE**: "nothing is sent (no flush worker)" is the register's implicit safe state, but the contract's send-side checks (§5 consent/privacy -> sent/blocked/skipped) exist nowhere; the ONLY barrier is the absence of a worker — an absence no test asserts and no gate protects. This is the weakest safe default in the whole register.
- **REQUIRES_EXPERIMENT**: none — these are code facts. A contract-conformance test (mapper vs 정본 union + mandatory payload fields) is ordinary engineering, not an experiment.
- **REQUIRES_LEGAL_OR_POLICY_REVIEW**: consent verification requirement before any user-level signal transmission; identifier-format compliance with the Option B security direction.
- **REQUIRES_LEO_PRODUCT_DECISION**: R-1 owner designation; **whether outbox enqueue should be flag-gated or stopped now** (and existing rows cleaned) before any 1B design; which contract document is canonical.
- **CONFIDENCE**: HIGH — every load-bearing line re-verified first-hand.

## U-08 — Provenance / OrderItem linkage

- **FABLE5_CHALLENGE**: Challenged whether the claimed "trace anchors" amount to provenance. They anchor two DISJOINT sides with no shared key; the intended meeting point (RecOutcomeFeedback) has the weakest DDL guarantees of the three tables.
- **DIRECT_EVIDENCE**: `schema.prisma:862-871` + `migration.sql:50-63` actual columns (7-column skeleton) vs V3-04 §3.2 proposal — the DDL DROPS ~10 proposed fields (subject_ref, furef, product_id, sku_id, feedback_type, satisfaction_score, adverse_flag, signal_source, source_ref, content_hash, occurred_at, raw_text_stored) and FLIPS two nullabilities (order_item_id nullable->NOT NULL; semantic_label nullable->NOT NULL) (HISTORICAL_EVIDENCE vs CURRENT_CODE); `ids.ts` has NO feedbackId generator (contrast recommendationId/recOutcomeEventId/subjectRef/anonymousRef); no feedbackId format CHECK (contrast `RecommendationEvent_recId_fmt_chk` regex, migration.sql:19); consult route body carries no orderId/orderItemId; CommerceEvent schema has no orderItemId column; RecOutcomeEvent anchors orderItemId (`@@unique`, D-O1) while CommerceEvent anchors foundation_trace_id — no table joins the two; RecOutcomeFeedback.recommendationId has NO FK, NO CHECK, NO index.
- **FACTS_SUPPORTED**: all 5 KNOWN_FACTS bullets.
- **FACTS_NOT_SUPPORTED**: one precision correction — if the register's trace anchors are read as enabling end-to-end provenance, that overstates: order-side and consult-side anchors share no key, so provenance chains cannot be joined today.
- **HIDDEN_ASSUMPTIONS**: (1) recommendationId on feedback assumed trustworthy provenance — any string (or dangling id) is storable, unverifiable at DB level. (2) createdAt assumed sufficient temporal provenance — V3-04's occurred_at (signal time) was collapsed into record time, losing event-time for late-reported adverse reactions (weeks after use). (3) Subject provenance assumed recoverable via OrderItem join — true only while OrderItem rows persist and Order identity semantics stay stable; the feedback table itself diverges from the XOR+secretVersion identity pattern of BOTH sibling tables. (4) raw_text_stored invariant assumed enforced — the column was dropped; the invariant now exists only in documents.
- **MISSING_UNKNOWNS**: feedbackId generation/collision/forgery semantics (dbtest inserts 'fb1'); RESTRICT-vs-Cascade deletion deadlock should be recorded as FACT (the register's own adversarial hint "FK CASCADE?" resolves to RESTRICT); no cross-column adverse coherence rule (V3-04 §5 adverse_flag override logic has no DDL counterpart); the cross-anchor join gap — future V3-11D has NO designed provenance column linking a feedback row to the consultation trace that produced its label; whether any RecOutcomeEvent/CommerceEvent rows exist anywhere is UNVERIFIED_RUNTIME.
- **IRREVERSIBLE_RISKS**: provenance must be captured at write time or never — consultation raw text is by design never stored (correct for privacy), so a backfilled label without a trace column is permanently unreconstructable; the deletion deadlock is a one-way operational entanglement hiding inside an additive migration.
- **REWARD_HACKING_RISKS**: "attribution implemented" claimable because the enum passes CHECK tests while the only writer hard-codes organic; "provenance anchors PASS" claimable from green vitest suites while the flag is OFF and zero rows exist in any environment.
- **SAFE_DEFAULT_CHALLENGE**: "the table is inert" hides that its DDL guarantees are strictly weaker than both siblings (no identity XOR, no id format CHECK, no uniqueness, no raw_text invariant, no recommendationId rule). If G-D1 is approved under schedule pressure, the path of least resistance is writing into this under-constrained table AS-IS, silently inheriting every dropped V3-04 safeguard. The schema itself — not just the missing writer — is an open gate item.
- **REQUIRES_EXPERIMENT**: none — join-key and provenance-column design is contract work, not experiment.
- **REQUIRES_LEGAL_OR_POLICY_REVIEW**: none specific beyond U-03's erasure propagation (join-only subject linkage complicates erasure).
- **REQUIRES_LEO_PRODUCT_DECISION**: whether provenance/identity/dedup columns are a hard precondition for ANY feedback writer; characterize the V3-04->DDL narrowing as "contract narrowed, invariants demoted from DDL to docs" and decide which invariants must return to DDL.
- **CONFIDENCE**: HIGH (schema/migration/ids.ts read first-hand in prior spot-checks).

## U-09 — Safety / learning / correction boundary

- **FABLE5_CHALLENGE**: Challenged the comfort that fail-closed safety machinery protects the learning boundary. The machinery that is real and tested protects a DIFFERENT pipeline (consultation); the one rule that governs the actual learning path is the least conservative and enforced by nothing. The core risk is MISATTRIBUTION, not absence.
- **DIRECT_EVIDENCE**: `core.py:530,543,548,1452,1456` raise-only severity gate + `:578-593` AI-primary adverse class with uncertainty backstop -> mild/caution, pass forbidden; `:1624-1628` contract-level fail-closed backstop — all CONSULTATION-scoped (CURRENT_CODE); `Cosmile/app/src/lib/adverse.ts:17` severityOf unknown -> 'severe' (fails UP) + matrixEffect §5.3 branches — **matrixEffect has NO production consumer** (only the vitest imports it; TEST); dictionary §2.4 unknown severity -> record as 'low'+reported (fails DOWN) with a 30d re-evaluation that exists NOWHERE in code (negative grep); `recOutcomeEventService.ts:2` semantic prohibition; grep 'retract' across FOUNDATION/foundation-control/Cosmile = zero; 'contradicted' settable by nothing; no actor/authorization column anywhere records WHO could set verified/contradicted (V3-07 requires human safety review for verified correction — unenforceable).
- **FACTS_SUPPORTED**: all 4 KNOWN_FACTS bullets.
- **FACTS_NOT_SUPPORTED**: none contradicted. Precision caveat: "raise-only gate exists" must not be read as "the learning boundary is gated" — the feedback->memory path has zero implementation and therefore zero gates.
- **HIDDEN_ASSUMPTIONS**: (1) fail-closed direction assumed uniform — adverse.ts fails UP while the dictionary rule for unknown severity fails DOWN, and the DB cannot represent "severity unknown" except as 'low'. (2) The safety matrix assumed to protect memory — it is dead code; §5.3 effects (immediate_safety_block etc.) are currently unenforceable strings. (3) 'contradicted' assumed a controlled correction input — no authorization model exists. (4) Adverse-priority over commerce assumed mechanical — precedence between an early adverse row and a later satisfied row on the same item is design prose only (multiple rows legal, no aggregation code).
- **MISSING_UNKNOWNS**: who/what may write 'verified'/'contradicted' (privileged transitions) — deserves its own unknown; the 30d re-evaluation scheduler required by the unknown->low rule and §6.1 path 1; cross-pipeline severity translation ownership (consultation red_flag findings and commerce feedback severity currently CANNOT exchange signals at all); precedence/aggregation semantics for conflicting rows.
- **IRREVERSIBLE_RISKS**: (a) once feedback drives LongTermMemoryFact, an erroneous 'contradicted'/resolution event deactivates a safety fact with no actor audit trail; (b) unknown-severity events persisted as 'low' with no re-eval job accumulate understated safety severity in durable memory — the original uncertainty is not stored and cannot be reconstructed.
- **REWARD_HACKING_RISKS**: a future learning implementation could truthfully cite "raise-only gate PASS (core.py)" and green matrix tests (T11/T12) while shipping an unguarded feedback->memory path — the tests prove helper arithmetic, not enforcement; any U-09 success claim should be required to show a CONSUMER of matrixEffect plus a blocked-demotion regression (neither exists); the unknown->low recording rule systematically biases stored severity downward, making adverse look milder with no filter applied — V3-09's anti-gaming clauses guard aggregation, not this upstream down-recording.
- **SAFE_DEFAULT_CHALLENGE**: two-level failure. (a) Component level: every verified safe default (uncertainty->caution, unknown->severe, matrix-out->block) lives in consultation-scoped or unwired code; the one rule governing the actual path (unknown->low) is the least conservative and unenforced. (b) Composition level (cross-register): U-09's SAFE_DEFAULT expects safety-relevant feedback to "trigger conservative non-diagnostic guidance and review", but U-01 forbids collecting feedback, U-03 forbids persisting it, and U-05 forbids sending it — jointly, a customer reporting a severe post-purchase adverse reaction TODAY leaves no sanctioned record and receives no sanctioned follow-up unless it happens to arrive through the pre-existing consultation path, which the defaults neither name nor scope. The register presents each default as individually safe without checking their composition.
- **REQUIRES_EXPERIMENT**: none — this is design/authorization work, not experiment.
- **REQUIRES_LEGAL_OR_POLICY_REVIEW**: duty-to-act question for severe adverse reports (health-adjacent); who bears responsibility for an unstaffed review queue.
- **REQUIRES_LEO_PRODUCT_DECISION**: what happens TODAY when a severe adverse report arrives through the existing consultation path (the composition gap above); the correction-authority model (who may set verified/contradicted); which fail-closed direction governs unknown severity (up vs down) — the current documents contradict each other.
- **CONFIDENCE**: HIGH (dead-code status of matrixEffect and the three-enum disjunction re-verified via negative greps).

---

# Global sections

## G1. Reviewed files (aggregate, deduplicated)

CURRENT_CODE — Cosmile (`shadow/m4-cosmile-memory` @ e4ed668): `app/prisma/schema.prisma`; `app/prisma/migrations/20260706120000_v3_11b_learning_commerce_memory/{migration.sql,down.sql}`; `app/prisma/migrations/20260709120000_v3_11c2_d_o1_rec_outcome_orderitem_unique/migration.sql`; `app/src/types/{recOutcome.ts,commerceEvent.ts}`; `app/src/lib/{recOutcomeEventService.ts,commerceEventService.ts,foundationSignalMapper.ts,memoryCandidate.ts,ids.ts,shopper.ts,checkout.ts,adverse.ts,consultationMeta.ts}`; `app/src/lib/events/{foundationDecisionEvent.ts,piiPolicy.ts}`; `app/src/lib/slice/mockAdapters.ts`; `app/src/adapters/{foundationClient.ts,cosmileSemanticAdapter.ts}`; `app/src/app/api/checkout/mock-complete/route.ts`; `app/src/app/api/slice/{consult,consult-foundation}/route.ts`; `app/src/app/api/consultation/{foundation-dev,meta/mention}/route.ts`; `app/src/app/api/events/route.ts`; `app/src/app/orders/[orderId]/page.tsx`; `app/src/app/account/orders/page.tsx`; `app/.env.example` (+ `.env.local` key NAMES only). CURRENT_CODE — foundation-control: `foundation_http_service/{server.py,core.py,contracts.py,semantic_router.py,llm_guard.py,deepseek_composer.py,ingress_gate.py}`; `scripts/{foundation_routing_wider_eval.py,foundation_http_service_test.py}`; `tests/test_cosmile_memory_reuse_decision.py`; `cosmile_loop/cosmile_memory_reuse_adapter.py`. TEST: `app/scripts/{v3_11.vitest.ts,v3_11c2_rec_outcome.vitest.ts,v3_11b_db_integration.dbtest.py,smoke-commerce.mjs,mock-commerce-scenario-100.mjs}`. CANONICAL_AUTHORITY: `foundation-docs/설계문서/cosmile/COSMILE_V3_11C2_COMMERCE_MEMORY_DESIGN.md`; `Cosmile/설계자료/COSMILE_Foundation_Signal_Contract.md`; `Cosmile/app/docs/COSMILE_TO_FOUNDATION_SIGNAL_CONTRACT.md`. HISTORICAL_EVIDENCE: `foundation-docs/docs/reports/control/COSMILE_MEMORY_V3_{02,04,06,07,09,10_GATE_RESULT,11C2_REC_OUTCOME_EVENT_GATE_PLAN,11D_SEMANTIC_EXTRACTION_GATE_PLAN,DATA_DICTIONARY_CANONICAL}*.md`. Register + freeze: `V3_PACKAGE1A_UNKNOWN_REGISTER.md` (full 474 lines) + `10_LOOP_STATE.md` (freeze-state lines only) + git freeze-commit verification.

## G2. Excluded scope

No DB query or row read (dev.db observed as file metadata only); no secret VALUE read (key file existence/mode and env key NAMES only); no live endpoint or model call; no provider console; no deployed-runtime state (all flag-state claims are code-read = UNVERIFIED_RUNTIME for actual environments); no file patching, no runtime/schema modification, no reviewed-repo commits; no final-design authorship or product-policy choice; no Package 1B design.

## G3. Independence declaration

- Forbidden files NOT opened: `ADVISOR_INDEPENDENT_ASSESSMENT.md`; `runs/foundation/20260710_v3_package1a_*`; `runs/cosmile/20260710_v3_package1a_*`; any `ACTOR_COMPARISON_MATRIX.md` / `FOUNDER_DECISION_PACKAGE.md` / `FOUNDER_ACCEPTANCE_SHEET.md`.
- Incidental METADATA exposure, disclosed for completeness (content never opened): (a) a filename-level `find` listing during evidence sweep included `ADVISOR_INDEPENDENT_ASSESSMENT.md`; (b) `git log` in foundation-docs showed the commit SUBJECT "docs(runs): Cosmile blind independent assessment U-01..U-09 (Package 1A)" (1b44760). Neither file's content was read.
- Process observation (non-blocking, for future runs): freeze commit `fab82c45` committed `ADVISOR_INDEPENDENT_ASSESSMENT.md` in the SAME commit/directory as the register — any blind actor cloning the freeze commit has the Advisor's answers one file away from the question set. Future freezes should commit the register BEFORE any assessment exists.
- Method disclosure: see "Method and evidence classes" above (session-internal parallel verification passes; verdicts and load-bearing re-verification by the reviewer).

## G4. Unknown addenda (additions — NOT silent register edits; source and reason per item)

- **ADD-01 Feedback authenticity / abuse / poisoning.** Source: adversarial sweep, grep abuse|fraud|poison|fake over register = 0 hits. Reason: fake/incentivized/competitor feedback attacks both learning value (U-06) and safety response (U-09: malicious adverse reports); no unknown covers authenticity.
- **ADD-02 Sensitive-population x feedback (pregnancy / minors / medical).** Source: grep 임신|pregnan|미성년|연령 over V3-07 = 0 hits; register has no such unknown — while foundation-control CLAUDE.md §8 treats pregnancy overreach as a first-class STOP axis for consultation. Reason: feedback from pregnant users/minors about K-beauty/health products crosses the same axis with zero register coverage.
- **ADD-03 CommerceEvent.propertiesJson free-text governance.** Source: CURRENT_CODE first-hand — `app/src/app/api/events/route.ts` persists arbitrary client `properties` via `sanitizeProperties` (`commerceEventService.ts:11-23`) whose protection is an exact-key BLOCKLIST (`piiPolicy.ts` PII_FORBIDDEN_KEYS/RAW_TEXT_FORBIDDEN_KEYS) + email/phone/card value regexes + 1000-char cap; keys like `feedbackText`/`comment`/`review` are NOT blocked, and free-form Korean health text matches no value pattern. A direct POST with `{"feedbackText":"피부가 따가워요"}` under a whitelisted eventType persists durable user text TODAY. Reason: touches U-01 (an input path beyond consultation exists), U-03 (no deletion lineage for propertiesJson), U-05 (raw-text non-persistence violated in spirit); blocklist design also contradicts the workspace whitelist-first principle. Highest-value addendum — the register's own U-01 HOW_TO_RESOLVE asks for exactly this verification; the answer is: yes, one exists.
- **ADD-04 Classifier / provider version drift and label reproducibility.** Source: grep provider|version across register hits only U-05 retention context. Reason: a provider/model change silently shifts semantic_label distributions, making historical labels incomparable and invalidating any U-02 calibration; no unknown owns reproducibility.
- **ADD-05 Human-review operations (staffing, SLA, cost, unstaffed-queue behavior).** Source: U-09 MISSING_EVIDENCE names ownership only; U-02 SAFE_DEFAULT routes uncertainty into a review pipeline that has no owner today. Reason: multiple defaults route items into a queue that operationally does not exist.
- **ADD-06 Register dependency ordering (meta-unknown).** Source: register full read — no DEPENDS_ON field; dependencies exist only in prose (U-02/U-06/U-08 conditional on the U-01 product decision). Reason: parallel blind actors may answer against different assumed input modes, making outputs incomparable; founder decisions should be ordered (U-01 first).
- **ADD-07 Documentation-state / historical-doc supersession.** Source: first-hand doc headers — DATA_DICTIONARY self-labels "status: CANONICAL ... 유일한 ... 정본" with no supersession marker; V3-10 says "DESIGN_APPROVED_WITH_LIMITS"; V3-04/06/07 carry ownership claims with NO status label; V3-11D carries no HOLD banner on the doc itself (HOLD recorded only externally); the approved 2026-07-09 design references NONE of the V3-0x corpus. Reason: a repo-local worker following design-first discipline could take these as sufficient approval to implement V3-04 §9.5 — the exact failure the rule exists to prevent. Remedy direction: STATUS banners (HISTORICAL_DESIGN_NOT_PACKAGE1_APPROVED / SUPERSEDED_BY) — a doc change, deliberately NOT made by this read-only pass.
- **ADD-08 Outbox enqueue governance (active, unflagged raw-id accumulation).** Source: U-07/U-03 findings, first-hand. Reason: the register frames outbox risk around a future flush worker; the write side needs its own decision NOW (flag-gate? stop? clean existing rows?). Candidate for the earliest founder decision in the package.
- **ADD-09 Metric governance / anti-Goodhart ownership.** Source: XCUT review — V3-09's anti-gaming rules exist only in an unapproved historical doc; zero metric-integrity rules survive into the approved design. Reason: KPI definitions, denominator changes, and pilot-report audit need an owner independent of implementing actors before any pilot.

## G5. No-prelaunch-proof list (cannot be proven before pilot / live-like evidence)

1. Provider-side retention/deletion of transmitted text (U-05) — contractual/console evidence only; a canary proves transmission, not deletion.
2. Real-auth identity behavior — XOR under real sessions, guest cookie loss rates, stitching UX (U-04) — mock-auth structure proves nothing about production auth.
3. Real feedback distribution, volume, selection bias, and abuse rate (U-01/U-06/ADD-01).
4. Classifier accuracy/calibration on real Korean feedback incl. sarcasm and mixed statements (U-02) — corpus must come from real or realistic input.
5. Any value/uplift claim (U-06) — and even then only with pre-registered KPI/denominator/horizon/stop rule.
6. Deployed runtime state: whether flags are OFF, whether outbox/CommerceEvent rows exist and in what volume, in any real environment (all UNVERIFIED_RUNTIME here).
7. Human-review queue viability (ADD-05) — staffing/SLA under real volume.
8. Host-level surfaces: backups capturing dev.db or key files, OS crash dumps, framework request logs (U-05).

## G6. False-certainty findings (ranked)

1. **"No flush worker = boundary safe" (U-07/U-03).** The outbox WRITE side is active, unflagged, on 8+ routes, persisting RAW `canonicalUserId` with an unverified `user_consented` label and out-of-contract signal types. The register's safe-default frame ("nothing flows to Foundation") is true only of transmission — the boundary-crossing payload is already materialized. This breaks the register's implicit "inert until flag/worker" model and is the single most decision-relevant correction.
2. **Self-certifying invariants (U-05).** `core.py:50` INVARIANTS is a constant: `raw_text_stored=false`/`pii_stored=false` are declarative labels spread into every response and echoed by Cosmile as evidence. Any audit that reads response fields will "confirm" non-persistence forever, including after a regression.
3. **"External send when enabled" (U-05).** `core.py:1616` hardcodes `"compose": True` on the canonical contract endpoint; with the key file present, external transmission is the default behavior of every consult_contract call on this host — not an opt-in.
4. **Vocabulary read as capability (U-02).** The 10-value CHECK + green enum tests can be cited as "semantic classification verified"; they constrain storage vocabulary only. No classifier, corpus, or calibration exists; `unclear` is not a default and nothing routes uncertainty to it.
5. **Adjacent safety machinery misattributed (U-09).** The tested raise-only gates protect consultation; matrixEffect is consumer-less dead code; the only rule governing the actual learning path (unknown severity -> record as 'low') fails DOWN and is enforced by nothing.
6. **Historical documents readable as approval (ADD-07).** "CANONICAL" / "DESIGN_APPROVED_WITH_LIMITS" / unlabeled ownership claims / missing HOLD banner — with no supersession chain in either direction.
7. **Paper mechanisms counted as protections (U-04/U-03).** allowLink=false consulted by nothing; outbox 5-state lifecycle comment-only; tombstone columns set by no code path; WITH_LIMITS/WITH_RISK suffixes drop off in downstream citation.
8. **Register wording "write 호출 0" (U-01).** Literally false — test-harness raw-SQL inserts exist (class TEST); the true claim is "no product write path".
9. **Fail-open observability (U-04/U-06).** `xor_violation` vs `write_failed` indistinguishable at the route observable; best-effort catches convert data loss into "no errors observed".

## G7. Self-review (Sentinel 6 rules applied to this document)

- Every load-bearing claim carries file:line; the top findings were re-verified FIRST-HAND by the reviewer this session: `foundationSignalMapper.ts:30-31,45-58` + `commerceEventService.ts:58` (FC-1); `core.py:50-52` + spread sites (FC-2); `core.py:1616` (FC-3); `migration.sql:50-63` DDL + orders UI absence + zero-writer greps (U-01/U-02/U-08); `piiPolicy.ts` + `sanitizeProperties` blocklist (ADD-03); `schema.prisma:899` allowLink zero-runtime-refs (U-04).
- Verified NOT first-hand by the reviewer (verifier-pass evidence, spot-check sampled, citations re-checkable): V3-04 full field-table delta, dictionary/V3-07 section line numbers, wider-eval utterance count, `.secrets` key-file mode bits. Nothing in the verdicts depends solely on an unsampled claim.
- Not verified at all (stated as such throughout): runtime/deployed state, DB contents, provider behavior, secret values.
- Deviation disclosure: in-session parallel verification passes vs the brief's "no sub-agent creation" line — interpretation stated openly in the Method section rather than hidden.

## G8. Blind-pass status

**`BLIND_ASSESSMENT_COMPLETE`**

Return to: **Advisor**. This document challenges the frozen register; it does not choose product policy, author final design, or approve anything.
