# Advisor Integrated P0/P1 Decision Package

MISSION_ID: `FOUNDATION_INTERNAL_CAPABILITY_AND_RUNTIME_BASELINE_V1`

PACKAGE_STATUS: `CANDIDATE_PENDING_INDEPENDENT_STATIC_CHALLENGE`

EVIDENCE_CEILING: `FOUNDATION_LOCAL_SOURCE_ONLY`

## 1. Authority and containment

Leo authorized P0 admission/estimate, a Foundation-local P1 shallow static census, an Independent Reviewer protocol challenge, and documentation-only evidence. P2, P3, P4, execution, implementation, other product/control repository inspection, credentials, spending, and runtime access remain prohibited.

Authority integrity passed for the approved instruction and approval record. The Advisor admitted the work as `PROCEED_WITH_LIMITS` and selected only the Foundation Worker and an independent Foundation Reviewer. The separate Cosmile browser mission remained at its owner-credential gate and was neither inspected nor advanced.

The Foundation repository remained at:

- branch `shadow/foundation-shared-memory-v0`
- HEAD `33570b9d7db79c991bb216b6a2dc80880ba1f2d6`
- upstream ahead/behind `0/0`
- tracked state clean
- exactly two pre-existing untracked files, byte-identical before and after the census

No build, lint, test, import execution, runtime, DB, endpoint, network, model/provider, dependency installation, product write, stage, commit, or push occurred in Foundation.

## 2. Canonical P1 evidence

Worker result:

- `runs/shared/FOUNDATION_INTERNAL_CAPABILITY_AND_RUNTIME_BASELINE_V1/10_P1_FOUNDATION_SHALLOW_CENSUS_RESULT.md`
- SHA-256 `abf8ea4fd4d72c44a09c459c1f141349bf2cb85b004f9a2887ebbb7d32d41123`
- 49,683 bytes, UTF-8 PASS
- published byte-identically from the actor-isolated temporary artifact

Worker pointer:

- `runs/shared/FOUNDATION_INTERNAL_CAPABILITY_AND_RUNTIME_BASELINE_V1/11_P1_FOUNDATION_SHALLOW_CENSUS_POINTER.md`
- SHA-256 `2b0a6a2ffa279d4e7a53f3b82f0febc4327d891ca675f5d1527322e7acb69e2c`
- published byte-identically

The result maps 268 tracked files into 22 capability rows across six domains. Every row keeps `SOURCE`, `BUILD`, `TEST`, `RUNTIME`, `INTEGRATION`, `AUTHORITY`, and `TARGET_FIT` independent. Because execution was prohibited, no source/test/doc presence was promoted into an at-HEAD execution claim.

## 3. Current-state shallow map

### Source-present Foundation-local capability groups

- canonical data access and deterministic intake layers;
- ingredient-registry expansion working set;
- customer-safety/trust, response-gate, LMR, and Brain shadow logic;
- Foundation self-protection/security Layer A source;
- internal in-process API and Foundation-side Cosmile/SIASIU contract surfaces;
- feature-flag containment and typed configuration/contracts;
- shared-memory v0 shadow and commerce-evidence C-shadow WU1–WU6 source;
- test, fixture, eval, and governance/design-document surfaces.

### Absent or external/unresolved at this Foundation-local boundary

- canonical data corpus is external by path contract and was not inspected;
- vault-to-Foundation ingest implementation is design-only;
- search/grounding implementation is not present in this repository;
- packaging/dependency manifest is absent;
- deployment, CI, service, and monitoring implementations are absent;
- canonical shared-memory contract and downstream consumer conformance require separately authorized cross-repository evidence.

### Containment facts established only from source

- no tracked Python import lines for network clients, DB drivers, web frameworks, or model providers were found;
- live/write flags are source-level HARD_OFF, and commerce-evidence C-shadow defaults OFF;
- model adapters are disabled placeholders;
- shared-memory/commerce evidence contains no source-level endpoint, transport, consumer, persistent store, or network path.

These are static facts, not executed containment proof.

## 4. Material contradictions, risks, and unknowns

The Worker recorded ten documentation/source-shape contradictions without correcting them. The highest-impact items are:

1. `HANDOFF.md` and several counts/status statements predate the current materialized/shared-memory/commerce-evidence tree.
2. No packaging/dependency manifest exists, while current documentation refers to an external SIASIU virtual environment; standalone importability is unknown.
3. Canonical product/knowledge data is external, so availability, shape, and stated counts are unverified.
4. Canonical shared-memory contract conformance and downstream adapter conformance are unverified because other repositories were correctly excluded.
5. Multiple trust surfaces have confusing near-duplicate names and materialized wrapper/source shapes.
6. Two modules retain a nonexistent `app/` sys.path insertion; runtime effect is unknown.
7. Foundation Docs Sync Policy is mandated by `CLAUDE.md` but exists as an untracked file.
8. Deployment/monitoring ownership and repository location are unresolved.
9. All at-HEAD `BUILD`, `TEST`, `RUNTIME`, and `INTEGRATION` axes remain unverified.

No risk is accepted and none of these findings is fixed in this mission.

## 5. Selected later probe proposal — not authorized

Recommended order if Leo later authorizes exact probe handoffs:

1. `P2-A` — stdlib-only AST/import-resolution integrity in an isolated, no-network/no-DB boundary.
2. `P2-B` — bounded vault-independent repo-local test suites.
3. `P2-C` — targeted HARD_OFF/gate-0 containment behavior.
4. `P3-B` — synthetic shadow-eval rerun and diff against the committed historical report.
5. `P3-A` — vault-dependent read-only regression only after separate data-boundary approval.

The exact commands, targets, risks, dependencies, evidence, and per-probe estimates are in Worker result §8. This package recommends no automatic execution and does not convert the proposal into authority.

## 6. Estimate and actor plan

### Completed-authority envelope

- P0/P1 engineering estimate: `3.0–6.0 workdays`
- P0/P1 elapsed estimate: `2.0–4.0 working days`
- external cash cost: `$0`, excluding already-provisioned agent compute
- confidence: `MEDIUM`

### Recommended later outer envelope — not authorized

- P2: `1.5–3.0 engineering days`, `1.0–2.0 elapsed working days`
- P3: `1.5–3.0 engineering days`, `1.0–2.0 elapsed working days`
- P4: `3.0–6.0 engineering days`, `2.0–4.0 elapsed working days`
- total P2–P4 outer envelope: `6.0–12.0 engineering days`, `4.0–8.0 elapsed working days`
- expected external cash: `$0` for local-only work; any provider/API/data-boundary cost requires separate approval

Potential later actors:

- Foundation Worker for exact Foundation-local probes;
- Independent Reviewer for adversarial evidence verification;
- Foundation Advisor for authority/evidence routing;
- Leo/GPT for every P2+ authorization and data/cross-repository boundary;
- no Control, Designer, Cosmile, or SIASIU actor for the selected Foundation-local P2 probes.

## 7. Decision requested after review

The Reviewer is asked only to challenge factual accuracy, axis discipline, path grounding, authority containment, proposal safety, and decision readiness. Reviewer PASS will not authorize P2 or certify runtime behavior.

If the reviewed package passes, Strategy/Leo may later choose whether to authorize any individual P2 probe. The recommended first decision is whether to authorize `P2-A` alone, because standalone importability is the highest-leverage unresolved Foundation-local question.

## 8. Frozen final status before review

- P0: `COMPLETE_PENDING_REVIEW`
- P1: `COMPLETE_PENDING_REVIEW`
- P2: `NOT_AUTHORIZED`
- P3: `NOT_AUTHORIZED`
- P4: `NOT_AUTHORIZED`
- implementation: `NOT_AUTHORIZED`
- Foundation product changes: `NONE`
- other repository inspection: `NONE`
- Cosmile browser mission: `PRESERVED_AT_OWNER_CREDENTIAL_GATE`
- next mission: `NOT_AUTHORIZED`

`HARD_STOP_BEFORE_P2: ACTIVE`
