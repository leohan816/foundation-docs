# FABLE5 DESIGN REVIEW — Agent Office M01 Exact Advisor Delivery Bridge

- Mission: `AGENT_OFFICE_M01_EXACT_ADVISOR_DELIVERY_ACTIVATION` (AO-WU-18) · Actor: **Fable5 Reviewer** (`reviewer-fable5`) · Pass: `DESIGN_REVIEW__AGENT_OFFICE_EXACT_ADVISOR_DELIVERY` · Level 3 · Skill: `/fable-sentinel` · Date: 2026-07-11
- Target: `d1708809` (base `9c403da`) on `shadow/agent-office-m01` — verified `d170880 = HEAD = origin` from git directly; delta = exactly 10 Markdown files, +1083/-36, **zero non-.md files** (docs-only claim reproduced by name-only diff, not trusted from the Worker report)
- **VERDICT: `PASS`** — a design PASS approves neither implementation nor delivery; the serial train (Worker implementation -> Fable5 implementation/security review -> Advisor synthetic rehearsal AO-WU-21 -> final audit) remains required.

## 1. Reviewed artifacts (direct)
`AGENT_OFFICE_EXACT_ADVISOR_DELIVERY_BRIDGE_DESIGN.md` (836 lines: structure, DQ register, invariants, schemas 5-9, transport 7, crash matrix 8, ingress 9, kill/fallback 11, scope 12, plan 13, test/threat matrices 14, rehearsal 15, nine-criterion traceability 16, deferrals 17); `AGENTS.md`/`CLAUDE.md` deltas; all seven canonical doc deltas + FEATURE_INDEX status rows; job artifacts 00/01/02/03; `WORKER_DESIGN_RESULT.md` (claims cross-checked against git: 10 files, clean staged/unstaged, target commit match); V2/transport/registry/kill-switch/activation/manifest evidence as referenced.

## 2. The 14 mandated checks — all verified
1. **DQ-01..08 resolved from evidence, no new authority/policy**: register 3 resolves each with honest qualifiers (`NO_CAPABILITY_YET`, `DURABLE_PORT_REQUIRED`, `REGISTRY_REFRESH_PREREQUISITE`, `EXECUTION_DEFERRED_TO_AO-WU-21`); the doc states no DQ requires a new Leo/GPT decision — confirmed by content.
2. **Destination fixed, not browser-selectable/generalizable**: fixed `foundation-advisor/$9/0/%9` with "There is no destination selector" (4:96); two-key trusted-config selection (5.1) + registry match + live `tmux display-message` double preflight (DQ-02); browser data cannot set target/path/buffer/command/key/role/executable (DQ-03).
3. **Immutable exact pointer only**: canonical v1 envelope written to an owner-only immutable pointer artifact; file-loaded buffer; no message body in tmux input (6, 7.3).
4. **No interpolation/wildcard/broadcast/sync/terminal/Worker/Reviewer dispatch**: `shell:false` fixed argv, no `eval` (7.3:371), invariants 4:101-102; 12: "Worker and Reviewer sessions are unrepresentable as gateway destinations"; no generic process runner exposed.
5. **Durable idempotency + crash boundaries incl. at/after paste start**: identity tuple (8.1), fsynced journal (8.2), crash matrix (8.3:429-438) — `PASTE_STARTED`/`PASTE_CONFIRMED`/`SUBMIT_STARTED` are ambiguous and never auto-resent; replay returns `ALREADY_DELIVERED`; the in-memory Map is demoted to cache, durable port is the authority.
6. **Receipt / ACK / intake / decision / resume distinct**: DQ-05 "Delivery never equals ACK, and ACK never equals intake, decision, or resume"; separate records 9.2-9.5 with state ordering 10.
7. **Evidence ingress closed-schema and non-elevating**: committed Git blobs under `advisor/**` verified by repository/commit/path/blob/SHA-256/ancestry/correlation via internal port (no HTTP `advisor_operator`); routine Advisor routing limited to already-authorized V2 scope; "material decisions remain Leo/GPT-only" with authority proof (9.4).
8. **Fail-closed kill/disable/expiry/restart/stale/preflight/ambiguity, no auto-enable**: DQ-06 + 11; healthy external state cannot clear a local latch; re-enable needs a new exact activation grant; 12: successful LocalBootstrap login "cannot create, refresh, choose, or re-enable" delivery.
9. **Loopback-only scope**: 12 read in full — bind exactly `127.0.0.1:4317`, no CORS/proxy/TLS/HSTS, `viewer`+`leo_input` only, no remote/private/public/Tailscale listener; DB/Hermes/production absent (Hermes remains an uncomposed disabled stub).
10. **Prerequisites explicit, not hidden**: DQ-02 names the registry window-ID gap and requires an Advisor-published refreshed reviewed row BEFORE activation; :490-496 requires Advisor to publish and reload a narrowly scoped inbox-read instruction with "Missing reload evidence blocks activation"; 13:686-687 assigns both to Advisor's governed authority with Fable5 review of the actual schemas/instruction.
11. **Nine Leo/GPT success criteria traceable**: 16 maps each criterion to implementation path, test/review proof, and actual-rehearsal proof; every row honestly `DESIGNED_CANDIDATE` ("No criterion is complete from this design candidate").
12. **Rehearsal bounded**: synthetic, non-sensitive, fixed Advisor pane only; proves duplicate non-delivery and fail-closed negatives; cleanup stops the server, consumes/removes proof material, deletes test buffers, latches delivery disabled, records listener/lock/credential absence (15.1-15.3); execution deferred to AO-WU-21 and authorizes no operational use.
13. **Implementable without invention**: closed schemas for config/lease/capability/envelope/journal/ingress records, exact argv sequence, rejection codes, unit/contract/integration test list and threat/failure matrix (14) — the only open item is Advisor-published evidence (window-ID row), not Worker policy invention.
14. **Docs-only, nothing hidden, no weakened safety**: zero non-.md in the delta; the AGENTS/CLAUDE change retires the exhausted six-file bootstrap allowlist as historical evidence and binds scope to the exact committed Advisor handoff while explicitly keeping every actor/safety/review/Git/STOP rule active and declaring the current assignment design-only; canonical docs carry `DESIGNED_EXACT_ADVISOR_DELIVERY_CANDIDATE__PENDING_FABLE5` ("no delivery authority, capability, port, runtime behavior, or actual tmux input exists"). Mission manifest v5 adds AO-WU-16..21 and moves AO-WU-15 to depend on AO-WU-21 — the serial train is preserved.

## 3. Findings
None blocking. Named prerequisites carried by the design itself (to be evidenced before activation, verified at the next passes): P-A refreshed registry row incl. current window ID (DQ-02); P-B Advisor narrow inbox-read instruction publish + reload evidence (:490-496). Implementation obligations pinned by the design for the next review: durable journal port as idempotency authority (DQ-04) and capability minting only after full snapshot/lease validation (DQ-01).

## 4. Verdict rationale
All fourteen mandated checks pass on direct evidence; the design is fail-closed at every boundary, adds no actor or authority, leaves nothing material to Worker invention, and is honest about its two mechanical prerequisites and deferred rehearsal. Per the V2 contract: **PASS** — implementation (AO-WU-19), implementation/security review (AO-WU-20), and the synthetic rehearsal (AO-WU-21) remain separately gated.

## 5. Self-review (Sentinel 6 rules)
Every claim cites the actual design text at line/section or my own git output (chain, name-only diff, upstream equality); Worker/Advisor texts were cross-checked only. Read-only: no patch, no authority material, no server, no tmux input, no rehearsal; only this result + pointer written. Not verified (stated): runtime behavior (none exists for the bridge); Advisor-side instruction content (to be published; reviewed at the next pass).

Return to: **Advisor**.
