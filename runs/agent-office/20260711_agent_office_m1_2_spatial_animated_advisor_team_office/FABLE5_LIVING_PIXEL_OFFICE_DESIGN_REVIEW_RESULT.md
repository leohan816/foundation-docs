# FABLE5 REVIEW — Living Pixel-Office Renderer and Prototype-Gate Design (AO12-PWU-06)

- Mission: `AGENT_OFFICE_M1_2_SPATIAL_ANIMATED_ADVISOR_TEAM_OFFICE` · Pass: **DESIGN_REVIEW__M1_2_LIVING_PIXEL_OFFICE_RENDERER_AND_PROTOTYPE_GATE** · Actor: **Fable5 Reviewer** (`reviewer-fable5`) · Level 3 · Skill: `/fable-sentinel` (+ review-classification reference loaded this pass) · Date: 2026-07-12
- Target: `48c8dbd -> 9611d0da1479ca5e7a9677641fe767a6b39b4a38` = origin; exactly 5 canonical Markdown paths, +2002/-25; worktree clean; ancestry verified; the three new documents' sha256 match the Worker declaration (recomputed by me).
- **VERDICT: `PASS`** (clean) — per the gate this opens **only** the bounded synthetic prototype `AO12-PWU-07..10` after Advisor issues the exact new handoff. It does not authorize full integration (`AO12-PWU-12` stays blocked on explicit durable Leo/GPT `AO12-PWU-11` approval), production selection, dependency installation, media tooling, or any next mission.

## What I read and reproduced (first-hand)

- All five changed documents in full at `9611d0d` (renderer design 570 lines; sprite/animation system 543; implementation plan 736; master-design and FEATURE_INDEX patch hunks line-by-line), plus decision 59, patch manifest, brief 62, validation 64, Worker result/pointer.
- **Repo-fact reproduction** (design §2 table, all verified by my own commands): React/React-DOM exactly `19.2.7`; `pixi.js`/`@pixi/react` absent from `package.json` **and** lockfile (0 matches); `ffmpeg`, `ffprobe`, `gifski`, `convert`, `magick` all absent from PATH; the artifact root is **not** ignored at the design base (`git check-ignore` negative — exactly as the design records); Playwright `1.61.1` installed; PWA/cache-policy/static-shell/placeholder-inventory files exist; Vite virtual entry separates production (`runtime/entry.tsx`) from the synthetic demo (verified in prior AO12-D review and unchanged here); delta contains zero runtime/source/test/config/package paths (my `git diff --name-only`).

## The 14 mandatory verifications

1. **Living world, not cards-in-canvas**: the renderer design commits to one bounded tile floor (16x16 logical tiles, 32x48 actors, walls/desks/computers/glass room/lounge/walkways/hubs/booth/signs/boards/Channy facilities, §6), keeps dense text out of the canvas (§9: concise HUD only; details stay DOM), and makes "a continuous office, not DOM cards rendered into a canvas" an explicit prototype gate (§15). Sprite §2 additionally bans separate cards/rooms that disappear on selection. **Designed as required.**
2. **Pods coexist / invariants exact**: §6.2 — every registered Pod on one floor, four-column deterministic grid sorted by stable projectId, selection never reorders/relocates; one hub character per exact Advisor `roleInstanceId`, Pods hold references, never clones; unverified mission renders static `NO_VERIFIED_MISSION`. Assignment/Advisor fail-closed behavior is inherited by consuming the existing validated projection + cue reducer (§5, sprite §1), and sprite §9 renders `ASSIGNMENT_CONFLICT` with no task/ambient animation and forbids a second actor copy. SIASIU naming current in the palette table (sprite §6). **Exact.**
3. **React + @pixi/react feasibility**: grounded in the actual repo (facts reproduced above) and official-source evidence rows (§3) covering StrictMode double-invoke, client-only layout effects, @pixi/react 8 = React 19 + Pixi 8, `extend` bounded catalogue, memoized `useTick`, async `Application.init`, private ticker, `resizeTo` + `ResizeObserver`, context-loss events, texture/AnimatedSprite behavior, teardown-with-texture-destroy, Vite lazy chunks, and Playwright screencast. Lifecycle §8 addresses startup state machine, resize/density clamps, fixed-step private clock (1000/30ms, max 2 catch-up, `autoUpdate=false`), context loss (one bounded reinit after verified snapshot, second loss pins DOM static), and a 7-step ordered teardown with StrictMode idempotence counts (exactly one canvas/ticker/observer/listener set). SSR/PWA §11: client-only lazy chunk, eager shell must not import Pixi, hashed same-origin assets only, offline-before-cache selects DOM/M1, protected data stays `no-store`. Direct-imperative and DOM-only alternatives are explicitly evaluated and rejected with reasons, and a blocking `@pixi/react` defect stops for reviewed correction rather than silent architecture switch (§4.2). **Feasible at design level; the design itself mandates prototype proof of the unprovable-here claims (StrictMode/Canvas parity/bundle) — correct epistemics.**
4. **DOM mirror cannot silently diverge**: one pure `LivingOfficeFrameModel` is the only input to both Pixi and DOM (§5); no second projection; every frame carries `frameKey` + exact entity IDs; the mirror asserts same `frameKey`/entity set, and a mismatch selects DOM static with a redacted `RENDERER_SEMANTIC_DIVERGENCE` diagnostic (sprite §13); Pixi's opt-in accessibility overlay is explicitly not imported (would create a second divergence surface, §5); canvas is `aria-hidden`; the mirror is the complete keyboard/screen-reader/reduced-motion/static/forced-colors/mobile surface (§9-10, sprite §14). Parity is also a pure-test obligation (sprite §15.1). **Enforced by construction + gate, not by intention.**
5. **Actor and Channy cycles**: sprite §5.2 requires exact animation IDs with minimum frames/durations/loop gates for idle/walk/sit/type/review/carry-document/return-result/coffee/rest/lounge (plus static frames for all remaining operational states); §7 gives Channy roam/sit/eat/drink/sleep/play plus structured reflectors, `48x40` geometry, and a zero-authority contract (`roleInstanceId: none`, no subscriptions/commands/evidence). **Complete against the brief's list.**
6. **Original-asset boundary**: atlas contract (sprite §3) — at most four texture sources with fixed maxima; code-native indexed-palette sources; manifest requires ownership `ORIGINAL_PROJECT_AUTHORED`, license id/path, source + generated-RGBA hashes, `containsExternalReference:false`, script/font flags false; byte-equal regeneration rule; any hash/license/geometry/external-reference failure selects DOM static/M1; protected-style imitation banned (sprite §2, inherited from decision 59); replacement requires atomic hash/license update + separate review and cannot change identity/authority. **Sufficient.**
7. **Movement only from accepted evidence**: sprite §8 maps all 16 existing cue kinds without changing eligibility/source/precedence/duration; routes computed only after the existing projector resolves endpoints (§6.3); §9-10 restate the 3/1/1 caps, coalescing, overflow-to-log, snapshot/selection/camera no-enqueue, duplicate-revision conflict, stale/critical cancellation without replay; ambient bursts bounded 600ms + ≥8s static, browser-local, never mission history; deterministic clock excludes `Date.now()`/random (§11). **No invented activity path exists in the design.**
8. **Camera presentation-only**: camera state is browser-local `{centerX, centerY, zoom, mode, target}` and never written to events/projections (§7); zoom clamped to tested steps; camera/selection/proximity cannot create authority, change z-order identity, or infer location (§12, sprite §12); live region announces only selection/view mode. **Exact.**
9. **Thirteen scenes direct and reproducible**: matrix rows `PIXEL-V01..V13` each pin viewport/DPR, an exact named fixture, an exact scripted-clock millisecond, expected visible content, and required proof (§14); five delivery PNGs map to V01/V02/V06/V07/V12 exactly as decision 59 requires; the 26-second timeline covers all ten Founder scene classes (full office 0-2s, camera 2-4s + 24-26s, identity 4-6s, walking 6-8s, Channy roam 8-10s, handoff 10-13s, typing 13-15s, reviewer 15-17s, lounge 17-19s, Channy eat/sleep 19-22s, panels 24-26s) with WAITING_LEO->BLOCKED beyond them; every fact labelled `SYNTHETIC PROTOTYPE`. **Sufficient to demonstrate the Founder intent.**
10. **Media provenance enforced**: the WebM must be an actual continuous Playwright screencast of the running loopback prototype (recording spec + exact command + non-reused preview + fixed viewport/locale/timezone/theme); ffprobe-verified 20.000-30.000s; MP4 and GIF are exact ffmpeg conversions **of that WebM** (commands pinned; GIF = contiguous 6.0-13.0s segment, which the timeline proves contains walking + Channy roaming + Advisor handoff); the eight filenames/paths are exact; per-file pointer fields require absolutePath/size/sha256/scenario/commit/captureCommand/conversionCommand/duration/viewport/logicalTime with hashes computed only after handles close; still-assembly/hover/other-session substitution is expressly forbidden. **Enforced.**
11. **ffmpeg/ffprobe absence fail-closed**: my own PATH check reproduces the absence (including gifski/convert/magick). Entry condition 8 requires exact local executables; design `PASS` explicitly does not authorize system/npm installation; capture/conversion preflight failure stops **before** dependency installation or source edits and returns a prerequisite blocker; missing libx264/palettegen/paletteuse likewise stops rather than switching codecs. **No hidden substitute or installation assumption.**
12. **Gates measurable**: renderer §13 is a numeric hard-gate table (eager ≤12KiB gzip, lazy chunk ≤300KiB, atlas ≤256KiB, cached start ≤500ms p95, cold throttled ≤1500ms p95, frame/projection ≤8ms p95, camera ≤50/100ms, long tasks 0, objects ≤1600, textures ≤4 / ≤32MiB, heap ≤25MiB, retained artifacts 0); PWU-09 prescribes sample counts (600 frames after 60 warm-up, 100 camera cycles, 20+20 heap cycles with GC), fresh-build zero-marker gate, ignored/untracked proof, and honest miss handling (tier downgrade, never budget rewriting or semantic removal). **Measurable, not aspirational.**
13. **PASS opens only the prototype**: stated independently in the renderer design §15, plan §1/§2/§10, master-design pointer, FEATURE_INDEX rows, and the Founder-gate section (silence/chat/Fable5 PASS/Advisor validation/test success explicitly cannot unblock PWU-12; only the durable `APPROVE_LIVING_PIXEL_OFFICE_VISUAL_DIRECTION` for the exact prototype). Plan §8 STOP conditions include prototype-without-PASS and integration-without-approval. **Unambiguous; process self-consistency (declared order vs WorkUnit map vs sequence chart) checked — no contradiction.**
14. **No hidden change**: the delta is five Markdown files only (my name-only diff); no runtime/package/test/asset/auth/authority/transport/DB/network path; the master-design/FEATURE_INDEX patches are visible supersession + honest statuses (design-only, packages absent, converter unavailable, prototype not authorized) matching the actual 58->59 decision history; prototype file surfaces deliberately exclude production runtime files and require that production entry/composition/Dashboard never import the prototype graph. **Clean.**

## Findings (classified per the handoff vocabulary)

**No `DESIGN_DEFECT`, `PRODUCT_INTENT_DEFECT`, `SECURITY_OR_AUTHORITY_DEFECT`, `ACCESSIBILITY_DEFECT`, or `EVIDENCE_GAP`.**

1. `[EXECUTION_PREREQUISITE]` **Media toolchain**: `ffmpeg`/`ffprobe` (with `libx264`/`palettegen`/`paletteuse` capability) are absent on the host (reproduced). The design handles this fail-closed with no substitute and no installation authority; a future exact handoff must supply/authorize an approved local converter before `AO12-PWU-09`. Confirmed sufficient at design level.
2. `[EXECUTION_PREREQUISITE]` **Prototype preflight set**: artifact-root ignore rule (`/artifacts/m1-2-visual-prototype/` — currently not ignored, reproduced), Playwright screencast start/stop capability proof, free loopback port, and configured Chromium availability are all entry-gated with stop-on-failure. Confirmed specified.
3. `[INFORMATIONAL]` **Dependency pins**: `@pixi/react@8.0.5` / `pixi.js@8.19.0` are registry-evidence candidates that cannot be verified offline in this review (packages absent by design); the plan re-validates them at install time with stop-on-failure peer/license/audit/type/build gates and forbids opportunistic version changes. Adequate.
4. `[INFORMATIONAL]` **Honest unprovables**: the design itself states that support claims do not prove this repository's StrictMode teardown, Canvas parity, bundle budget, context-loss behavior, or deterministic baselines, and routes each to mandatory prototype proof — the correct design-level treatment.

## Verdict rationale

Every load-bearing claim was verified against the actual documents, the actual repository state (versions, absences, ignore status reproduced by my own commands), the frozen predecessor contracts, and the decision record. The design inherits the reviewed truth/authority layers by consumption rather than reinterpretation, adds enforcement mechanisms (frameKey parity, atlas hash/license gates, fail-closed tool preflights, numeric budgets) rather than aspirations, and multiply locks the gate so a design `PASS` cannot leak authority beyond `AO12-PWU-07..10`. Per V2 and the review-classification discipline (no implementation blocker, no undecidable value, no process self-contradiction): **PASS** (clean).

## Self-review

All claims trace to my command outputs, the exact committed documents at `9611d0d`, or pinned predecessor contracts; Worker/Advisor summaries were distrusted and re-derived; repo facts cited by the design were independently reproduced. Read-only for Agent Office (clean at `9611d0d` = origin; nothing run that mutates it); no dependency installed, no tool fetched, no media created; no new agent/sub-agent/session/delegated context; no prototype/integration/final approval granted; only this result + pointer written to Foundation Docs.

Return to: **Advisor**.
