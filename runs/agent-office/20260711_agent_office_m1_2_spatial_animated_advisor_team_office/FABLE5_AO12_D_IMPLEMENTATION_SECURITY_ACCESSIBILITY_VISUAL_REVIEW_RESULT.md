# FABLE5 AO12-D REVIEW — Implementation, Security, Accessibility, and Visual (Final Batch)

- Mission: `AGENT_OFFICE_M1_2_SPATIAL_ANIMATED_ADVISOR_TEAM_OFFICE` (AO12-D, `AO12-IWU-12..14` + correction `AO12-D-A1`) · Actor: **Fable5 Reviewer** (`reviewer-fable5`) · Level 3 · Skill: `/fable-sentinel` · Date: 2026-07-12
- Target: cumulative `f9d0533 -> 48c8dbd9f2c5ecea68c28e85137d75db595ef5f9` (= `origin/shadow/agent-office-m1-2-spatial-office`; graph exactly `f9d0533 -> da5ecc9 -> 48c8dbd`); cumulative 36 files +2758/-170 (= 30 AO12-D paths ∪ 11 A1 paths, 5 overlapping); A1 delta = exactly the 11 declared paths.
- **VERDICT: `PASS`** — closes the AO12-D review gate only. No final Leo/GPT approval is granted; no next mission is authorized.

## Independent evidence (my own executions at 48c8dbd)

- Full Vitest `npm test -- --run`: **77 files / 452 tests, exit 0** (includes the 13 runtime-composition lifecycle tests, redaction, contract, compatibility, surface, rollback, authenticated performance budget, and the A1 fresh-bundle acceptance gate).
- Default-demo Playwright: **43/43 pass (1.1m)**. Composed authenticated Playwright: **3/3 pass** (the 3 cases assert all 7 AO12-D baselines plus the 3 M1 composed baselines, byte-exact).
- `npm run build` pass; **my own fresh `dist/dashboard` scan: all six fixture markers = 0**; emitted bundle sha256 `50787269a05d02c4ea78ac74332535c6a9d495c2b5cd1f332c980f6a4f7ede86` = exactly the Worker-declared hash.
- Naming gate: **passed, 273 files**. `npm run audit:dependencies`: 0 vulnerabilities. `package.json`/`package-lock.json` sha256 = base `f9d0533` exactly.
- Baselines: **26 total PNGs; all 26 blob-identical between `da5ecc9` and `48c8dbd` (A1 changed no image); all 19 pre-AO12-D baselines blob-identical to base `f9d0533`; all 7 AO12-D worktree blobs = HEAD** (per-file `git hash-object` vs `rev-parse`).
- Boundary scans: authenticated builder + selector have no node:/child_process/net/http/gateway/tmux import and no fetch/spawn/write/storage/WebSocket call (my grep + the in-repo capability tests).
- Cleanup verified: no listener on 4173/4317, no chromium/vite process, worktree restored clean at 48c8dbd (my own `test-results-composed/` output removed).
- **Disclosure (process honesty):** my first background demo-suite invocation ran concurrently with the full Vitest suite, a Vite build, and image-crop browser launches on the same host; it exited 0 but printed a truncated log ending `4 passed (9.5m)`, which is not a valid record of the 43-case suite. I discarded that invocation as non-evidentiary, confirmed the definition (`--list` = 43 tests in 8 files), and reran cleanly: **43/43 in 1.1m**. The authoritative demo-suite evidence is the clean rerun; no failure was ever reported by either invocation.

## The 15 mandatory items

1. **Scope/authority**: cumulative delta = brief allowlist exactly; explicit-path check shows `src/server/auth/`, `src/server/sse/`, `src/adapters/` (gateways/transport), `src/runtime/composition.ts`, and `src/runtime/operational-config.ts` untouched. `src/server/application.ts` gains only the additive optional `spatialOffice` type field (+2 lines). No product or authority expansion.
2. **Validated inputs, no leakage**: `authenticated-projection.ts` (863 lines, read directly) enforces closed schemas — unknown-key rejection at wrapper/slice/fact/activity levels, UUIDv7 on every source ID, canonical UTC on `evaluatedAt`/`activityEffectiveFrom`/**`optionalExpiresAt`** (assert + expiry-after-effective sanity). The redaction test proves forbidden locator/credential/transport/prose fields absent from the serialized presentation, that polluted cue slices are **rejected (throw), not ignored**, and that the builder source has no auth/gateway/transport capability. The server-side runtime projection folds open-alert severity to the pod input and, on any construction defect, omits only the additive field while preserving M1 `sceneRoles`.
3. **Single Advisor / fail-closed / no clone / naming**: the authenticated path feeds the unchanged AO12-C projector/reducer (per-instance Advisor equality gates, suppress-never-clone), missing/multiple assignment renders `UNASSIGNED`/conflict static; naming gate 273 files (my run); no-clone asserted in surface tests and visible in the images (single registered Advisor, no duplicates).
4. **No invented/replayed cue**: client origin mapping read directly — `start` = INITIAL_SNAPSHOT, SSE `projection` event = LIVE_DELTA (revision must equal event id), `reset_required` = CURSOR_RESET_SNAPSHOT, reconnect = RELOAD_SNAPSHOT; `fullSnapshotVerified` for all non-live origins after strict parse; the AO12-C reducer (unchanged, previously reviewed line-by-line) marks snapshot IDs seen and enqueues zero; only strictly-newer accepted LIVE_DELTA enqueues.
5. **Degradation preserves truth**: `compatibility.ts` selector (read directly) — reduced-motion/requested-static/critical-alert/source-degraded → `STATIC` with exact reason codes; invalid/unknown/absent schema → `M1_FIXED_STATIONS`; the client wraps everything in a catch → M1 fallback + cue-state reset (`SPATIAL_SCHEMA_INVALID_M1_FALLBACK`). All facts remain rendered in the images.
6. **Session lifecycle via the real server path**: `clearSession` and `stop` reset session, cursor, and the private cue reducer state. The runtime-composition test runs both `REVOKE` and `EXPIRE` (10ms server heartbeat, real session lifetime) against the composed loopback server and asserts SESSION_EXPIRED, mutation port removed, `projection` and `spatial` undefined in the client snapshot, and **server-side SSE `connectionCount() === 0`** — the server heartbeat/session path, not an assumed local expiry. `session_revoked` is emitted by the pre-existing (untouched) M1 SSE module. Passed in my 77/452 run.
7. **Boundaries unchanged**: item-1 path proof + package/lock equality + audit 0 + no DB/network/deployment path in the delta + post-run listener/process checks clean.
8. **A1 truly closed (before/after reproduced)**: at `da5ecc9` the production component contained the fixture import (2 references) and `projection?:` optional; at `48c8dbd` both are 0. The component now requires `projection` and uses a discriminated union — `surfaceKind: 'AUTHENTICATED'` carries `fixtureKind?: never` (synthetic masquerade is a type error), `surfaceKind: 'SYNTHETIC'` requires an explicit `fixtureKind` + projection; the demo entry injects both fixtures explicitly (asserted by the acceptance test, including exactly two `surfaceKind="SYNTHETIC"` occurrences). My fresh build contains none of the six markers.
9. **Fresh-build gate cannot be fooled**: the acceptance test builds via Vite's API using the repository's own `vite.config.ts` in production mode — the config's virtual entry resolves to `src/ui/runtime/entry.tsx` (the real production entry; the demo entry exists only under the synthetic-demo mode) — into an isolated `mkdtemp` directory (never a pre-existing `dist`), asserts the emitted JS file set is non-empty, scans every emitted `.js` recursively, and removes the directory in `finally`. Stale output, wrong entry, and empty-output passes are structurally excluded.
10. **Fallback chain presentation-only**: FULL → RESTRAINED → STATIC → M1_FIXED_STATIONS; the selector is a pure function of the validated candidate + media/requested inputs; `runtime-app` simply omits the `authenticatedSpatial` prop for M1 (Dashboard renders the unchanged `OfficeScene`); no event append, ledger/manifest write, config/auth/session mutation, or process action exists in the layer (boundary scans + rollback tests).
11. **Accessibility/responsive**: my reruns cover axe/keyboard/focus/44px/live-region gates (vitest a11y suites + composed reduced-motion/accessibility case + demo suite); the seven images visually confirm containment at desktop/tablet/mobile/320/200%, forced-colors focus + non-color meaning, and reduced-motion static equivalence with facts intact.
12. **Seven AO12-D PNGs directly inspected** (worktree blob = HEAD verified per file first; composed captures are viewport-region images by spec):
    1. *desktop 1440x900*: authenticated M01 shell (읽기 전용, LOOPBACK_PRIVATE/LOCAL_BOOTSTRAP chips, Logout/PWA recovery) wrapping the AO12-D section; honest `알 수 없음 STRUCTURED_ACTIVITY_EVENT_MISSING` banner; `MODE FULL / SPATIAL_FULL_SELECTED`; explicit presentation controls; the **actual composed registration** (one team `UNKNOWN / CURRENT`, one Advisor) — no invented cue, actor, or synthetic control. **Acceptable.**
    2. *tablet 1024x768*: chips wrap to two rows; focused team card ring visible; all facts contained. **Acceptable.**
    3. *320x720*: single-column stack; banner wraps mid-token; no horizontal loss. **Acceptable.**
    4. *mobile 390x844*: clean two-per-row chips; full metadata; no loss. **Acceptable.**
    5. *200% text 390x844*: long tokens wrap mid-token (`LOCAL_BOOTSTRAP_AUTHENT/ICATED`, `STRUCTURED_ACTIVI/TY_EVENT_MISSING`); controls grow; no clipping. **Acceptable.**
    6. *forced-colors 1440x900*: black/white/system outlines; focus ring on the team card; status text/shapes legible without color. **Acceptable.**
    7. *reduced-motion 1440x900*: `MODE STATIC / SPATIAL_FULL_SELECTED` and `STATIC / ACCEPTED LIVE_DELTA ONLY` badge — motion demoted, facts identical. **Acceptable.**
    Plus: **all 26 baseline bytes confirmed unchanged by A1** (26/26 blob-equal `da5ecc9` = `48c8dbd`).
13. **Reproductions recorded**: commands and counts above (vitest 77/452; demo 43/43 clean rerun; composed 3/3; build + six-marker scan 0; naming 273; audit 0; authenticated reducer p95 budget inside the vitest run; composed browser gates inside the composed run; boundary scans; cleanup checks).
14. **Canonical docs accurate**: the as-built additions (contract §2.4 authenticated timestamp/correspondence gate, A1 boundary statement, measured values, `PENDING_INDEPENDENT_REVIEW_AND_ADVISOR_ACCEPTANCE` statuses) match the code and evidence I verified; normative language is preserved. **No divergence found — no defect classification needed.** Formally: the `DEFERRED_WITH_GATE` note from my AO12-C review (expiry-format validation is upstream-owned) **is closed by AO12-D**: the authenticated slice boundary now asserts canonical UTC on `optionalExpiresAt` (plus expiry-after-effective), and malformed input rejects the whole spatial candidate to M1.
15. **M1/authority intact**: M1 demo + composed baselines byte-equal and their suites green in my reruns; final approval remains Leo/GPT; no browser dispatch capability (scans); loopback-only servers with zero residual listeners; no automatic next mission (docs and result statuses explicit).

## Findings

**None blocking.**

- `[INFO]` AO12-C review's `DEFERRED_WITH_GATE` (validated-slice expiry) is verified **CLOSED** by the AO12-D authenticated boundary (see item 14).
- `[INFO]` Reviewer-process disclosure: one saturated concurrent demo-suite invocation produced a truncated non-evidentiary log (`4 passed (9.5m)`, exit 0) and was discarded; the clean rerun (43/43, 1.1m) is the evidence of record. Not a product defect; recorded for run-history honesty.
- `[INFO]` The composed AO12-D screenshots are viewport-region captures (per spec), not full-page; my per-image findings apply to the asserted regions, which include every claimed fact surface.

## Verdict rationale
The authenticated integration consumes only strictly validated accepted inputs and leaks nothing; the A1 production-bundle boundary is closed and empirically reproduced before/after with a structurally sound fresh-build gate; session end (logout/expiry/revocation/refresh-failure/stop) clears protected and private cue state against the real server SSE/heartbeat path; degradation is deterministic, reason-coded, presentation-only, and M1-preserving; all seven images directly inspected and acceptable; all 26 baselines and every boundary byte-verified; every hard gate reproduced by my own runs. Per V2: **PASS**.

## Self-review
All claims trace to my command outputs, direct diff/file reads at pinned commits, or directly inspected images; Worker/Advisor summaries were distrusted and re-derived; the one anomalous invocation was discarded and disclosed, not silently absorbed. Agent Office remained read-only (worktree restored clean at 48c8dbd; no patch, no commit/push there); no DB/secret/auth-material access; no persistent server/listener/browser process remains; no new agent/sub-agent/session/delegated context; no final approval granted; no next mission started; only this result + pointer written to Foundation Docs.

Return to: **Advisor**.
