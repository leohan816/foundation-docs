# FABLE5 REVIEW — Pixi Public-Export Compatibility Bridge Design Delta

- Mission: `AGENT_OFFICE_M1_2_SPATIAL_ANIMATED_ADVISOR_TEAM_OFFICE` · Pass: **DESIGN_REVIEW__PIXI_PUBLIC_EXPORT_COMPATIBILITY_BRIDGE_DELTA** · Actor: **Fable5 Reviewer** (`reviewer-fable5`) · Level 3 · Skill: `/fable-sentinel` · Date: 2026-07-12
- Target: `9611d0d -> 56385b894ff18fd65a4e59f2c4e75f73ea2a56d9` = origin; parent = base exactly; **exactly 3 canonical Markdown paths** (+625/-12; 1 added, 2 modified); all three doc sha256 recomputed = Worker declarations; prepared prototype worktree byte-preserved (61 porcelain entries, untouched by this review).
- **VERDICT: `PASS`** (clean) — opens only a new exact prototype-implementation correction handoff from Advisor. It does not approve the implementation, the prototype visual direction, full authenticated integration, production, or M1.2 final closure. `IMPLEMENTATION_STATUS` remains `PAUSED` until that handoff.

## Decisive first-hand evidence (my own probes, scratchpad-only, repository unmodified)

1. **The problem is real and the count is exact**: compiling a one-line import of the `@pixi/react` vendor declaration graph under the repository's exact compiler (installed TypeScript `6.0.3`, `strict`, `exactOptionalPropertyTypes`, `noUncheckedIndexedAccess`, **`skipLibCheck: false`**, NodeNext, React 19 types) produces **exactly 52 `error TS` diagnostics** — WebGPU/DOM `GPU*` identifier conflicts with `lib.dom.d.ts`, `subgroupMin/MaxSize` modifier conflicts, and exact-optional bitmap-font type failures — precisely the classes the design describes.
2. **The solution is feasible as written**: the normative declaration skeleton (§5 of the bridge doc, transcribed verbatim) plus a consumer file exercising the entire exported surface — all 11 `PixelApplication` props with the ref port, container ref `position.set`/`scale.set`, the full 9-method chained graphics draw with both `fill` overloads, `createPixelTexture` + `source.scaleMode` + `destroy`, `usePixelTick` with `deltaMS`, and the frozen literal-typed runtime record — compiles with **zero diagnostics** under the same exact compiler settings (`tsc --noEmit` exit 0, repo TypeScript 6.0.3, `@types/react` 19.2.17).
3. **Bare-Node limitation reproduced**: `import('@pixi/react')` under the bare Node ESM loader fails with `ERR_MODULE_NOT_FOUND` on the extensionless internal `react-reconciler/constants` specifier — confirming the design's refusal to treat bare-Node execution as the runtime gate (and its refusal to "fix" that with a deep import).
4. **Public-root surface verified from the installed packages**: `@pixi/react` root exports include `Application`, `extend`, `useTick` (export map `.` -> `lib/index.mjs` — the **same file** the prepared deep imports load, so wrapper behavior preservation is byte-grounded); `pixi.js` root exports `Container`, `Graphics`, `Sprite`, `Texture`, `VERSION`, and the CJS root loads in Node with constructors as functions, `Texture.from` a function, and `VERSION === '8.19.0'`. Installed manifests and lockfile pin `@pixi/react@8.0.5` / `pixi.js@8.19.0`; installed TypeScript is `6.0.3`.
5. **Defect inventory independently recounted**: exactly **6 `@ts-expect-error`** directives paired with **6 relative `node_modules` imports** across exactly the four named files (facility-sprites 2+2, pixel-world-scene 2+2, renderer-boundary 1+1, world-clock 1+1); zero `@ts-ignore`; no other prototype/test file has a deep import or suppression — the 4-file cleanup allowlist is complete.

## The 20 mandatory questions

1. **Option A preserved exactly**: every decision-72 constraint appears as a binding invariant (TS 6.0.3, global `skipLibCheck:false`, exact pins, root-only imports, deep-import/suppression prohibition, prototype-only + `DEFERRED_WITH_GATE`, same-Reviewer gate); the delta grants no authority itself — every mutation is gated on this clean PASS plus a new exact Advisor handoff. No authority added or removed.
2. **Toolchain/package invariants preserved**: verified in the prepared `package.json`/lock (pins exact), `tsconfig.json` (`skipLibCheck:false`, `exactOptionalPropertyTypes`, `noUncheckedIndexedAccess`), installed TS 6.0.3; the committed tree still has the original three runtime dependencies (the delta touched docs only).
3. **Single executable importer**: §4 topology — only `pixi-public-export-bridge.js` may import the literal `@pixi/react`/`pixi.js`/`react` roots; all prototype TS/TSX imports the local bridge; regression 1 enforces it.
4. **Declaration free of escape hatches**: the skeleton imports type-only from `react` and `./contracts.js` only; no vendor type, `any`, wildcard module, module/global augmentation, index signature, deep path, or suppression — verified by direct inspection and by my clean strict compile of the skeleton itself.
5. **Bounded inventory = actual use, exactly**: my greps of the prepared consumers — graphics calls used are exactly the 9 declared chainable methods (`cacheAsTexture`/`updateCacheTexture` used 0 times; correctly excluded); `AnimatedSprite` used 0 times (correctly excluded); Application props passed are exactly the 11 declared; ref usage is exactly `getApplication`/`getCanvas`; container ref is `position.set`/`scale.set`; sprite props are exactly `{roundPixels, texture}` with no ref; texture use is `Texture.from(canvas)` + `source.scaleMode` + `destroy`; tick use is `deltaMS` + enable flag. Neither a missing required call nor speculative surface.
6. **Skeleton coherent and sufficient**: proven by the zero-diagnostic compile of skeleton + full-surface consumer under the exact repo strictness and React 19 types (probe 2 above).
7. **Wrappers preserve behavior through public roots**: the public root resolves to the same `lib/index.mjs` the running prototype already loaded via deep imports, so the wrapped values are identical bytes; registration is centralized (`extend` once with exactly Container/Graphics/Sprite); prop/ref shapes match prepared usage 1:1 (item 5).
8. **Export existence/package identity independently supported**: probe 4 above (export maps, root module export statements, CJS-root runtime values, manifest + lockfile pins).
9. **Bare-Node correctly rejected as the gate**: reproduced (probe 3); the design routes runtime proof to the actual Vite/browser boundary and explicitly forbids accommodating the Node loader with a deep import; the contract test may inspect manifests/source in Node but must not execute the root there.
10. **Runtime checks exact**: enumerated init checks (component non-null; `extend`/`useTick`/`createElement`/`forwardRef` functions; three constructors; `Texture.from` function; `VERSION === '8.19.0'`; "no truthiness-only namespace check") plus call-boundary validation of canvas/ticker fields/renderer name/ref methods/`deltaMS` finiteness — sufficient to reject missing/malformed exports, lifecycle ports, texture/ticker calls, and wrong versions.
11. **Fail-closed before stale canvas, no global handler**: bridge init throws a bridge-specific error before the canvas becomes authoritative; lazy-chunk rejection keeps DOM-static/M1; later failures are caught by the existing renderer boundary; if fallback propagation cannot be proven, implementation stops rather than adding a global error handler or widening the API.
12. **StrictMode/ownership/teardown preserved**: ownership table keeps every resource with its existing owner (application/canvas, private ticker, refs, deferred `destroy(true)` texture, listener registry) and regression 7 re-requires StrictMode symmetry and zero retained resources.
13. **Contract tests non-self-authenticating**: the frozen expected-version record must be matched against both the installed manifest and the lockfile; root-only imports, exact declaration manifest, runtime identity in the Vite browser build, no-escape-hatch scans, and compiler invariants are separate gates (regressions 1-5).
14. **`batch-gates.test.ts` correction exact and non-weakening**: the current committed assertion is an exact `toEqual` object of the three pinned runtime dependencies (read at lines 78-82); the proposed change replaces it with the exact five pinned dependencies — same exact-equality form, stricter coverage. (In the preserved prepared worktree this test is currently red against the prepared 5-dependency `package.json`; that is precisely the known, gated one-file correction — not a hidden failure.)
15. **Allowlist complete and minimal**: my repo-wide scans confirm the 6 pairs live only in the four named files; the three new files plus `batch-gates.test.ts` are the only additions; nothing else asserts the dependency list; `demo-entry.tsx` needs no change (no Pixi import — verified).
16. **Nothing changes until authority**: the committed delta is 3 docs; prepared prototype/media/baselines/package/lock remain uncommitted, byte-preserved worktree state; my review left it untouched (all probes in the session scratchpad; `tsc --noEmit`; read-only Node requires).
17. **Prototype-only + `DEFERRED_WITH_GATE`**: stated in the bridge status line, §9 promotion gates (six conditions including explicit Leo/GPT authority and a new mission), §10 STOP conditions, and both patched companions.
18. **Boundaries unchanged**: docs-only delta (verified path set); the design adds no auth/authority/delivery/transport/DB/secret/public/remote/production/Hermes surface and forbids them in STOP conditions; next-mission remains unauthorized.
19. **Commit scope/upstream/worktree**: HEAD = target = origin; parent = base; exactly the three documentation paths; prepared worktree preserved (61 porcelain entries with untracked expansion, consistent with the Worker's 69-entry dirty/untracked/ignored manifest).
20. **Implementable without invention**: binding normative skeleton (names fixed), enumerated init/call checks, exact failure policy, exact allowlist, exact one-file test change, enumerated regressions and promotion gates — the implementing Worker decides no type, runtime, failure, or promotion policy. My probe compiled the skeleton and a full consumer exactly as an implementer would, with zero gaps.

## Findings

**No `DESIGN_DEFECT`, `TYPE_CONTRACT_DEFECT`, `RUNTIME_COMPATIBILITY_DEFECT`, `FAIL_CLOSED_OR_LIFECYCLE_DEFECT`, `TEST_GATE_DEFECT`, `SCOPE_OR_AUTHORITY_DEFECT`, or `DOCUMENTATION_STALE`.**

- `[INFORMATIONAL]` The 52-diagnostic vendor incompatibility and the zero-diagnostic bounded-declaration solution were both reproduced first-hand under the exact repository compiler (this review's scratchpad probes); the design's necessity and feasibility are empirical, not asserted.
- `[INFORMATIONAL]` `tests/acceptance/batch-gates.test.ts` is currently red **in the preserved prepared worktree only** (prepared 5-dependency `package.json` vs committed 3-dependency assertion); this is the documented, PASS-gated one-file correction, and the committed tree is self-consistent.
- `[INFORMATIONAL]` Bare-Node ESM import failure of the `@pixi/react` root was reproduced (`ERR_MODULE_NOT_FOUND` on `react-reconciler/constants`), confirming the design's browser-boundary runtime-gate placement.
- `[DEFERRED_WITH_GATE]` Full authenticated/production use of the bridge — already the design's own classification, with six explicit promotion conditions; confirmed correctly gated.

## Verdict rationale

Every load-bearing claim was re-derived from the actual commit, the actual prepared sources, the actual installed packages/lockfile/compiler, and reproducible probes: the vendor problem reproduces at exactly 52 diagnostics; the bounded declaration compiles cleanly with the full consumer surface; the public roots provably export every required value and resolve to the same module bytes the running prototype already used; the inventory matches prepared usage with nothing missing and nothing speculative; the one-file test correction is exact and stricter; scope, pause, and promotion gates are multiply locked. Per V2: **PASS** (clean).

## Self-review

All claims trace to my command outputs, direct file reads at the pinned commits, or scratchpad-only compiler/runtime probes; Worker/Advisor conclusions were distrusted and re-derived; the repository (including the byte-preserved prepared prototype worktree) was not modified — no file written, no package installed, no server started, no media touched, no snapshot updated. No new agent/sub-agent/session/delegated context; no implementation, patch, or approval; only this result + pointer written to Foundation Docs.

Return to: **Advisor**.
