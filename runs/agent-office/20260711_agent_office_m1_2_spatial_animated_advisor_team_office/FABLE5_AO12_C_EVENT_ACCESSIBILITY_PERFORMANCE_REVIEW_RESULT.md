# FABLE5 AO12-C REVIEW — Event Truth, Accessibility, and Measured Performance

- Mission: `AGENT_OFFICE_M1_2_SPATIAL_ANIMATED_ADVISOR_TEAM_OFFICE` (AO12-C, `AO12-IWU-09..11`) · Actor: **Fable5 Reviewer** (`reviewer-fable5`) · Level 3 · Skill: `/fable-sentinel` (SKILL.md + delta-review reference loaded this session) · Date: 2026-07-11
- Target: `4b751c6a -> f9d0533437c0cf9efa7be76650ad79f0cb0d9353` = `origin/shadow/agent-office-m1-2-spatial-office`; parent = base exactly; 30 files, +4035/-106; worktree clean (verified before and after my runs; my own `test-results-composed/` output was removed to restore the pre-review state).
- **VERDICT: `PASS`** — closes the AO12-C review gate only. AO12-D remains `NOT_STARTED_NOT_AUTHORIZED`; this review grants no final approval.

## Independent evidence (all my own executions at f9d0533)

- Full Vitest `npm test -- --run`: **70 files / 418 tests, exit 0** (includes the four focused cue/route/Channy UI files and the performance budget file).
- Focused AO12-C Playwright (`spatial-office-motion.spec.ts` + `spatial-office-accessibility.spec.ts`): **15/15 pass**, all seven committed baselines byte-matched live rendering (`maxDiffPixelRatio: 0`).
- Full default-demo Playwright: **43/43 pass**. Composed authenticated Playwright: **3/3 pass**.
- `npm run lint` pass · `npm run typecheck` pass · `npm run build` pass · `npm run audit:dependencies`: 0 vulnerabilities · naming gate: **passed, 256 files scanned**.
- **Benchmark reproduced first-hand** (browser, configured Chromium, my run): active-frame p95 **0.40ms** (gate 8) · long tasks **0** (gate: none >50ms in 10s) · desktop pod-switch p95 **54.1ms** (gate 100) · 4x-CPU-throttled p95 **53.6ms** (gate 200) · DOM **599** (gate 1600) · SVG **180** (gate 800) · CDP GC-bounded retained-heap growth after 20 selection cycles **196,604 bytes** (gate 25MiB) · post-navigation teardown: 0 cue/pose nodes, 0 animations. Fixture descriptor sha256 independently recomputed = `6fc7fe9aee1811575c99b4c6b9972626053f26e645827b16e77c2363b4f201cf` (exact match to Worker/canonical docs).
- `dist/dashboard` production bundle scan: **0 hits** for `spatial-cue.v1` / `spatial-motion` / `MOTION_SPATIAL` / `cueId` / `SpatialCueEnvelope` / `surface=spatial` / benchmark markers.
- `package.json`/`package-lock.json` sha256 **equal to base**; all 6 M1, 3 composed-M1, and 6 AO12-B baselines **blob-equal to base** (git hash-object vs `4b751c6a:<path>` per file).
- Source-boundary scan of all spatial UI sources: no adapter/gateway/runtime/server/observation/transport/persistence/node:/child_process/net/http/fs import; no fetch/WebSocket/EventSource/storage/sendBeacon call (my grep + the in-repo boundary test).
- No persistent Vite/Playwright/Chromium process after my runs (process table checked).

## The 16 required coverage items

1. **Scope exact**: `git diff --name-status` = exactly the 30 brief-allowlisted paths (11 sources, 7 test files, 7 new PNGs under the two exact AO12-C baseline dirs, 5 canonical docs). No production entry (`main.tsx`/runtime-app/composition), package, lockfile, config, auth, transport, or M1-baseline path in the delta. No hidden AO12-D integration: production mount untouched and the bundle scan is clean.
2. **Cue creation requires accepted structured evidence**: `cue-projector.ts` gates every candidate on schema version + verified source authority + verified manifest, selected pod, `CURRENT`/`CONNECTED`, non-critical alert, **UUIDv7-shaped source IDs each present in the exact mission accepted-event set**, verified assignment (project/mission/WorkUnit letter-match), single responsible Advisor Team + single exact Advisor roleInstanceId, per-kind required-evidence kinds, and state/activity/observable correspondence (`stateSourceEventId` must be in `sourceEventIds`; `requiredObservableName` must equal the domain projection). Prose/terminal/process/proximity/timestamp-shaped extra properties are ignored (mapping test pollutes input and asserts identical output). Non-`LIVE_DELTA` origins can never enqueue (`NOT_LIVE_DELTA` returns candidateCueId for seen-marking only).
3. **Deterministic IDs, dedup, precedence, bounds, overflow, reset, cancellation, no replay**: `cueId = sha256(canonical-json([schema, project, mission, roleInstance, workUnit-or-null, kind, sorted sources]))` — byte-verified against Node `createHash` in tests, order-insensitive. Reducer: caps 3 pending / 1 route / 1 per actor / 1 per WorkUnit with same-entity coalescing to the precedence winner; precedence table matches the contract ordering (RECOVERY 110 … IDLE_RELOCATE 0) with mission-sequence then lexical source tie-breaks; excess eligible cues become `OVERFLOW_TO_LOG` entries and are marked seen; full snapshots mark all current IDs seen and enqueue zero; equal-revision + same fingerprint = idempotent no-op; equal-revision + different content = `PROJECTION_CONFLICT` (clear queue, `requiresFullSnapshot`); conflict state exits **only** via a verified full snapshot with revision >= applied, which also resets the seen sets to the snapshot's own IDs (§8.4-safe eviction); seen-set overflow (512 cue / 2048 source) fails closed to cleared cues + full-snapshot requirement (test-verified at limit+1). Selection, orientation change, and hidden tab clear active cues; the motion spec proves the real (non-frozen) selection and orientation paths clear with no replay and no focus movement.
4. **Assignment / Single Advisor Team failures suppress and never clone**: `ASSIGNMENT_UNKNOWN`/`ASSIGNMENT_CONFLICT`/`SOURCE_CONFLICT`, missing/duplicated Advisor team, ≠1 Advisor roleInstanceId, or actor/pod instance mismatch each suppress (ACTOR scope) — 18-variant fail-closed test includes duplicated identical instance IDs and wrong-instance cases. Rendering never duplicates a character to satisfy a view (routes/component test asserts no actor clone).
5. **Positive motion creates no claims**: on-surface labels negate claims ("Testing; no pass claim", "Verdict return; not approval", "Result return; not completion", "Patch return; not dispatch"); header states "no authority or transport effect"; inspector footer "No action, acknowledgement, dispatch, or authority control exists on this fixture surface."; contract §15 non-change confirmed (no browser dispatch path exists — boundary scans).
6. **Operational state overrides ambient**: `VerifiedIdleLounge` excludes any actor with an operational cue and returns `OPERATIONAL_OVERRIDE` for non-IDLE states; verified idle requires exact current/connected/verified `IDLE` with assignment verified, renders at most one actor, and prints the full no-availability/no-progress disclaimer (visible in every baseline).
7. **Channy non-operational reflector only**: fixed safe precedence implemented exactly (stale/offline static → BLOCKED → WAITING_LEO → completion → follow-accepted-route (FULL tier only, reuses the existing cue's ID, adds no route) → neutral ambient); pure props, no event subscription/control/command; caption `NON_ACTOR / NO_AUTHORITY / NO_ASSIGNMENT / NO_COMMAND` + "Primary status, alert, board, and activity log remain independent." — all on-surface in the baselines and unit-tested.
8. **Routes bounded, single-pod, cleaned, static-equivalent**: endpoint inventory is derived from the actually selected pod's rendered zones + its displayed actors (never from the cue's claim); missing/ambiguous/hidden endpoint or STATIC tier → immediate static origin→target timeline text; exactly one actor/document pair; Web Animations with **transform/opacity only**, `durationMs` bounded 150–1200 from reviewed constants, one iteration, onfinish + unmount cancel; no auto-scroll or focus movement (test-asserted). `control-recovery` is not in the rendered floor's inventory, so RECOVERY renders static-only on this surface (fail-safe direction, contract-permitted).
9. **All seven PNGs directly inspected** (worktree blob = HEAD blob verified per file before viewing; tall captures additionally re-viewed as native-scale crops — 3 regions mobile, 4 regions 200%):
   1. *full-desktop 1440x900* (1440x4015): three distinct legible operational cue panels with non-claim labels, exactly one route actor/document pair frozen mid-track, three structured log entries with zone routes and static codes, one verified-idle COFFEE entry with the full disclaimer, Channy as "Structured WAITING_LEO reflector", complete 13-field board + redaction; no clipping/overlap. **Acceptable.**
   2. *restrained-desktop* (1440x4000): exactly one cue panel (highest-precedence WAITING_LEO) while the log preserves all three full facts; MODE RESTRAINED on-surface. **Acceptable.**
   3. *reduced-static desktop* (1440x3967): zero route motion; static "advisor-anchor -> leo-decision-destination / DECISION_REQUEST_TEXT" equivalence; all three operational facts static; log intact. **Acceptable.**
   4. *tablet 1024x768* (1024x5199): both Team areas recognizable (FOUNDATION detail + VIBENEWS summary), three actor tiles contained, frozen route pair + full log, no clipping/overlap/page overflow. **Acceptable.**
   5. *mobile-static 390x844* (390x7190): one focused pod, no miniature route layer — static timeline text only; complete zone non-inference labels, board + redaction, log 3 full facts; long IDs wrap; no horizontal loss. **Acceptable.**
   6. *forced-colors 1440x900* (1440x4015): black/white/system borders; visible solid focus ring on the motion control; route-pair chips outlined; state text/shapes/log/links distinguishable without color; full parity. **Acceptable.**
   7. *200%-text 390x844* (390x14897): every heading/label/ID wraps (mid-token `overflow-wrap` on `advisor.foundatio…`, `DECISION_REQUEST_/TEXT`, `work:control.foundation.prima/ry`); static route equivalence and all three log facts legible; inspector complete; no fixed-height truncation or horizontal loss. **Acceptable.**
   No accepted-but-bad baseline, unreadable density, clipping, incoherent overlap, page overflow, focus loss, forced-color loss, or 200% composition defect in any image.
10. **Accessibility evidence**: my focused run re-executed axe WCAG 2.2 A/AA (0 violations), 44px on every visible button, roving focus + Enter selection with focus retention, independent `aria-live` region, `role="log"` on a wrapper with intact list semantics, reduced-motion `getAnimations()===0` with identical facts, forced-colors focus/state assertions, mobile/tablet/desktop/320px/short-landscape containment matrix, decorative-removal meaning survival, and loopback-only (`127.0.0.1:4173`) request gate.
11. **Reruns recorded**: commands and counts in "Independent evidence" above — full Vitest 70/418; focused Playwright 15/15; full demo 43/43; composed 3/3; lint/typecheck/build/audit/naming — sufficient to validate every Worker count I relied on (all matched).
12. **Performance methodology inspected + reproduced**: the vitest budget file builds the exact 12-pod/64-assignment/200-WorkUnit long-KO/EN 6-alert 3-burst fixture (12 pod controls asserted rendered), 20 warmups + 200 samples of the real project→initial→delta reduction (p95 gate ≤8ms), design-load DOM/SVG counts at the 12-pod render (1324/224 ≤1600/800), incremental production JS honestly 0 (not mounted) plus a 12,314-byte source-gzip diagnostic, and — honestly labeled — jsdom pod-switch and allocation numbers as *diagnostics only*, with the hard pod-switch/retained-heap gates delegated to the browser spec (CDP `collectGarbage` + `Runtime.getHeapUsage`, 4x CPU throttle profile, nearest-rank p95). I reproduced the browser gates with my own run (values above); every hard target passes with wide margin. `FULL` classification is configured-reference-only and says so.
13. **Production exclusion + default behavior**: `dist/dashboard` marker scan 0 hits (my build); motion spec test 1 proves `/` still mounts the M1 office-scene and `surface=spatial-static` still renders `SYNTHETIC_NON_OPERATIONAL_STATIC` with `data-motion-tier=STATIC` and zero motion cues; all six AO12-B baselines byte-equal base (mechanically matched in my static-spec rerun within the 43).
14. **SIASIU naming + no external additions**: naming gate 256 files pass (my run); package/lockfile byte-equal base; asset inventory untouched; identity doc reasserts no external image/font/asset; no new dependency.
15. **Git/publication scope**: branch `shadow/agent-office-m1-2-spatial-office`; HEAD = target = origin; parent = base; non-force push evidenced by upstream equality; worktree clean including untracked (restored after my own test output); Worker result/pointer at the declared foundation-docs paths; only result+pointer staged there.
16. **No forbidden change**: no DB/secret/auth/transport/remote/public/prod/live/browser-dispatch/persistent-server change — delta paths, boundary scans, bundle scan, and process checks above.

## Findings

**None blocking.** Non-blocking observations:

- `[DEFERRED_WITH_GATE]` **Expiry-format validation is upstream-owned.** The projector compares `optionalExpiresAt <= evaluatedAt` lexically — chronologically correct for the canonical UTC format the domain enforces at write time (`assertUtcTimestamp`: RFC3339-with-milliseconds + `toISOString()` round-trip). A malformed expiry string reaching the projector would fail-open on the expiry gate alone (all other gates still apply). Contract §2.1 defines the projector input as a *validated slice*, so this is not an AO12-C defect; the AO12-D wiring review must verify the production projection path feeds only domain-validated activities (the existing write-time assertion) or add slice-boundary validation there. Tests cover expiry-at-`evaluatedAt` (inclusive) but not malformed-format at the projector boundary.
- `[INFO]` `REVIEW_HANDOFF` always uses `result-desk` as the source zone (the contract row offers "Result desk/Advisor Hub"); a conforming subset.
- `[INFO]` The §9 ambient-burst budget (≤600ms + ≥8s static) is satisfied trivially: AO12-C implements **no** ambient/Channy animation object — the only Web Animation is the single route pair; `data-channy-motion="BOUNDED"` is attribute-only presentation.
- `[INFO]` RECOVERY renders static-only on this surface because `control-recovery` is not part of the rendered floor inventory — fail-safe, contract-permitted.

Canonical-doc delta reviewed line-by-line: as-built status/traceability/benchmark recording only; normative language preserved ("must continue to prove at least"); AO12-D prohibitions restated.

## Verdict rationale
Every load-bearing Worker claim was re-derived from the actual diff, sources, tests, images, and my own executions; the cue truth layer implements the frozen contract to the letter (16 kinds, envelope, deterministic ID, gates, suppression, precedence, budgets, dedup/reset/no-replay); presentation is bounded, cleaned up, and claim-free; all seven baselines directly inspected and acceptable; every hard performance target independently reproduced; production selection and all preserved baselines byte-equal base. Per V2: **PASS**.

## Self-review
All claims trace to my command outputs, the actual diff/files, or directly inspected images; Worker/Advisor summaries were cross-checked only, and each number I repeated was independently reproduced. Read-only for Agent Office (worktree restored to clean; no patch, no commit/push there); no DB/secret/auth/remote access; no persistent server or browser process remains; no new agent/sub-agent/session/delegated context; AO12-D not started or authorized; only this result + pointer written to Foundation Docs.

Return to: **Advisor**.
