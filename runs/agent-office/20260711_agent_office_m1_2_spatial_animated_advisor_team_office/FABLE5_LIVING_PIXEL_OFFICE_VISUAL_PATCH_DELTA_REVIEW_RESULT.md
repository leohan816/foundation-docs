# FABLE5 REVIEW — Living Pixel-Office Visual Patch Delta (AO12-PWU-11-R1)

- Mission: `AGENT_OFFICE_M1_2_SPATIAL_ANIMATED_ADVISOR_TEAM_OFFICE` · Pass: **DESIGN_REVIEW__M1_2_VISUAL_PATCH_DELTA_AND_ACCESSIBILITY** (Level 3 narrow delta) · Actor: **Fable5 Reviewer** (`reviewer-fable5`) · Skill: `/fable-sentinel` · Date: 2026-07-12
- Target: `c535877b -> ac8ba75d3a128385beaeeac58ae5bf54c03d23f2` = origin; exactly two commits (`364f407` visual refine + `ac8ba75` fact binding); 43 files +1980/-318; worktree clean; artifact-99 intermediate identity not used.
- **VERDICT: `PASS`** (clean) — closes only `AO12-PWU-11-R1` and routes the reviewed candidate to Leo/GPT for `AO12-PWU-11-D2`. No full integration, Founder acceptance, or production authority.

## The 13 required checks (all verified first-hand)

1. **Always-visible labels**: every visible actor in all 13 baselines carries a compact card: role glyph in a colored circle (A/W/R/C/D) with a **state ring dot**, role text, model line, session line, operational state, and a `SYNTHETIC FIXTURE` source tag — text + glyph + ring combined, never color alone.
2. **Exact registry sessions**: the fact source (`fixtures/prototype-projection.ts`, read directly) wraps sessions in `verifiedRegistryFact()` with exactly the 98A-verified names (`foundation-advisor`, `foundation-control`, `foundation`, `cosmile`, `siasiu`, `agent-office`, `reviewer-fable5`, `VibeNews-advisor`, `VibeNews`, `VibeNews-designer`); my repo-wide grep finds **no** `foundation-worker`/`cosmile-worker`/`siasiu-worker`/`vibenews-worker` session facts (the only `'control'` hits are projectId/glyph keys, not session facts); the rendered labels in the baselines match exactly.
3. **Models**: only Agent Office Worker = `Codex 5.6 SOL` and Fable5 Reviewer = `Fable5` via `verifiedMissionArtifactFact()`; every other actor uses `unverifiedFact()` and renders literal `UNKNOWN` (visible in every baseline).
4. **Fail-closed**: `normalizeActorFact` (frame-projector, read directly) maps null/undefined/non-object/`UNVERIFIED`-source/blank-after-trim to `PIXEL_ACTOR_UNKNOWN`; states must be `SYNTHETIC_FIXTURE`-sourced members of a closed 14-value enum else `UNKNOWN`; facts flow only from explicit inputs — no project/pod/position/proximity/prose inference path exists.
5. **Drawer**: the actor drawer consumes exactly the ten required facts (role, project, advisorTeam, reportsToAdvisor, sessionName, model, state, mission, workUnit, evidenceFreshness — enumerated from the component) with per-fact `data-actor-fact-source` attribution, `role="dialog"`, Escape close, Tab containment, close-button focus on open, and invoker focus restore; semantic/static presentations carry the same labels (V13 static shows all ten actor cards over the DOM-static office).
6. **Label tracking/readability**: labels follow actors under camera focus/zoom (V07 close-up) and along routes (V04/V10 traversing Advisor/courier); readable on desktop, mobile (V12), and static (V13); compact overlay leaves the office primary with no incoherent overlap.
7. **Channy motion**: the deterministic sequence (timeline, read directly) interleaves SIT (2.5s), WALK, STOP, SNIFF, SIT, PLAY, then food/drink/sleep segments with `easeInOutCubic` easing and long pauses — walk/stop/sniff/sit/eat/drink/sleep/play all present; HUD states "Slow eased Bedlington walk with long neutral pauses; authorityRole none".
8. **Bedlington appearance**: V07/V08/V09 close-ups show the redrawn original pixel Bedlington — rounded wool head, narrow dark muzzle, arched back, slim legs, cream/light coat, darker ears.
9. **Modern palette**: light oak/ivory/sand office with warm-gray walkway and glass-blue accents; project colors reduced to sign trims, clothing, and accents; light HUD; office world primary (all 13 baselines).
10. **Baseline scope exact**: my git check — exactly **13 PNGs changed, 0 outside the living-prototype directory, all 26 historical baselines byte-identical to `c535877b`**; the updates reflect the authorized restyle/labels, concealing nothing (each inspected).
11. **Media current-source**: my own reruns regenerated the evidence (schema v3, `officeSurfaceTheme: light`); verifier exit 0 with durations WebM 27.4s / MP4 27.36s / GIF 7.000s, exactly 8 files, root `0700`, ignored, 0 tracked; deliverable PNG hashes remain byte-deterministic against the committed baselines; a WebM frame at 11s (my capture) shows the patched palette with the labelled Advisor carrying the document mid-traverse — continuous motion under the patch. (Same disclosure as the base review: the recording re-captures per run, so WebM bytes are run-specific; the verifier validated the current set.)
12. **Regressions intact**: my runs — unit suite **88 files / 514 tests exit 0** (incl. the new overlay tests 16/16 focused, bridge 24/24), dedicated prototype train **20/20** with budgets re-measured (cached startup p95 480.6ms, 4xCPU 816ms, active-frame p95 0.5ms, camera p95 43ms, long tasks 0, texture sources 4, mount-cycle heap growth 0, retained 0), verifier 39/13/26; structured-evidence-only, single character per instance (FRAME PARITY / 25 ENTITIES on-surface), Single Advisor Team, static/M1 fallback (V13 + "Open unchanged M1 fallback"), prototype isolation and production exclusion unchanged (delta touches only pixel prototype surfaces, tests, verifier, and canonical docs).
13. **No hidden authority**: the 43-path delta contains no package/lock/auth/transport/DB/remote path; no integration, Founder-acceptance, or next-mission artifact; `AO12-PWU-11-D2` remains the explicit next gate.

## Findings

**None blocking.** `[INFORMATIONAL]` WebM per-run re-capture (disclosed, unchanged from base review); `[INFORMATIONAL]` unit totals grew 87/505 → 88/514 from the authorized new overlay test file. `[DEFERRED_WITH_GATE]` full integration, as designed.

## Verdict rationale
Every patch requirement from decision 97 and clarification 98A is implemented and verified against actual source, tests, baselines, and media: exact registry-backed identity facts with literal-UNKNOWN fail-closed handling, the complete ten-field accessible drawer, tracked readable labels, slow eased eight-state Bedlington Channy, and the modern light palette — with all 26 historical baselines byte-identical, all gates green in my own runs, and every preserved boundary intact. Per V2: **PASS**.

## Self-review
All claims trace to my command outputs, direct source/diff reads at the pinned commits, and directly inspected baselines/frames; Worker/Advisor summaries distrusted; Agent Office read-only (clean at ac8ba75d = origin; only ignored media regenerated via the product's own reproduction paths); no listener/process remains; no new agent/session/context; only this result + pointer written.

Return to: **Advisor**.
