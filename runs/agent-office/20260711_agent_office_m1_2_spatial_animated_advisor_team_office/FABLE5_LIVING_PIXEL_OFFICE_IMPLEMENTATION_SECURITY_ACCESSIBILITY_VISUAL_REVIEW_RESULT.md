# FABLE5 REVIEW — Living Pixel-Office Prototype (Implementation, Security, Accessibility, Visual)

- Mission: `AGENT_OFFICE_M1_2_SPATIAL_ANIMATED_ADVISOR_TEAM_OFFICE` · Pass: **IMPLEMENTATION_SECURITY_ACCESSIBILITY_VISUAL_REVIEW__LIVING_PIXEL_OFFICE_PROTOTYPE** (AO12-PWU-10 gate) · Actor: **Fable5 Reviewer** (`reviewer-fable5`) · Level 3 · Skill: `/fable-sentinel` · Date: 2026-07-12
- Target: `56385b8 -> c535877b61ad8a1e3d74dca5c6fec0ada4cac3f8` = origin; parent = base exactly; single commit; **92 paths +7278/-14 = expected exactly**; worktree clean before and after my review (my own test outputs removed; ignored media dir regenerated by my reproduction as disclosed below).
- **VERDICT: `PASS`** — closes only the independent technical prototype review gate. **No Founder visual acceptance, no full-integration authority, no production authority, no next mission.** `AO12-PWU-11` (Leo/GPT visual direction) is the next decision gate; full integration remains `DEFERRED_WITH_GATE`.

## Reproduced evidence (my own executions at c535877b)

- Full unit suite: **87 files / 505 tests, exit 0**. Focused bridge contract: **24/24**. `npm run lint` and `npm run typecheck` (TS 6.0.3, `skipLibCheck:false`) pass. `build:core`/`build:dashboard` pass; audit 0 vulnerabilities; naming gate **331 files pass**.
- Browser trains: default **43 passed + 20 prototype tests skipped**; composed **3/3**; dedicated prototype **20/20 (clean rerun)** — my first chained invocation of the three trains produced a truncated `1 passed / 18 did not run` prototype result (exit 0) and was discarded as non-evidentiary; the immediate standalone rerun is the evidence of record and also re-measured every browser budget: cached startup p95 **430.8ms** (≤500), 4x-CPU **764.5ms** (≤1500), active-frame p95 **0.1ms**/max 2ms (≤8), camera p95 **34.9ms** (≤50), long tasks **0**, canvases 1, texture sources **4** (≤4), focus/mount-cycle heap growth **0/0**, retained canvases/roots **0/0**.
- Evidence verifier (my run): **PASS — accepted 39 / living 13 / reconciled 26; changed-from-design-base exactly 39; working/staged/untracked 0; artifact root 0700, ignored, 0 tracked, exactly 8 files**. Verifier code inspected: exact-39 unique set, living directory must equal its exact 13 files, observed changed∪staged∪untracked baselines must **equal** the accepted set (both directions), all 26 reconciled must differ from design base, six production markers, listener checks, ffprobe duration gates — no prefix/wildcard/warning/env escape.
- Bridge conformance: the committed `pixi-public-export-bridge.d.ts` is **byte-identical to the normative skeleton I compile-verified in the clean bridge-design review**; the `.js` imports exactly the three literal public roots; module-init asserts every value shape and `VERSION === '8.19.0'`; `extend` called once with exactly Container/Graphics/Sprite; call-boundary validation before every prototype callback. Repo-wide: **0 `@ts-expect-error`/`@ts-ignore`, 0 deep/package-internal imports**; only the four prepared consumers import the bridge; the ESLint delta is exactly one `disableTypeChecked` block for the single bridge `.js` path. `batch-gates.test.ts` now asserts the exact five pinned runtime dependencies (`toEqual` exact-object; non-weakening).
- Production isolation: my fresh `dist/dashboard` scan — pixi/living-pixel/bridge/fixture/scenario markers **0**; all six A1 markers **0**; the two bare-`SYNTHETIC` substring hits are pre-existing M1 dashboard strings (`SYNTHETIC_READ_ONLY` badge logic and a negative assurance sentence), not prototype leakage. The acceptance boundary test (fresh isolated build) ran inside my 505.
- Cleanup: no listener on 4173/4317 and no browser/encoder/server process after all runs; worktree restored clean.

## Baseline reconciliation verified at the root (load-bearing)

The 26 reconciled historical baselines were regenerated under Advisor records 83/85/87 as a **stale-evidence correction**. I verified the premise with my own eyes: the old M1 desktop baseline (extracted from `f9d0533`) **renders the forbidden legacy name "Shashu Worker / Shashu desk"**, while the committed replacement renders **"SIASIU Worker / SIASIU desk"** with otherwise identical scene composition. The mechanism that hid this for months is also verified: the M1 `office-scene` spec compares at `maxDiffPixelRatio: 0.005` (~6.5k pixels on 1440x900), which silently absorbed the small label difference — my earlier byte-equality checks proved only file immutability, never render-match. **All 26 reconciled PNGs directly inspected** (M1 3, spatial-static 6 — reduced is byte-identical to desktop, spatial-motion 4, accessibility 3, composed-M1 3 now SIASIU-labelled, AO12-D 7): all populated current-source renders of the compositions I previously reviewed in depth, correct labels, no blank/corrupt/clipped/mislabeled/semantically conflicting image. The regeneration cause profile (browser/runtime update to configured Chromium 149 + the M1 naming staleness) changes rendering, not product behavior; source/CSS/fixtures/tests for those surfaces are unchanged in the delta (diff-verified).

## All 13 living-prototype PNGs directly inspected (product intent)

1. **V01 full office**: one warm pixel office world dominates the viewport — walls/wood floor, glass meeting room, sign room, Advisor hubs, Reviewer booth, coffee lounge, Channy bed/food/water, east-west walkway, and **all six project Pods** (Agent Office orange, Control slate, Cosmile coral, Foundation navy, SIASIU mint, VibeNews purple) each with sign, desks, computers, boards, plants, and a distinct-clothing character; camera controls are DOM buttons; technical panels sit below the world; on-surface `SYNTHETIC PROTOTYPE / AO12-PWU-07..09` and `FRAME PARITY / 25 ENTITIES`. **This is a living game-like office, not the card dashboard.**
2. **V02**: Foundation focus, WORKING typing microcycle at the desk, HUD "bounded typing does not change progress".
3. **V03**: VibeNews focus with two purple-clad VibeNews actors and adjacent SIASIU pod; "identity remains distinct while every Pod stays on the floor".
4. **V04**: frozen handoff arrival — route overlay traced, document in the selected worker's hands ("One Advisor and one document follow one accepted synthetic route"); the traverse itself is proven in motion evidence below (per the matrix, V04's required proof is the recording/GIF).
5. **V05**: REVIEWING state, "Independent review presentation; no verdict or approval claim".
6. **V06**: verified-idle coffee at the lounge, `VERIFIED_IDLE` with no-availability disclaimer.
7. **V07/V08/V09**: Channy (white Bedlington-terrier sprite) roaming on the walkway / eating at the food bowl / sleeping at the bed, each labelled neutral with `authorityRole none` / "no mission implication" / "no system-idle implication".
8. **V10**: WAITING_LEO with persistent banner "visual direction decision required; no decision claim".
9. **V11**: BLOCKED — red banner, blocker badge on the actor, barrier strip, "task and ambient routes suppressed".
10. **V12 mobile**: one focused readable Pod with prev/next/picker/zoom/full-office controls; no miniature floor; no horizontal loss.
11. **V13 reduced-motion**: `DOM_STATIC` badges, canvas replaced by the static semantic office, "zero ticker and no replay", full HUD facts retained.

## Media evidence (recomputed and directly inspected)

- Artifact root `0700`, ignored, zero tracked paths, exactly the eight required files. Durations by my ffprobe: WebM 27.04s (20-30 ✔), MP4 27.08s, GIF 7.000s at 720x450; WebM/MP4 1440x900.
- **Determinism proven twice**: my dedicated-train reproduction regenerated the five deliverable PNGs **byte-identical to the Worker-declared hashes** (e.g., `full-office.png` = `6cad6dca…` on both my runs), and they equal the committed V01/V02/V06/V07/V12 baselines hash-for-hash.
- **Disclosure — WebM lineage**: the recording spec re-records on every dedicated-train run, so my reproduction **replaced the Worker's WebM** with my own capture (real-time screencast is not byte-deterministic: 27.36s then 27.04s across my runs vs the Worker's 27.08s — all in range). The on-disk **MP4 and GIF hashes still equal the Worker-declared values exactly** (their conversions of their WebM, attested by their verifier run); my verifier run passed against my WebM. I did not overwrite the MP4/GIF.
- **Frames directly inspected from the actual recording (my capture)**: 7s — the Foundation worker mid-walk on the walkway with an empty desk ("Verified Worker bounded walk"); 11s — **the Advisor sprite carrying a document mid-traverse on the walkway** with the route line ("One Advisor and one document follow one accepted synthetic route"; no clone); 23s — WAITING_LEO with vacated desk, decision path, and persistent banner; GIF frame at 5s shows the same handoff traverse (the GIF is the 6.0-13.0s segment = walk + Channy roam + handoff); MP4 frame at 11s matches the WebM scene lineage. Continuous motion and state transitions are real, current-source, and labelled `SYNTHETIC PROTOTYPE` throughout.

## The ten product-intent questions — all YES with evidence

(1) One shared office world dominates (V01, all frames); (2) all six Pods spatially present, focus/zoom preserves the world (V01-V03, camera indicator states); (3) every required facility visibly represented (V01/V06-V09); (4) actors are original sprite characters with materially distinct walk/sit/type/review/carry/return/coffee/idle presentations (V02/V04-V06, frames at 7s/11s); (5) Channy demonstrates roam/eat/sleep in stills plus roam in motion — the authorized prototype subset — with zero operational authority on-surface (V07-V09); (6) six projects distinguished by glyph + name + clothing + trim, never color alone (V01 nav + pods); (7) technical panels secondary (bottom drawer buttons); (8) the recorded timeline visibly includes worker movement, Advisor handoff, lounge idle, Channy, WAITING_LEO, and BLOCKED (frames + V10/V11 + the 26s tour structure); (9) all motion derives from the accepted synthetic cue path through the unchanged AO12-C projector/reducer (source-verified; suppression/no-replay/no-clone re-asserted in the 505 and visible: single Advisor, one route, honest banners); (10) mobile/reduced/forced/keyboard/mirror/200% preserved (V12/V13 + the reconciled accessibility set + axe/keyboard gates inside my prototype train).

## Architecture/security/regression checks

Single-Advisor/no-clone/fail-closed inherited through the unchanged validated projection + cue reducer (delta adds no second interpretation — source-verified); bridge lifecycle ownership per the reviewed design; DOM-static/M1 fallback proven live in the prototype train (Canvas-subset classification, both-renderers-fail => static, context-loss + bounded retry + focus restoration tests); StrictMode/teardown/zero-retention re-measured (0/0/0/0); deterministic fixture + presentation clock boundaries (fixture sha asserted; `FROZEN FRAME`/`TOUR RUNNING` chips); production bundle excludes all prototype code (my scan + acceptance gate); no auth/authority/delivery/transport/gateway/DB/secret/remote path in the 92-path delta (name-status verified — only the authorized surfaces); no external asset (code-native atlases with inventory; atlas tests inside the 505); ignored media untracked (verifier + `git ls-files` 0); prototype-only status and `DEFERRED_WITH_GATE` full integration restated across canonical docs.

## Findings

**None blocking.**

- `[INFORMATIONAL]` WebM re-record non-determinism: each dedicated-train run re-captures the screencast, so the artifact WebM's bytes/duration vary per run (27.08/27.36/27.04s observed; always in the 20-30s gate); the five PNG deliverables are byte-deterministic (proven twice); MP4/GIF on disk remain the Worker's declared conversion lineage. Recommendation (non-gating): a future handoff could note that WebM hash values attest a specific capture, not a reproducible byte artifact.
- `[INFORMATIONAL]` My first chained three-train invocation yielded a truncated non-evidentiary prototype-train log (`1 passed / 18 did not run`, exit 0); the immediate standalone rerun (20/20) is the evidence of record — disclosed for run-history honesty.
- `[INFORMATIONAL]` Two bare-`SYNTHETIC` substrings in the production bundle are pre-existing M1 dashboard strings (`SYNTHETIC_READ_ONLY` fixture badge and a negative assurance sentence); all six A1 markers and all pixel/prototype markers are 0.
- `[INFORMATIONAL]` Stale-baseline reconciliation root verified first-hand: old M1 baseline rendered forbidden `Shashu` under the 0.005 M1 tolerance; new baselines render `SIASIU`; spatial baseline changes are configured-runtime re-renders of unchanged reviewed sources.
- `[DEFERRED_WITH_GATE]` Full authenticated integration (`AO12-PWU-12..13`) — remains blocked on explicit durable Leo/GPT `AO12-PWU-11` approval and a new exact Advisor handoff, as designed.

## Verdict rationale

Every load-bearing Worker claim was reproduced or directly inspected: identity/scope/ancestry exact; zero suppressions and zero deep imports with the bridge byte-identical to the clean-PASS design skeleton; all unit/browser/perf/verifier gates green in my own runs with margins; the exact-39 baseline governance enforced by code I read; all 39 baselines and the media package directly inspected with the reconciliation premise verified at its root; the primary experience is demonstrably a living game-like pixel office with truthful, evidence-bounded motion and intact accessibility/fallback/security boundaries; production remains prototype-free. Per V2: **PASS**.

## Self-review

All claims trace to my command outputs, direct reads at pinned commits, or directly inspected images/frames; Worker/Advisor summaries were distrusted and re-derived; anomalies (train truncation, WebM lineage effect of my own reproduction) are disclosed, not absorbed. Agent Office read-only (no patch; worktree clean at c535877b = origin; my scratch outputs removed; ignored media regenerated only through the product's own reproduction commands); no DB/secret/remote access; no listener or process remains; no new agent/sub-agent/session/delegated context; no Founder acceptance or integration authority granted; only this result + pointer written to Foundation Docs.

Return to: **Advisor**.
